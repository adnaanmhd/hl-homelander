// Stub Signup screen — real body (Google Sign-In + consent + Terms-of-Use
// modal + animated logo) lands in plan 02-09. This stub keeps the navigator
// graph compiling.
import React from 'react';
import ScreenContainer from '../../ui/primitives/ScreenContainer';
import Text from '../../ui/primitives/Text';

export default function SignupScreen() {
  return (
    <ScreenContainer accessibilityLabel="Signup screen">
      <Text variant="title28">Signup</Text>
    </ScreenContainer>
  );
}
