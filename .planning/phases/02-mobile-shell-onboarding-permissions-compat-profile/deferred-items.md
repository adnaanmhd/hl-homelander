# Phase 2 — Deferred Items

Out-of-scope discoveries logged by individual plan executors. These items are
NOT to be auto-fixed inside the discovering plan; they belong to a different
plan (or are pre-existing issues) and require their owning plan's PR to land.

| Discovered by | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Owner                                                                         |
| ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| 02-08         | ~~PermissionsScreen 6 tests fail because plan 02-10's GREEN commit was mis-labeled and only landed plan 02-08 files.~~ **RESOLVED** at commit `bd8f4df` (~16:17:11) — the plan 02-10 GREEN content (PermissionsScreen.tsx 208 LOC, test refinements, `Ban` icon in vitest.setup.ts) ended up bundled into a commit mis-labeled `test(02-09): add 9 failing SignupScreen tests (RED)` due to the multi-agent worktree contention described in plan 02-10 SUMMARY § Deviations. All 6 PermissionsScreen tests now pass against HEAD.                                                                                    | — (closed)                                                                    |
| 02-08         | `__tests__/screens/RigTutorialScreen.test.tsx` — 6 RED tests committed at `6485455`; the GREEN implementation lives unstaged in the worktree (RigTutorialScreen.tsx 243-line full body) and was never committed.                                                                                                                                                                                                                                                                                                                                                                                                      | Plan 02-11 owner — commit GREEN.                                              |
| 02-08         | `__tests__/navigation/RootNativeStack.test.tsx` — 3 navigation tests fail (`useAppStore is not a function` because `vi.mock('../../src/state/appStore', ...)` factory in the test returns an object literal with no callable). The test was authored at plan 02-05 and worked at the time; the factory was overridden by a later vi.mock change that's now stale.                                                                                                                                                                                                                                                     | Plan 02-05 owner (or whichever plan most recently touched the test).          |
| 02-10         | Plan 02-10's commit history is mislabeled in two places due to multi-agent worktree contention (concurrent agents executing 02-08, 02-09, 02-10, 02-11 in the same worktree, lint-staged's `git stash`-based hook clobbering each other's staged trees). Commit `9fdf290` claims "feat(02-10) GREEN" but ships only `versionService.ts` + `semver.ts`; commit `bd8f4df` claims "test(02-09) RED" but actually ships the plan 02-10 GREEN PermissionsScreen + test refinements + `Ban` icon. End-state tree is correct; commit messages are not. Per `<destructive_git_prohibition>` no history rewrite was attempted. | — (history accepted as-is; surfaced for awareness during PR review of Wave 2) |
