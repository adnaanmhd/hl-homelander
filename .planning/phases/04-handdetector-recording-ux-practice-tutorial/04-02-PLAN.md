---
phase: 04-handdetector-recording-ux-practice-tutorial
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/android/app/build.gradle
  - apps/mobile/android/app/src/main/assets/hand_landmarker.task
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/handdetector/HumynHandDetectorModule.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/handdetector/HumynHandDetectorPackage.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/phonestate/HumynPhoneStateModule.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/phonestate/HumynPhoneStatePackage.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/battery/HumynBatteryModule.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/battery/HumynBatteryPackage.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/screenbrightness/HumynScreenBrightnessModule.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/screenbrightness/HumynScreenBrightnessPackage.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/beep/HumynBeepModule.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/beep/HumynBeepPackage.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainApplication.kt
  - apps/mobile/src/native/HumynHandDetector.ts
  - apps/mobile/src/native/HumynPhoneState.ts
  - apps/mobile/src/native/HumynBattery.ts
  - apps/mobile/src/native/HumynScreenBrightness.ts
  - apps/mobile/src/native/HumynBeep.ts
  - apps/mobile/__tests__/native/HumynHandDetector.test.ts
  - apps/mobile/__tests__/native/HumynPhoneState.test.ts
  - apps/mobile/__tests__/native/HumynBattery.test.ts
  - apps/mobile/__tests__/native/HumynScreenBrightness.test.ts
  - apps/mobile/__tests__/native/HumynBeep.test.ts
autonomous: true
requirements: [HAND-01, HAND-08, REC-08, REC-10]
must_haves:
  truths:
    - 'Five new Kotlin native modules (HumynHandDetector, HumynPhoneState, HumynBattery, HumynScreenBrightness, HumynBeep) compile and are registered in MainApplication.getPackages()'
    - "Each module has a matching JS binding under apps/mobile/src/native/ that throws the canonical 'X native module not registered — check ...MainApplication.kt' error when the native module is absent"
    - 'hand_landmarker.task (~7.8 MB) is bundled under android/app/src/main/assets/ and build.gradle declares com.google.mediapipe:tasks-vision:0.10.21'
    - 'isHandDetectorAvailable() returns false when NativeModules.HumynHandDetector is undefined (HAND-08 silent-bypass discriminant)'
  artifacts:
    - path: 'apps/mobile/src/native/HumynHandDetector.ts'
      provides: 'detectHands(path, minConfidence):Promise<number> + cleanup() + isHandDetectorAvailable()'
      exports: ['detectHands', 'cleanup', 'isHandDetectorAvailable']
    - path: 'apps/mobile/src/native/HumynPhoneState.ts'
      provides: 'start()/stop() + onAudioFocusChanged(listener):EmitterSubscription'
      exports: ['start', 'stop', 'onAudioFocusChanged']
    - path: 'apps/mobile/src/native/HumynBattery.ts'
      provides: 'start()/stop() + onBatteryChanged(listener):EmitterSubscription'
      exports: ['start', 'stop', 'onBatteryChanged']
    - path: 'apps/mobile/src/native/HumynScreenBrightness.ts'
      provides: 'set(value:number):Promise<void> (value 0..1, or -1 to restore)'
      exports: ['set']
    - path: 'apps/mobile/src/native/HumynBeep.ts'
      provides: 'playTone(name:string):Promise<void>'
      exports: ['playTone']
    - path: 'apps/mobile/android/app/src/main/assets/hand_landmarker.task'
      provides: 'MediaPipe HandLandmarker model bundle'
    - path: 'apps/mobile/android/app/build.gradle'
      provides: 'com.google.mediapipe:tasks-vision:0.10.21 dependency'
      contains: 'tasks-vision:0.10.21'
  key_links:
    - from: 'apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainApplication.kt'
      to: 'the 5 new ReactPackages'
      via: 'packages.add(...) in getPackages()'
      pattern: 'HumynHandDetectorPackage'
    - from: 'apps/mobile/src/native/HumynHandDetector.ts'
      to: 'NativeModules.HumynHandDetector'
      via: 'ensure() guard'
      pattern: 'HumynHandDetector native module not registered'
---

<objective>
Ship the five new in-house Kotlin native module SHELLS + their JS bindings + register them in `MainApplication.kt`, add the MediaPipe `tasks-vision:0.10.21` Gradle dep, and bundle the `hand_landmarker.task` model asset. Wave 2 plans (04-04, 04-05) fill in the module bodies; this plan establishes the contract surface, the JS bindings (including the HAND-08 `isHandDetectorAvailable()` discriminant), and the test scaffolds.

Purpose: Phase 4's RecordingScreen + useRecordingLifecycle call these five modules; nothing downstream typechecks/tests without the bindings and the canonical "not registered" error contract.
Output: 10 Kotlin files (5 modules + 5 packages), 5 JS bindings, MainApplication registration, build.gradle dep, model asset, 5 native-binding unit tests.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md
@CLAUDE.md
@apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainApplication.kt
@apps/mobile/android/app/src/main/java/ai/humynlabs/capture/updater/HumynUpdaterModule.kt
@apps/mobile/android/app/src/main/java/ai/humynlabs/capture/updater/HumynUpdaterPackage.kt
@apps/mobile/src/native/HumynCompat.ts
@apps/mobile/src/native/HumynCapture.ts
@apps/mobile/__tests__/native/HumynCapture.test.ts

<interfaces>
<!-- Canonical 3-file native-module triad (copy structure from these) -->
From apps/mobile/android/app/src/main/java/ai/humynlabs/capture/updater/HumynUpdaterModule.kt:
  @ReactModule(name = X.NAME) class XModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaModule(reactContext) {
    companion object { const val NAME = "X" }
    private val bgExecutor = Executors.newSingleThreadExecutor()
    override fun getName(): String = NAME
    @ReactMethod fun foo(arg: String, promise: Promise) { bgExecutor.execute { try { ... promise.resolve(map) } catch (t: Throwable) { promise.reject("CODE", "${t::class.simpleName}: ${t.message}", t) } } }
  }
From apps/mobile/android/app/src/main/java/ai/humynlabs/capture/updater/HumynUpdaterPackage.kt:
  class XPackage : ReactPackage {
    override fun createNativeModules(reactContext: ReactApplicationContext): List<NativeModule> = listOf(XModule(reactContext))
    override fun createViewManagers(reactContext: ReactApplicationContext): List<ViewManager<*, *>> = emptyList()
  }
From apps/mobile/src/native/HumynCompat.ts (the ensure() guard pattern):
  function ensure(): X { const native = NativeModules.X as X | undefined; if (!native) throw new Error('X native module not registered — check apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainApplication.kt'); return native; }
From apps/mobile/src/native/HumynCapture.ts (lazy NativeEventEmitter for event modules):
  let _emitter: NativeEventEmitter | null = null; function emitter(){ if(_emitter==null) _emitter = new NativeEventEmitter(NativeModules.X); return _emitter; }
  export function onFoo(listener): EmitterSubscription { return emitter().addListener('onFoo', listener); }  // caller MUST .remove() on unmount (T-3.3-04)
From apps/mobile/android/app/src/main/java/ai/humynlabs/capture/capture/HumynCaptureModule.kt (RCTDeviceEventEmitter emit):
  reactApplicationContext.getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter::class.java).emit(name, payload)
From apps/mobile/android/app/src/main/java/ai/humynlabs/capture/capture/HumynCaptureModule.kt (applyKeepScreenOn — the UI-thread window-mutation idiom HumynScreenBrightness reuses):
  val activity: Activity = reactApplicationContext.currentActivity ?: return; activity.runOnUiThread { activity.window?... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HumynHandDetector shell + MediaPipe Gradle dep + hand_landmarker.task asset + JS binding + test</name>
  <behavior>
    - `isHandDetectorAvailable()` returns `false` when `NativeModules.HumynHandDetector` is undefined, `true` when present
    - `detectHands(path, minConfidence)` rejects with an Error containing `HumynHandDetector native module not registered` when the native module is absent
    - when the native module IS present, `detectHands(path, minConf)` forwards `(path, minConf)` verbatim and resolves with the native return value (a number); `cleanup()` forwards no args and resolves
    - `detectHands(path)` called without `minConfidence` passes `0.5` as the default
  </behavior>
  <read_first>
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/updater/HumynUpdaterModule.kt + HumynUpdaterPackage.kt (the shell template)
    - apps/mobile/src/native/HumynCompat.ts (the ensure() guard + block-comment docstring convention)
    - apps/mobile/__tests__/native/HumynCapture.test.ts (the `vi.resetModules()` + `vi.doMock('react-native', ...)` + `vi.doUnmock` test pattern — your HumynHandDetector.test.ts mirrors this)
    - apps/mobile/android/app/build.gradle (current `dependencies {}` block — add the MediaPipe line; confirm `google()` is in the project repos in `apps/mobile/android/build.gradle`)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § "In-house Kotlin native modules" HumynHandDetector row + "Code Examples" § HumynHandDetector Kotlin module (lines 616-696) + § HumynHandDetector.ts JS binding
    - figure-app-hands.md (the reverse-engineered HandLandmarker pattern — confirms IMAGE mode, numHands=2, CPU delegate, detectHands(path) signature)
  </read_first>
  <action>
    1. **Gradle:** in `apps/mobile/android/app/build.gradle` `dependencies {}`, add `implementation "com.google.mediapipe:tasks-vision:0.10.21"`. Confirm `google()` (Google Maven) is among the repositories in `apps/mobile/android/build.gradle` (it is in a standard RN project — the artifact is on Google Maven, released 2025-01-31, NOT Maven Central). Do NOT use `tasks-vision` 0.10.33+ (iOS pod parity pin per CLAUDE.md).
    2. **Model asset:** download `hand_landmarker.task` (~7.8 MB, the public MediaPipe HandLandmarker float16 bundle) from https://storage.googleapis.com/mediapipe-models/hand_landmarker/hand_landmarker/float16/1/hand_landmarker.task (Google's model card asset; the byte-identical copy is also at `apk-extracted/assets/hand_landmarker.task` in this repo from Figure's APK — you may copy that if the download fails). Place it at `apps/mobile/android/app/src/main/assets/hand_landmarker.task`. Verify the file size is between 7 MB and 9 MB.
    3. **Kotlin shell** `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/handdetector/HumynHandDetectorModule.kt` — package `ai.humynlabs.capture.handdetector`; `@ReactModule(name = HumynHandDetectorModule.NAME)`, `companion object { const val NAME = "HumynHandDetector" }`, `override fun getName() = NAME`, `private val bgExecutor = Executors.newSingleThreadExecutor()`. Two `@ReactMethod`s: `fun detectHands(path: String, minConfidence: Double, promise: Promise)` and `fun cleanup(promise: Promise)`. SHELL BODY (the real MediaPipe body lands in plan 04-04): `detectHands` → `bgExecutor.execute { promise.reject("NOT_IMPLEMENTED", "HumynHandDetector.detectHands not implemented until plan 04-04") }`; `cleanup` → `promise.resolve(null)`. Add the block-comment docstring (purpose / mirrors-which-file / "Plan 04-04 wires the MediaPipe body").
    4. **Kotlin package** `HumynHandDetectorPackage.kt` — copy `HumynUpdaterPackage.kt` verbatim, swap class names → `class HumynHandDetectorPackage : ReactPackage { createNativeModules → listOf(HumynHandDetectorModule(reactContext)); createViewManagers → emptyList() }`.
    5. **JS binding** `apps/mobile/src/native/HumynHandDetector.ts` — mirror `HumynCompat.ts`: `interface HumynHandDetectorNativeModule { detectHands(path: string, minConfidence: number): Promise<number>; cleanup(): Promise<void>; }`; `function ensure(): HumynHandDetectorNativeModule { const native = NativeModules.HumynHandDetector as ... | undefined; if (!native) throw new Error('HumynHandDetector native module not registered — check apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainApplication.kt'); return native; }`; `export const isHandDetectorAvailable = (): boolean => NativeModules.HumynHandDetector != null;` (HAND-08 discriminant); `export async function detectHands(path: string, minConfidence = 0.5): Promise<number> { return ensure().detectHands(path, minConfidence); }`; `export async function cleanup(): Promise<void> { const native = NativeModules.HumynHandDetector as ... | undefined; if (native) await native.cleanup(); }`. Block-comment docstring.
    6. **Test** `apps/mobile/__tests__/native/HumynHandDetector.test.ts` — mirror `HumynCapture.test.ts`: a "not registered" describe (detectHands rejects with `/HumynHandDetector native module not registered/`; `isHandDetectorAvailable()` returns false) and a "registered" describe (`vi.doMock('react-native', () => ({ NativeModules: { HumynHandDetector: { detectHands: vi.fn().mockResolvedValue(2), cleanup: vi.fn().mockResolvedValue(undefined) } } }))` then assert `detectHands('/x.jpg', 0.5)` forwards `('/x.jpg', 0.5)` and resolves `2`; `detectHands('/x.jpg')` forwards `0.5` default; `isHandDetectorAvailable()` returns true). `vi.resetModules()` in `beforeEach`, `vi.doUnmock('react-native')` in `afterEach`.
  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/native/HumynHandDetector.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix apps/mobile test -- --run __tests__/native/HumynHandDetector.test.ts` exits 0
    - `apps/mobile/android/app/build.gradle` contains `tasks-vision:0.10.21`
    - `apps/mobile/android/app/src/main/assets/hand_landmarker.task` exists and `wc -c < apps/mobile/android/app/src/main/assets/hand_landmarker.task` is between 7000000 and 9000000
    - `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/handdetector/HumynHandDetectorModule.kt` contains `const val NAME = "HumynHandDetector"`
    - `apps/mobile/src/native/HumynHandDetector.ts` contains `isHandDetectorAvailable` and `HumynHandDetector native module not registered`
  </acceptance_criteria>
  <done>HandDetector shell + Gradle dep + model asset + JS binding (with HAND-08 discriminant) + test landed; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HumynPhoneState + HumynBattery + HumynScreenBrightness + HumynBeep shells + JS bindings + tests</name>
  <behavior>
    - `HumynScreenBrightness.set(0.05)` rejects with `/HumynScreenBrightness native module not registered/` when the native module is absent; forwards `0.05` verbatim and resolves when present
    - `HumynScreenBrightness.set(-1)` is accepted (the "restore system default" sentinel)
    - `HumynPhoneState.start()` / `stop()` and `HumynBattery.start()` / `stop()` forward no args and resolve when present; `onAudioFocusChanged(listener)` / `onBatteryChanged(listener)` return an object with a `.remove()` method (EmitterSubscription contract)
    - `HumynBeep.playTone('battery_alert')` forwards `'battery_alert'` verbatim and resolves when present
  </behavior>
  <read_first>
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/updater/HumynUpdaterModule.kt + HumynUpdaterPackage.kt (shell template)
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/capture/HumynCaptureModule.kt — the `emitEvent()` (RCTDeviceEventEmitter) idiom + the `applyKeepScreenOn()` UI-thread window-mutation idiom (HumynScreenBrightness's real body in plan 04-05 reuses this verbatim; the shell here just needs the method signature)
    - apps/mobile/src/native/HumynCapture.ts (the lazy `NativeEventEmitter` + `on*(listener): EmitterSubscription` + "caller MUST .remove()" docstring — HumynPhoneState.ts + HumynBattery.ts mirror this)
    - apps/mobile/src/native/HumynCompat.ts (the ensure() guard — HumynScreenBrightness.ts + HumynBeep.ts mirror this)
    - apps/mobile/__tests__/native/HumynCapture.test.ts (the test pattern, incl. the `setupEmitterMock()` "constructor spy" for the event-binding describe blocks)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § "In-house Kotlin native modules" table (HumynPhoneState / HumynBattery / HumynScreenBrightness rows) + Open Question 7 (the HumynBeep over SoundPool note) + "Code Examples" § HumynScreenBrightness
    - CLAUDE.md "Do NOT Use" — NO READ_PHONE_STATE, NO ACCESS_NOTIFICATION_POLICY, NO WRITE_SETTINGS, NO TelephonyManager/PhoneStateListener for call detection, NO react-native-sound/react-native-track-player
  </read_first>
  <action>
    For each of the four modules, create the `{name}/Humyn{Name}Module.kt` + `{name}/Humyn{Name}Package.kt` shell pair under `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/` and the matching `apps/mobile/src/native/Humyn{Name}.ts` JS binding. Use the `HumynUpdaterModule` shell + `HumynUpdaterPackage` verbatim-copy + `HumynCompat.ts`/`HumynCapture.ts` binding patterns from `<interfaces>`. The real bodies land in plan 04-05; shells reject with `"NOT_IMPLEMENTED"` / resolve trivially.

    1. **HumynPhoneState** — package `ai.humynlabs.capture.phonestate`, `NAME = "HumynPhoneState"`. `@ReactMethod`s: `fun start(promise: Promise)` (shell: `promise.resolve(null)`), `fun stop(promise: Promise)` (shell: `promise.resolve(null)`). Add a docstring: "Plan 04-05 wires the AudioManager.OnAudioFocusChangeListener body (NO TelephonyManager, NO READ_PHONE_STATE — see 04-RESEARCH Pitfall 2). Emits `onAudioFocusChanged` via RCTDeviceEventEmitter." JS binding `HumynPhoneState.ts`: `interface { start(): Promise<void>; stop(): Promise<void>; }`, `ensure()` guard ("HumynPhoneState native module not registered — check ...MainApplication.kt"), `export async function start()/stop()`; lazy `NativeEventEmitter(NativeModules.HumynPhoneState)`, `export function onAudioFocusChanged(listener: (e: { focus: 'gain' | 'loss' | 'transient_loss' | 'transient_loss_can_duck' }) => void): EmitterSubscription { return emitter().addListener('onAudioFocusChanged', listener); }` with the "caller MUST .remove() on unmount" docstring.
    2. **HumynBattery** — package `ai.humynlabs.capture.battery`, `NAME = "HumynBattery"`. `@ReactMethod`s: `fun start(promise: Promise)`, `fun stop(promise: Promise)` (shells resolve). Docstring: "Plan 04-05 wires the Intent.ACTION_BATTERY_CHANGED sticky-broadcast receiver; emits `onBatteryChanged({level: Double 0..1, isCharging: Boolean})`." JS binding `HumynBattery.ts`: `interface { start(): Promise<void>; stop(): Promise<void>; }`, `ensure()` guard, `export async function start()/stop()`; lazy `NativeEventEmitter`, `export function onBatteryChanged(listener: (e: { level: number; isCharging: boolean }) => void): EmitterSubscription` with the leak-warning docstring.
    3. **HumynScreenBrightness** — package `ai.humynlabs.capture.screenbrightness`, `NAME = "HumynScreenBrightness"`. `@ReactMethod fun set(value: Double, promise: Promise)` — SHELL BODY: `promise.resolve(null)` (the real `currentActivity.window.attributes.screenBrightness = ...` body is plan 04-05; reuse `HumynCaptureModule.applyKeepScreenOn`'s `reactApplicationContext.currentActivity ?: return` + `runOnUiThread` idiom there). Docstring noting `value ∈ [0,1]` or `-1` → `WindowManager.LayoutParams.BRIGHTNESS_OVERRIDE_NONE` (system default); NOT `Settings.System.SCREEN_BRIGHTNESS` (would need WRITE_SETTINGS). JS binding `HumynScreenBrightness.ts`: `interface { set(value: number): Promise<void>; }`, `ensure()` guard ("HumynScreenBrightness native module not registered — check ...MainApplication.kt"), `export async function set(value: number): Promise<void> { return ensure().set(value); }` with a docstring: "value 0..1 to override per-window brightness; -1 to restore the system default. Used by RecordingScreen: set(0.05) at gate exit, set(-1) on stop AND on unmount (REC-08)."
    4. **HumynBeep** — package `ai.humynlabs.capture.beep`, `NAME = "HumynBeep"`. `@ReactMethod fun playTone(name: String, promise: Promise)` — SHELL: `promise.resolve(null)` (the real `SoundPool`-over-bundled-`.wav` body + the generated `.wav` assets are plan 04-05). Docstring: "Plays a pre-baked .wav from assets/audio/ via SoundPool. names: 'battery_alert' (520 Hz / 200 ms), 'thermal_alert' (descending 440→560→680 Hz). engineering-handoff §6.1 — pre-bake to avoid AudioContext latency variance. DO NOT use react-native-sound / react-native-track-player (CLAUDE.md)." JS binding `HumynBeep.ts`: `interface { playTone(name: string): Promise<void>; }`, `ensure()` guard, `export async function playTone(name: string): Promise<void> { return ensure().playTone(name); }`.
    5. **MainApplication.kt** registration — in `getPackages()`, after `packages.add(HumynCapturePackage())`, append: `packages.add(HumynHandDetectorPackage())`, `packages.add(HumynPhoneStatePackage())`, `packages.add(HumynBatteryPackage())`, `packages.add(HumynScreenBrightnessPackage())`, `packages.add(HumynBeepPackage())` with the matching `import ai.humynlabs.capture.handdetector.HumynHandDetectorPackage` (etc.) imports. ALSO in `onCreate()` (after `HumynForegroundNotification.ensureChannel(this)`), register the react-native-orientation-locker activity lifecycle: `registerActivityLifecycleCallbacks(org.wonday.orientation.OrientationActivityLifecycle.getInstance())` with the `import org.wonday.orientation.OrientationActivityLifecycle` import (per the orientation-locker README; plan 04-01 added the MainActivity.onConfigurationChanged override + manifest configChanges). NOTE: this is a surgical edit to a Phase-3-owned file — keep the diff minimal and additive; do NOT reorder or modify the existing package adds or onCreate body.
    6. **Tests** — `apps/mobile/__tests__/native/HumynPhoneState.test.ts`, `HumynBattery.test.ts`, `HumynScreenBrightness.test.ts`, `HumynBeep.test.ts`. Each mirrors `HumynCapture.test.ts`: a "not registered" describe (methods reject with `/Humyn{Name} native module not registered/`) and a "registered" describe (`vi.doMock('react-native', ...)` injecting the stub, assert verbatim arg forwarding + resolution). For HumynPhoneState/HumynBattery, also add an "event subscriptions" describe using the `setupEmitterMock()` constructor-spy pattern from HumynCapture.test.ts asserting `onAudioFocusChanged`/`onBatteryChanged` call `addListener` with the right event name and return an object with `.remove()`.

  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/native/HumynPhoneState.test.ts __tests__/native/HumynBattery.test.ts __tests__/native/HumynScreenBrightness.test.ts __tests__/native/HumynBeep.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix apps/mobile test -- --run __tests__/native/HumynPhoneState.test.ts __tests__/native/HumynBattery.test.ts __tests__/native/HumynScreenBrightness.test.ts __tests__/native/HumynBeep.test.ts` exits 0
    - `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainApplication.kt` contains all of `HumynHandDetectorPackage`, `HumynPhoneStatePackage`, `HumynBatteryPackage`, `HumynScreenBrightnessPackage`, `HumynBeepPackage`
    - `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainApplication.kt` contains `OrientationActivityLifecycle`
    - `apps/mobile/src/native/HumynScreenBrightness.ts` contains `HumynScreenBrightness native module not registered`
    - `apps/mobile/src/native/HumynPhoneState.ts` contains `onAudioFocusChanged` and `EmitterSubscription`
    - `apps/mobile/src/native/HumynBattery.ts` contains `onBatteryChanged`
    - `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/phonestate/HumynPhoneStateModule.kt` does NOT contain `TelephonyManager` or `READ_PHONE_STATE` or `PhoneStateListener`
    - the full suite `npm --prefix apps/mobile test -- --run` exits 0
  </acceptance_criteria>
  <done>Four module shells + packages + JS bindings + tests landed; MainApplication registers all five new packages + the orientation-locker lifecycle; no forbidden APIs; full suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                                                      |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| JS → Kotlin native modules | `detectHands(path, minConfidence)`, `screenBrightness.set(value)`, `beep.playTone(name)` cross from untrusted-ish JS into Kotlin |
| Android OS permissions     | new native modules must add NO new permissions                                                                                   |
| bundled asset              | `hand_landmarker.task` ships in the APK                                                                                          |

## STRIDE Threat Register

| Threat ID | Category                                | Component                        | Disposition | Mitigation Plan                                                                                                                                                                                               |
| --------- | --------------------------------------- | -------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-4.2-01  | Elevation of privilege / Spec violation | `HumynPhoneStateModule.kt`       | mitigate    | NO `TelephonyManager` / `PhoneStateListener` / `READ_PHONE_STATE` — pure `AudioManager` (the real body is plan 04-05; the shell adds nothing). Acceptance criterion greps the file for the forbidden symbols. |
| T-4.2-02  | Elevation of privilege                  | `HumynScreenBrightnessModule.kt` | mitigate    | per-window `WindowManager.LayoutParams.screenBrightness` only — NOT `Settings.System.SCREEN_BRIGHTNESS` (would need `WRITE_SETTINGS`). Docstring + body (plan 04-05) enforce this.                            |
| T-4.2-03  | Tampering                               | `hand_landmarker.task` integrity | accept      | bundled read-only asset from a trusted source (Google's MediaPipe model card / the byte-identical copy in `apk-extracted/`); not user-supplied, not downloaded at runtime; APK signing covers it              |
| T-4.2-04  | Tampering (low — app-internal)          | `detectHands(path)`              | mitigate    | the path is app-internal `cacheDir`; the Kotlin body (plan 04-04) treats `decodeFile` returning null as a graceful reject (not a crash); `minConfidence` clamped to `[0,1]` in plan 04-04                     |
| T-4.2-05  | Spec violation                          | `MainApplication.kt` edit        | accept      | minimal additive edit (append 5 `packages.add` + 1 `registerActivityLifecycleCallbacks`); no permissions; the `__tests__/manifests/manifests.test.ts` invariant gate still passes                             |

</threat_model>

<verification>
- `npm --prefix apps/mobile test -- --run` exits 0 (all five new native-binding tests + the pre-existing suite green).
- `hand_landmarker.task` size between 7 MB and 9 MB.
- `MainApplication.kt` registers all five new packages + `OrientationActivityLifecycle`.
- `HumynPhoneStateModule.kt` grep-clean of `TelephonyManager` / `READ_PHONE_STATE` / `PhoneStateListener`.
</verification>

<success_criteria>

- Five new Kotlin native module shells + packages compile and are registered in MainApplication.
- Five JS bindings exist with the canonical "not registered" error contract; `isHandDetectorAvailable()` is the HAND-08 discriminant.
- MediaPipe `tasks-vision:0.10.21` Gradle dep + `hand_landmarker.task` model asset bundled.
- Full mobile test suite green.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-02-SUMMARY.md`
</output>
