---
phase: 04-handdetector-recording-ux-practice-tutorial
plan: 09
type: execute
wave: 5
depends_on: ['04-04', '04-05', '04-06', '04-07', '04-08']
files_modified:
  - apps/mobile/src/screens/recording/RecordingScreen.tsx
  - apps/mobile/src/screens/recording/useHandGate.ts
  - apps/mobile/src/lib/buildCaptureOpts.ts
  - apps/mobile/src/lib/remoteConfigGate.ts
  - apps/mobile/__tests__/screens/recording/handGate.test.tsx
  - apps/mobile/__tests__/lib/buildCaptureOpts.test.ts
  - apps/mobile/__tests__/screens/RecordingScreen.test.tsx
  - apps/mobile/__tests__/navigation/route-registry.test.ts
autonomous: true
requirements:
  [
    HAND-01,
    HAND-03,
    HAND-04,
    HAND-06,
    HAND-07,
    HAND-08,
    HAND-09,
    HAND-11,
    HAND-12,
    HAND-14,
    REC-01,
    REC-04,
    REC-05,
    REC-07,
    REC-08,
    REC-16,
    ONB-03,
    ONB-04,
    ONB-05,
  ]
must_haves:
  truths:
    - "RecordingScreen mounts a <Camera> (VisionCamera 4.7.3, preview + takePhoto only) on the ultrawide lens read from compat.lastResult.v1.checks.ultrawideDfov.cameraId, with photoQualityBalance='speed', isActive only during the gate substate, onInitialized → CAMERA_READY (gate.loading→waiting)"
    - 'HAND-12: a single throwaway takePhoto() fires after onInitialized to pre-warm the photo pipeline'
    - "useHandGate poll loop (every gate.cadenceMs ms while gate.phase==='waiting'): takePhoto → RNFS.moveFile to cacheDir/hand-gate/{ulid}.jpg → HumynHandDetector.detectHands(path, minConf) → count===2 ? GATE_HIT : GATE_MISS → RNFS.unlink(path) in finally; cacheDir/hand-gate sweep on RecordingScreen mount + the existing app-launch sweep handles stragglers"
    - 'HAND-08: isHandDetectorAvailable()===false → dispatch GATE_BYPASS immediately (silent, same UX as Skip); HAND-07: Skip → GATE_SKIP, no voice cue/haptic, brightness still drops to 5%'
    - "gate-pass (passed, not skipped/bypassed) → Vibration.vibrate(80) + speakCue('Recording started') + VoiceCue pill text 'Recording started' for 1.8s (REC-15) + HumynScreenBrightness.set(0.05); then for ALL exit kinds (passed/skipped/bypassed): set(0.05) → setCameraActive(false) → settle → HumynCapture.start(buildCaptureOpts(...)) → CAPTURE_STARTED, or on reject set(-1) + CAPTURE_START_FAILED + toast per error code"
    - "buildCaptureOpts assembles the full CaptureSessionOpts (taskId/taskName/taskCategory/taskSetting/contributor{name,email,age,gender,consent:true}/isPractice/startGate{type:'hand_detection',passed,skipped,bypassed,durationMs,consecutiveHitsRequired,platformCadenceMs}/location:null/appVersion/dfovDegrees) — contributor sourced from appStore.user/the verified /me data, NEVER consent defaulted"
    - 'HAND-11: gate targetHits/cadenceMs/minHandDetectionConfidence read from Firebase Remote Config (gate.consecutive_hits_required default 5, gate.cadence_ms default 400, gate.min_hand_detection_confidence default 0.5) with hard-coded fallbacks on fetch failure'
    - "HAND-14: logEvent('recording_gate_skipped', { locale, ... }) on Skip/bypass (no image data — a coarse luminance proxy at most); logEvent('recording_gate_started') on gate enter, 'recording_gate_passed' on pass"
    - "REC-01: Orientation.lockToLandscape() on mount, unlockAllOrientations() on unmount; REC-08: HumynScreenBrightness.set(-1) on stop AND on unmount; post-stop routing per §7h (practice → PracticeComplete; real ≥60s → toast '{Hh Mm} added to your contribution.' + Home; real <60s → toast 'Recording too short — discarded.' + ready); REC-05: re-press starts fresh (RESET_FOR_FRESH)"
    - "mounts useRecordingLifecycle(...) with the documented callbacks (onStop wired to HumynCapture.stop() + the §7h routing; showToast/voiceCue/setAlert wired to the screen's transient UI + ttsVoice.speakCue + the recState alerts)"
    - "route-registry.test.ts: adds 'PracticeIntro' and 'PracticeComplete' to the REQUIRED_PHASE_4_ROUTES block created by plan 04-07 (this plan depends_on both 04-06 — which registers those OnboardingStack routes — and 04-07 — which owns the test and added 'Recording' to that block); the Pattern-54 invariant now covers all three Phase-4 routes"
  artifacts:
    - path: 'apps/mobile/src/screens/recording/RecordingScreen.tsx'
      provides: 'the fully-wired recording surface — VC mount, gate poll loop, gate-pass transition, HumynCapture.start, useRecordingLifecycle, brightness, orientation, routing'
      min_lines: 200
    - path: 'apps/mobile/src/screens/recording/useHandGate.ts'
      provides: 'the photo-to-disk → detectHands → GATE_HIT/MISS poll loop'
      min_lines: 50
    - path: 'apps/mobile/src/lib/buildCaptureOpts.ts'
      provides: 'CaptureSessionOpts assembly from route params + appStore.user + compat result + RemoteConfig gate result'
      exports: ['buildCaptureOpts']
    - path: 'apps/mobile/src/lib/remoteConfigGate.ts'
      provides: 'readGateConfig() — HAND-11 RemoteConfig reads with hard-coded fallbacks'
      exports: ['readGateConfig']
  key_links:
    - from: 'apps/mobile/src/screens/recording/useHandGate.ts'
      to: 'HumynHandDetector.detectHands'
      via: 'takePhoto → RNFS.moveFile(cacheDir/hand-gate/{ulid}.jpg) → detectHands(path, minConf)'
      pattern: 'detectHands'
    - from: 'apps/mobile/src/screens/recording/RecordingScreen.tsx'
      to: 'HumynCapture.start'
      via: 'gate-pass transition → buildCaptureOpts(...) → HumynCapture.start(opts)'
      pattern: 'HumynCapture.start'
    - from: 'apps/mobile/src/screens/recording/RecordingScreen.tsx'
      to: 'compat.lastResult.v1.checks.ultrawideDfov.cameraId'
      via: 'MMKV read at mount → VC device filter + buildCaptureOpts.dfovDegrees + startGate'
      pattern: 'ultrawideDfov'
---

<objective>
Wire the live recording surface onto the plan-04-07 shell: the VisionCamera `<Camera>` mount (preview + `takePhoto()` only) on the compat-chosen ultrawide lens, the HAND-12 pre-warm, the `useHandGate` photo-to-disk poll loop (HAND-03/04/13 — `takePhoto` → `cacheDir/hand-gate/{ulid}.jpg` → `HumynHandDetector.detectHands` → `GATE_HIT`/`GATE_MISS`), the HAND-08 silent-bypass / HAND-07 Skip paths, the TTS-masked gate-pass→active transition (HAND-09: vibrate 80ms → `speakCue('Recording started')` → `HumynScreenBrightness.set(0.05)` → `setCameraActive(false)` → settle → `HumynCapture.start(buildCaptureOpts(...))`), the `buildCaptureOpts` CaptureSessionOpts assembly (the richer-than-it-looks D-API-02 shape — full `contributor`, `startGate`, `dfovDegrees`, `appVersion`), the HAND-11 RemoteConfig gate reads, the HAND-14 analytics, the REC-01 orientation lock, the REC-08 brightness restore on stop+unmount, the §7h post-stop routing, REC-05 re-press-starts-fresh, and the `useRecordingLifecycle` mount (from plan 04-08). Also extends `route-registry.test.ts`'s `REQUIRED_PHASE_4_ROUTES` block (created by plan 04-07 for `'Recording'`) with `'PracticeIntro'` + `'PracticeComplete'` — those OnboardingStack routes are registered by plan 04-06, and this plan depends_on both 04-06 and 04-07 so the additive test edit is safe here (04-06 and 04-07 are in the same plan-wave, so neither could own all three Pattern-54 entries cleanly).

Purpose: this is the integration plan that makes the recording surface actually record. ±1 ms drift re-measurement on this new camera handoff is the [BLOCKING] gate in plan 04-10's smoke runbook.
Output: the fully-wired `RecordingScreen.tsx` + `useHandGate.ts` + `buildCaptureOpts.ts` + `remoteConfigGate.ts` + the route-registry.test.ts PracticeIntro/PracticeComplete additions + 3 tests.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-UI-SPEC.md
@CLAUDE.md
@design-spec.md
@engineering-handoff.md
@idea-brief.md
@apps/mobile/src/screens/recording/RecordingScreen.tsx
@apps/mobile/src/screens/recording/recState.ts
@apps/mobile/src/screens/recording/useRecordingLifecycle.ts
@apps/mobile/src/screens/recording/components/GateRing.tsx
@apps/mobile/src/navigation/OnboardingStack.tsx
@apps/mobile/src/navigation/RootNativeStack.tsx
@apps/mobile/__tests__/navigation/route-registry.test.ts
@apps/mobile/src/native/HumynCapture.ts
@apps/mobile/src/native/HumynHandDetector.ts
@apps/mobile/src/native/HumynScreenBrightness.ts
@apps/mobile/src/lib/ttsVoice.ts
@apps/mobile/src/lib/durationFormat.ts
@apps/mobile/src/state/appStore.ts
@apps/mobile/src/state/keys.ts
@apps/mobile/src/state/mmkv.ts
@shared/types/src/CaptureSessionOpts.ts
@shared/types/src/CompatResult.ts
@apps/mobile/src/util/analytics.ts
@apps/mobile/__tests__/native/HumynCapture.test.ts
@apps/mobile/__tests__/screens/compat/CompatRunningScreen.test.tsx

<interfaces>
<!-- CaptureSessionOpts (Phase 3 D-API-02 — buildCaptureOpts must produce a shape that passes this Zod schema) -->
{ taskId: string(min1), taskName: string(min1), taskCategory: string(min1), taskSetting: 'indoor'|'outdoor',
  contributor: { name: string(min1), email: email, age: int|null, gender: 'male'|'female'|'non-binary'|'prefer-not-to-say'|null, consent: literal(true) },
  isPractice: boolean,
  startGate: { type: literal('hand_detection'), passed: boolean, skipped: boolean, bypassed: boolean, durationMs: int>=0, consecutiveHitsRequired: int>0, platformCadenceMs: int>0 },
  location: string|null, appVersion: regex /^\d+\.\d+\.\d+(?:[+-].+)?$/, dfovDegrees: number>0 }
<!-- HumynCapture.start resolves { sessionId, segmentId, recordingId, filenameBase }; rejects Error(code:'thermal_throttling'|'permission_revoked'|'storage_full'|'internal_error') -->
<!-- VisionCamera 4.7.3 (preview + takePhoto only — NEVER the HEVC pipeline): -->
<Camera ref={camRef} device={ultrawideDevice} isActive={substate==='gate'} photo={true} photoQualityBalance="speed" onInitialized={() => dispatch({type:'CAMERA_READY'})} />
const ultrawideDevice = getAvailableCameraDevices().find(d => d.id === cameraId) ?? useCameraDevice('back', { physicalDevices: ['ultra-wide-angle-camera'] })  // try id-match first, fall back to the physicalDevices filter (Assumption A2 / Open Q 1)
camRef.current?.takePhoto({ flash: 'off', enableShutterSound: false }) → PhotoFile{ path, width, height, orientation, ... }  // qualityPrioritization is the <Camera> prop, NOT a takePhoto option
// VC has no explicit unmount() — `isActive={false}` (or conditional-render) releases Camera2.
<!-- useRecordingLifecycle (plan 04-08) — call signature -->
useRecordingLifecycle({ substate, isPractice, durationMs, callbacks: { onStop, showToast, voiceCue, setAlert } })
const { checkStartGuards } = useRecordingLifecycle({...})
<!-- compat.lastResult.v1 (CompatResult schema, Phase 2 D-COMPAT-05) — checks.ultrawideDfov: { cameraId: string, measuredDeg: number, ... } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: remoteConfigGate.ts (HAND-11) + buildCaptureOpts.ts (CaptureSessionOpts assembly) + buildCaptureOpts.test.ts</name>
  <behavior>
    - `readGateConfig()` returns `{ targetHits, cadenceMs, minHandDetectionConfidence }` — from Firebase Remote Config keys `gate.consecutive_hits_required` / `gate.cadence_ms` / `gate.min_hand_detection_confidence` when available, falling back to `{ targetHits: 5, cadenceMs: 400, minHandDetectionConfidence: 0.5 }` (Android defaults) on a fetch failure or missing key; values are clamped to sane ranges (`targetHits` ≥ 1, `cadenceMs` ≥ 100, `minHandDetectionConfidence` ∈ [0,1])
    - `buildCaptureOpts(args)` returns a `CaptureSessionOpts` object that `CaptureSessionOptsSchema.parse(...)` ACCEPTS — every required field present, `contributor.consent === true` (literal), `appVersion` matches the semver regex, `dfovDegrees > 0`, `startGate.consecutiveHitsRequired > 0`, `startGate.platformCadenceMs > 0`
    - for a practice recording: `taskId: '__practice__'`, `taskName: 'Practice — 60 sec'`, `isPractice: true`, `taskCategory: 'practice'` (or whatever the schema accepts — `taskCategory` is `string.min(1)`), `taskSetting: 'indoor'`
    - `contributor` is sourced from `appStore.user` / the verified `/me` data — `name`, `email` from there; `age`/`gender` mapped to the narrow enum (`gender` coerced from the `/me` shape to `'male'|'female'|'non-binary'|'prefer-not-to-say'|null` — any unrecognized value → `null`); `consent` is `true` (sourced from the consent state being present — NEVER hardcoded-without-checking; if the consent state is absent the function throws rather than defaulting `consent: true`)
    - `dfovDegrees` from `compat.lastResult.v1.checks.ultrawideDfov.measuredDeg`; `startGate.{passed,skipped,bypassed,durationMs}` from the gate result; `startGate.{consecutiveHitsRequired,platformCadenceMs}` from `readGateConfig()`; `location: null`; `appVersion` from `BuildConfig.VERSION_NAME` (via the AppFlavor native module's `getConstants()`)
  </behavior>
  <read_first>
    - shared/types/src/CaptureSessionOpts.ts (the Zod schema — buildCaptureOpts must produce a parseable shape; note `contributor.consent: z.literal(true)`, `appVersion` semver regex, `dfovDegrees` positive, `taskSetting: z.enum(['indoor','outdoor'])`)
    - apps/mobile/__tests__/native/HumynCapture.test.ts lines ~29-54 (the `VALID_OPTS` fixture — the exact shape buildCaptureOpts should produce; mirror it)
    - shared/types/src/CompatResult.ts + shared/types/src/me.ts + apps/mobile/src/state/appStore.ts (the `UserDisplay` shape — `email`, `name`, `age`, `gender`, `consent` (a `ConsentState | null`); reconcile `gender` to the narrow `CaptureSessionOpts.contributor.gender` enum — Assumption A9)
    - apps/mobile/src/native/AppFlavor.ts (the AppFlavor native module — `getConstants()` exposes `BuildConfig.VERSION_NAME` / `applicationId`; use `VERSION_NAME` for `appVersion`)
    - apps/mobile/src/state/keys.ts (`KEYS.COMPAT_LAST_RESULT = 'compat.lastResult.v1'`) + apps/mobile/src/state/mmkv.ts (the mmkv instance to read it)
    - @react-native-firebase/remote-config (already a dep; `remoteConfig().getValue('gate.cadence_ms').asNumber()` etc.; `remoteConfig().setDefaults({...})` + `remoteConfig().fetchAndActivate()`)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § "Code Examples — Gate-pass → active transition" (the `enterActive` recipe — the exact `opts` shape) + § "Integration Points — Mobile → Firebase Remote Config" (the HAND-11 keys + defaults: Android `5/400/0.5`, iOS `3/600/0.5`) + § Assumption A9 (the contributor mapping) + § Security V14 (RemoteConfig must have hard-coded defaults; V11 — sourcing `consent` honestly)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md § "RecordingScreen.tsx — building CaptureSessionOpts is richer than it looks"
  </read_first>
  <action>
    1. `apps/mobile/src/lib/remoteConfigGate.ts` — NEW. Export `readGateConfig(): Promise<{ targetHits: number; cadenceMs: number; minHandDetectionConfidence: number }>`. Body: try `remoteConfig().setDefaults({ 'gate.consecutive_hits_required': 5, 'gate.cadence_ms': 400, 'gate.min_hand_detection_confidence': 0.5 })` then `await remoteConfig().fetchAndActivate()` (best-effort; catch and proceed); then `const targetHits = Math.max(1, Math.round(remoteConfig().getValue('gate.consecutive_hits_required').asNumber() || 5)); const cadenceMs = Math.max(100, Math.round(remoteConfig().getValue('gate.cadence_ms').asNumber() || 400)); const minHandDetectionConfidence = Math.min(1, Math.max(0, remoteConfig().getValue('gate.min_hand_detection_confidence').asNumber() || 0.5));` — wrap the whole thing in try/catch returning the hard-coded Android defaults `{ targetHits: 5, cadenceMs: 400, minHandDetectionConfidence: 0.5 }` on any failure. Top docstring: "HAND-11 — gate cadence/target/confidence are Firebase Remote Config keys (so they can be retuned without an app release — e.g. lower minHandDetectionConfidence if Skip-rate >30% in a locale per Pitfall 8/12). MUST degrade gracefully to the hard-coded defaults on fetch failure (Security V14). The keys are cross-platform-aware (iOS defaults 3/600/0.5) per SC#1 even though iOS is descoped from this MVP."
    2. `apps/mobile/src/lib/buildCaptureOpts.ts` — NEW. Export `buildCaptureOpts(args: { taskId: string; taskName: string; taskCategory: string; taskSetting: 'indoor' | 'outdoor'; isPractice: boolean; gate: { passed: boolean; skipped: boolean; bypassed: boolean; durationMs: number }; gateConfig: { targetHits: number; cadenceMs: number }; compat: { ultrawideDfovMeasuredDeg: number }; user: { name: string; email: string; age: number | null; gender: string | null; consentPresent: boolean }; appVersion: string; }): CaptureSessionOpts`. Assemble exactly the `<interfaces>` shape: `contributor: { name: args.user.name, email: args.user.email, age: args.user.age, gender: coerceGender(args.user.gender), consent: true }` — but FIRST `if (!args.user.consentPresent) throw new Error('Cannot start a capture session without recorded consent');` (V11 — never default `consent: true` without the consent state being present). `coerceGender(g)` → returns `g` if it's one of `'male'|'female'|'non-binary'|'prefer-not-to-say'`, else `null`. `startGate: { type: 'hand_detection', passed: args.gate.passed, skipped: args.gate.skipped, bypassed: args.gate.bypassed, durationMs: Math.max(0, Math.round(args.gate.durationMs)), consecutiveHitsRequired: args.gateConfig.targetHits, platformCadenceMs: args.gateConfig.cadenceMs }`. `location: null`. `appVersion: args.appVersion`. `dfovDegrees: args.compat.ultrawideDfovMeasuredDeg`. Top docstring: "Assembles the Phase-3 D-API-02 CaptureSessionOpts for HumynCapture.start(). The Kotlin side re-validates with the same Zod shape (defense-in-depth across the JS↔Kotlin trust boundary, T-3.3-01) — this builder MUST produce a shape that passes CaptureSessionOptsSchema. Never default contributor.consent — source it from the verified /me consent state (V11)."
    3. `apps/mobile/__tests__/lib/buildCaptureOpts.test.ts` — `import { CaptureSessionOptsSchema } from '@humyn/shared-types';` then assert: `CaptureSessionOptsSchema.parse(buildCaptureOpts(<valid args>))` does NOT throw (the assembled opts parse OK — both practice and non-practice); `buildCaptureOpts({ ...args, user: { ...args.user, consentPresent: false } })` THROWS `/without recorded consent/`; `gender: 'something-weird'` → coerced to `null` in the output; `gate.durationMs: 1500.7` → `1501` (rounded); `appVersion: '1.0.0-apk'` parses (the semver-with-suffix regex). Also test `readGateConfig()` returns the defaults when `remoteConfig` is mocked to throw (mock `@react-native-firebase/remote-config`).
  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/lib/buildCaptureOpts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix apps/mobile test -- --run __tests__/lib/buildCaptureOpts.test.ts` exits 0
    - `apps/mobile/src/lib/buildCaptureOpts.ts` exists, exports `buildCaptureOpts`, contains `consent: true` and a throw when consent is absent (grep: `without recorded consent`) and `type: 'hand_detection'`
    - `apps/mobile/src/lib/remoteConfigGate.ts` exists, exports `readGateConfig`, contains `gate.consecutive_hits_required`, `gate.cadence_ms`, `gate.min_hand_detection_confidence`, and the fallback values `5`, `400`, `0.5`
    - the test asserts `CaptureSessionOptsSchema.parse(buildCaptureOpts(...))` does not throw and that `consentPresent:false` makes it throw
    - the full suite `npm --prefix apps/mobile test -- --run` exits 0
  </acceptance_criteria>
  <done>remoteConfigGate.ts (HAND-11 reads + fallbacks) + buildCaptureOpts.ts (D-API-02 shape, consent never defaulted) + test landed; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useHandGate poll loop + the VC <Camera> mount + HAND-12 pre-warm + HAND-08/HAND-07 paths + cacheDir sweep + handGate test</name>
  <behavior>
    - when RecordingScreen reaches the `gate` substate and `isHandDetectorAvailable() === false` → immediately `dispatch({ type: 'GATE_BYPASS' })` + `logEvent('recording_gate_bypassed', { locale })` — no camera poll loop, no voice cue (HAND-08, same UX as Skip)
    - when `isHandDetectorAvailable() === true`: the `<Camera>` is mounted with `isActive={substate==='gate'}`, `photo={true}`, `photoQualityBalance="speed"`, `device` = the ultrawide device (`getAvailableCameraDevices().find(d => d.id === cameraId)` first, falling back to `useCameraDevice('back', { physicalDevices: ['ultra-wide-angle-camera'] })`), `onInitialized` → `dispatch({ type: 'CAMERA_READY' })` (gate.loading → waiting — HAND-06) and then a single throwaway `takePhoto({flash:'off', enableShutterSound:false})` is fired and discarded (HAND-12 pre-warm)
    - `useHandGate` runs a recursive-`setTimeout` loop at `gate.cadenceMs` ms WHILE `substate==='gate' && gate.phase==='waiting'`: each tick → `camRef.current.takePhoto({flash:'off', enableShutterSound:false})` → `await RNFS.mkdir(cacheDir/hand-gate).catch(()=>{})` → `await RNFS.moveFile(photo.path, ${cacheDir}/hand-gate/${ulid()}.jpg)` → `const count = await detectHands(dest, minConf).catch(() => 0)` → `dispatch(count === 2 ? { type: 'GATE_HIT' } : { type: 'GATE_MISS' })` → `RNFS.unlink(dest).catch(()=>{})` in a `finally`. The loop is cleared on transition out of `gate.waiting` / unmount.
    - `gate.phase` reaching `'confirmed'` (via target hits, Skip, or bypass) stops the loop and triggers the gate-pass transition (Task 3)
    - on RecordingScreen mount: a one-shot `RNFS.readDir(cacheDir/hand-gate).then(files => files.forEach(f => RNFS.unlink(f.path).catch(()=>{})))` sweep clears stragglers from a crashed previous gate session (in addition to the Phase-3 app-launch sweep — Security V8/V12, don't leave camera frames around)
    - tapping Skip → `dispatch({ type: 'GATE_SKIP' })` + `logEvent('recording_gate_skipped', { locale })` (HAND-07/HAND-14 — silent, no voice/haptic; the brightness drop still happens in Task 3's transition for the skipped case)
  </behavior>
  <read_first>
    - apps/mobile/src/screens/recording/recState.ts (the actions `CAMERA_READY`, `GATE_HIT`, `GATE_MISS`, `GATE_SKIP`, `GATE_BYPASS`; the `gate` sub-state)
    - apps/mobile/src/native/HumynHandDetector.ts (`detectHands(path, minConfidence)`, `isHandDetectorAvailable()`)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § Pattern 1 (the photo-to-disk poll loop — the verbatim recipe; the `photoQualityBalance="speed"` <Camera> prop note; `takePhoto({flash,enableShutterSound})` returns `PhotoFile{path,...}`) + § Pitfall 9 (the HAND-12 pre-warm — fire one throwaway takePhoto after onInitialized; bind takePhoto behind onInitialized) + § Pitfall 10 (`photoQualityBalance="speed"` to keep the photo small) + § Open Question 1 (the VC device-matching-by-cameraId — try id first, fall back to physicalDevices) + § Security V8/V12 (cacheDir cleanup) + § "Code Examples — Gate poll loop" (the verbatim JS)
    - apps/mobile/src/screens/compat/CompatRunningScreen.tsx (the recursive-timer + ref-cleanup discipline — `imuTickRef` + `stopImuTick()` + the `cancelled` ref)
    - apps/mobile/vitest.setup.ts (the `react-native-vision-camera` mock from plan 04-01 — `Camera.takePhoto` resolves a `{path}` ; `useCameraDevice`/`getAvailableCameraDevices` return the stub device; the test uses these)
    - apps/mobile/src/state/keys.ts (`KEYS.COMPAT_LAST_RESULT`) + apps/mobile/src/state/mmkv.ts (read `compat.lastResult.v1` for `checks.ultrawideDfov.cameraId` + `.measuredDeg`)
    - apps/mobile/src/util/analytics.ts (`logEvent` — the recording-gate events were added in plan 04-06)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md § "src/screens/recording/useHandGate.ts (optional split)"
  </read_first>
  <action>
    1. `apps/mobile/src/screens/recording/useHandGate.ts` — NEW. Export `useHandGate(args: { active: boolean; cadenceMs: number; minConfidence: number; camRef: React.RefObject<{ takePhoto(opts: { flash: 'off'; enableShutterSound: boolean }): Promise<{ path: string }> }>; dispatch: (a: { type: 'GATE_HIT' | 'GATE_MISS' }) => void; })`. Body: a `useEffect` keyed on `[args.active, args.cadenceMs]`: when `args.active` (i.e. `substate==='gate' && gate.phase==='waiting'`), run a recursive `setTimeout(tick, args.cadenceMs)` where `tick` does the photo→move→detectHands→dispatch→unlink-in-finally sequence (the verbatim 04-RESEARCH Pattern 1 recipe; use a small inline ULID or `react-native-uuid`'s v4 for the temp filename — re-check whether `react-native-uuid` is in deps; it is); a `cancelled` ref + the `setTimeout` ref are cleared in the cleanup. Top docstring citing HAND-03/HAND-04/HAND-13 + Pattern 1.
    2. In `apps/mobile/src/screens/recording/RecordingScreen.tsx` (modifying the plan-04-07 shell):
       - On mount: read `compat.lastResult.v1` from MMKV → `const cameraId = parsed?.checks?.ultrawideDfov?.cameraId ?? null; const dfovMeasuredDeg = parsed?.checks?.ultrawideDfov?.measuredDeg ?? null;` (D-CAM-03). Call `Orientation.lockToLandscape()` (REC-01) and add the device/UI orientation listeners (the UI listener dispatches `LANDSCAPE_DETECTED` on `LANDSCAPE-LEFT`/`LANDSCAPE-RIGHT` to leave `rotate-prompt`; the device listener is handled by `useRecordingLifecycle`). Read the gate config: `const [gateCfg] = useState(() => ({ targetHits: 5, cadenceMs: 400, minHandDetectionConfidence: 0.5 })); useEffect(() => { readGateConfig().then(setGateCfg); }, [])` — and pass `{ targetHits: gateCfg.targetHits, cadenceMs: gateCfg.cadenceMs }` into `initialRecState(routeParams, ...)` (or dispatch an action that updates `gate.targetHits`/`gate.cadenceMs` once RC resolves — re-read recState; if `initialRecState` takes `gateConfig`, prefer reading RC synchronously-defaulted then updating). Fire the cacheDir/hand-gate sweep on mount (`RNFS.readDir(${cacheDir}/hand-gate).then(fs => fs.forEach(f => RNFS.unlink(f.path).catch(()=>{}))).catch(()=>{})`).
       - The `<Camera>` JSX in the `gate` substate: `const ultrawide = getAvailableCameraDevices().find(d => d.id === cameraId) ?? useCameraDevice('back', { physicalDevices: ['ultra-wide-angle-camera'] })` — but note `useCameraDevice` is a hook so it must be called unconditionally at the top; do `const fallbackDevice = useCameraDevice('back', { physicalDevices: ['ultra-wide-angle-camera'] }); const devices = useCameraDevices(); const ultrawide = useMemo(() => devices.find(d => d.id === cameraId) ?? fallbackDevice, [devices, cameraId, fallbackDevice]);` — re-check the VC 4.7.3 hook API. Render `<Camera ref={camRef} device={ultrawide} isActive={substate === 'gate'} photo={true} photoQualityBalance="speed" onInitialized={onCameraInitialized} style={...} />`. `onCameraInitialized` → `dispatch({ type: 'CAMERA_READY' })` then `camRef.current?.takePhoto({ flash: 'off', enableShutterSound: false }).then(p => { /* discard — HAND-12 pre-warm */ RNFS.unlink(p.path).catch(()=>{}); }).catch(()=>{})`.
       - On entering `gate`: `logEvent('recording_gate_started', { locale: <device locale> })`. If `!isHandDetectorAvailable()` → `dispatch({ type: 'GATE_BYPASS' })` + `logEvent('recording_gate_bypassed', { locale })` immediately (HAND-08).
       - Mount `useHandGate({ active: substate === 'gate' && state.gate.phase === 'waiting', cadenceMs: state.gate.cadenceMs, minConfidence: gateCfg.minHandDetectionConfidence, camRef, dispatch })`.
       - The Skip link's `onPress` (already in the shell from 04-07) → `dispatch({ type: 'GATE_SKIP' })` + `logEvent('recording_gate_skipped', { locale })` (HAND-07/HAND-14 — silent). The HAND-14 event MUST NOT include image data — `locale` (`en-IN`/`pt-BR`) is the cohort dimension; optionally a coarse average-Y-luminance proxy if cheap to compute, never the frame.
       - On unmount (the `useEffect` cleanup): `Orientation.unlockAllOrientations()` + `HumynScreenBrightness.set(-1)` (REC-08 — restore brightness on exit even via an abnormal path; Pitfall 6) + `useHandGate`'s own cleanup handles the poll loop.
    3. `apps/mobile/__tests__/screens/recording/handGate.test.tsx` — render RecordingScreen (or `useHandGate` in isolation via a tiny test component) with `react-native-vision-camera` + `react-native-fs` + `HumynHandDetector` mocked + fake timers. Assert: (a) `isHandDetectorAvailable()` mocked to `false` → `GATE_BYPASS` dispatched (the rendered state moves to gate.confirmed-bypassed) + `logEvent('recording_gate_bypassed', ...)` fired; (b) with the detector available: advancing the fake clock by `cadenceMs` runs one tick — `takePhoto` called, `RNFS.moveFile` called with a `cacheDir/hand-gate/*.jpg` dest, `detectHands(dest, minConf)` called, `RNFS.unlink(dest)` called in finally; (c) `detectHands` mocked to resolve `2` → `GATE_HIT` dispatched; mocked to resolve `1` → `GATE_MISS` dispatched; mocked to reject → treated as `GATE_MISS` (count 0); (d) after `targetHits` consecutive `2`s → `gate.phase` reaches `'confirmed'` and the poll loop stops; (e) Skip tap → `GATE_SKIP` + `logEvent('recording_gate_skipped', ...)` with NO image-data prop; (f) on mount the `cacheDir/hand-gate` sweep (`RNFS.readDir` then `unlink` per file) runs; (g) `onInitialized` → `CAMERA_READY` + one throwaway `takePhoto` (HAND-12) — assert `takePhoto` called at least once after `onInitialized` before any GATE_HIT. Mirror `CompatRunningScreen.test.tsx` for the fake-timer + ref-cleanup parts.
  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/screens/recording/handGate.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix apps/mobile test -- --run __tests__/screens/recording/handGate.test.tsx` exits 0
    - `apps/mobile/src/screens/recording/useHandGate.ts` exists and contains `detectHands`, `moveFile`, `unlink`, and `hand-gate`
    - `apps/mobile/src/screens/recording/RecordingScreen.tsx` contains `Camera`, `photoQualityBalance`, `onInitialized`, `isHandDetectorAvailable`, `GATE_BYPASS`, `ultrawideDfov`, `lockToLandscape`, `recording_gate_skipped`, and `recording_gate_started`
    - `RecordingScreen.tsx` contains `HumynScreenBrightness.set(-1)` (the on-unmount restore) — grep
    - `handGate.test.tsx` has a `recording_gate_skipped` assertion that the analytics props do NOT include any `image`/`frame`/`bitmap` key
    - the full suite `npm --prefix apps/mobile test -- --run` exits 0
  </acceptance_criteria>
  <done>useHandGate poll loop + VC <Camera> mount on the compat-chosen ultrawide lens + HAND-12 pre-warm + HAND-08 bypass + HAND-07 Skip + cacheDir sweep + orientation lock + brightness-restore-on-unmount + handGate test landed; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: gate-pass→active TTS-masked transition + HumynCapture.start + post-stop §7h routing + useRecordingLifecycle mount + route-registry.test.ts PracticeIntro/PracticeComplete additions + RecordingScreen.test.tsx</name>
  <read_first>
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § Pattern 2 (the TTS-masked camera handoff — the VERBATIM sequence: dispatch GATE_CONFIRMED → if passed Vibration.vibrate(80) + Tts.speak('Recording started.') → HumynScreenBrightness.set(0.05) → setCameraActive(false) → await settle (~80ms, TUNABLE) → try HumynCapture.start(opts) → CAPTURE_STARTED, catch → Tts.speak(error line) + set(-1) + CAPTURE_START_FAILED + toast) + § "Code Examples — Gate-pass → active transition" (the `enterActive` recipe) + § Pitfall 1 (the ±1ms drift re-measurement on the new handoff — the settle delay is the tuning knob; [BLOCKING] manual gate in plan 04-10) + § Pitfall 6 (brightness restore on stop AND unmount) + the design-spec §7h post-stop routing
    - design-spec.md §7d (VoiceCue pill 1.8s) + §7g/§7h (post-stop routing: practice → Practice-done screen; real ≥60s → toast "{Hh Mm} added to your contribution." + Home; real <60s → toast "Recording too short — discarded." + Ready substate) + §18.2 (Stop confirm modal) + UI-SPEC § "Error / failure states" (the exact toast/voice strings: thermal_throttling → voice "Phone too warm" → Ready; permission_revoked → re-prompt; storage_full → "Not enough storage to record" modal; "{Hh Mm} added to your contribution." e.g. "11m added to your contribution.")
    - apps/mobile/src/screens/recording/useRecordingLifecycle.ts (the hook from plan 04-08 — the call signature: `useRecordingLifecycle({ substate, isPractice, durationMs, callbacks: { onStop, showToast, voiceCue, setAlert } })` returning `{ checkStartGuards }`)
    - apps/mobile/src/native/HumynCapture.ts (`start(opts)` resolves `{sessionId,segmentId,recordingId,filenameBase}`, rejects `Error(code:'thermal_throttling'|'permission_revoked'|'storage_full'|'internal_error')`; `stop()` resolves after the final segment finalizes; `onSessionStop` event) + `HumynScreenBrightness.set` + `apps/mobile/src/lib/ttsVoice.ts` (`speakCue` + `pickAndSetEnInVoice`) + `apps/mobile/src/lib/buildCaptureOpts.ts` + `apps/mobile/src/lib/durationFormat.ts` (`formatContributionDuration`) + `apps/mobile/src/native/AppFlavor.ts` (for `appVersion`)
    - apps/mobile/src/state/appStore.ts (`user` slice — `name`, `email`, `age`, `gender`, `consent`; also any `setContrib`/contribution-increment action — REC-04 says "update `contrib`"; if there's a `setContribution`/`incrementContribution` action use it, else just route to Home and let the next /me fetch refresh it)
    - apps/mobile/__tests__/native/HumynCapture.test.ts (the `VALID_OPTS` fixture shape buildCaptureOpts mirrors) + apps/mobile/__tests__/screens/compat/CompatRunningScreen.test.tsx (the screen-test mocking conventions)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md § "RecordingScreen.tsx (screen, owns reducer + VC + transition)"
    - apps/mobile/__tests__/navigation/route-registry.test.ts (the locked Pattern-54 invariant — plan 04-07 already created a `REQUIRED_PHASE_4_ROUTES = ['Recording']` block; this plan ADDS `'PracticeIntro'` and `'PracticeComplete'` to it. Those two OnboardingStack routes were registered by plan 04-06 — re-read `apps/mobile/src/navigation/OnboardingStack.tsx` to confirm both `name="PracticeIntro"` and `name="PracticeComplete"` are present. This plan depends_on both 04-06 and 04-07, so both the routes AND 04-07's edit to this test are already in place when this runs. Do NOT touch the `phase3Plus` list or the `'Recording'` entry — 04-07 owns those.)
    - NOTE for the executor: Task 3 is a dense integration task — hold the whole gate-pass→active→stop→route flow in working context while you implement it (the §7h routing, `buildCaptureOpts` wiring, `useRecordingLifecycle` mount, and start-guard wiring all interlock). Implement + run the focused test after each sub-step rather than at the end.
  </read_first>
  <action>
    Continue modifying `apps/mobile/src/screens/recording/RecordingScreen.tsx`:
    1. **gate-pass → active transition** — when `state.gate.phase` becomes `'confirmed'` (via a `useEffect` keyed on `state.gate.phase`), run the transition (Pattern 2 verbatim): `const passed = !state.gate.skipped && !state.gate.bypassed;` → `if (passed) { Vibration.vibrate(80); speakCue('Recording started'); setVoiceCue({ text: 'Recording started', visible: true }); setTimeout(() => setVoiceCue(v => ({ ...v, visible: false })), 1800); logEvent('recording_gate_passed', { locale }); logEvent('recording_started'); }` → `await HumynScreenBrightness.set(0.05);` (REC-08 / HAND-07/HAND-09 — even on skip/bypass) → `setCameraActive(false)` (drive `isActive={false}` on the `<Camera>` — there's no `unmount()`) → `await new Promise(r => setTimeout(r, SETTLE_MS))` where `const SETTLE_MS = 80; // [TUNABLE — re-measure ±1ms drift on the gate→record handoff in plan 04-10's smoke; bump if HumynCapture.start() stalls]` → `try { const r = await HumynCapture.start(buildCaptureOpts({ taskId: routeParams.taskId, taskName: routeParams.taskName, taskCategory: routeParams.taskCategory ?? 'practice', taskSetting: routeParams.taskSetting ?? 'indoor', isPractice: routeParams.isPractice, gate: { passed, skipped: state.gate.skipped, bypassed: state.gate.bypassed, durationMs: (state.gate.confirmedAt ?? 0) - (state.gate.startedAt ?? 0) }, gateConfig: { targetHits: state.gate.targetHits, cadenceMs: state.gate.cadenceMs }, compat: { ultrawideDfovMeasuredDeg: dfovMeasuredDeg ?? 0 }, user: { name: appStore.user?.name ?? '', email: appStore.user?.email ?? '', age: appStore.user?.age ?? null, gender: appStore.user?.gender ?? null, consentPresent: appStore.user?.consent != null }, appVersion: AppFlavor.getConstants().BuildConfig?.VERSION_NAME ?? '1.0.0' })); dispatch({ type: 'CAPTURE_STARTED', now: performance.now(), ...r }); } catch (e) { const code = (e as any)?.code; speakCue(code === 'thermal_throttling' ? 'Phone too warm' : code === 'permission_revoked' ? 'Camera permission needed' : 'Could not start recording'); await HumynScreenBrightness.set(-1); showToast(code === 'storage_full' ? 'Not enough storage to record.' : code === 'thermal_throttling' ? 'Phone too warm — let it cool and try again.' : 'Could not start recording.'); dispatch({ type: 'CAPTURE_START_FAILED', error: e }); }`. Call `pickAndSetEnInVoice()` once on RecordingScreen mount (or at `ready`-substate enter) so the TTS voice is selected before the first `speakCue`.
    2. **active duration TICK** — while `substate==='active'`, a `setInterval` (~250ms) dispatches `{ type: 'TICK', durationMs: performance.now() - (state.startedAt ?? performance.now()) }` so the timer + minute-bar update; cleared on leaving `active`.
    3. **post-stop §7h routing** — the `onStop` callback passed to `useRecordingLifecycle` AND the stop-button / modal-Stop handlers all funnel through one `handleStop(reason)` function: `if (state.ended) return; await HumynCapture.stop().catch(()=>{}); await HumynScreenBrightness.set(-1); Orientation.unlockAllOrientations();` then route: `if (routeParams.isPractice) { logEvent('recording_stopped'); speakCue('Recording stopped'); navigation.replace('PracticeComplete'); }` (practice — sub-60s practice STILL routes to PracticeComplete, no minimum; use `navigation.getParent?.()` if PracticeComplete is in OnboardingStack — re-read OnboardingStack: PracticeComplete is there, so `navigation.replace` won't reach it from the RootNativeStack `Recording` route — instead `navigation.reset` or navigate via the parent; the cleanest is `navigation.getParent?.()?.reset({ index: 0, routes: [{ name: 'OnboardingStack', state: { routes: [{ name: 'PracticeComplete' }] } }] })` OR — simpler — register `PracticeComplete` ALSO as a RootNativeStack sibling? No — keep it in OnboardingStack per CONTEXT D-NAV-04. Use `navigation.getParent?.()` to navigate within OnboardingStack. Re-read the navigators + RESEARCH § Pattern 5 for the exact API; the goal is: practice exit → PracticeCompleteScreen). `else if (state.durationMs >= 60_000) { logEvent('recording_stopped'); speakCue('Recording stopped'); showToast(\`${formatContributionDuration(state.durationMs)} added to your contribution.\`); /* optionally appStore.incrementContribution(state.durationMs) if such an action exists; else the next /me fetch refreshes */ navigation.getParent?.()?.navigate?.('MainTabs') ?? navigation.navigate('MainTabs'); }` (real ≥60s → toast + Home). `else { logEvent('recording_too_short'); showToast('Recording too short — discarded.'); dispatch({ type: 'RESET_FOR_FRESH' }); }` (real <60s — discarded; the file deletion is owned by HumynCapture per REC-07 — confirm via Phase 3 03-CONTEXT D-FS-*: if HC discards sub-60s segments at finalize, the screen just shows the toast; if NOT, escalate to Phase 3 — add a note in the SUMMARY; per 04-RESEARCH Open Q 3 + Assumption A10, read 03-CONTEXT D-FS-* and document the outcome). The `onSegmentComplete` (10-min auto-segment cut) and `onSegmentStart` (next segment) events are SILENT — no gate re-run, no voice cue (CAP-10 / D-SEG-01); subscribe to them only to keep `recordingId`/`filenameBase` current for telemetry.
    4. **X-button handler** — `if (substate === 'active') dispatch({ type: 'X_PRESSED' })` (→ stop-confirm modal, REC-06); else (`rotate-prompt`/`ready`/`gate`) `navigation.goBack()` silently (HAND-10 — pre-record exit, no confirmation, no data to discard; clean up: `HumynScreenBrightness.set(-1)` if brightness was dropped, `Orientation.unlockAllOrientations()`). The StopConfirmModal's `onStop` → `handleStop('manual')`; `onKeepRecording` → `dispatch({ type: 'STOP_CONFIRM_CANCEL' })`. The active-substate stop button → `handleStop('manual')`.
    5. **start guards** — when the user taps the record button (`ready` → `START_PRESSED` → `pre-flight`), in the `pre-flight` substate's effect: `const g = await checkStartGuards(); if (g.blocked) { showToast(g.toast); dispatch({ type: 'PRE_FLIGHT_FAILED' }); return; }` (REC-16 storage <5GB / battery <5% — refuse, return to ready) `else dispatch({ type: 'PRE_FLIGHT_OK', now: performance.now() })` (→ gate substate). (Pre-record thermal is owned by HumynCapture's `start()` reject — handled in the gate-pass transition's catch; the pre-flight here covers storage + battery.)
    6. **mount useRecordingLifecycle** — `const { checkStartGuards } = useRecordingLifecycle({ substate: stateSubstate, isPractice: routeParams.isPractice, durationMs: state.durationMs, callbacks: { onStop: (reason) => handleStop(reason), showToast: (t) => setToast({ text: t, visible: true }), voiceCue: (t) => { speakCue(t); setVoiceCue({ text: t, visible: true }); setTimeout(() => setVoiceCue(v => ({ ...v, visible: false })), 1800); }, setAlert: (which, on) => dispatch(on ? { type: which === 'battery' ? 'BATTERY_ALERT' : 'THERMAL_ALERT' } : { type: 'CLEAR_ALERT', which }) } })`. (If `recState` doesn't have a `CLEAR_ALERT` action, just leave the alert flag set until the recording stops — re-read recState; the alerts are overlay flags, they don't need clearing mid-record.)
    7. **RecordingScreen.test.tsx** (extend the plan-04-07 render test) — mock all the natives + libs (`HumynCapture` `start`/`stop`/`onSessionStop`/`onSegmentComplete`/`onError`/`onThermalAbort`, `HumynScreenBrightness.set`, `HumynHandDetector`, `react-native-vision-camera`, `react-native-tts`, `react-native-fs`, `react-native-orientation-locker`, `react-native`'s `Vibration`, `@react-native-firebase/remote-config`, `appStore`) + fake timers + navigation. Assert: (a) on mount → `Orientation.lockToLandscape()` called; (b) on `gate.phase` → `'confirmed'` (passed): `Vibration.vibrate(80)` + `speakCue('Recording started')` (via the mocked Tts.speak) + `HumynScreenBrightness.set(0.05)` + `HumynCapture.start(<opts that pass CaptureSessionOptsSchema>)` called → `CAPTURE_STARTED` → substate `'active'`; (c) `HumynCapture.start` mocked to reject `{code:'thermal_throttling'}` → `HumynScreenBrightness.set(-1)` + `showToast` + substate back to `'ready'`; (d) gate confirmed via Skip (`gate.skipped`) → NO `Vibration.vibrate(80)`, NO `speakCue('Recording started')`, but `HumynScreenBrightness.set(0.05)` STILL called + `HumynCapture.start` STILL called (HAND-07); (e) practice recording (`isPractice:true`) stopped → `HumynCapture.stop()` + navigation toward `PracticeComplete` (assert the route arg); (f) real recording stopped with `durationMs >= 60_000` → `showToast` containing `formatContributionDuration(durationMs)` (e.g. a string ending `added to your contribution.`) + navigation toward `MainTabs`; (g) real recording stopped with `durationMs < 60_000` → `showToast('Recording too short — discarded.')` + `RESET_FOR_FRESH` (substate back to `ready`, REC-05); (h) on unmount → `HumynScreenBrightness.set(-1)` + `Orientation.unlockAllOrientations()` called; (i) `checkStartGuards` mocked to return `{ blocked: true, toast: 'Not enough storage to record.' }` → after `START_PRESSED`/`pre-flight`, `showToast('Not enough storage to record.')` + substate back to `ready` (REC-16). Mirror `CompatRunningScreen.test.tsx`.
    8. **route-registry.test.ts** — extend the `REQUIRED_PHASE_4_ROUTES` array that plan 04-07 created (currently `['Recording']`) to `['Recording', 'PracticeIntro', 'PracticeComplete']` — those two OnboardingStack routes were registered by plan 04-06. Update the block's comment to: "Phase 4 — Recording (RootNativeStack, plan 04-07) + PracticeIntro/PracticeComplete (OnboardingStack, plan 04-06); the PracticeIntro/PracticeComplete entries are added by plan 04-09 because 04-06 and 04-07 are in the same plan-wave and 04-09 depends on both. Removal of any of the three from the navigators fails CI (D-NAV-01 / Pattern 54)." Do NOT touch the `phase3Plus` list or the existing `'Recording'` entry — plan 04-07 owns those; this is purely an additive change to the array + its comment. Run `npm --prefix apps/mobile test -- --run __tests__/navigation/route-registry.test.ts` to confirm green.
  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/screens/RecordingScreen.test.tsx __tests__/screens/recording/handGate.test.tsx __tests__/navigation/route-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix apps/mobile test -- --run __tests__/screens/RecordingScreen.test.tsx __tests__/screens/recording/handGate.test.tsx __tests__/navigation/route-registry.test.ts` exits 0
    - `apps/mobile/src/screens/recording/RecordingScreen.tsx` contains `HumynCapture.start(`, `buildCaptureOpts`, `HumynScreenBrightness.set(0.05)`, `HumynScreenBrightness.set(-1)`, `Vibration.vibrate(80)`, `speakCue`, `useRecordingLifecycle`, `formatContributionDuration`, `pickAndSetEnInVoice`, `RESET_FOR_FRESH`, and `SETTLE_MS` (the tunable settle delay with the drift-re-measurement comment)
    - the file contains the toast string `added to your contribution.` and `Recording too short — discarded.`
    - the file's gate-pass transition gates `Vibration.vibrate(80)` + `speakCue('Recording started')` behind `passed` (i.e. NOT fired on skip/bypass) — grep: `passed` appears before `Vibration.vibrate(80)`
    - the `onSegmentComplete` / `onSegmentStart` handlers are SILENT (no `speakCue`/no gate re-run — grep: the file does NOT call `dispatch` with a gate-reset action inside an `onSegmentComplete`/`onSegmentStart` handler)
    - `RecordingScreen.test.tsx` asserts the Skip path does NOT fire `Vibration.vibrate(80)` but DOES fire `HumynScreenBrightness.set(0.05)`
    - `apps/mobile/__tests__/navigation/route-registry.test.ts` `REQUIRED_PHASE_4_ROUTES` contains `'Recording'`, `'PracticeIntro'`, AND `'PracticeComplete'` (this plan added the latter two); `'Recording'` is NOT in any must-be-absent set
    - the full suite `npm --prefix apps/mobile test -- --run` exits 0
  </acceptance_criteria>
  <done>The full gate-pass→active TTS-masked transition + HumynCapture.start(buildCaptureOpts) + §7h post-stop routing + start guards + useRecordingLifecycle mount + the PracticeIntro/PracticeComplete additions to route-registry.test.ts's REQUIRED_PHASE_4_ROUTES block + the comprehensive RecordingScreen test landed; full suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                                    | Description                                                                                        |
| --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| camera frame JPEG → cacheDir                                                | gate frames (may contain hands/faces) written to `cacheDir/hand-gate/{ulid}.jpg`                   |
| JS → `HumynCapture.start(opts)`                                             | the CaptureSessionOpts map crosses into the native module (re-validated Kotlin-side)               |
| MMKV `compat.lastResult.v1` → lens selection + dfovDegrees                  | the gate uses the compat-chosen ultrawide lens                                                     |
| `appStore.user` (verified /me data) → `contributor` in the capture metadata | `consent` must be sourced honestly                                                                 |
| Camera2 ownership handoff (VC → HumynCapture)                               | a new moment where another process just released the camera — the ±1ms drift invariant is at stake |

## STRIDE Threat Register

| Threat ID | Category                                                 | Component                                                         | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------- | -------------------------------------------------------- | ----------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-4.9-01  | Capture-quality invariant (the project's non-negotiable) | the VC→HumynCapture camera handoff under a TTS-masked time budget | mitigate    | the `SETTLE_MS` delay before `HumynCapture.start()` is a tunable constant with a drift-re-measurement comment; plan 04-10's smoke runbook re-measures `onSegmentComplete.drift.{max,mean,p99}` on Pixel-class hardware for a gate-started recording (must not regress Phase 3 smoke 7's mean 0.594 / p99 0.728 ms) — this is a `[BLOCKING]` manual gate. If it regresses, bump `SETTLE_MS` or escalate a "HC.start() polls for camera availability" change to Phase 3. (CLAUDE.md banner — re-introducing audio is forbidden; this plan does not touch audio.) |
| T-4.9-02  | Information disclosure                                   | camera-frame JPEGs left in `cacheDir/hand-gate/`                  | mitigate    | written to `cacheDir` (internal storage, not external, not in backup); `RNFS.unlink(dest)` in the poll loop's `finally` (delete on each gate-check resolve); a mount-time sweep + the Phase-3 app-launch sweep clear stragglers; the HAND-12 pre-warm photo is also unlinked. The frames are never logged. (Security V8/V12.)                                                                                                                                                                                                                                  |
| T-4.9-03  | Information disclosure                                   | HAND-14 telemetry                                                 | mitigate    | `recording_gate_skipped` carries `locale` (the cohort dimension) and at most a coarse average-Y-luminance proxy — NEVER image data (that's PII per the analytics.ts PII guard). The test asserts no `image`/`frame`/`bitmap` prop. (Pitfall 8/12.)                                                                                                                                                                                                                                                                                                             |
| T-4.9-04  | Spec evasion (consent bypass in metadata)                | `buildCaptureOpts` constructing `contributor.consent`             | mitigate    | `buildCaptureOpts` THROWS if `consentPresent` is false — `consent: true` is never defaulted; it's sourced from the verified `/me` consent state. The Phase-3 Kotlin side also re-validates with `z.literal(true)` (defense-in-depth, T-3.3-01).                                                                                                                                                                                                                                                                                                                |
| T-4.9-05  | DoS (resource exhaustion / leaks)                        | the gate poll loop / Camera mount / timers                        | mitigate    | `photoQualityBalance="speed"` keeps the photo small (Pitfall 10 — paired with the Kotlin RGB_565/320×240/recycle); the poll `setTimeout` + the TICK `setInterval` + the VoiceCue timers are cleared on substate change / unmount; the `<Camera>` is `isActive={false}` outside the gate substate (releases Camera2); `useRecordingLifecycle` (plan 04-08) handles its own subscription teardown.                                                                                                                                                               |
| T-4.9-06  | Spec violation                                           | concurrent camera access                                          | mitigate    | strictly sequential: `<Camera isActive={false}>` (VC releases Camera2) → `await SETTLE_MS` → `HumynCapture.start()` (HC opens Camera2). The transition code never holds both.                                                                                                                                                                                                                                                                                                                                                                                  |

</threat_model>

<verification>
- `npm --prefix apps/mobile test -- --run` exits 0 (buildCaptureOpts, handGate, RecordingScreen, and the pre-existing suite green).
- `RecordingScreen.tsx` grep-clean: contains `HumynCapture.start(`/`buildCaptureOpts`/`set(0.05)`/`set(-1)`/`Vibration.vibrate(80)`/`speakCue`/`useRecordingLifecycle`/`SETTLE_MS`; the Skip path doesn't fire the vibrate/TTS-cue but does drop brightness.
- `buildCaptureOpts(...)` produces a shape that `CaptureSessionOptsSchema.parse` accepts; throws when consent is absent.
- The 10-min auto-segment cut handlers are silent (no gate re-run — CAP-10).
- `route-registry.test.ts` `REQUIRED_PHASE_4_ROUTES` now lists `'Recording'`, `'PracticeIntro'`, `'PracticeComplete'`; `route-registry.test.ts` exits 0.
</verification>

<success_criteria>

- The recording surface fully wired: VC `<Camera>` on the compat-chosen ultrawide lens, HAND-12 pre-warm, the `useHandGate` poll loop, HAND-08 bypass / HAND-07 Skip, the HAND-09 TTS-masked gate-pass→active transition, `HumynCapture.start(buildCaptureOpts(...))` (the full D-API-02 shape, consent never defaulted), HAND-11 RemoteConfig gate reads, HAND-14 analytics (no image data), REC-01 orientation lock, REC-08 brightness restore on stop+unmount, the §7h post-stop routing, REC-05 re-press-starts-fresh, REC-16 start guards, and the `useRecordingLifecycle` mount.
- Comprehensive RecordingScreen + handGate + buildCaptureOpts tests.
- `route-registry.test.ts`'s `REQUIRED_PHASE_4_ROUTES` extended with `'PracticeIntro'`/`'PracticeComplete'` (the Pattern-54 invariant now covers all three Phase-4 routes).
- The `SETTLE_MS` tunable + drift-re-measurement comment in place for plan 04-10's `[BLOCKING]` smoke gate.
- Full mobile test suite green.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-09-SUMMARY.md`
</output>
