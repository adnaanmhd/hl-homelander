---
phase: 04-handdetector-recording-ux-practice-tutorial
plan: 08
type: execute
wave: 4
depends_on: ['04-02', '04-07']
files_modified:
  - apps/mobile/src/screens/recording/useRecordingLifecycle.ts
  - apps/mobile/src/lib/ttsVoice.ts
  - apps/mobile/src/lib/durationFormat.ts
  - apps/mobile/src/screens/tasks/TasksPlaceholderScreen.tsx
  - apps/mobile/__tests__/screens/recording/useRecordingLifecycle.test.tsx
  - apps/mobile/__tests__/lib/ttsVoice.test.ts
  - apps/mobile/__tests__/lib/durationFormat.test.ts
  - apps/mobile/__tests__/screens/recording/devAffordance.test.tsx
autonomous: true
requirements:
  [REC-04, REC-07, REC-09, REC-10, REC-11, REC-12, REC-13, REC-14, REC-15, REC-16, ONB-05, ONB-06]
must_haves:
  truths:
    - "useRecordingLifecycle implements the idea-brief.md §10 policy table: AppState 'background'/'inactive' mid-record → stop(); audio-focus sustained loss / transient-loss-not-quickly-regained → stop() (call answered / alarm); audio-focus transient-loss-then-gain-within-window → no-op (call declined, REC-13); orientation device→portrait while active → stop()+toast; battery ≤15% transition → alert+continue (toast + HumynBeep.playTone('battery_alert') + Vibration.vibrate([0,100,50,100]) + Tts.speak + alerts.battery flag, NO stop); battery ≤5% transition mid-record → stop(); battery <5% at start → refuse-new (toast); storage <5GB at start (REC-16) → refuse-new (toast); HumynCapture onError({code:'storage_full'|'permission_revoked'}) → stop(); logout → stop(); HumynCapture onThermalAbort → Tts.speak + alerts.thermal flag + HumynBeep.playTone('thermal_alert') + Vibration.vibrate(800); HumynCapture.start() reject 'thermal_throttling' → toast (handled by the screen, not the hook)"
    - 'useRecordingLifecycle enforces the practice 60s hard cap: when isPractice && active && durationMs >= 60_000 → calls onStop (ONB-05); the same lifecycle alerts run for practice and real (ONB-06)'
    - "every subscription (AppState, Linking, Orientation UI + device, HumynPhoneState.onAudioFocusChanged, HumynBattery.onBatteryChanged, HumynCapture.onError/onThermalAbort, the 60s storage/battery periodic guard timer) is .remove()'d / cleared in the single useEffect cleanup"
    - 'ttsVoice.pickAndSetEnInVoice() picks the en-IN female → any en-IN → en-US female → first en-* voice (filtering notInstalled), calls Tts.setDefaultRate(1.0, true) + Tts.setDefaultPitch(0.95), and falls back gracefully to the engine default when voices() returns []'
    - "TasksPlaceholderScreen has a __DEV__-gated long-press handler on the heading that pushes 'Recording' with the hardcoded non-practice test task; the handler is undefined / the affordance absent when __DEV__ is false"
    - 'the codebase does NOT request ACCESS_NOTIFICATION_POLICY / READ_PHONE_STATE and does NOT programmatically toggle DND (REC-09)'
  artifacts:
    - path: 'apps/mobile/src/screens/recording/useRecordingLifecycle.ts'
      provides: 'the §10 policy table as a hook over AppState/orientation/audio-focus/battery/storage/HumynCapture events'
      min_lines: 120
    - path: 'apps/mobile/src/lib/ttsVoice.ts'
      provides: 'pickAndSetEnInVoice() — the REC-14 voice fallback chain'
      exports: ['pickAndSetEnInVoice']
    - path: 'apps/mobile/src/lib/durationFormat.ts'
      provides: "formatContributionDuration(ms) — '<1min→Xs', '<1hr→Xm', '≥1hr→Xh Ym' floored to previous minute (HOME-06 / REC-04 formatter)"
      exports: ['formatContributionDuration']
  key_links:
    - from: 'apps/mobile/src/screens/recording/useRecordingLifecycle.ts'
      to: 'HumynCapture.stop()'
      via: 'policy resolver → onStop callback → HumynCapture.stop()'
      pattern: 'onStop'
    - from: 'apps/mobile/src/screens/tasks/TasksPlaceholderScreen.tsx'
      to: 'Recording route'
      via: "if (__DEV__) onLongPress → navigation.push('Recording', { taskId: 'cooking_chop_vegetables', ..., isPractice: false })"
      pattern: '__DEV__'
---

<objective>
Implement the supporting modules for the live recording surface: the `useRecordingLifecycle` hook (the `idea-brief.md §10` policy table over AppState / orientation / audio-focus / battery / storage / HumynCapture-error / thermal, plus the practice 60s hard cap), the `ttsVoice.ts` en-IN voice fallback chain (REC-14), the `durationFormat.ts` HOME-06/REC-04 contribution-duration formatter, and the `__DEV__`-gated debug affordance on `TasksPlaceholderScreen` that pushes the `Recording` route with a hardcoded non-practice test task. None of these touch `RecordingScreen.tsx` (that's plan 04-09, which imports `useRecordingLifecycle` against the documented signature here).

Purpose: REC-04/REC-07 (post-stop voice cue + toast formatter), REC-09 (no DND — negative req asserted), REC-10/REC-11 (battery alert + beep + haptic + end-at-5%), REC-12/REC-13 (call answered=stop / declined=continue), REC-14/REC-15 (TTS voice + duplicated cue text), REC-16 (storage start-guard), ONB-05 (60s practice hard cap), ONB-06 (alerts active during practice).
Output: `useRecordingLifecycle.ts` + `ttsVoice.ts` + `durationFormat.ts` + `__DEV__` affordance on TasksPlaceholder + 4 tests.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-UI-SPEC.md
@CLAUDE.md
@idea-brief.md
@engineering-handoff.md
@apps/mobile/src/hooks/useForegroundUserRehydrate.ts
@apps/mobile/src/native/HumynCapture.ts
@apps/mobile/src/native/HumynPhoneState.ts
@apps/mobile/src/native/HumynBattery.ts
@apps/mobile/src/native/HumynScreenBrightness.ts
@apps/mobile/src/native/HumynBeep.ts
@apps/mobile/src/screens/recording/recState.ts
@apps/mobile/src/screens/tasks/TasksPlaceholderScreen.tsx
@apps/mobile/src/util/analytics.ts
@apps/mobile/__tests__/navigation/ForegroundRehydrate.test.tsx
@apps/mobile/__tests__/screens/TasksPlaceholderScreen.test.tsx

<interfaces>
<!-- The useRecordingLifecycle hook signature — plan 04-09's RecordingScreen calls this VERBATIM -->
type LifecycleCallbacks = {
  /** Stop the active recording. The screen wires this to HumynCapture.stop() then routes per §7h
   *  (practice → PracticeComplete; real ≥60s → toast "{Hh Mm} added to your contribution." + Home;
   *  real <60s → toast "Recording too short — discarded." + ready). */
  onStop: (reason: 'manual' | 'background' | 'phone_call' | 'alarm' | 'orientation' | 'battery_critical'
    | 'storage_full' | 'permission_revoked' | 'logout' | 'thermal' | 'practice_hard_cap') => void;
  /** Show a transient toast on the recording surface. */
  showToast: (text: string) => void;
  /** Speak a voice cue (TTS) AND duplicate it as the VoiceCue pill text (REC-15). */
  voiceCue: (text: string) => void;
  /** Set an overlay alert flag (battery/thermal) — recording continues underneath. */
  setAlert: (which: 'battery' | 'thermal', on: boolean) => void;
};
function useRecordingLifecycle(args: {
  substate: RecSubstate;          // from the recState reducer
  isPractice: boolean;
  durationMs: number;             // current active-recording duration (the screen's TICK value)
  callbacks: LifecycleCallbacks;
}): {
  /** Pre-record start guards (REC-16 storage <5GB / battery <5%): the screen calls this before
   *  dispatching START_PRESSED / before HumynCapture.start(); returns null if OK, or a refuse-reason
   *  + toast text if blocked. */
  checkStartGuards: () => Promise<{ blocked: true; toast: string } | { blocked: false }>;
};
<!-- HumynCapture event helpers (already exist) -->
import { onError, onThermalAbort, stop as captureStop } from '../../native/HumynCapture';  // each on*() returns EmitterSubscription — caller MUST .remove()
<!-- HumynPhoneState / HumynBattery (plan 04-02 bindings) -->
import { start as phoneStart, stop as phoneStop, onAudioFocusChanged } from '../../native/HumynPhoneState';
import { start as batteryStart, stop as batteryStop, onBatteryChanged } from '../../native/HumynBattery';
import { playTone } from '../../native/HumynBeep';
<!-- orientation-locker -->
import Orientation, { OrientationType } from 'react-native-orientation-locker';
<!-- storage -->
import RNFS from 'react-native-fs';  // RNFS.getFSInfo() → { totalSpace, freeSpace }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: durationFormat.ts (HOME-06/REC-04 formatter) + ttsVoice.ts (REC-14 voice fallback) + their tests</name>
  <behavior>
    - `formatContributionDuration(0)` → `'0s'`; `formatContributionDuration(45_000)` → `'45s'`; `formatContributionDuration(59_999)` → `'59s'`; `formatContributionDuration(60_000)` → `'1m'`; `formatContributionDuration(90_000)` → `'1m'` (floored); `formatContributionDuration(3_599_999)` → `'59m'`; `formatContributionDuration(3_600_000)` → `'1h 0m'`; `formatContributionDuration(3_930_000)` → `'1h 5m'` (65.5min → 1h 5m, floored to previous minute) — `<1min → Xs`, `<1hr → Xm`, `≥1hr → Xh Ym` floored to the previous minute (HOME-06 wording)
    - `pickAndSetEnInVoice()` over a mocked `Tts.voices()` returning `[{id:'en-in-x-ene-female-local', language:'en-IN', notInstalled:false}, {id:'en-us-x', language:'en-US', notInstalled:false}]` → calls `Tts.setDefaultVoice('en-in-x-ene-female-local')` (step 1, en-IN female-ish)
    - over `[{id:'en-in-x-ene-local', language:'en-IN', notInstalled:false}, {id:'en-us-x-female', language:'en-US', notInstalled:false}]` (no en-IN looks-female) → `Tts.setDefaultVoice('en-in-x-ene-local')` (step 2, any en-IN)
    - over `[{id:'en-us-x-female', language:'en-US', notInstalled:false}, {id:'fr-x', language:'fr-FR', notInstalled:false}]` → `Tts.setDefaultVoice('en-us-x-female')` (step 3, en-US female-ish)
    - over `[{id:'en-gb-x', language:'en-GB', notInstalled:false}]` → `Tts.setDefaultVoice('en-gb-x')` (step 4, first en-*)
    - `notInstalled:true` voices are filtered out before the chain
    - over `[]` (Tts.voices() returns empty) → does NOT call setDefaultVoice (falls back to the engine default), still calls `Tts.setDefaultRate(1.0, true)` + `Tts.setDefaultPitch(0.95)`
    - `pickAndSetEnInVoice()` always calls `Tts.getInitStatus()` first and `Tts.setDefaultRate(1.0, true)` + `Tts.setDefaultPitch(0.95)` regardless of voice availability
  </behavior>
  <read_first>
    - idea-brief.md §13 (TTS voice spec: rate 1.0 / pitch 0.95 / volume 0.85; the fallback chain en-IN female → en-IN neutral → en-US female → first en-*) + engineering-handoff.md §6.3 (the same chain — confirm the exact ordering)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § Pattern 4 (the rate-scale CORRECTION: `setDefaultRate(rate)` wants 0.01–0.99 with translation; pass `setDefaultRate(1.0, true)` with `skipTransform=true` to get Android's raw normal-speed rate; volume is `androidParams.KEY_PARAM_VOLUME:0.85` per `speak()` call, NOT a `setDefaultVolume`) + § "Code Examples — en-IN TTS voice fallback" (the verbatim `pickAndSetEnInVoice` recipe + the `looksFemale` heuristic regex)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md § "src/lib/ttsVoice.ts (NET-NEW utility)" + "src/util/semver.ts" (the pure-util file shape)
    - apps/mobile/src/services/profileService.ts (the existing `durationFormatter` / `formatDuration(seconds)` from Phase 2 plan 02-17 — REUSE it if it already implements the HOME-06 spec; HOME-06 says "<1m → Xs, <1h → Xm, ≥1h → Xh Ym floored to previous minute". If profileService has `formatDuration(seconds)` matching this, `durationFormat.ts` should just re-export / wrap it as `formatContributionDuration(ms)` = `formatDuration(Math.floor(ms/1000))`. Re-read profileService.ts before creating a duplicate.)
    - apps/mobile/vitest.setup.ts (the `react-native-tts` mock from plan 04-01)
  </read_first>
  <action>
    1. `apps/mobile/src/lib/durationFormat.ts` — NEW (or thin wrapper). Export `formatContributionDuration(ms: number): string` implementing `<1min → '${s}s'`, `<1hr → '${m}m'`, `≥1hr → '${h}h ${m}m'` all floored to the previous minute (so 90s → '1m', 65.5min → '1h 5m'). If `apps/mobile/src/services/profileService.ts` already exports a matching `formatDuration(seconds)` (HOME-06), implement `formatContributionDuration` as `formatDuration(Math.floor(ms / 1000))` and add a comment "REC-04 formatter — same rule as HOME-06; delegates to profileService.formatDuration". Top docstring citing HOME-06/REC-04.
    2. `apps/mobile/src/lib/ttsVoice.ts` — NEW. Export `pickAndSetEnInVoice(): Promise<void>` per the 04-RESEARCH § "Code Examples — en-IN TTS voice fallback" recipe: `await Tts.getInitStatus()`; `let voices = await Tts.voices().catch(() => [])`; `const usable = voices.filter(v => !v.notInstalled)`; `const looksFemale = (v) => /female|woman|-fem|fenrir|salli|joanna|amy|raveena/i.test(\`${v.name ?? ''}${v.id ?? ''}\`)`; `const id = usable.find(v => v.language === 'en-IN' && looksFemale(v))?.id ?? usable.find(v => v.language === 'en-IN')?.id ?? usable.find(v => v.language === 'en-US' && looksFemale(v))?.id ?? usable.find(v => (v.language ?? '').toLowerCase().startsWith('en'))?.id`; `if (id) await Tts.setDefaultVoice(id)`; `Tts.setDefaultRate(1.0, true)`; `Tts.setDefaultPitch(0.95)`. Top docstring: "REC-14 — en-IN female → en-IN any → en-US female → first en-*. rate-scale note: setDefaultRate(rate) wants 0.01–0.99 translated; pass (1.0, true) for Android's raw normal speed (04-RESEARCH Pattern 4). volume is androidParams.KEY_PARAM_VOLUME:0.85 per speak() — set at each Tts.speak call site (RecordingScreen / useRecordingLifecycle), not here." Also export a tiny `speakCue(text: string)` helper that calls `Tts.speak(text, { androidParams: { KEY_PARAM_VOLUME: 0.85, KEY_PARAM_PAN: 0, KEY_PARAM_STREAM: 'STREAM_MUSIC' } })` so call sites don't repeat the androidParams.
    3. Tests: `apps/mobile/__tests__/lib/durationFormat.test.ts` (the `<behavior>` formatter bullets) + `apps/mobile/__tests__/lib/ttsVoice.test.ts` (the `<behavior>` voice-chain bullets — mock `react-native-tts` per-test with `vi.doMock('react-native-tts', () => ({ default: { getInitStatus: vi.fn().mockResolvedValue('success'), voices: vi.fn().mockResolvedValue(<the test's voice array>), setDefaultVoice: vi.fn(), setDefaultRate: vi.fn(), setDefaultPitch: vi.fn(), speak: vi.fn() } }))` then `vi.resetModules()` + dynamic import).
  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/lib/durationFormat.test.ts __tests__/lib/ttsVoice.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix apps/mobile test -- --run __tests__/lib/durationFormat.test.ts __tests__/lib/ttsVoice.test.ts` exits 0
    - `apps/mobile/src/lib/ttsVoice.ts` exists, contains `pickAndSetEnInVoice`, `setDefaultRate(1.0, true)` (or `setDefaultRate(1, true)`), `setDefaultPitch(0.95)`, and the four-step fallback chain (`'en-IN'` appears twice, `'en-US'`, `startsWith('en')`)
    - `apps/mobile/src/lib/durationFormat.ts` exists and exports `formatContributionDuration`
    - `formatContributionDuration(90_000)` returns `'1m'` and `formatContributionDuration(3_930_000)` returns `'1h 5m'` (assert in the test)
    - the full suite `npm --prefix apps/mobile test -- --run` exits 0
  </acceptance_criteria>
  <done>durationFormat.ts (REC-04 formatter) + ttsVoice.ts (REC-14 fallback chain + speakCue helper) + tests landed; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useRecordingLifecycle hook — the idea-brief.md §10 policy table + practice 60s hard cap + start guards</name>
  <behavior>
    - on mount (substate becomes 'gate' or 'active'), the hook subscribes to: `AppState`, `Linking` (RN), `Orientation.addOrientationListener` (UI) + `Orientation.addDeviceOrientationListener` (device), `HumynPhoneState.start()` + `onAudioFocusChanged`, `HumynBattery.start()` + `onBatteryChanged`, `HumynCapture.onError`, `HumynCapture.onThermalAbort`, and starts a 60s `setInterval` periodic guard (storage `RNFS.getFSInfo()` + battery cross-check) — and the cleanup `.remove()`s/`stop()`s/`clearInterval`s ALL of them
    - AppState change to `'background'` while `substate==='active'` → `callbacks.onStop('background')`; to `'inactive'` followed by `'background'` → still `onStop('background')` (the inactive-then-background chain)
    - `onAudioFocusChanged({focus:'loss'})` while active → `onStop('phone_call')` (permanent loss = answered call / done)
    - `onAudioFocusChanged({focus:'transient_loss'})` while active → starts a ~7s timer; if `onAudioFocusChanged({focus:'gain'})` arrives before it fires → cancel the timer, NO stop (REC-13 — call declined / brief interruption); if the timer fires (still no gain) OR a `{focus:'loss'}` arrives → `onStop('phone_call')` (answered) / `onStop('alarm')` for the alarm case — note: the §10 table treats alarm-while-recording as stop; the heuristic can't distinguish call-from-alarm by focus type alone, so `onStop('phone_call')` covers both (the `reason` is for telemetry/toast wording — both stop). Re-read 04-RESEARCH Pattern 3 + Assumption A12.
    - `Orientation` device listener fires `'PORTRAIT'` or `'PORTRAIT-UPSIDEDOWN'` while `substate==='active'` → `callbacks.onStop('orientation')` + `callbacks.showToast(<the §10 wording — "Recording stopped — keep the phone in landscape.">)`
    - `onBatteryChanged({level})` crossing from >0.15 to ≤0.15 while active → `callbacks.setAlert('battery', true)` + `callbacks.showToast('Battery low. Consider charging soon.')` + `HumynBeep.playTone('battery_alert')` + `Vibration.vibrate([0,100,50,100])` + `callbacks.voiceCue('Battery low. Consider charging soon.')` — NO stop (REC-10, recording continues)
    - `onBatteryChanged({level})` crossing to ≤0.05 while active → `callbacks.onStop('battery_critical')` (REC-11)
    - `isPractice && substate==='active' && durationMs >= 60_000` → `callbacks.onStop('practice_hard_cap')` (ONB-05 — fires exactly at 60s regardless of any other lifecycle event; the hard cap takes precedence)
    - `HumynCapture.onError({code:'storage_full'})` while active → `callbacks.onStop('storage_full')` + `callbacks.showToast(<storage toast>)`
    - `HumynCapture.onError({code:'permission_revoked'})` while active → `callbacks.onStop('permission_revoked')`
    - `HumynCapture.onThermalAbort(...)` → `callbacks.voiceCue('Phone too hot, stopping recording')` + `callbacks.setAlert('thermal', true)` + `HumynBeep.playTone('thermal_alert')` + `Vibration.vibrate(800)` — does NOT call onStop (HC self-stops within ~2.5s; the screen reacts to the subsequent `onSessionStop`); when HC then stops, the screen shows the "Recording stopped — phone needs to cool." toast (the hook just fires the alert)
    - a `logout` signal (an external `onLogout` event the hook also subscribes to — or the screen passes a `loggedOut` flag) → `callbacks.onStop('logout')`
    - `checkStartGuards()` returns `{ blocked: true, toast: 'Not enough storage to record.' }` when `RNFS.getFSInfo().freeSpace < 5e9` (REC-16, <5GB); returns `{ blocked: true, toast: <battery toast> }` when the last-seen battery level is `< 0.05` (and not charging); else `{ blocked: false }`
    - DND is NEVER toggled — the hook does not touch `ACCESS_NOTIFICATION_POLICY` / Settings / any DND API (REC-09)
  </behavior>
  <read_first>
    - idea-brief.md §10 (the canonical lifecycle-edge policy table — read it row by row; this is the contract `useRecordingLifecycle` implements) + §13 (TTS spec)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § Pattern 3 (the §10 policy table → hook mapping, lines 380-412) + § "Code Examples" (the policy mapping) + § Pitfall 5 (every subscription `.remove()`'d in the single useEffect cleanup) + § Pitfall 11 (the 60s battery periodic cross-check) + § Open Question 5 (HumynCapture.stop() is fire-and-await; §10 events don't need to time to segment boundaries) + § Anti-Patterns ("no READ_PHONE_STATE / ACCESS_NOTIFICATION_POLICY / no DND toggle"; "the 60s practice hard cap is JS-owned")
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md § "src/screens/recording/useRecordingLifecycle.ts (hook, event-driven)" + § "useEffect-lifecycle screen with ref-tracked timers + cancellation"
    - apps/mobile/src/hooks/useForegroundUserRehydrate.ts (the `useEffect` + `AppState.addEventListener('change', ...)` + `return () => sub.remove()` + swallow-with-telemetry idiom)
    - apps/mobile/src/native/HumynCapture.ts (the `onError`/`onThermalAbort`/`stop` API; the `CaptureErrorEvent` shape `{ code, message, recoverable, segmentId? }`) + HumynPhoneState.ts + HumynBattery.ts + HumynBeep.ts + HumynScreenBrightness.ts (the bindings from plan 04-02)
    - apps/mobile/src/screens/recording/recState.ts (the `RecSubstate` type — the hook's `substate` arg)
    - apps/mobile/src/util/analytics.ts (`logEvent` — the hook may emit a `recording_stopped` event with the `reason` for telemetry, no PII)
    - CLAUDE.md "Do NOT Use" — NO DND toggle, NO ACCESS_NOTIFICATION_POLICY, NO READ_PHONE_STATE
  </read_first>
  <action>
    Create `apps/mobile/src/screens/recording/useRecordingLifecycle.ts` exporting `useRecordingLifecycle(args)` exactly per the `<interfaces>` signature. Implementation:
    - Top block-comment docstring reproducing the `idea-brief.md §10` policy table (the `<behavior>` bullets) row by row, citing §10.
    - The hook body: a single `useEffect` keyed on `[args.substate, args.isPractice, args.callbacks]` (use stable refs for `callbacks` and `durationMs` via `useRef` so the effect doesn't churn — update `durationMsRef.current = args.durationMs` on every render via a separate non-effect assignment or a tiny effect). When `args.substate === 'gate' || args.substate === 'active'`, set up all the subscriptions (see `<interfaces>` imports); when it leaves those substates (or on unmount), the cleanup runs. The cleanup collects every `EmitterSubscription` / `AppState` sub / `Orientation` listener / `clearInterval` / pending `setTimeout` (the audio-focus 7s timer) and `.remove()`s / `stop()`s / clears each. Use a `subs: Array<{ remove: () => void }>` accumulator + a `timers: ReturnType<typeof setTimeout>[]` accumulator pattern.
    - Implement the policy resolver: each event source maps to one of `onStop(reason)` | `setAlert+showToast+beep+haptic+voiceCue (continue)` | `voiceCue+setAlert+beep+haptic (thermal — no stop)` | no-op, exactly per the `<behavior>` table. The battery threshold-crossing logic: keep a `lastBatteryLevelRef`; on each `onBatteryChanged`, compare to the last; fire the ≤15% alert only on the *crossing* (>0.15 → ≤0.15), the ≤5% stop only on the crossing (>0.05 → ≤0.05); update the ref. The audio-focus heuristic: on `transient_loss`, `const t = setTimeout(() => onStop('phone_call'), 7000); timers.push(t)`; on `gain`, `clearTimeout(t)`; on `loss`, `clearTimeout(t); onStop('phone_call')`. The practice hard cap: a separate small effect or a `setTimeout(() => onStop('practice_hard_cap'), Math.max(0, 60_000 - durationMs))` armed when `isPractice && substate==='active'` (so it fires at exactly 60s of recording — ONB-05; if the recording is already past 60s when active starts, fire immediately).
    - `checkStartGuards()`: `const fs = await RNFS.getFSInfo(); if (fs.freeSpace < 5e9) return { blocked: true, toast: 'Not enough storage to record.' }; if (lastBatteryLevelRef.current >= 0 && lastBatteryLevelRef.current < 0.05 && !lastBatteryChargingRef.current) return { blocked: true, toast: 'Battery too low to start a recording. Charge to at least 15%.' }; return { blocked: false };` (REC-16 + the battery <5% start guard). Use the exact toast strings from UI-SPEC § "Error / failure states" if it gives them; otherwise the strings above.
    - The 60s periodic guard `setInterval` (during `active`): every 60s, re-poll `RNFS.getFSInfo()` — if `freeSpace` drops below a low threshold mid-record, that's also caught by HC's `onError({code:'storage_full'})`, so the periodic guard is belt-and-suspenders; also re-read the battery level (`BatteryManager.BATTERY_PROPERTY_CAPACITY` isn't directly available from JS, so just rely on the `onBatteryChanged` events + log a breadcrumb). Keep this simple.
    - The hook emits NO log statement containing the JWT/sub/file paths (V7). It MAY `logEvent('recording_stopped', { reason })` (no PII) — register that event in analytics.ts EVENT_NAMES if not already there (it was added in plan 04-06).
    - Add a test: `apps/mobile/__tests__/screens/recording/useRecordingLifecycle.test.tsx` — render the hook via `@testing-library/react`'s `renderHook` (or a tiny test component) with all the native modules mocked (`vi.mock('../../src/native/HumynPhoneState', ...)`, `HumynBattery`, `HumynCapture` `onError`/`onThermalAbort`, `HumynBeep`, `react-native-orientation-locker`, `react-native-fs`, `react-native`'s `AppState`/`Vibration`) and fake timers; one `it()` per `<behavior>` bullet — AppState background → onStop('background'); audio-focus loss → onStop('phone_call'); audio-focus transient-loss-then-gain-within-7s → no onStop; audio-focus transient-loss + advance 7s → onStop('phone_call'); orientation device→portrait while active → onStop('orientation') + showToast; battery crossing ≤15% → setAlert('battery',true) + showToast + HumynBeep.playTone('battery_alert') + Vibration.vibrate([0,100,50,100]) + voiceCue, NO onStop; battery crossing ≤5% → onStop('battery_critical'); HumynCapture.onError storage_full → onStop('storage_full') + showToast; HumynCapture.onError permission_revoked → onStop('permission_revoked'); HumynCapture.onThermalAbort → voiceCue + setAlert('thermal',true) + HumynBeep.playTone('thermal_alert') + Vibration.vibrate(800), NO onStop; isPractice + active + advance 60s → onStop('practice_hard_cap'); checkStartGuards() blocked when freeSpace<5GB; unmount → all subs .remove()'d (assert the mock's remove fns were called). Mirror `ForegroundRehydrate.test.tsx` for the AppState part + the CompatRunningScreen fake-timer pattern.
  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/screens/recording/useRecordingLifecycle.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix apps/mobile test -- --run __tests__/screens/recording/useRecordingLifecycle.test.tsx` exits 0
    - `apps/mobile/src/screens/recording/useRecordingLifecycle.ts` exists, exports `useRecordingLifecycle`, and contains references to `onAudioFocusChanged`, `onBatteryChanged`, `onError`, `onThermalAbort`, `playTone`, `getFSInfo`, `addDeviceOrientationListener`, `AppState`, `Vibration.vibrate`
    - the file does NOT contain `ACCESS_NOTIFICATION_POLICY`, `READ_PHONE_STATE`, `Settings.setSetting`, or any DND-toggle reference (grep returns 0) — REC-09
    - the file references `60_000` or `60000` (the practice hard cap) and `0.15` and `0.05` (the battery thresholds) and `7000` or `7_000` (the audio-focus heuristic timeout)
    - `__tests__/screens/recording/useRecordingLifecycle.test.tsx` has ≥ 10 `it(` cases
    - the full suite `npm --prefix apps/mobile test -- --run` exits 0
  </acceptance_criteria>
  <done>useRecordingLifecycle implements the full §10 policy table + the practice 60s hard cap + checkStartGuards, with leak-clean subscription teardown; ≥10-case hook test green; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: __DEV__-gated debug affordance on TasksPlaceholderScreen + devAffordance test</name>
  <behavior>
    - when `__DEV__ === true`: the TasksPlaceholderScreen heading has a `onLongPress` handler that calls `navigation.push('Recording', { taskId: 'cooking_chop_vegetables', taskName: 'Practice — Chop vegetables', isPractice: false, taskCategory: 'cooking', taskSetting: 'indoor' })`
    - when `__DEV__ === false`: the long-press handler is undefined / the Pressable wrapper around the heading is absent (the entire press handler is inside `if (__DEV__) { ... }` so Metro dead-code-eliminates it)
    - the screen still renders the existing "Tasks — coming in Phase 6." copy + the TopBar (no regression)
  </behavior>
  <read_first>
    - apps/mobile/src/screens/tasks/TasksPlaceholderScreen.tsx (the current minimal screen — TopBar + a Text body; you add the `__DEV__`-gated long-press)
    - apps/mobile/__tests__/screens/TasksPlaceholderScreen.test.tsx (the existing test — extend it or add a new devAffordance test file)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § Pitfall 7 (wrap the ENTIRE press handler in `if (__DEV__)`; test it's absent in release builds) + § "Code context — __DEV__-gated dev affordance" + 04-CONTEXT.md `<specifics>` (long-press >800ms on the heading text; hardcoded task `{ taskId: 'cooking_chop_vegetables', taskName: 'Practice — Chop vegetables', isPractice: false, taskCategory: 'cooking', taskSetting: 'indoor' }`)
    - task-taxonomy.md (confirm `cooking_chop_vegetables` is a real task slug — if the exact slug differs, use whatever the taxonomy lists for "chop vegetables" under Cooking; the choice is planner-discretion per CONTEXT, any clean 65-task entry works)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md § "__DEV__ / flavor-scoped affordance"
    - apps/mobile/src/ui/primitives/Pressable.tsx (the Pressable primitive — `onLongPress` + `delayLongPress`)
  </read_first>
  <action>
    1. In `apps/mobile/src/screens/tasks/TasksPlaceholderScreen.tsx`: import `useNavigation` and `Pressable` from `../../ui/primitives/Pressable`. Wrap the heading `<Text>` in a conditionally-rendered `Pressable` ONLY when `__DEV__`:
    ```tsx
    const navigation = useNavigation();
    const onDebugLongPress = __DEV__ ? () => {
      navigation.push('Recording', { taskId: 'cooking_chop_vegetables', taskName: 'Practice — Chop vegetables', isPractice: false, taskCategory: 'cooking', taskSetting: 'indoor' });
    } : undefined;
    // ... in render:
    {__DEV__ ? (
      <Pressable onLongPress={onDebugLongPress} delayLongPress={800} accessibilityLabel="tasks-heading">
        <Text variant="body" tone="secondary" style={...}>Tasks — coming in Phase 6.</Text>
      </Pressable>
    ) : (
      <Text variant="body" tone="secondary" style={...}>Tasks — coming in Phase 6.</Text>
    )}
    ```
    Add a comment: "__DEV__-gated non-practice debug entry to RecordingScreen (Phase 4 plan 04-08 / D-NAV-02). Production apkRollout/playStore builds set __DEV__===false → Metro dead-code-eliminates the long-press handler AND the Pressable wrapper. Phase 6 replaces this placeholder with the real Tasks list + Task details + Start Recording CTA."  Keep the TopBar + the existing copy unchanged.
    2. `apps/mobile/__tests__/screens/recording/devAffordance.test.tsx` — mock navigation; (a) with `globalThis.__DEV__ = true` (the test env — vitest typically has `__DEV__` truthy; if not, set it): assert a `tasks-heading` Pressable is rendered and a `fireEvent` `onLongPress` (or directly invoking the `onLongPress` prop via `getByLabelText('tasks-heading').props.onLongPress()`) calls `navigation.push('Recording', { taskId: 'cooking_chop_vegetables', taskName: 'Practice — Chop vegetables', isPractice: false, taskCategory: 'cooking', taskSetting: 'indoor' })`; (b) with `globalThis.__DEV__ = false` (set it before importing the screen — `vi.stubGlobal('__DEV__', false)` then `vi.resetModules()` + dynamic import): assert the rendered tree does NOT contain a `tasks-heading` Pressable / the long-press handler is absent, AND the screen still renders "Tasks — coming in Phase 6." Restore `__DEV__` afterward.
  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/screens/recording/devAffordance.test.tsx __tests__/screens/TasksPlaceholderScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix apps/mobile test -- --run __tests__/screens/recording/devAffordance.test.tsx __tests__/screens/TasksPlaceholderScreen.test.tsx` exits 0
    - `apps/mobile/src/screens/tasks/TasksPlaceholderScreen.tsx` contains `__DEV__` and `navigation.push('Recording'` and `isPractice: false`
    - `apps/mobile/src/screens/tasks/TasksPlaceholderScreen.tsx` — the `navigation.push('Recording'...)` call is inside an `if (__DEV__)` / `__DEV__ ?` guard (grep: the line `__DEV__` appears before `navigation.push('Recording'` in the file)
    - `apps/mobile/__tests__/screens/recording/devAffordance.test.tsx` has a `__DEV__ === false` case asserting the affordance is absent
    - the full suite `npm --prefix apps/mobile test -- --run` exits 0
  </acceptance_criteria>
  <done>__DEV__-gated debug affordance on TasksPlaceholder + a test proving it's stripped when __DEV__===false landed; full suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                            | Description                                                         |
| ------------------------------------------------------------------- | ------------------------------------------------------------------- |
| OS events (AppState / audio focus / battery / orientation) → policy | the §10 policy fires `stop()` on the right edges                    |
| HumynCapture error events → policy                                  | `onError({code})` / `onThermalAbort` cross from the native module   |
| `__DEV__` build flag → which code ships                             | the debug affordance must be dead-code-eliminated in release builds |
| `Tts` engine, `RNFS` filesystem                                     | TTS speech + free-space reads                                       |

## STRIDE Threat Register

| Threat ID | Category                                 | Component                                                      | Disposition | Mitigation Plan                                                                                                                                                                                                                            |
| --------- | ---------------------------------------- | -------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-4.8-01  | Elevation of privilege / Info disclosure | the `__DEV__` debug affordance shipped to production           | mitigate    | the ENTIRE press handler + the Pressable wrapper are inside `if (__DEV__)` — Metro dead-code-eliminates them when `__DEV__===false` (release builds). `devAffordance.test.tsx` has a `__DEV__===false` case asserting absence (Pitfall 7). |
| T-4.8-02  | Spec violation                           | DND / notifications                                            | mitigate    | `useRecordingLifecycle` touches NO DND / `ACCESS_NOTIFICATION_POLICY` / Settings API (REC-09). Acceptance criterion greps for absence of those symbols.                                                                                    |
| T-4.8-03  | DoS (self-inflicted)                     | listener/timer leaks on substate change / unmount              | mitigate    | every subscription / `setTimeout` / `setInterval` is collected and `.remove()`'d / cleared in the single `useEffect` cleanup (Pitfall 5); the hook also calls `HumynPhoneState.stop()` + `HumynBattery.stop()` on cleanup.                 |
| T-4.8-04  | Info disclosure                          | logging in the policy hook                                     | mitigate    | the hook logs only `logEvent('recording_stopped', { reason })` (no PII — the analytics.ts allowlist + the PII guard apply); no JWT/sub/file-path logging.                                                                                  |
| T-4.8-05  | DoS (data loss bias)                     | the audio-focus answered-vs-declined heuristic mis-classifying | accept      | the heuristic errs toward stopping (7s timer fires → stop) — stopping a recording is recoverable; missing a stop loses nothing but the user's intent. The 7s timeout is a tunable adjusted in Wave-5 on-hardware smoke (Assumption A12).   |

</threat_model>

<verification>
- `npm --prefix apps/mobile test -- --run` exits 0 (durationFormat, ttsVoice, useRecordingLifecycle (≥10 cases), devAffordance (incl. __DEV__===false), and the pre-existing suite green).
- `useRecordingLifecycle.ts` grep-clean of `ACCESS_NOTIFICATION_POLICY` / `READ_PHONE_STATE` / DND-toggle APIs; references the §10 thresholds (`0.15`, `0.05`, `60_000`, `7000`).
- `TasksPlaceholderScreen.tsx` — the `navigation.push('Recording'...)` is `__DEV__`-guarded.
</verification>

<success_criteria>

- `useRecordingLifecycle` implements the full `idea-brief.md §10` policy table + the practice 60s hard cap + `checkStartGuards()` (REC-16 + battery <5% guard), with leak-clean subscription teardown; tested ≥10 cases.
- `ttsVoice.pickAndSetEnInVoice()` (REC-14 fallback chain, `setDefaultRate(1.0, true)` + `setDefaultPitch(0.95)`) + `speakCue` helper; `durationFormat.formatContributionDuration` (REC-04 / HOME-06 rule).
- `__DEV__`-gated debug affordance on TasksPlaceholderScreen, proven stripped when `__DEV__===false`.
- No DND / forbidden permissions.
- Full mobile test suite green.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-08-SUMMARY.md`
</output>
