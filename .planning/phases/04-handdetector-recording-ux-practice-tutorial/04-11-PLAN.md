---
phase: 04-handdetector-recording-ux-practice-tutorial
plan: 11
type: execute
wave: 1
depends_on: [04-07, 04-08, 04-09, 04-10]
files_modified:
  - apps/mobile/src/screens/recording/RecordingScreen.tsx
  - apps/mobile/src/screens/recording/recState.ts
  - apps/mobile/src/screens/recording/useRecordingLifecycle.ts
  - apps/mobile/src/screens/recording/components/RotatePrompt.tsx
  - apps/mobile/__tests__/screens/recording/RecordingScreen.test.tsx
  - apps/mobile/__tests__/screens/recording/recState.test.ts
  - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md
autonomous: true
gap_closure: true
requirements: [REC-01, REC-04, REC-05, REC-06, ONB-03, ONB-05, ONB-08, HAND-09, HAND-10, HAND-11]

must_haves:
  truths:
    - 'In a release build (__DEV__ === false) a user who physically rotates the device to landscape advances the recording surface from rotate-prompt → ready and can press the record button'
    - 'From ready, pressing record reaches the gate substate, which runs the MediaPipe hand-gate, and (on gate-pass) reaches the active recording state — the whole SC1–SC5 chain is reachable without __DEV__-only affordances'
    - 'A new user, after the 60s practice recording, reaches PracticeComplete then MainTabs (because the recording surface is reachable, PracticeComplete runs, the per-account practice flag is written, and computeInitialRoute stops routing to RigTutorial)'
    - 'The RemoteConfig gate.consecutive_hits_required / gate.cadence_ms values reach the reducer — GateRing target, the poll cadence, and the per-segment metadata (consecutiveHitsRequired / platformCadenceMs) all reflect the live RemoteConfig values, not the hard-coded 5 / 400'
    - 'Exiting the screen (X button, backgrounding, unmount) during the gate→record handoff does not leave an orphaned native capture session / stuck foreground-service notification'
    - 'A practice recording auto-stops at ~60s even if the stop-confirm modal was open mid-record'
  artifacts:
    - path: 'apps/mobile/src/screens/recording/RecordingScreen.tsx'
      provides: 'production orientation→LANDSCAPE_DETECTED dispatch effect; SET_GATE_CONFIG dispatch from readGateConfig().then(); HumynCapture.stop() in the mount-effect cleanup + after a cancelled start(); IN-02/IN-09 cleanup'
      contains: 'LANDSCAPE_DETECTED'
    - path: 'apps/mobile/src/screens/recording/recState.ts'
      provides: 'SET_GATE_CONFIG reducer action that updates gate.targetHits / gate.cadenceMs while still in a pre-gate substate'
      contains: 'SET_GATE_CONFIG'
    - path: 'apps/mobile/src/screens/recording/useRecordingLifecycle.ts'
      provides: "practice 60s cap re-armed off wall-clock startedAt (not the frozen TICK duration) so the stop-confirm modal can't overrun it; dead `timers` array removed (IN-01)"
    - path: 'apps/mobile/__tests__/screens/recording/RecordingScreen.test.tsx'
      provides: 'a render test that mounts RecordingScreen with the DEFAULT initialRecState (no __test_initialState) and asserts the surface leaves rotate-prompt when the device-orientation listener reports LANDSCAPE-LEFT/RIGHT'
      contains: 'rotate-prompt'
    - path: '.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md'
      provides: "§2 amendment: the rotate-prompt → ready transition is walked by PHYSICALLY rotating the device, with the note that the apkRollout debug build's __DEV__===true would mask CR-01"
      contains: 'physically rotat'
  key_links:
    - from: 'RecordingScreen rotate-prompt effect'
      to: 'recReducer LANDSCAPE_DETECTED'
      via: "Orientation.addDeviceOrientationListener + Orientation.getDeviceOrientation → dispatch({ type: 'LANDSCAPE_DETECTED' })"
      pattern: "dispatch\\(\\{ ?type: ?'LANDSCAPE_DETECTED'"
    - from: 'RecordingScreen readGateConfig().then()'
      to: 'recReducer SET_GATE_CONFIG'
      via: "dispatch({ type: 'SET_GATE_CONFIG', targetHits, cadenceMs })"
      pattern: 'SET_GATE_CONFIG'
    - from: 'RecordingScreen mount-effect cleanup'
      to: 'HumynCapture.stop()'
      via: 'unconditional HumynCapture.stop().catch(() => undefined) on unmount'
      pattern: "HumynCapture\\.stop\\(\\)"
---

<objective>
Close the Phase-4 verification gaps that all live in the recording-surface JS module
cluster: CR-01 (the phase-goal blocker — the recording surface is unreachable in any
non-`__DEV__` build), WR-01 (RemoteConfig cadence/target keys never reach the reducer),
WR-02 (orphaned native session + stuck FGS on a gate→record exit), WR-07 (practice 60s
cap overrun via the stop-confirm modal), plus the cheap in-file cleanups WR-06 / IN-01 /
IN-02 / IN-09, and the `04-MANUAL-SMOKE.md` §2 amendment that CR-01's `missing[]` calls for.

Purpose: Phase 4 was scored 0/5 success criteria solely because of CR-01 — the code is
substantive and largely correct, but `initialRecState()` always starts at `rotate-prompt`
and the only production code path out of it (`{ type: 'LANDSCAPE_DETECTED' }`) is dispatched
exclusively by a `{__DEV__ ? … : null}` Pressable that is dead-code-eliminated in release.
Fixing CR-01 makes SC1–SC5 reachable; the WR fixes harden the same surface against orphaned
sessions and the practice-cap overrun the verifier also flagged in this file.
Output: a reachable, hardened recording surface; a render test that would have caught CR-01;
the amended smoke runbook.

Out of scope (do NOT touch here): the Kotlin native modules (`HumynHandDetectorModule.kt`
WR-03 / `HumynBeepModule.kt` WR-04 → plan 04-12); `SETTLE_MS` value changes (WR-05 — the
on-hardware `[BLOCKING]` re-measure in `04-MANUAL-SMOKE.md` §5b owns that; this plan only
re-comments the constant); the `HumynCapture` native capture path; audio re-introduction;
anything in Phases 5/6/7.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-VERIFICATION.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-REVIEW.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-UI-SPEC.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-09-SUMMARY.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-08-SUMMARY.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-07-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs — extracted from the codebase, no exploration required. -->

`apps/mobile/src/screens/recording/recState.ts`:

```typescript
export type RecSubstate = 'rotate-prompt' | 'ready' | 'pre-flight' | 'gate' | 'active' | 'stop-confirm' | 'stopped';
export type RecAction =
  | { type: 'LANDSCAPE_DETECTED' }    // rotate-prompt → ready (no-op elsewhere)
  | { type: 'START_PRESSED' }         // ready → pre-flight
  | { type: 'PRE_FLIGHT_OK'; now: number }
  | { type: 'CAPTURE_STARTED'; now: number }
  | { type: 'TICK'; durationMs: number }
  | ... ;
// initialRecState(params, gateConfig?) ALREADY accepts an optional 2nd arg:
export function initialRecState(
  params: { taskId: string; taskName: string; isPractice: boolean },
  gateConfig?: { targetHits?: number; cadenceMs?: number },
): RecState;
// internal helper used by RESET_FOR_FRESH / CAPTURE_START_FAILED:
function idleGate(targetHits: number, cadenceMs: number): RecState['gate'];
// gate sub-shape: { phase: 'idle'|'loading'|'waiting'|'confirmed'; consecutiveHits; targetHits; cadenceMs; skipped; bypassed; startedAt; confirmedAt }
// DEFAULT_TARGET_HITS = 5, DEFAULT_CADENCE_MS = 400 (Android)
```

`apps/mobile/src/lib/remoteConfigGate.ts`:

```typescript
export interface GateConfig {
  targetHits: number;
  cadenceMs: number;
  minHandDetectionConfidence: number;
}
export const GATE_DEFAULTS: GateConfig = {
  targetHits: 5,
  cadenceMs: 400,
  minHandDetectionConfidence: 0.5,
};
export async function readGateConfig(): Promise<GateConfig>; // best-effort; never throws
```

`react-native-orientation-locker` (already a dep; mocked in vitest.setup.ts with
`getDeviceOrientation`, `getInitialOrientation`, `addOrientationListener`,
`addDeviceOrientationListener`, `removeDeviceOrientationListener`):

```typescript
import Orientation, { type OrientationType } from 'react-native-orientation-locker';
// OrientationType values: 'PORTRAIT' | 'LANDSCAPE-LEFT' | 'LANDSCAPE-RIGHT' | 'PORTRAIT-UPSIDEDOWN' | 'UNKNOWN' | 'FACE-UP' | 'FACE-DOWN'
Orientation.getDeviceOrientation((o: OrientationType) => void): void;          // fire-once, current PHYSICAL orientation (ignores the lock)
Orientation.addDeviceOrientationListener((o: OrientationType) => void): void;  // PHYSICAL orientation stream
Orientation.removeDeviceOrientationListener(fn): void;
```

Note: `Orientation.addOrientationListener` (the _UI_ orientation, which follows the
landscape lock) is the analog the research §"Orientation lock + rotation detection" block
prescribes for leaving `rotate-prompt`; `addDeviceOrientationListener` (PHYSICAL) also
works and is what `useRecordingLifecycle` already imports for the active-record rotation
edge. Either is acceptable — use the _device_ (physical) listener so a single import covers
both the rotate-prompt entry and (if you wire it there) the active-record exit; fire
`Orientation.getDeviceOrientation(...)` once on effect-mount so the device-already-in-
landscape-on-mount case is handled.

`apps/mobile/src/lib/buildCaptureOpts.ts` (already consumes gateConfig — verify the live
values flow through):

```typescript
buildCaptureOpts({ ..., gateConfig: { targetHits: number; cadenceMs: number }, ... });
// → opts.startGate.consecutiveHitsRequired = gateConfig.targetHits
// → opts.startGate.platformCadenceMs       = gateConfig.cadenceMs
```

`apps/mobile/src/native/HumynCapture` (Phase 3): `stop(): Promise<void>` — rejects
`Error(code='no_active_session')` harmlessly when nothing is running.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — wire the production orientation→LANDSCAPE_DETECTED path + the no-`__test_initialState` render test</name>
  <files>apps/mobile/src/screens/recording/RecordingScreen.tsx, apps/mobile/src/screens/recording/components/RotatePrompt.tsx, apps/mobile/__tests__/screens/recording/RecordingScreen.test.tsx</files>
  <read_first>
    - apps/mobile/src/screens/recording/RecordingScreen.tsx (the file being modified — note ~lines 237-252 the mount-effect, ~lines 332-347 the pre-flight effect for the `if (state.substate !== X) return;` pattern, ~lines 598-602 the `rotate-prompt` chrome that renders `<RotatePrompt onPretendRotated={() => dispatch({ type: 'LANDSCAPE_DETECTED' })} />`)
    - apps/mobile/src/screens/recording/components/RotatePrompt.tsx (lines 26-46 — the `{__DEV__ ? <Pressable .../> : null}` "Pretend I rotated →" pill; KEEP it as an *additional* dev affordance, do not remove)
    - apps/mobile/src/screens/recording/recState.ts (the `LANDSCAPE_DETECTED` case ~line 150-151 — `rotate-prompt → ready`; the substate transition table comment lines 14-37)
    - apps/mobile/__tests__/screens/recording/RecordingScreen.test.tsx (lines 1-118 — the per-file `vi.mock('react-native-orientation-locker', …)` at lines 101-118 currently only ships `lockToLandscape`/`unlockAllOrientations`/`addDeviceOrientationListener`/`removeDeviceOrientationListener`/`addOrientationListener`/`removeOrientationListener`; it does NOT ship `getDeviceOrientation`/`getInitialOrientation` — you must add those to the per-file mock, AND make `addDeviceOrientationListener` capturable so the test can drive the callback)
    - apps/mobile/vitest.setup.ts (lines ~760-790 — the global orientation-locker mock that DOES ship `getDeviceOrientation`/`getInitialOrientation` — mirror its shape in the per-file mock)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md (the "Orientation lock + rotation detection — JS (REC-01, §10 rotation edge)" block ~lines 806-830 — the prescribed `onUi`/`onDevice` listener pattern; and Pitfall 4 — orientation-locker New-Arch caveat)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-UI-SPEC.md (the rotate-prompt visual/interaction spec — confirm no chrome change is needed; this is a pure behavior wiring fix, the `RotateCw` icon + caption stay)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md (line ~261 — the `useRecordingLifecycle` orientation-listener fan-in pattern; leak discipline — every listener gets removed in cleanup)
  </read_first>
  <behavior>
    - Test (new, in `RecordingScreen.test.tsx`): mount `<RecordingScreen />` with the DEFAULT `initialRecState` — i.e. NO `__test_initialState` prop, NO route param overrides beyond what the existing render harness already supplies — and assert the screen renders the `rotate-prompt` body (query by `accessibilityLabel="rotate-prompt"` — it's there). Then have the test drive the captured `addDeviceOrientationListener` callback with `'LANDSCAPE-LEFT'` (wrapped in `act(...)`) and assert the screen has left `rotate-prompt` — i.e. the `accessibilityLabel="rotate-prompt"` node is gone AND the `accessibilityLabel="recording-record-button"` node (the `ready` substate's 88×88 record button) is now present. A second assertion: a fresh mount where `Orientation.getDeviceOrientation` is mocked to call back with `'LANDSCAPE-RIGHT'` synchronously → the screen mounts directly into `ready` (the device-already-in-landscape-on-mount case) — `accessibilityLabel="rotate-prompt"` is never present, `accessibilityLabel="recording-record-button"` is present from first render-flush. A third assertion: driving the listener with `'PORTRAIT'` while in `rotate-prompt` does NOT advance the surface (still shows `rotate-prompt`).
    - Test must NOT inject `__test_initialState` (that's the whole point — the existing `RecordingScreen.test.tsx` / `handGate.test.tsx` cases all inject it, which is why they missed CR-01).
  </behavior>
  <action>
    1. In `RecordingScreen.tsx`, add a new `useEffect` (place it near the other gate effects, e.g. just after the existing mount effect ~line 252, or just before the `pre-flight → gate` effect ~line 332) — guarded on `state.substate === 'rotate-prompt'`:
       ```ts
       // CR-01 — the PRODUCTION rotate-prompt → ready path. Without this the
       // surface is a dead-end in any non-__DEV__ build (the RotatePrompt
       // "Pretend I rotated →" pill is __DEV__-only and dead-code-eliminated
       // in release). Use the device-orientation (PHYSICAL) listener so it
       // works regardless of the landscape lock, plus a fire-once read for the
       // device-already-in-landscape-on-mount case.
       useEffect(() => {
         if (state.substate !== 'rotate-prompt') return;
         const onOrient = (o: OrientationType) => {
           if (o === 'LANDSCAPE-LEFT' || o === 'LANDSCAPE-RIGHT') {
             dispatch({ type: 'LANDSCAPE_DETECTED' });
           }
         };
         Orientation.getDeviceOrientation((o) => onOrient(o as OrientationType));
         Orientation.addDeviceOrientationListener(onOrient);
         return () => Orientation.removeDeviceOrientationListener(onOrient);
       }, [state.substate]);
       ```
       — import `type OrientationType` from `react-native-orientation-locker` alongside the existing `import Orientation from 'react-native-orientation-locker'` (i.e. `import Orientation, { type OrientationType } from 'react-native-orientation-locker';`).
       The reducer's `LANDSCAPE_DETECTED` case already no-ops outside `rotate-prompt`, and this effect tears down once `substate` changes, so a late listener fire is harmless either way.
    2. KEEP the existing `<RotatePrompt onPretendRotated={() => dispatch({ type: 'LANDSCAPE_DETECTED' })} />` line exactly as-is — the `__DEV__` "Pretend I rotated →" pill stays as a developer affordance. In `RotatePrompt.tsx`, update the file-header comment to note that the production rotate-prompt exit is now the `RecordingScreen` device-orientation effect, and the `__DEV__` pill is a *supplementary* dev shortcut (so the next reader doesn't think the pill is the only exit). No code change in `RotatePrompt.tsx` beyond the comment.
    3. In `RecordingScreen.test.tsx`: extend the per-file `vi.mock('react-native-orientation-locker', …)` (lines ~101-118) to add `getDeviceOrientation` and `getInitialOrientation` (mirroring `vitest.setup.ts` ~lines 777-779: `getDeviceOrientation: vi.fn((cb) => cb('PORTRAIT'))`, `getInitialOrientation: vi.fn(() => 'PORTRAIT')`). Capture the listener registered via `addDeviceOrientationListener` — either by having the mock push to a module-scoped array the test reads, or by `vi.mocked(Orientation.addDeviceOrientationListener).mock.calls[0][0]`. Then add a new `describe('CR-01 — rotate-prompt is reachable in a non-__DEV__ build', …)` block with the three assertions from `<behavior>`. Drive the listener inside `act(() => listener('LANDSCAPE-LEFT'))`. For the "device already in landscape on mount" assertion, override `getDeviceOrientation` for that test to call back synchronously with `'LANDSCAPE-RIGHT'` BEFORE the screen mounts (re-`vi.mocked(...).mockImplementationOnce(...)` or pass a fresh mock).
    4. Do NOT touch any existing test in `RecordingScreen.test.tsx` — only add the new `describe` block + the mock-shape additions (the existing cases keep injecting `__test_initialState`; they're unaffected by the new orientation-mock keys).
  </action>
  <verify>
    <automated>cd apps/mobile && npx vitest run __tests__/screens/recording/RecordingScreen.test.tsx __tests__/screens/recording/handGate.test.tsx __tests__/screens/recording/recState.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "LANDSCAPE_DETECTED" apps/mobile/src/screens/recording/RecordingScreen.tsx` shows the new `dispatch({ type: 'LANDSCAPE_DETECTED' })` inside an `onOrient`/`useEffect` that is NOT wrapped in `__DEV__` (the only `__DEV__`-adjacent dispatcher is still the `RotatePrompt onPretendRotated` prop).
    - `grep -n "getDeviceOrientation\|addDeviceOrientationListener" apps/mobile/src/screens/recording/RecordingScreen.tsx` shows both calls in the new `rotate-prompt` effect.
    - `grep -n "OrientationType" apps/mobile/src/screens/recording/RecordingScreen.tsx` shows the type import is present.
    - `grep -n "getDeviceOrientation\|getInitialOrientation" apps/mobile/__tests__/screens/recording/RecordingScreen.test.tsx` shows the per-file orientation mock now ships them.
    - `grep -n "rotate-prompt\|__test_initialState" apps/mobile/__tests__/screens/recording/RecordingScreen.test.tsx` shows a new `describe`/`it` block referencing `rotate-prompt` that does NOT pass `__test_initialState` to `<RecordingScreen>`.
    - `npx vitest run` (the three recording test files) is green, including the new render test.
    - `grep -n "__DEV__" apps/mobile/src/screens/recording/components/RotatePrompt.tsx` still shows the dev pill is present (it was kept).
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "RecordingScreen|RotatePrompt" || echo "no type errors in touched files"</automated>
  </verify>
  <done>In a release build the recording surface leaves `rotate-prompt` when the device is in (or rotates to) landscape — SC1–SC5 are reachable; a render test that mounts the screen with the default state would have caught CR-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-01 / WR-02 / WR-07 — RemoteConfig gate values reach the reducer; no orphaned capture on a gate→record exit; practice cap survives the stop-confirm modal</name>
  <files>apps/mobile/src/screens/recording/recState.ts, apps/mobile/src/screens/recording/RecordingScreen.tsx, apps/mobile/src/screens/recording/useRecordingLifecycle.ts, apps/mobile/__tests__/screens/recording/recState.test.ts</files>
  <read_first>
    - apps/mobile/src/screens/recording/recState.ts (the file being modified — the `RecAction` union lines 77-97, the `idleGate(targetHits, cadenceMs)` helper lines 104-115, `initialRecState` lines 117-135, the reducer lines 137-281; note `LANDSCAPE_DETECTED` is the only pre-gate-substate action today)
    - apps/mobile/src/screens/recording/RecordingScreen.tsx (~lines 166-169 `useReducer(recReducer, initialRecState({ taskId, taskName, isPractice }))` — NO gateConfig; ~lines 222-231 the `readGateConfig().then((cfg) => setGateCfg(cfg))` effect; ~lines 388-397 `useHandGate` gets `cadenceMs: state.gate.cadenceMs`; ~line 441 `buildCaptureOpts` gets `gateConfig: { targetHits: state.gate.targetHits, cadenceMs: state.gate.cadenceMs }`; ~lines 247-252 the mount-effect cleanup; ~lines 402-489 the `gate.confirmed → active` `run()` with the `cancelled` flag; ~lines 530-539 `handleClose`)
    - apps/mobile/src/screens/recording/useRecordingLifecycle.ts (the file being modified — `monitoring = substate === 'gate' || substate === 'active'` ~line 186; the practice-cap effect ~lines 336-348 — `remaining = Math.max(0, PRACTICE_HARD_CAP_MS - durationMsRef.current)`; the hook does NOT currently receive `startedAt`)
    - apps/mobile/src/lib/remoteConfigGate.ts (`readGateConfig() → { targetHits, cadenceMs, minHandDetectionConfidence }`, clamped, never throws)
    - apps/mobile/src/lib/buildCaptureOpts.ts (lines ~48, 91-92 — `gateConfig: { targetHits, cadenceMs }` → `startGate.consecutiveHitsRequired` / `startGate.platformCadenceMs`)
    - apps/mobile/__tests__/screens/recording/recState.test.ts (the file being extended — the existing `recReducer` test cases; add a `SET_GATE_CONFIG` case alongside)
    - apps/mobile/src/native/HumynCapture.ts (the `stop()` binding — rejects `no_active_session` harmlessly)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-REVIEW.md (WR-01 ~lines 114-120, WR-02 ~lines 122-128, WR-07 ~lines 174-180 — the precise file:line anchors + the recommended fixes)
  </read_first>
  <behavior>
    - `recReducer` SET_GATE_CONFIG (new): `recReducer(state, { type: 'SET_GATE_CONFIG', targetHits: 7, cadenceMs: 500 })` when `state.substate` is a pre-gate substate (`'rotate-prompt'` or `'ready'` or `'pre-flight'`) → returns a state with `gate.targetHits === 7` and `gate.cadenceMs === 500` (and the rest of `gate` untouched — `phase: 'idle'`, `consecutiveHits: 0`, …). When `state.substate` is `'gate'` / `'active'` / `'stop-confirm'` / `'stopped'` → returns `state` unchanged (the action is a no-op once the gate has been entered, so it can't perturb an in-progress gate). `targetHits` is floored at 1, `cadenceMs` at 100 (mirror `remoteConfigGate`'s clamps so a garbage RC value can't break the ring/poll).
    - `recState.test.ts`: add cases for SET_GATE_CONFIG on `'ready'` (updates), on `'gate'` (no-op), and the clamps (`targetHits: 0 → 1`, `cadenceMs: 10 → 100`).
  </behavior>
  <action>
    **WR-01 — RemoteConfig gate values reach the reducer:**
    1. In `recState.ts`: add `| { type: 'SET_GATE_CONFIG'; targetHits: number; cadenceMs: number }` to the `RecAction` union; add a `case 'SET_GATE_CONFIG':` to `recReducer` — if `state.substate` is `'rotate-prompt'` / `'ready'` / `'pre-flight'`, return `{ ...state, gate: { ...state.gate, targetHits: Math.max(1, Math.round(action.targetHits)), cadenceMs: Math.max(100, Math.round(action.cadenceMs)) } }`; otherwise return `state`. Add a line to the substate-transition-table comment block describing it (e.g. `SET_GATE_CONFIG on a pre-gate substate → gate.{targetHits,cadenceMs} updated (no-op once 'gate' entered) — HAND-11 RemoteConfig`).
    2. In `RecordingScreen.tsx`: in the existing `readGateConfig().then((cfg) => { … })` effect (~line 222-231), in addition to `setGateCfg(cfg)`, `dispatch({ type: 'SET_GATE_CONFIG', targetHits: cfg.targetHits, cadenceMs: cfg.cadenceMs })`. (The reducer's own substate guard means a late resolve after the user already pressed record is a harmless no-op.) Leave the `initialRecState({ taskId, taskName, isPractice })` call as-is — the lazy-dispatch path is simpler than re-keying the reducer and keeps the default 5/400 as the pre-resolve value.
    3. Verify the chain: `useHandGate`'s `cadenceMs: state.gate.cadenceMs` and `GateRing`'s `target={state.gate.targetHits}` and `buildCaptureOpts`'s `gateConfig: { targetHits: state.gate.targetHits, cadenceMs: state.gate.cadenceMs }` all already read from `state.gate.*` — once SET_GATE_CONFIG lands, the live values flow through with no further change. (No new wiring needed; just confirm the reads point at `state.gate.*` and not at `gateCfg.*` or a literal.)

    **WR-02 — no orphaned native capture session on a gate→record exit:**
    4. In `RecordingScreen.tsx` mount-effect cleanup (~lines 247-252, the one that does `Orientation.unlockAllOrientations()` / `HumynScreenBrightness.set(-1)` / `cleanupHandDetector()`): add an unconditional `HumynCapture.stop().catch(() => undefined)` — it rejects `no_active_session` harmlessly when nothing is running, and recalls the camera/encoders/IMU/FGS that a `start()` already in flight (or just-resolved) brought up. (Order doesn't matter relative to the other cleanup calls.)
    5. In the `gate.confirmed → active` `run()` (~lines 411-489): after `const r = await HumynCapture.start(opts);`, the existing `if (cancelled) return;` short-circuits the `dispatch` — change it to also stop the session: `if (cancelled) { HumynCapture.stop().catch(() => undefined); return; }`. (Belt-and-suspenders with the cleanup `stop()`; this one fires the instant `start()` resolves on a cancelled run, the cleanup one fires on the actual unmount.)
    6. `handleClose` (~lines 530-539) for non-`active` substates currently does only `set(-1)` / `unlockAllOrientations()` / `goBack()` — no change needed there *if* the mount-effect cleanup `stop()` from step 4 is in place (unmount via `goBack()` triggers the cleanup). Leave `handleClose` as-is; the cleanup-effect `stop()` is the single chokepoint.

    **WR-07 — practice 60s cap survives the stop-confirm modal:**
    7. The cleanest fix per WR-07's recommendation: recompute `remaining` against wall-clock elapsed when the practice-cap effect re-arms, not against the frozen `durationMs`. Pass `startedAt: state.startedAt` from `RecordingScreen` into `useRecordingLifecycle(...)` (add it to `UseRecordingLifecycleArgs` and the destructure; keep it in a `startedAtRef`). In the practice-cap effect, compute `remaining = Math.max(0, PRACTICE_HARD_CAP_MS - (startedAtRef.current != null ? nowMs() - startedAtRef.current : durationMsRef.current))` — where `nowMs()` is the same `performance.now()`-with-`Date.now()`-fallback helper the screen uses (extract a tiny local `nowMs()` in `useRecordingLifecycle.ts` or import the screen's — a local copy is fine, it's 3 lines). Also change the practice-cap effect's deps: it currently only re-runs on `[isPractice, substate]`; keep that (re-arming on the `active`-re-entry from "Keep recording" is exactly when the stale-duration bug bit, and recomputing off wall-clock fixes it).
    8. Additionally, keep `monitoring` true through `stop-confirm` so the §10 safety stops (battery_critical / orientation / phone_call / storage_full) aren't torn down while a recording runs underneath an open modal: change `const monitoring = substate === 'gate' || substate === 'active';` to `const monitoring = substate === 'gate' || substate === 'active' || substate === 'stop-confirm';`. The inner `isActive = () => substateRef.current === 'active'` already returns false during `stop-confirm`, so the *handlers* still won't fire a stop while the modal is open — but the *subscriptions* stay live (instead of being cleaned up and re-created), which is the leak/teardown WR-07 flags. The TICK effect in `RecordingScreen.tsx` (~lines 510-517) early-returns on `substate !== 'active'` — leave that as-is; the wall-clock `remaining` recompute in step 7 makes the frozen `durationMs` irrelevant for the cap.
    9. (IN-01, swept here since it's in the same file:) remove the dead `const timers: Array<ReturnType<typeof setTimeout>> = [];` declaration in `useRecordingLifecycle.ts` (~line 192) and its `for (const t of timers) clearTimeout(t);` cleanup loop (~line 329) — nothing is ever pushed to it.

    **Tests:**
    10. In `recState.test.ts`: add the SET_GATE_CONFIG cases from `<behavior>`.

  </action>
  <verify>
    <automated>cd apps/mobile && npx vitest run __tests__/screens/recording/recState.test.ts __tests__/screens/recording/RecordingScreen.test.tsx __tests__/screens/recording/handGate.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SET_GATE_CONFIG" apps/mobile/src/screens/recording/recState.ts` shows it in the `RecAction` union AND a `case 'SET_GATE_CONFIG':` in `recReducer`.
    - `grep -n "SET_GATE_CONFIG" apps/mobile/src/screens/recording/RecordingScreen.tsx` shows the `dispatch({ type: 'SET_GATE_CONFIG', ... })` inside the `readGateConfig().then(...)` effect.
    - `grep -n "HumynCapture.stop" apps/mobile/src/screens/recording/RecordingScreen.tsx` shows a `HumynCapture.stop()` call in the mount-effect cleanup AND in the `if (cancelled)` branch after `await HumynCapture.start(opts)` (in addition to the existing one in `handleStop`).
    - `grep -n "stop-confirm" apps/mobile/src/screens/recording/useRecordingLifecycle.ts` shows `stop-confirm` is now part of the `monitoring` predicate.
    - `grep -n "startedAt" apps/mobile/src/screens/recording/useRecordingLifecycle.ts` shows `startedAt` is now an arg + a `startedAtRef`, and the practice-cap `remaining` is computed off `nowMs() - startedAtRef.current`.
    - `grep -n "startedAt" apps/mobile/src/screens/recording/RecordingScreen.tsx` shows `startedAt: state.startedAt` is passed into `useRecordingLifecycle(...)`.
    - `grep -nc "const timers" apps/mobile/src/screens/recording/useRecordingLifecycle.ts` returns 0 (IN-01 dead array removed).
    - `npx vitest run` (recState + RecordingScreen + handGate) green, including the new SET_GATE_CONFIG cases.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "recState|RecordingScreen|useRecordingLifecycle" || echo "no type errors in touched files"</automated>
  </verify>
  <done>RemoteConfig `gate.consecutive_hits_required` / `gate.cadence_ms` drive the ring target, the poll cadence, and the per-segment metadata; exiting during the gate→record handoff stops the native session (no stuck FGS); a practice recording auto-stops at ~60s of wall-clock even with the stop-confirm modal open, and the §10 safety stops stay subscribed under it.</done>
</task>

<task type="auto">
  <name>Task 3: IN-02 / IN-09 / WR-06 in-file polish + the 04-MANUAL-SMOKE.md §2 amendment</name>
  <files>apps/mobile/src/screens/recording/RecordingScreen.tsx, .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md</files>
  <read_first>
    - apps/mobile/src/screens/recording/RecordingScreen.tsx (~lines 754, 786 — `borderRadius: 999` in the `overlayTip` and `toast` StyleSheet entries; ~line 285 `handleStop` does `await HumynCapture.stop().catch(() => undefined)` and then unconditionally proceeds with the success path / toast even if `stop()` rejected; ~lines 98-103 the `SETTLE_MS = 80` `[TUNABLE]` constant; ~lines 312-327 `useRecordingLifecycle({ substate, isPractice, durationMs, callbacks })` — no `loggedOut`; the import of `radii` — currently NOT imported, `colors, spacing` are at line 48)
    - apps/mobile/src/ui/tokens.ts (lines ~239-247 — `export const radii = { ..., pill: 999 };`)
    - apps/mobile/src/screens/recording/useRecordingLifecycle.ts (lines ~120-129 the `loggedOut?` arg + ~lines 350-356 the `if (loggedOut && substate === 'active')` effect — currently inert because RecordingScreen never passes `loggedOut`)
    - apps/mobile/src/state/appStore.ts (the store shape — check whether there's a `jwt` / auth-token field that flips to null on logout; if there is, wire `loggedOut`; if there is NOT a logout signal exposed there today, do NOT invent one — note it instead per the action below)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md (the file being amended — §2 "Practice E2E" lines ~28-50, especially the bullet "**RecordingScreen — pre-flight → ready**" ~line 36 which currently just says the screen auto-rotates + locks; §1 pre-flight line ~17 which already notes the debug `apkRollout` build ships `__DEV__===true`; §7 amendments protocol at the bottom)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-REVIEW.md (IN-02 ~lines 192-198, IN-09 ~lines 248-256, WR-06 ~lines 166-172)
  </read_first>
  <action>
    **IN-02 — radii.pill:**
    1. In `RecordingScreen.tsx`: import `radii` from `'../../ui/tokens'` (add it to the existing `import { colors, spacing } from '../../ui/tokens';` → `import { colors, radii, spacing } from '../../ui/tokens';`). Replace the two `borderRadius: 999` literals (the `overlayTip` and `toast` StyleSheet entries) with `borderRadius: radii.pill`. (The `closeBtn` `borderRadius: 18`, `recordButton` `borderRadius: 44`, `stopButton` `borderRadius: 32`, `stopSquare` `borderRadius: 4`, `minuteBar` etc. are deliberate fixed half-dimensions / small radii — leave those alone; only the two `999`s become `radii.pill`.)

    **IN-09 — handleStop logs / surfaces a finalize failure:**
    2. In `handleStop` (~line 285): change `await HumynCapture.stop().catch(() => undefined);` to capture the rejection and log it (and, on the real-recording paths where the file was supposed to finalize, surface a "finalize failed" toast). Concretely:
       ```ts
       const stopErr = await HumynCapture.stop().then(() => null).catch((e) => e);
       if (stopErr) {
         logEvent('recording_stop_failed', { code: (stopErr as { code?: string } | undefined)?.code ?? 'unknown' });
       }
       ```
       — then, on the `durationMs >= 60_000` real-recording branch, if `stopErr` is truthy, show `showToast('Recording saved, but finalizing failed — it may not upload.')` *instead of* the `"{Hh Mm} added to your contribution."` toast. On the practice branch and the sub-60s discard branch, the `stopErr` log is enough (a practice clip that didn't finalize cleanly is low-stakes; a sub-60s clip is being discarded anyway). Keep the rest of `handleStop`'s flow (the navigation, the `RESET_FOR_FRESH`) unchanged — a stop-finalize failure does NOT block returning the user to Home/ready; we just don't claim a clean save. Add `'recording_stop_failed'` to the analytics-event-name list if there's a typed enum of event names anywhere (`grep -rn "recording_stopped\|recording_too_short" apps/mobile/src` to find it); if event names are free-form strings, no enum change needed.

    **WR-06 — wire or document `loggedOut`:**
    3. Check `apps/mobile/src/state/appStore.ts` for a logout signal (a `jwt` / auth-token / session field that becomes null/undefined on logout). IF such a field exists: pass `loggedOut={<that field is null>}` (e.g. `loggedOut: useAppStore((s) => s.jwt == null)`) into `useRecordingLifecycle(...)` so the §10 "logout while active → onStop('logout')" policy is live. IF no such logout signal is exposed on the store today (there's no logout path reachable from the recording surface — WR-06 says it's latent): leave the hook call as-is, but add a short comment at the `useRecordingLifecycle(...)` call site noting "// WR-06: `loggedOut` intentionally unwired — no logout path is reachable from the recording surface today; wire `loggedOut={…}` here when one is added (the hook's §10 logout effect is dormant until then)." Do NOT remove the `loggedOut` arg/effect from the hook (the §10 contract is documented; the wiring TODO lives at the call site).

    **04-MANUAL-SMOKE.md §2 amendment (CR-01's `missing[]` item 3):**
    4. In `04-MANUAL-SMOKE.md` §2 "Practice E2E", expand the "**RecordingScreen — pre-flight → ready**" bullet (and/or add a new bullet immediately after it) to explicitly walk the `rotate-prompt → ready` transition by PHYSICALLY rotating the device to landscape — NOT via the `__DEV__` "Pretend I rotated →" pill. Concretely add wording like:
       > - [ ] **RecordingScreen — rotate-prompt → ready (CR-01 regression check)** — on first entry the screen shows the `rotate-prompt` body (the `RotateCw` icon + "Rotate to landscape and mount on rig"); **physically rotate the phone to landscape** → the surface advances to `ready` (the 88×88 record button appears). Do NOT use the `__DEV__`-only "Pretend I rotated →" pill for this step. **⚠ The `apkRollout` *debug* build ships `__DEV__ === true`, which makes that pill visible and would mask the CR-01 release-build dead-end** — for this step either (a) install a *release/staging* `apkRollout` build (`./gradlew installApkRolloutRelease` — `__DEV__` false), or (b) on the debug build, deliberately ignore the pill and only count the *physical-rotation* path as the pass. (If the device is already in landscape when RecordingScreen mounts — e.g. it was held landscape from PracticeIntro — the screen should land directly in `ready` with no `rotate-prompt` flash; that also counts as a pass for the fire-once `getDeviceOrientation` read.)
       Also add a one-line note to the §2 "Acceptance" line that the rotate-prompt → ready transition happens via physical rotation (not the dev pill) and that the debug-build `__DEV__` caveat applies. Keep all the other §2 boxes intact — this is an addition, not a rewrite. Do NOT touch §3/§4/§5/§5b/§6/§7 of the runbook.

  </action>
  <verify>
    <automated>cd apps/mobile && npx vitest run __tests__/screens/recording/RecordingScreen.test.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "RecordingScreen" || echo "ok"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nc "borderRadius: 999" apps/mobile/src/screens/recording/RecordingScreen.tsx` returns 0; `grep -n "radii.pill" apps/mobile/src/screens/recording/RecordingScreen.tsx` shows ≥2 uses and `grep -n "import.*radii" apps/mobile/src/screens/recording/RecordingScreen.tsx` shows the import.
    - `grep -n "recording_stop_failed\|stopErr" apps/mobile/src/screens/recording/RecordingScreen.tsx` shows `handleStop` now captures + logs the `HumynCapture.stop()` rejection and shows a "finalizing failed" toast on the real-recording ≥60s path.
    - `grep -n "loggedOut\|WR-06" apps/mobile/src/screens/recording/RecordingScreen.tsx` shows either a real `loggedOut={…}` prop wired from the store OR the documented "intentionally unwired" comment at the `useRecordingLifecycle(...)` call site.
    - `grep -n "physically rotat\|Pretend I rotated\|__DEV__" .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md` shows the new §2 bullet that walks rotate-prompt → ready by physical rotation and flags the debug-build `__DEV__` masking caveat.
    - `npx vitest run __tests__/screens/recording/RecordingScreen.test.tsx` green; `npx tsc --noEmit` reports no new errors in `RecordingScreen.tsx`.
  </acceptance_criteria>
  <done>The recording screen uses `radii.pill` for its pills; a stop-finalize failure is logged and surfaced (not silently claimed as success); `loggedOut` is either wired or its TODO is recorded at the call site; the smoke runbook §2 walks the CR-01-relevant rotate-prompt → ready transition by physical rotation with the debug-`__DEV__` caveat called out.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                                     | Description                                                                                                                                                                                                                              |
| ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| device sensors → app (Orientation listener)                                  | physical-orientation events drive the rotate-prompt → ready transition; the only "input" is an enum value from the OS-backed orientation lib — no user-controlled data                                                                   |
| app → camera / encoders / IMU / foreground service (HumynCapture.start/stop) | the recording surface brings up (and must tear down) device-level capture resources + a persistent FGS notification; a failure to tear down is a privacy/resource concern, not an external attack surface                                |
| Firebase Remote Config → app (gate config)                                   | RC supplies `gate.{consecutive_hits_required,cadence_ms}`; already clamped in `remoteConfigGate.ts` and re-clamped in the `SET_GATE_CONFIG` reducer case — a garbage RC value can't break the ring/poll or block the gate (Security V14) |

## STRIDE Threat Register

| Threat ID | Category                                           | Component                                                                                                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                 |
| --------- | -------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-4.11-01 | I (Information disclosure) / D (Denial of service) | `RecordingScreen` gate→record handoff — `HumynCapture.start()` in flight when the user exits              | mitigate    | Task 2 steps 4–5: unconditional `HumynCapture.stop().catch(() => undefined)` in the mount-effect cleanup + a `stop()` after a cancelled `start()` resolves — no orphaned camera/IMU/FGS, no stuck "recording" notification implying the camera is still on                      |
| T-4.11-02 | T (Tampering)                                      | RemoteConfig `gate.{cadence_ms,consecutive_hits_required}` flowing into the reducer via `SET_GATE_CONFIG` | mitigate    | `SET_GATE_CONFIG` re-clamps (`targetHits ≥ 1`, `cadenceMs ≥ 100`) AND is a no-op once `substate === 'gate'` — a hostile/garbled RC push can neither break the gate UI nor mutate an in-progress gate; the hard-coded 5/400 remain the unconditional pre-resolve fallback        |
| T-4.11-03 | D (Denial of service)                              | the new `rotate-prompt` orientation effect — a flapping device-orientation stream                         | accept      | the reducer's `LANDSCAPE_DETECTED` case no-ops outside `rotate-prompt` and the effect tears down once `substate` changes; a flapping listener at worst dispatches a few no-op actions — bounded, no resource growth, no need to debounce                                        |
| T-4.11-04 | R (Repudiation)                                    | `handleStop` swallowing a `HumynCapture.stop()` rejection then claiming a clean save (IN-09)              | mitigate    | Task 3 step 2: log `recording_stop_failed` with the error code + show a "finalizing failed — it may not upload" toast on the real-recording path instead of the "{Hh Mm} added" toast — the user (and analytics) aren't told a recording was saved cleanly when finalize failed |

No new attack surface is introduced (no new permissions, no new IPC, no new network calls, no new file paths). ASVS L1: the relevant controls are input validation on the RC-sourced gate config (already present + reinforced) and resource teardown on the capture session (the WR-02 fix). No `high`-severity item — nothing blocks.
</threat_model>

<verification>
- `cd apps/mobile && npx vitest run` — the full mobile suite is green, including the new no-`__test_initialState` render test in `RecordingScreen.test.tsx` and the new `SET_GATE_CONFIG` cases in `recState.test.ts`.
- `cd apps/mobile && npx tsc --noEmit -p tsconfig.json` — no new type errors.
- `grep -n "LANDSCAPE_DETECTED" apps/mobile/src/screens/recording/RecordingScreen.tsx` — a production (non-`__DEV__`) dispatcher exists (the new `rotate-prompt` orientation effect).
- `grep -n "HumynCapture.stop" apps/mobile/src/screens/recording/RecordingScreen.tsx` — `stop()` is called in the mount-effect cleanup AND after a cancelled `start()`.
- `grep -n "SET_GATE_CONFIG" apps/mobile/src/screens/recording/recState.ts apps/mobile/src/screens/recording/RecordingScreen.tsx` — the action exists in the reducer and is dispatched from `readGateConfig().then()`.
- `grep -n "physically rotat" .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md` — the §2 amendment is present.
- `grep -nc "borderRadius: 999" apps/mobile/src/screens/recording/RecordingScreen.tsx` — returns 0 (IN-02 done).
- `grep -nc "const timers" apps/mobile/src/screens/recording/useRecordingLifecycle.ts` — returns 0 (IN-01 done).
</verification>

<success_criteria>

- CR-01 closed: in a release build the recording surface advances `rotate-prompt → ready` on physical landscape (or mounts directly into `ready` if already landscape), so SC1–SC5 of the Phase-4 goal are all reachable.
- WR-01 closed: RemoteConfig `gate.consecutive_hits_required` / `gate.cadence_ms` reach the reducer and drive the GateRing target, the poll cadence, and the per-segment metadata.
- WR-02 closed: no orphaned native capture session / stuck FGS notification on a gate→record-handoff exit.
- WR-07 closed: a practice recording auto-stops at ~60s wall-clock even with the stop-confirm modal open; the §10 safety stops stay subscribed under it.
- WR-06 / IN-01 / IN-02 / IN-09 resolved (wired-or-documented / dead code removed / token used / failure surfaced).
- `04-MANUAL-SMOKE.md` §2 walks the rotate-prompt → ready transition by physical rotation, with the debug-`__DEV__` masking caveat called out.
- A render test that mounts `RecordingScreen` with the default state would have caught CR-01 — the regression-detection gap is closed.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-11-SUMMARY.md`
</output>
