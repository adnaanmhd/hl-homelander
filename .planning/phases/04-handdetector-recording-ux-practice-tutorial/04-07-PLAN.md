---
phase: 04-handdetector-recording-ux-practice-tutorial
plan: 07
type: execute
wave: 3
depends_on: ['04-01']
files_modified:
  - apps/mobile/src/screens/recording/recState.ts
  - apps/mobile/src/screens/recording/components/GateRing.tsx
  - apps/mobile/src/screens/recording/components/VoiceCuePill.tsx
  - apps/mobile/src/screens/recording/components/StopConfirmModal.tsx
  - apps/mobile/src/screens/recording/components/AlertPill.tsx
  - apps/mobile/src/screens/recording/components/RotatePrompt.tsx
  - apps/mobile/src/screens/recording/RecordingScreen.tsx
  - apps/mobile/src/navigation/RootNativeStack.tsx
  - apps/mobile/src/ui/tokens.ts
  - apps/mobile/__tests__/screens/recording/recState.test.ts
  - apps/mobile/__tests__/screens/RecordingScreen.test.tsx
  - apps/mobile/__tests__/navigation/route-registry.test.ts
  - apps/mobile/__tests__/visual/RecordingScreen.visual.test.tsx
  - apps/mobile/__tests__/visual/__image_snapshots__/recording-rotate-prompt.png
  - apps/mobile/__tests__/visual/__image_snapshots__/recording-ready.png
  - apps/mobile/__tests__/visual/__image_snapshots__/recording-gate-ring-0.png
  - apps/mobile/__tests__/visual/__image_snapshots__/recording-gate-ring-50.png
  - apps/mobile/__tests__/visual/__image_snapshots__/recording-gate-ring-100.png
  - apps/mobile/__tests__/visual/__image_snapshots__/recording-active-t10s.png
  - apps/mobile/__tests__/visual/__image_snapshots__/recording-active-t05m32s.png
  - apps/mobile/__tests__/visual/__image_snapshots__/recording-stop-confirm-modal.png
autonomous: true
requirements:
  [REC-01, REC-02, REC-03, REC-05, REC-06, HAND-02, HAND-04, HAND-05, HAND-06, HAND-10, REC-15]
must_haves:
  truths:
    - 'recState.ts exports the RecState type (verbatim from engineering-handoff.md §4.3) + a recReducer with all transitions: rotate-prompt→ready, ready→pre-flight, pre-flight→gate / pre-flight→ready (thermal), gate.loading→gate.waiting, GATE_HIT increments / GATE_MISS resets consecutiveHits to 0, consecutiveHits>=targetHits→gate.confirmed(passed), SKIP→gate.confirmed(skipped), BYPASS→gate.confirmed(bypassed), CAPTURE_STARTED→active, CAPTURE_START_FAILED→ready, active→stop-confirm, stop-confirm→active / →stopped, STOP→stopped, practice-hard-cap→stopped, double-stop guard via ended'
    - "RecordingScreen renders the substate-driven dark-theme chrome (top 3px minute-bar, 36px circular X, top-row task name, 3s 'Don't exit while recording.' overlay, 130×130 gate ring + prompt + Skip link, 88×88 coral record button on ready, 32px mono HH:MM:SS timer + 64×64 white stop button on active, voice-cue pill, alert pill, rotate-prompt body) for a given substate — no live camera yet"
    - 'RootNativeStack registers a Recording route (sibling of MainTabs) with gestureEnabled:false, headerShown:false, animation:fade'
    - "route-registry.test.ts moves 'Recording' out of the phase3Plus must-be-absent list and adds it to a REQUIRED_PHASE_4_ROUTES block asserting 'Recording' (RootNativeStack) is registered — NOTE: this plan asserts ONLY 'Recording'; the 'PracticeIntro'/'PracticeComplete' (OnboardingStack) required-route assertions are owned by plan 04-09 (which depends_on both 04-06 — registers them — and 04-07 — owns this test), so they are NOT in scope here; this plan is the sole owner of route-registry.test.ts within plan-wave 3"
  artifacts:
    - path: 'apps/mobile/src/screens/recording/recState.ts'
      provides: 'RecState type + recReducer + initialRecState(routeParams) — the §4.3 state machine'
      exports: ['RecState', 'recReducer', 'initialRecState']
      min_lines: 90
    - path: 'apps/mobile/src/screens/recording/RecordingScreen.tsx'
      provides: 'the dark-theme recording surface shell rendering substate-driven chrome'
      min_lines: 80
    - path: 'apps/mobile/src/screens/recording/components/GateRing.tsx'
      provides: '130×130 SVG progress ring (6px stroke, accent fill on rgba(255,255,255,.18) track, clockwise via strokeDashoffset, instant snap-to-0 on miss)'
      contains: 'strokeDashoffset'
    - path: 'apps/mobile/src/navigation/RootNativeStack.tsx'
      provides: 'Recording route registration'
      contains: 'Recording'
  key_links:
    - from: 'apps/mobile/src/screens/recording/RecordingScreen.tsx'
      to: 'recReducer'
      via: 'useReducer(recReducer, initialRecState(route.params))'
      pattern: 'useReducer'
    - from: 'apps/mobile/src/navigation/RootNativeStack.tsx'
      to: 'RecordingScreen'
      via: '<Root.Screen name="Recording" component={RecordingScreen} ...>'
      pattern: 'name="Recording"'
---

<objective>
Build the recording-surface scaffold: the `recState.ts` state machine (verbatim shape from `engineering-handoff.md §4.3` — full `recReducer` with every transition), the five recording UI components (`GateRing`, `VoiceCuePill`, `StopConfirmModal`, `AlertPill`, `RotatePrompt`), the `RecordingScreen.tsx` shell that renders substate-driven dark-theme chrome (NO live camera / NO state-machine wiring / NO lifecycle hook — that's plans 04-08 + 04-09), the `Recording` route in `RootNativeStack`, the `route-registry.test.ts` update (locked Pattern 54), and the 8 static-surface visual baselines.

Purpose: HAND-02/HAND-04/HAND-05/HAND-06/HAND-10 (the gate ring + reducer semantics), REC-01/REC-02/REC-03/REC-05/REC-06 (the surface chrome + reducer transitions), REC-15 (voice-cue pill renders the spoken text). Plans 04-08/04-09 wire the live camera, lifecycle, and gate-pass transition onto this scaffold.
Output: recState.ts + 5 components + RecordingScreen shell + RootNativeStack route + route-registry update + recState reducer tests + RecordingScreen render test + 8 visual baselines.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-UI-SPEC.md
@CLAUDE.md
@design-spec.md
@engineering-handoff.md
@prototype.html
@apps/mobile/src/components/CompatRing.tsx
@apps/mobile/src/components/LogoutModal.tsx
@apps/mobile/src/components/SoftUpgradeBanner.tsx
@apps/mobile/src/screens/compat/CompatRunningScreen.tsx
@apps/mobile/src/navigation/RootNativeStack.tsx
@apps/mobile/src/state/initialRoute.ts
@apps/mobile/src/ui/tokens.ts
@apps/mobile/__tests__/state/initialRoute.test.ts
@apps/mobile/__tests__/navigation/route-registry.test.ts
@apps/mobile/__tests__/visual/RigTutorialScreen.visual.test.tsx
@apps/mobile/__tests__/visual/_utils/renderToImage.ts

<interfaces>
<!-- RecState type — VERBATIM from engineering-handoff.md §4.3 / 04-RESEARCH lines 322-343 -->
type RecState = {
  taskId: string; taskName: string; isPractice: boolean;
  startedAt: number | null;            // performance.now() at active-recording start
  durationMs: number;
  cap: 60_000 | 1_200_000;             // practice = 60s, real "doc value" 20m — JS owns ONLY the practice 60s hard cap; real recordings have no JS cap (HC auto-segments at 10m)
  ended: boolean;                       // double-stop guard
  alerts: { battery?: boolean; thermal?: boolean };
  gate: {
    phase: 'idle' | 'loading' | 'waiting' | 'confirmed';
    consecutiveHits: number;            // count===2 streak; resets to 0 on any miss
    targetHits: number;                 // 5 Android / 3 iOS (from RemoteConfig gate.consecutive_hits_required)
    cadenceMs: number;                  // 400 Android / 600 iOS (from RemoteConfig gate.cadence_ms)
    skipped: boolean;                   // user tapped Skip
    bypassed: boolean;                  // HandDetector native module unavailable
    startedAt: number | null;           // performance.now() at gate enter
    confirmedAt: number | null;         // performance.now() at gate exit (success/skip/bypass)
  };
};
// substate enum (planner-derived discriminant from the §4.3 diagram):
type RecSubstate = 'rotate-prompt' | 'ready' | 'pre-flight' | 'gate' | 'active' | 'stop-confirm' | 'stopped';
// → add `substate: RecSubstate` to RecState (the §4.3 type names states; the planner reproduces it as a discriminant).
// `gate.phase` is the sub-discriminant within substate==='gate'. `alerts.battery`/`alerts.thermal` are overlay flags (recording continues), not substates.

<!-- CompatRing.tsx — the SVG ring technique GateRing copies (design-spec §5.8: build a FRESH component, same technique) -->

const AnimatedCircle = Animated.createAnimatedComponent(Circle); const SIZE = 130; const STROKE = 6; const RADIUS = (SIZE - STROKE)/2; const CIRC = 2*Math.PI*RADIUS;
<AnimatedCircle cx={SIZE/2} cy={SIZE/2} r={RADIUS} stroke={colors.accent} strokeWidth={STROKE} fill="none" strokeLinecap="round"
strokeDasharray={`${CIRC} ${CIRC}`} strokeDashoffset={offset} transform={`rotate(-90 ${SIZE/2} ${SIZE/2})`} />
// track circle behind it: stroke = rgba(255,255,255,.18). Animated.timing(offset, { toValue: CIRC\*(1 - hits/target), duration: ..., useNativeDriver: false }) — BUT a non-2-hand miss SNAPS offset to CIRC instantly (no animation).

<!-- LogoutModal.tsx — the two-action confirm-modal template StopConfirmModal copies (Pattern 66 inFlightRef) -->

<Modal transparent visible animationType="fade" onRequestClose={cancel}><View style={scrim/_ rgba(0,0,0,.5) _/}><View style={card} accessibilityLabel="stop-confirm-modal">
<Text variant="sheetTitle">Stop recording?</Text><Text variant="body" tone="secondary">Recordings under 1 minute are discarded.</Text>
<View style={actions}><Button variant="outline" label="Keep recording" onPress={cancel}/><Button variant="coral" label="Stop" onPress={confirm}/></View>
</View></View></Modal>

<!-- RootNativeStack route to add (sibling of MainTabs) -->

<Root.Screen name="Recording" component={RecordingScreen} options={{ gestureEnabled: false, headerShown: false, animation: 'fade' }} />
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: recState.ts state machine + recState.test.ts</name>
  <behavior>
    - `initialRecState({ taskId, taskName, isPractice })` → `{ substate: 'rotate-prompt', taskId, taskName, isPractice, startedAt: null, durationMs: 0, cap: isPractice ? 60_000 : 1_200_000, ended: false, alerts: {}, gate: { phase: 'idle', consecutiveHits: 0, targetHits: 5, cadenceMs: 400, skipped: false, bypassed: false, startedAt: null, confirmedAt: null } }` (targetHits/cadenceMs may be overridden via an optional `gateConfig` arg from RemoteConfig — default 5/400)
    - `LANDSCAPE_DETECTED` on `rotate-prompt` → `ready`; no-op on any other substate
    - `START_PRESSED` on `ready` → `pre-flight`
    - `PRE_FLIGHT_OK` on `pre-flight` → `gate` with `gate.phase: 'loading'`, `gate.startedAt: <now>`
    - `PRE_FLIGHT_FAILED` (e.g. thermal/storage/battery start-guard) on `pre-flight` → `ready`
    - `CAMERA_READY` on `gate` while `gate.phase==='loading'` → `gate.phase: 'waiting'` (the accumulator does NOT start until the first frame — HAND-06)
    - `GATE_HIT` while `gate.phase==='waiting'` → `consecutiveHits + 1`; if `consecutiveHits + 1 >= targetHits` → `gate.phase: 'confirmed'`, `gate.confirmedAt: <now>`, `skipped:false`, `bypassed:false`
    - `GATE_MISS` while `gate.phase==='waiting'` → `consecutiveHits: 0` (HAND-04 instant reset; the ring snap-to-0 is the component's job)
    - `GATE_SKIP` on `gate` → `gate.phase: 'confirmed'`, `gate.skipped: true`, `gate.confirmedAt: <now>`
    - `GATE_BYPASS` on `gate` → `gate.phase: 'confirmed'`, `gate.bypassed: true`, `gate.confirmedAt: <now>`
    - `CAPTURE_STARTED` on `gate` (phase confirmed) → `active`, `startedAt: <now>`, `durationMs: 0`
    - `CAPTURE_START_FAILED` on `gate` (phase confirmed) → `ready`, reset `gate` to idle
    - `TICK` on `active` → `durationMs: now - startedAt` (or accept an explicit `durationMs` in the action); when `isPractice && durationMs >= 60_000` the reducer leaves `active` AS IS (the 60s cap is enforced by RecordingScreen/useRecordingLifecycle dispatching `STOP` — but provide a `PRACTICE_HARD_CAP` action that maps `active`→`stopped` for the explicit case) — re-read 04-RESEARCH to confirm; the cap is JS-owned (HC's own timer is 10m)
    - `X_PRESSED` on `active` → `stop-confirm`; on `rotate-prompt`/`ready`/`gate` it's a no-op at the reducer level (the screen handles the pre-record silent-dismiss navigation; HAND-10 — no confirmation modal pre-record)
    - `STOP_CONFIRM_CANCEL` on `stop-confirm` → `active`
    - `STOP_CONFIRM_STOP` on `stop-confirm` → `stopped`, `ended: true`
    - `STOP` (lifecycle policy / manual stop button) on `active` → `stopped`, `ended: true`; on `stop-confirm` → `stopped`, `ended: true`; if already `ended` it's a no-op (double-stop guard)
    - `BATTERY_ALERT` → set `alerts.battery: true` (overlay; recording continues — does NOT change substate); `THERMAL_ALERT` → `alerts.thermal: true` (overlay)
    - `RESET_FOR_FRESH` on `stopped` (real recording re-press) → back to `ready` with `gate` reset to idle, `durationMs:0`, `ended:false`, `alerts:{}` (REC-05 — fresh recording, no countdown)
    - no timeout transition exists (HAND-05 — gate runs indefinitely until pass or skip)
  </behavior>
  <read_first>
    - engineering-handoff.md §4.3 (the canonical `recState` shape — reproduce the type VERBATIM; the substate-enum decomposition is the planner's call as long as transitions match — see <interfaces> above)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § "recState shape — VERBATIM" (lines 318-347) + Pattern 1 (the gate poll loop the reducer feeds) + the Anti-Patterns ("building the 60s practice hard-cap inside HumynCapture" — it's JS-owned; "no timeout transition")
    - apps/mobile/src/state/initialRoute.ts (the "pure module returning a discriminated union, no side effects, top docstring listing the decision tree" precedent — recState.ts follows that FILE shape)
    - apps/mobile/__tests__/state/initialRoute.test.ts (the `baseState(overrides)` helper + one-assertion-per-branch pattern — recState.test.ts mirrors this)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md § "src/screens/recording/recState.ts (NET-NEW)" + § "recState.test.ts (reducer policy table)"
  </read_first>
  <action>
    Create `apps/mobile/src/screens/recording/recState.ts`:
    - Top block-comment docstring listing the substate transition table (the `<behavior>` bullets above), citing `engineering-handoff.md §4.3`.
    - Export `type RecSubstate`, `type RecState` (the verbatim §4.3 shape + the `substate: RecSubstate` discriminant — see `<interfaces>`), `type RecAction` (a discriminated union over `{ type: '...' }` for every action listed in `<behavior>`), `function initialRecState(params: { taskId: string; taskName: string; isPractice: boolean }, gateConfig?: { targetHits?: number; cadenceMs?: number }): RecState`, and `function recReducer(state: RecState, action: RecAction): RecState`. The reducer is a `switch (action.type)` with `switch (state.substate)` guards where needed; it is PURE — no side effects, no `performance.now()` calls inside the reducer (the caller passes `now` via the action payload where a timestamp is needed: `{ type: 'CAPTURE_STARTED', now: number, ... }`, `{ type: 'PRE_FLIGHT_OK', now: number }`, `{ type: 'TICK', durationMs: number }`, etc.). Implement every transition in `<behavior>` exactly. Unknown action types and invalid-for-substate actions return `state` unchanged.
    - Create `apps/mobile/__tests__/screens/recording/recState.test.ts` — mirror `initialRoute.test.ts`: a `baseState(overrides)` helper + one `it()` per `<behavior>` bullet (~25 cases). Cover the double-stop guard, the GATE_HIT-at-target→confirmed, the GATE_MISS→0 reset, the SKIP/BYPASS→confirmed(skipped/bypassed), the no-op-for-wrong-substate cases, the `RESET_FOR_FRESH` path, the `PRACTICE_HARD_CAP`→stopped, and `initialRecState` defaults (incl. `cap` = 60_000 for practice / 1_200_000 for real, and the `gateConfig` override).
  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/screens/recording/recState.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix apps/mobile test -- --run __tests__/screens/recording/recState.test.ts` exits 0
    - `apps/mobile/src/screens/recording/recState.ts` exists and exports `RecState`, `recReducer`, `initialRecState` (grep: `export type RecState`, `export function recReducer`, `export function initialRecState`)
    - the file contains the substate values `'rotate-prompt'`, `'ready'`, `'pre-flight'`, `'gate'`, `'active'`, `'stop-confirm'`, `'stopped'` and the gate-phase values `'idle'`, `'loading'`, `'waiting'`, `'confirmed'`
    - the file does NOT call `performance.now()` or `Date.now()` (the reducer is pure; timestamps come from action payloads) — grep returns 0 for `performance.now` and `Date.now` in recState.ts
    - `__tests__/screens/recording/recState.test.ts` has ≥ 20 `it(` cases
    - the full suite `npm --prefix apps/mobile test -- --run` exits 0
  </acceptance_criteria>
  <done>recState.ts pure state machine (verbatim §4.3 shape, all transitions) + ~25-case reducer test landed; full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Recording UI components + RecordingScreen shell + RootNativeStack route + route-registry.test.ts update + RecordingScreen render test</name>
  <read_first>
    - design-spec.md §7 (every recording substate 7a–7h — the verbatim layout, copy, dimensions, animation curves) + §18.2 (Stop confirm modal) + §19.1–§19.3 (toasts / voice-cue overlay / alert pill) + UI-SPEC § Spacing (the recording-surface specific offsets: top minute-bar 3px full-width, top X 36px circular, overlay-tip 88px from top, gate ring 130×130 6px stroke, active timer 84px from top, stop button 64×64 white circle w/ 22×22 coral square, record button 88×88 coral, voice-cue pill 260px wide, alert pill 38px from top / 14px from right) + UI-SPEC § Typography (gate prompt 17/24 500 white 95%, skip link 14/600 rgba(255,255,255,.7), task name 14/600, mono timer 32/32 600 +1px, overlay tip 13/18 caption, alert pill 12/600 white) + UI-SPEC § Color § "Dark recording surface" (the dark-theme tokens: bg #0A0A0A, accent #FF6A2D minute-bar + ring fill, coral #E84A38 record button + stop square, amber #F2A53C alert pill, overlay-tip bg rgba(0,0,0,.6) + 8px blur, toast bg rgba(26,26,26,.94), voice-cue bg white@96%, ring track rgba(255,255,255,.18))
    - UI-SPEC § Copywriting Contract (the verbatim copy: overlay tip "Don't exit while recording.", gate prompt "Mount the phone on your head and bring your hands in frame for 2 secs", gate loading caption "Preparing camera…", rotate-prompt label "Rotate to landscape and mount on rig", "Pretend I rotated →" debug pill, stop modal title "Stop recording?" `[confirm w/ PM]` / body "Recordings under 1 minute are discarded." LOCKED / actions "Keep recording" `btn-outline` + "Stop" `btn-coral`, "Start Recording" label under the record button, "Skip" link, voice cues "Recording started" / "Recording stopped", battery alert pill "Battery 15%", thermal alert pill "Phone too hot")
    - apps/mobile/src/components/CompatRing.tsx (the SVG ring technique — copy the strokeDasharray/strokeDashoffset/rotate(-90) approach; GateRing is a FRESH component per design-spec §5.8 but uses the same technique, with 6px stroke vs CompatRing's 8px and the rgba(255,255,255,.18) dark-theme track; the snap-to-0-on-miss bypasses Animated.timing and sets the value directly)
    - apps/mobile/src/components/LogoutModal.tsx (the two-action confirm-modal template — Pattern 66 `inFlightRef` re-entrancy guard, `<Modal transparent visible animationType="fade">`, scrim `rgba(0,0,0,0.5)`, `Button variant="outline"` + the destructive variant; StopConfirmModal needs a `coral` Button variant — check Button.tsx; if `variant="coral"` doesn't exist add it (fill `colors.coral #E84A38`, white label))
    - apps/mobile/src/components/SoftUpgradeBanner.tsx (the transient-banner shape — VoiceCuePill and AlertPill are auto-fading variants)
    - apps/mobile/src/screens/compat/CompatRunningScreen.tsx (the `useEffect` + ref-tracked-timer + cleanup screen pattern — RecordingScreen shell reuses the structure; NO live camera yet)
    - apps/mobile/src/navigation/RootNativeStack.tsx (the `<Root.Screen name="X" component={X} options={...} />` list — `ForceUpgrade` shows `gestureEnabled:false`; `LogoutModal` shows `animation:'fade'`; add Recording as a MainTabs sibling)
    - apps/mobile/__tests__/navigation/route-registry.test.ts (the locked Pattern-54 invariant — `Recording` is currently in the `phase3Plus = ['Recording', 'Player']` must-be-absent list at line ~125; this plan moves `'Recording'` OUT of that list and adds it to a new `REQUIRED_PHASE_4_ROUTES` block — assert ONLY `'Recording'` here. Do NOT add `'PracticeIntro'`/`'PracticeComplete'`: those are registered by sibling plan 04-06 in the same plan-wave (within-wave order is not guaranteed), so the `'PracticeIntro'`/`'PracticeComplete'` required-route assertions are added later by plan 04-09 — which depends_on both 04-06 and 04-07. This plan is the SOLE owner of route-registry.test.ts within plan-wave 3.)
    - apps/mobile/__tests__/visual/RigTutorialScreen.visual.test.tsx + apps/mobile/__tests__/visual/_utils/renderToImage.ts (the structural-wireframe-PNG snapshot pattern; the new lib mocks from plan 04-01 must be present — they are, since this plan depends_on 04-01)
    - apps/mobile/src/ui/tokens.ts (confirm `colors.recBg`/`#0A0A0A` exists; UI-SPEC § Color § "Tokens-only styling" lists the new dark-theme tokens that may need adding: overlay-tip rgba(0,0,0,.6), toast rgba(26,26,26,.94), voice-cue white@96%, ring track rgba(255,255,255,.18); add them to tokens.ts so the no-hex-literals gate stays green — the `__tests__/ui/no-hex-literals.test.ts` excludes `ui/tokens.ts` and `ui/primitives/*`)
  </read_first>
  <action>
    1. **tokens.ts** — add the dark-theme recording tokens UI-SPEC names that aren't already present: `colors.recOverlayTip` (`'rgba(0,0,0,0.6)'`), `colors.recToastBg` (`'rgba(26,26,26,0.94)'`), `colors.recVoiceCueBg` (`'rgba(255,255,255,0.96)'`), `colors.recRingTrack` (`'rgba(255,255,255,0.18)'`); confirm `colors.recBg` (`'#0A0A0A'`), `colors.accent`, `colors.coral`, `colors.amber` exist (add `colors.amber = '#F2A53C'` if missing). (All hex/rgba literals must live in tokens.ts so `__tests__/ui/no-hex-literals.test.ts` stays green.) Also add a `coral` and `accent` Button variant to `apps/mobile/src/ui/primitives/Button.tsx` if not already present (fill `colors.coral` / `colors.accent`, white label).
    2. **Components** under `apps/mobile/src/screens/recording/components/`:
       - `GateRing.tsx` — props `{ hits: number; target: number; loading: boolean }`. 130×130 SVG: a track `<Circle>` at `colors.recRingTrack`, an animated fill `<Circle>` at `colors.accent` (6px stroke, `strokeLinecap="round"`, `strokeDasharray`/`strokeDashoffset` clockwise via `rotate(-90 65 65)`, `useNativeDriver:false`). `Animated.timing` the offset toward `CIRC*(1 - hits/target)` on `hits` increase; on a `hits` DROP (a miss) set the Animated value DIRECTLY (no timing — instant snap-to-0, HAND-04). When `loading` is true, render a spinner inside the ring well + the caption "Preparing camera…" (`caption` variant, white) below, ring at 0 (HAND-06 — accumulator doesn't start until the first frame). `accessibilityLabel="gate-ring"`.
       - `VoiceCuePill.tsx` — props `{ text: string; visible: boolean; durationMs?: number }`. 260px-wide pill, `colors.recVoiceCueBg` background, dark text, centered; auto-fades after `durationMs ?? 1800` (design-spec §7d — 1.8s for "Recording started"). Renders `text` so the spoken cue is duplicated for accessibility (REC-15). `accessibilityLabel="voice-cue-pill"`.
       - `StopConfirmModal.tsx` — props `{ visible: boolean; onKeepRecording(): void; onStop(): void }`. Template from `LogoutModal.tsx`: `<Modal transparent visible={visible} animationType="fade" onRequestClose={onKeepRecording}>`, scrim, dark `card` (`accessibilityLabel="stop-confirm-modal"`), `<Text variant="sheetTitle">Stop recording?</Text>` (the `[confirm w/ PM]` recommended string; if PM supplied an override use it), `<Text variant="body" tone="secondary">Recordings under 1 minute are discarded.</Text>` (LOCKED), actions `<Button variant="outline" label="Keep recording" onPress={onKeepRecording} />` + `<Button variant="coral" label="Stop" onPress={() => { if (inFlightRef.current) return; inFlightRef.current = true; onStop(); }} />` (Pattern 66 re-entrancy ref). Dark-theme scrim/card values per UI-SPEC.
       - `AlertPill.tsx` — props `{ label: string; visible: boolean }`. Small pill anchored 38px from top / 14px from right, `colors.amber` background, white 12/600 text (battery: "Battery 15%"; thermal: "Phone too hot"). `accessibilityLabel="alert-pill"`.
       - `RotatePrompt.tsx` — props `{ onPretendRotated?(): void }` (the `__DEV__`-gated "Pretend I rotated →" debug pill). Centered body: an icon + `<Text variant="caption" style={{ color: white }}>Rotate to landscape and mount on rig</Text>`, and (only when `__DEV__`) an orange pill `<Pressable onPress={onPretendRotated}><Text variant="pillLabel">Pretend I rotated →</Text></Pressable>` (`btn-accent`-colored). `accessibilityLabel="rotate-prompt"`.
    3. **RecordingScreen.tsx** — NEW shell. `useReducer(recReducer, initialRecState(route.params))` where `route.params = { taskId, taskName, isPractice }` (the screen reads `route.params.taskId ?? '__practice__'`, `taskName ?? 'Practice — 60 sec'`, `isPractice ?? false`). Render the dark-theme surface: `<View style={{ flex:1, backgroundColor: colors.recBg }}>` with substate-driven children: a top 3px-tall full-width minute-bar (`colors.accent` width = `min(durationMs/60000, 1) * 100%` during active), a top-right 36px circular X (`<Pressable>` with Lucide `X` icon, `accessibilityLabel="recording-close"` — the screen's X handler dispatches `X_PRESSED` when `substate==='active'` else navigates back/Home silently per HAND-10 — but the actual navigation wiring is minimal here; just dispatch the action + a `navigation.goBack()` for pre-record substates), the top-row task name (`route.params.taskName`), a 3s "Don't exit while recording." overlay (`colors.recOverlayTip` bg, fades after 3s via `useEffect` + Reanimated, anchored 88px from top), and then `switch (substate)`:
       - `'rotate-prompt'` → `<RotatePrompt onPretendRotated={() => dispatch({ type: 'LANDSCAPE_DETECTED' })} />` (the `__DEV__` pill force-advances to ready — actually it should advance to `ready`; re-read the diagram: rotate-prompt → ready on LANDSCAPE_DETECTED, then a tap on the record button → pre-flight → gate. So the pill dispatches LANDSCAPE_DETECTED.)
       - `'ready'` → an 88×88 `colors.coral` circular record button with the label "Start Recording" (white 14/600) below — `onPress` dispatches `START_PRESSED` (the pre-flight→gate wiring is plan 04-09; here it's a stub that just dispatches).
       - `'pre-flight'` → a brief spinner (no chrome change; the thermal/storage/battery checks happen in plan 04-09 — here `pre-flight` just renders a spinner that auto-dispatches `PRE_FLIGHT_OK` after a tick so the visual baseline can land on `gate`).
       - `'gate'` → `<GateRing hits={gate.consecutiveHits} target={gate.targetHits} loading={gate.phase === 'loading'} />` + `<Text variant="tutBody" style={{ color: 'white' }}>Mount the phone on your head and bring your hands in frame for 2 secs</Text>` + a `<Pressable onPress={() => dispatch({ type: 'GATE_SKIP' })}><Text variant="skipLink">Skip</Text></Pressable>` visible from t=0 (HAND-02). (The live camera `<Camera>` mount + the poll loop + the gate-pass transition are plan 04-09 — here the gate substate renders the chrome only; the visual baselines mock `gate.consecutiveHits` at 0 / ~2.5 (50%) / target (100%).)
       - `'active'` → a 32px mono `HH:MM:SS` counter (`durationMs` formatted, 600 +1px tracking, anchored 84px from top), the minute-bar at the top, and a floating 64×64 white circular stop button with a 22×22 `colors.coral` square inside (`accessibilityLabel="recording-stop"`, `onPress` dispatches `STOP`). Also render `<VoiceCuePill text="Recording started" visible={...} />` (the visibility wiring is plan 04-09; here a prop). `<AlertPill label={alerts.battery ? 'Battery 15%' : alerts.thermal ? 'Phone too hot' : ''} visible={!!alerts.battery || !!alerts.thermal} />`.
       - `'stop-confirm'` → render the `'active'` chrome underneath + `<StopConfirmModal visible onKeepRecording={() => dispatch({ type: 'STOP_CONFIRM_CANCEL' })} onStop={() => dispatch({ type: 'STOP_CONFIRM_STOP' })} />`.
       - `'stopped'` → minimal (a brief blank/spinner; the post-stop toast routing is plan 04-09).
       Add a top docstring: "RecordingScreen shell — substate-driven dark-theme chrome (engineering-handoff §7). Plan 04-09 wires the live VisionCamera mount, the HAND-12 pre-warm, the useHandGate poll loop, the gate-pass→active TTS-masked transition, the CaptureSessionOpts construction, useRecordingLifecycle, the HAND-14 analytics, brightness restore, orientation lock, and post-stop routing. This shell only renders the chrome for a given substate so the visual baselines + the route can land in Wave 3."
       For the visual-baseline determinism: the screen must accept an optional `__test_initialState?: RecState` prop (or read a route param) so the visual tests can render it directly in each substate (rotate-prompt, ready, gate@0/50/100, active@t10s/t05m32s, stop-confirm). Document this `__test_initialState` escape hatch in the docstring (it's the same shape the visual tests use).
    4. **RootNativeStack.tsx** — add `import RecordingScreen from '../screens/recording/RecordingScreen';` and `<Root.Screen name="Recording" component={RecordingScreen} options={{ gestureEnabled: false, headerShown: false, animation: 'fade' }} />` as a MainTabs sibling (per the `<interfaces>` snippet; `headerShown:false` is the navigator default already, but set it explicitly). Update the navigator's comment block to list `Recording` among the siblings (per D-NAV-01).
    5. **route-registry.test.ts** — move `'Recording'` out of the `phase3Plus = ['Recording', 'Player']` must-be-absent list (leaving `['Player']`), and add a new `const REQUIRED_PHASE_4_ROUTES = ['Recording'];` block (mirroring `REQUIRED_PHASE_2_ROUTES`) with its own `it('registers screen name="Recording"')` loop asserting it's present in `ALL_NAVIGATOR_SOURCE`. Add a comment: "Phase 4 plan 04-07 — Recording (RootNativeStack sibling) now required; removal fails CI (D-NAV-01 / Pattern 54). PracticeIntro/PracticeComplete are added to this list by plan 04-09 (they're registered by plan 04-06 — same plan-wave as 04-07, so 04-09 — which depends on both — owns that addition to keep the wave graph clean)." Do NOT add `'PracticeIntro'`/`'PracticeComplete'` here — plan 04-09 does that. This plan is the only plan-wave-3 plan that touches route-registry.test.ts (04-06 does not).
    6. **RecordingScreen.test.tsx** — mock the new libs (already mocked globally by plan 04-01) + navigation + `logEvent` + `route` with `params: { taskId: '__practice__', taskName: 'Practice — 60 sec', isPractice: true }`. Assert: renders the dark surface (`accessibilityLabel="..."` present); for `__test_initialState` in `'rotate-prompt'` → renders the rotate-prompt label; in `'gate'` → renders the gate prompt + Skip link + GateRing; in `'active'` → renders the timer + the stop button; in `'stop-confirm'` → renders the StopConfirmModal with the LOCKED body "Recordings under 1 minute are discarded."; tapping Skip dispatches `GATE_SKIP` (reducer transitions gate→confirmed-skipped — assert the rendered state changed). Mirror `CompatRunningScreen.test.tsx` / `RigTutorialScreen.test.tsx`.
    7. **Visual baselines** — `apps/mobile/__tests__/visual/RecordingScreen.visual.test.tsx` — 8 cases, each rendering `<RecordingScreen route={{ params: {...} }} __test_initialState={<state>} />` (or the route-param escape hatch) and `expect(renderToImage(container)).toMatchImageSnapshot()` with the named baseline: `recording-rotate-prompt.png` (substate `rotate-prompt`, orientation mocked portrait), `recording-ready.png` (substate `ready`, camera-off), `recording-gate-ring-0.png` (substate `gate`, `gate.phase:'waiting'`, `consecutiveHits:0`), `recording-gate-ring-50.png` (`consecutiveHits` ≈ `Math.round(targetHits/2)`), `recording-gate-ring-100.png` (`consecutiveHits === targetHits`, `gate.phase:'confirmed'`), `recording-active-t10s.png` (substate `active`, `durationMs:10_000`), `recording-active-t05m32s.png` (substate `active`, `durationMs:332_000`), `recording-stop-confirm-modal.png` (substate `stop-confirm`). Skip live-camera substates, the 5%-brightness state, confetti, and thermal/battery alert pills (per D-WAVE-03). Generate + commit all 8 PNGs.
  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/screens/RecordingScreen.test.tsx __tests__/navigation/route-registry.test.ts __tests__/visual/RecordingScreen.visual.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix apps/mobile test -- --run __tests__/screens/RecordingScreen.test.tsx __tests__/navigation/route-registry.test.ts __tests__/visual/RecordingScreen.visual.test.tsx` exits 0
    - `apps/mobile/src/navigation/RootNativeStack.tsx` contains `name="Recording"`
    - `apps/mobile/__tests__/navigation/route-registry.test.ts` no longer lists `'Recording'` in any must-be-absent set (the `phase3Plus` list now contains only `'Player'`), and a `REQUIRED_PHASE_4_ROUTES` block asserts `'Recording'` is registered (this plan does NOT add `'PracticeIntro'`/`'PracticeComplete'` — plan 04-09 does)
    - `apps/mobile/src/screens/recording/RecordingScreen.tsx` exists, contains `useReducer`, `recReducer`, and the LOCKED body string `Recordings under 1 minute are discarded.` (via StopConfirmModal) and `Don't exit while recording.` and `Mount the phone on your head and bring your hands in frame for 2 secs`
    - `apps/mobile/src/screens/recording/components/GateRing.tsx` exists and contains `strokeDashoffset`
    - all 8 baseline PNGs exist under `apps/mobile/__tests__/visual/__image_snapshots__/`: `recording-rotate-prompt.png`, `recording-ready.png`, `recording-gate-ring-0.png`, `recording-gate-ring-50.png`, `recording-gate-ring-100.png`, `recording-active-t10s.png`, `recording-active-t05m32s.png`, `recording-stop-confirm-modal.png`
    - `apps/mobile/__tests__/ui/no-hex-literals.test.ts` still passes (any new hex/rgba lives in `ui/tokens.ts`)
    - the full suite `npm --prefix apps/mobile test -- --run` exits 0
  </acceptance_criteria>
  <done>5 recording components + RecordingScreen shell + RootNativeStack Recording route + route-registry update + RecordingScreen render test + 8 visual baselines landed; full suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                                                        |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| route params → RecordingScreen     | `taskId`/`taskName`/`isPractice` arrive from the navigator (PracticeIntro for practice; the `__DEV__` affordance for non-practice) |
| `__test_initialState` escape hatch | a test-only prop that injects an arbitrary RecState                                                                                |

## STRIDE Threat Register

| Threat ID | Category                                      | Component                                         | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                   |
| --------- | --------------------------------------------- | ------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-4.7-01  | Tampering / test-only surface leaking to prod | the `__test_initialState` prop on RecordingScreen | accept      | it's a render-only prop with no side effects; production callers (PracticeIntro, the `__DEV__` affordance) never pass it; it cannot mutate persisted state or trigger capture. Low risk. (If preferred, gate it behind `__DEV__` — but a render-only prop with no side effects is not a finding.) |
| T-4.7-02  | DoS (self-inflicted)                          | the 3s overlay / VoiceCuePill auto-fade timers    | mitigate    | every `setTimeout`/Reanimated animation is cleared in the `useEffect` cleanup (the CompatRunningScreen ref-tracked-timer pattern); RecordingScreen unmount cancels pending timers.                                                                                                                |
| T-4.7-03  | UI-thread mutation                            | the minute-bar / timer / overlay re-renders       | accept      | pure React/Reanimated UI state; no native window mutation in this shell (brightness is plan 04-09 via HumynScreenBrightness on the UI thread).                                                                                                                                                    |
| T-4.7-04  | Spec evasion                                  | the `__DEV__` "Pretend I rotated →" pill          | mitigate    | the pill is rendered only when `__DEV__` (RotatePrompt gates it); production builds dead-code-eliminate it. (The TasksPlaceholder `__DEV__` affordance is plan 04-08.)                                                                                                                            |

</threat_model>

<verification>
- `npm --prefix apps/mobile test -- --run` exits 0 (recState reducer tests ~25 cases, RecordingScreen render test, route-registry update, 8 visual baselines, and the pre-existing suite green).
- `RootNativeStack.tsx` registers `Recording`; route-registry test now requires `Recording` (via a new `REQUIRED_PHASE_4_ROUTES` block) and no longer treats `Recording` as absent. (`PracticeIntro`/`PracticeComplete` get added to that block by plan 04-09.)
- `no-hex-literals.test.ts` still passes (new dark-theme colors live in tokens.ts).
</verification>

<success_criteria>

- recState.ts pure state machine (verbatim §4.3 shape, every transition) with a comprehensive reducer test.
- 5 recording UI components (GateRing/VoiceCuePill/StopConfirmModal/AlertPill/RotatePrompt) per the UI-SPEC dark-theme contract.
- RecordingScreen shell rendering substate-driven chrome (no live camera/lifecycle yet) + the `__test_initialState` escape hatch for the visual baselines.
- RootNativeStack `Recording` route + route-registry.test.ts updated (Pattern 54 invariant now requires `Recording`; `PracticeIntro`/`PracticeComplete` added to the invariant by plan 04-09).
- 8 static-surface visual baselines committed.
- Full mobile test suite green.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-07-SUMMARY.md`
</output>
