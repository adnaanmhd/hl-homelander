---
phase: 04-handdetector-recording-ux-practice-tutorial
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/vitest.setup.ts
  - apps/mobile/android/app/src/main/AndroidManifest.xml
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainActivity.kt
  - apps/mobile/__tests__/native/phase4-deps.test.ts
autonomous: true
requirements: [REC-01, HAND-12]
must_haves:
  truths:
    - 'react-native-vision-camera 4.7.3, react-native-tts 4.1.1, react-native-fs 2.20.0, react-native-orientation-locker 1.7.0, react-native-worklets-core 1.6.3, react-native-reanimated ~3.16.7, @react-native-firebase/analytics 24.0.0 are in apps/mobile/package.json dependencies'
    - 'vitest.setup.ts mocks react-native-vision-camera, react-native-tts, react-native-fs, react-native-orientation-locker and the NativeModules.{HumynHandDetector,HumynPhoneState,HumynBattery,HumynScreenBrightness,HumynBeep} stubs'
    - 'MainActivity overrides onConfigurationChanged and AndroidManifest declares android:configChanges containing orientation and screenSize'
  artifacts:
    - path: 'apps/mobile/package.json'
      provides: 'Phase 4 RN library deps at the CLAUDE.md-pinned versions'
      contains: 'react-native-vision-camera'
    - path: 'apps/mobile/vitest.setup.ts'
      provides: 'jsdom mocks for the new Phase 4 libraries + native module stubs'
      contains: 'react-native-vision-camera'
    - path: 'apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainActivity.kt'
      provides: 'onConfigurationChanged override required by react-native-orientation-locker'
      contains: 'onConfigurationChanged'
  key_links:
    - from: 'apps/mobile/android/app/src/main/AndroidManifest.xml'
      to: 'MainActivity'
      via: 'android:configChanges attribute'
      pattern: 'configChanges=.*orientation'
---

<objective>
Land the Phase 4 foundation: install the locked RN library deps (VisionCamera 4.7.3 preview/takePhoto, react-native-tts, react-native-fs, react-native-orientation-locker, worklets-core, reanimated, @react-native-firebase/analytics), add jsdom mocks for them + the four/five new native modules, and add the `react-native-orientation-locker` Android manifest/activity wiring so `lockToLandscape()` works.

Purpose: Wave 2/3/4 plans import these libraries and mock them in tests; nothing downstream can typecheck or run tests without this plan.
Output: Updated `package.json`, `vitest.setup.ts`, `MainActivity.kt`, `AndroidManifest.xml`, and a smoke test asserting the mocks resolve.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md
@CLAUDE.md
@apps/mobile/vitest.setup.ts
@apps/mobile/package.json
@apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainActivity.kt
@apps/mobile/android/app/src/main/AndroidManifest.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 4 RN library deps at the CLAUDE.md-pinned versions</name>
  <read_first>
    - apps/mobile/package.json (current dep set — confirm react-native-svg, react-native-haptic-feedback, react-native-mmkv, lucide-react-native, @react-native-firebase/{app,auth,remote-config} are present; the new libs are NOT)
    - CLAUDE.md "Tech Stack — Pins" section (the exact pins) + "Version Compatibility Pinpoints"
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § "Standard Stack" + "Installation" (lines 210-233) + Open Question 2 (Skia-as-peer check)
  </read_first>
  <action>
    Run `npm install -w apps/mobile react-native-vision-camera@4.7.3 react-native-worklets-core@1.6.3 react-native-reanimated@~3.16.7 react-native-tts@4.1.1 react-native-fs@2.20.0 react-native-orientation-locker@1.7.0 @react-native-firebase/analytics@24.0.0` from the repo root.
    After install, inspect `apps/mobile/node_modules/react-native-vision-camera/package.json` `peerDependencies`: if `@shopify/react-native-skia` is listed as a (non-optional) peer, also run `npm install -w apps/mobile @shopify/react-native-skia@^1.12.0` (any 1.x ≥1.2.1 — NEVER 2.x). If `react-native-worklets-core` is not listed as a hard peer for `takePhoto()`-only usage you may leave it; the install above already added it so this is informational only.
    Confirm `apps/mobile/package.json` `dependencies` now lists all of: `react-native-vision-camera` (4.7.3 exact), `react-native-worklets-core`, `react-native-reanimated` (~3.16.7), `react-native-tts` (4.1.1), `react-native-fs` (2.20.0), `react-native-orientation-locker` (1.7.0), `@react-native-firebase/analytics` (24.0.0). Pin `react-native-vision-camera` and `react-native-tts` and `react-native-fs` and `react-native-orientation-locker` and `react-native-worklets-core` to exact versions (no `^`); `react-native-reanimated` may stay `~3.16.7`.
    Do NOT install `react-native-worklets` (the non-`-core` package), `@shopify/react-native-skia` 2.x, `react-native-sound`, or `react-native-track-player` — CLAUDE.md "Do NOT Use".
    Confirm `@react-native-firebase/crashlytics@24.0.0` is present in `package.json` (idea-brief §12 mandates Crashlytics); if absent, add it via `npm install -w apps/mobile @react-native-firebase/crashlytics@24.0.0`.
  </action>
  <verify>
    <automated>node -e "const p=require('./apps/mobile/package.json').dependencies; ['react-native-vision-camera','react-native-worklets-core','react-native-reanimated','react-native-tts','react-native-fs','react-native-orientation-locker','@react-native-firebase/analytics'].forEach(k=>{if(!p[k])throw new Error('missing '+k)}); if(p['react-native-vision-camera']!=='4.7.3')throw new Error('VC not pinned 4.7.3'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "..."` command above exits 0
    - `apps/mobile/package.json` `dependencies["react-native-vision-camera"]` is exactly `"4.7.3"` (no caret/tilde)
    - `apps/mobile/package.json` `dependencies` contains keys `react-native-tts`, `react-native-fs`, `react-native-orientation-locker`, `react-native-worklets-core`, `@react-native-firebase/analytics`, `react-native-reanimated`
    - `apps/mobile/package.json` does NOT contain a `react-native-worklets` key (only `react-native-worklets-core`)
    - if `@shopify/react-native-skia` was installed, its version starts with `1.` (not `2.`)
  </acceptance_criteria>
  <done>All seven (or eight w/ Skia) Phase 4 RN library deps present in apps/mobile/package.json at the CLAUDE.md-pinned versions; no forbidden libraries added.</done>
</task>

<task type="auto">
  <name>Task 2: Add jsdom mocks for the new libraries and the new native modules to vitest.setup.ts</name>
  <read_first>
    - apps/mobile/vitest.setup.ts (current mocks — react-native host-component shim, @react-navigation, react-native-screens, react-native-safe-area-context, react-native-mmkv, react-native-reanimated, react-native-haptic-feedback, react-native-permissions, lucide-react-native, react-native-svg, jest-image-snapshot expect.extend; NativeModules is stubbed to {} by default)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § "Validation Architecture" Wave 0 list (lines 936-949 — the exact mock surfaces needed) + Pitfall 4 (the worklets/orientation-locker notes)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md § "Visual snapshot baseline" + "RCTDeviceEventEmitter emit + NativeEventEmitter subscribe" (the EmitterSubscription shape new bindings expect)
  </read_first>
  <action>
    Add `vi.mock(...)` blocks to `apps/mobile/vitest.setup.ts` for:
    1. `react-native-vision-camera` — export `Camera` as a no-op forwardRef host component (a function component returning `null`); export `useCameraDevice(position, filter?)` and `useCameraDevices()` and `getCameraDevice()` returning a stub device `{ id: 'mock-cam-0', position: 'back', physicalDevices: ['ultra-wide-angle-camera'], formats: [] }`; the `Camera` ref instance must expose `takePhoto: vi.fn().mockResolvedValue({ path: '/tmp/mock-photo.jpg', width: 320, height: 240, orientation: 'landscape-right', isRawPhoto: false })` and `takeSnapshot: vi.fn().mockResolvedValue({ path: '/tmp/mock-snap.jpg' })`. Export `Camera.getAvailableCameraDevices = vi.fn().mockResolvedValue([{ id: 'mock-cam-0', position: 'back', physicalDevices: ['ultra-wide-angle-camera'] }])`.
    2. `react-native-tts` — default export object with `getInitStatus: vi.fn().mockResolvedValue('success')`, `voices: vi.fn().mockResolvedValue([{ id: 'en-in-x-ene-local', language: 'en-IN', quality: 300, notInstalled: false }, { id: 'en-us-x-tpf-local', language: 'en-US', quality: 300, notInstalled: false }])`, `setDefaultVoice: vi.fn().mockResolvedValue(undefined)`, `setDefaultRate: vi.fn()`, `setDefaultPitch: vi.fn()`, `setDefaultLanguage: vi.fn()`, `speak: vi.fn()`, `stop: vi.fn()`, `addEventListener: vi.fn(() => ({ remove: vi.fn() }))`, `removeAllListeners: vi.fn()`.
    3. `react-native-fs` — `CachesDirectoryPath: '/tmp/mock-caches'`, `DocumentDirectoryPath: '/tmp/mock-docs'`, `mkdir: vi.fn().mockResolvedValue(undefined)`, `moveFile: vi.fn().mockResolvedValue(undefined)`, `copyFile: vi.fn().mockResolvedValue(undefined)`, `unlink: vi.fn().mockResolvedValue(undefined)`, `exists: vi.fn().mockResolvedValue(false)`, `readDir: vi.fn().mockResolvedValue([])`, `getFSInfo: vi.fn().mockResolvedValue({ totalSpace: 64 * 1e9, freeSpace: 32 * 1e9 })`. Provide both a named-export shape and a default export of the same object (react-native-fs is consumed both ways).
    4. `react-native-orientation-locker` — default export object with `lockToLandscape: vi.fn()`, `lockToLandscapeLeft: vi.fn()`, `lockToLandscapeRight: vi.fn()`, `lockToPortrait: vi.fn()`, `unlockAllOrientations: vi.fn()`, `getOrientation: vi.fn((cb) => cb('PORTRAIT'))`, `getDeviceOrientation: vi.fn((cb) => cb('PORTRAIT'))`, `getInitialOrientation: vi.fn(() => 'PORTRAIT')`, `addOrientationListener: vi.fn()`, `removeOrientationListener: vi.fn()`, `addDeviceOrientationListener: vi.fn()`, `removeDeviceOrientationListener: vi.fn()`; also export a named `OrientationType` const map `{ PORTRAIT:'PORTRAIT', 'LANDSCAPE-LEFT':'LANDSCAPE-LEFT', 'LANDSCAPE-RIGHT':'LANDSCAPE-RIGHT', 'PORTRAIT-UPSIDEDOWN':'PORTRAIT-UPSIDEDOWN', UNKNOWN:'UNKNOWN' }` and an `OrientationLocker` no-op component.
    5. Extend the existing `react-native` mock so `NativeModules` includes `HumynHandDetector: { detectHands: vi.fn().mockResolvedValue(0), cleanup: vi.fn().mockResolvedValue(undefined) }`, `HumynPhoneState: { start: vi.fn().mockResolvedValue(undefined), stop: vi.fn().mockResolvedValue(undefined) }`, `HumynBattery: { start: vi.fn().mockResolvedValue(undefined), stop: vi.fn().mockResolvedValue(undefined) }`, `HumynScreenBrightness: { set: vi.fn().mockResolvedValue(undefined) }`, `HumynBeep: { playTone: vi.fn().mockResolvedValue(undefined) }`. If the existing setup leaves `NativeModules` as `{}` and individual tests `vi.doMock('react-native', ...)` per-file (the HumynCapture.test.ts pattern), DO NOT change that contract — instead just add a comment block in vitest.setup.ts documenting the canonical stub shapes so per-file mocks stay consistent (the per-file `vi.doMock` pattern is preserved). Re-read HumynCapture.test.ts before deciding which path; preserve the existing convention.
    Do NOT add `vi.mock` for `react-native-worklets-core` or `@shopify/react-native-skia` unless a downstream import fails — they are transitive VC peers, not directly imported by Phase 4 JS.
  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/native/phase4-deps.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/vitest.setup.ts` contains the string `react-native-vision-camera`
    - `apps/mobile/vitest.setup.ts` contains the string `react-native-orientation-locker`
    - `apps/mobile/vitest.setup.ts` contains the string `react-native-tts`
    - `apps/mobile/vitest.setup.ts` contains the string `react-native-fs`
    - `npm --prefix apps/mobile test -- --run __tests__/native/phase4-deps.test.ts` exits 0
    - the full pre-existing suite still passes: `npm --prefix apps/mobile test -- --run` exits 0
  </acceptance_criteria>
  <done>All five mock surfaces resolve under jsdom; phase4-deps.test.ts (Task 3) passes; the existing 344+ test suite stays green.</done>
</task>

<task type="auto">
  <name>Task 3: react-native-orientation-locker Android manifest/activity wiring + a deps-resolve smoke test</name>
  <read_first>
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainActivity.kt (current — ReactActivity subclass; needs the onConfigurationChanged override)
    - apps/mobile/android/app/src/main/AndroidManifest.xml (current `<activity android:name=".MainActivity">` block — needs android:configChanges extended)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § "Standard Stack" orientation-locker row (the exact MainActivity + manifest requirements) + Assumption A4 + Pitfall 4
    - apps/mobile/__tests__/manifests/manifests.test.ts (the existing manifest-invariant test pattern — your new manifest assertion follows the same shape)
  </read_first>
  <action>
    1. In `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainActivity.kt`, add the override:
    ```kotlin
    import android.content.res.Configuration
    import android.content.Intent
    // inside class MainActivity : ReactActivity():
    override fun onConfigurationChanged(newConfig: Configuration) {
        super.onConfigurationChanged(newConfig)
        val intent = Intent("onConfigurationChanged")
        intent.putExtra("newConfig", newConfig)
        sendBroadcast(intent)
    }
    ```
    (This is the exact `react-native-orientation-locker` README requirement — its `OrientationActivityLifecycle` listens for that broadcast.)
    2. In `apps/mobile/android/app/src/main/AndroidManifest.xml`, on the `<activity android:name=".MainActivity" ...>` element, ensure `android:configChanges` includes `orientation` and `screenSize` (typical value: `android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"` — merge with whatever is already there; do NOT remove existing flags).
    3. Note (no edit here — `MainApplication.kt` is owned by plan 04-02 which registers the native packages including the orientation-locker autolink): the `OrientationActivityLifecycle.getInstance()` registration in `MainApplication.onCreate()` is handled by plan 04-02 alongside the other package registrations. Add a one-line comment in MainActivity.kt referencing that.
    4. Create `apps/mobile/__tests__/native/phase4-deps.test.ts` — a smoke test that: (a) imports `react-native-vision-camera`, `react-native-tts` (default), `react-native-fs`, `react-native-orientation-locker` (default) and asserts the mocked surfaces exist (`Camera` is defined, `Tts.getInitStatus` is a function, `RNFS.getFSInfo` is a function, `Orientation.lockToLandscape` is a function); (b) asserts `react-native`'s `NativeModules` (or the per-file mock convention you chose in Task 2) can expose `HumynHandDetector` / `HumynScreenBrightness` stubs; (c) asserts the manifest file at `apps/mobile/android/app/src/main/AndroidManifest.xml` (read via Vite `?raw` or `fs.readFileSync`) contains `configChanges` with both `orientation` and `screenSize`; (d) asserts `MainActivity.kt` source contains `onConfigurationChanged`. Use the existing `?raw` import + ambient `.d.ts` pattern from `__tests__/ui/` source-grep tests if a source-string read is needed (re-read one of those tests for the idiom).
  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/native/phase4-deps.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainActivity.kt` contains `onConfigurationChanged`
    - `apps/mobile/android/app/src/main/AndroidManifest.xml` `android:configChanges` value contains both `orientation` and `screenSize`
    - `apps/mobile/__tests__/native/phase4-deps.test.ts` exists and `npm --prefix apps/mobile test -- --run __tests__/native/phase4-deps.test.ts` exits 0
    - the full suite `npm --prefix apps/mobile test -- --run` exits 0
  </acceptance_criteria>
  <done>orientation-locker Android wiring landed; phase4-deps smoke test green; the manifest invariant is now grep-asserted.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                                        |
| ------------------------- | -------------------------------------------------------------------------------------------------- |
| npm registry → app bundle | new third-party libraries enter the build                                                          |
| Android manifest → OS     | `android:configChanges` changes which config changes the OS hands to the activity vs recreating it |

## STRIDE Threat Register

| Threat ID | Category                 | Component                                     | Disposition | Mitigation Plan                                                                                                                                                                                         |
| --------- | ------------------------ | --------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| T-4.1-01  | Tampering / Supply chain | new RN deps (VC, tts, fs, orientation-locker) | mitigate    | install at the CLAUDE.md-pinned exact versions only; no `^` on VC/tts/fs/orientation-locker; lockfile committed; no forbidden libs added                                                                |
| T-4.1-02  | Spec violation           | AndroidManifest permissions                   | accept      | this plan adds NO permissions; the existing `__tests__/manifests/manifests.test.ts` invariant gate would catch an accidental `READ_PHONE_STATE`/`ACCESS_NOTIFICATION_POLICY` addition — none added here |
| T-4.1-03  | DoS (self-inflicted)     | `android:configChanges` extension             | accept      | extending configChanges with `orientation                                                                                                                                                               | screenSize` is exactly the orientation-locker requirement; the activity already handles config-driven re-layout via React Navigation; low risk |

</threat_model>

<verification>
- `npm --prefix apps/mobile test -- --run` exits 0 (full suite green, including the new phase4-deps.test.ts).
- `node -e` dep-presence check (Task 1 verify) exits 0.
- No new Android permissions in the merged manifest (the manifests.test.ts invariant still passes).
</verification>

<success_criteria>

- All Phase 4 RN library deps present at pinned versions in apps/mobile/package.json.
- vitest.setup.ts mocks the new libraries + native module stubs; downstream plans can import + test them.
- orientation-locker Android manifest/activity wiring done; landscape lock (REC-01) is buildable.
- Full mobile test suite green.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-01-SUMMARY.md`
</output>
