---
phase: 04-handdetector-recording-ux-practice-tutorial
plan: 05
type: execute
wave: 2
depends_on: ['04-02']
files_modified:
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/phonestate/HumynPhoneStateModule.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/battery/HumynBatteryModule.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/screenbrightness/HumynScreenBrightnessModule.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/beep/HumynBeepModule.kt
  - apps/mobile/android/app/src/main/assets/audio/battery_alert.wav
  - apps/mobile/android/app/src/main/assets/audio/thermal_alert.wav
autonomous: true
requirements: [REC-08, REC-10, REC-11, REC-12, REC-13]
must_haves:
  truths:
    - 'HumynPhoneStateModule requests AUDIOFOCUS_GAIN via an AudioFocusRequest and emits onAudioFocusChanged({focus}) on every AudioManager.OnAudioFocusChangeListener callback — no TelephonyManager, no PhoneStateListener, no READ_PHONE_STATE'
    - 'HumynBatteryModule registers a BroadcastReceiver for Intent.ACTION_BATTERY_CHANGED and emits onBatteryChanged({level: 0..1 Double, isCharging: Boolean}) on change'
    - 'HumynScreenBrightnessModule.set(value) writes activity.window.attributes.screenBrightness on the UI thread — value in [0,1], or -1/negative maps to WindowManager.LayoutParams.BRIGHTNESS_OVERRIDE_NONE (system default) — not Settings.System.SCREEN_BRIGHTNESS'
    - "HumynBeepModule.playTone(name) plays a pre-baked .wav from assets/audio/ via SoundPool — names 'battery_alert' (520 Hz / 200 ms) and 'thermal_alert' (descending 440→560→680 Hz); no react-native-sound / react-native-track-player"
    - 'all receivers/listeners/audio-focus-requests are unregistered/abandoned on stop() and on module invalidate'
  artifacts:
    - path: 'apps/mobile/android/app/src/main/java/ai/humynlabs/capture/phonestate/HumynPhoneStateModule.kt'
      provides: 'AudioManager.OnAudioFocusChangeListener body emitting onAudioFocusChanged'
      contains: 'OnAudioFocusChangeListener'
    - path: 'apps/mobile/android/app/src/main/java/ai/humynlabs/capture/screenbrightness/HumynScreenBrightnessModule.kt'
      provides: 'per-window screenBrightness override + restore'
      contains: 'screenBrightness'
    - path: 'apps/mobile/android/app/src/main/assets/audio/battery_alert.wav'
      provides: 'pre-baked 520 Hz / 200 ms mono 44.1 kHz alert tone'
    - path: 'apps/mobile/android/app/src/main/assets/audio/thermal_alert.wav'
      provides: 'pre-baked descending 440→560→680 Hz mono 44.1 kHz thermal tone'
  key_links:
    - from: 'HumynPhoneStateModule'
      to: 'useRecordingLifecycle'
      via: "RCTDeviceEventEmitter emit('onAudioFocusChanged', {focus})"
      pattern: 'onAudioFocusChanged'
    - from: 'HumynScreenBrightnessModule.set(-1)'
      to: 'WindowManager.LayoutParams.BRIGHTNESS_OVERRIDE_NONE'
      via: 'value < 0 branch'
      pattern: 'BRIGHTNESS_OVERRIDE_NONE'
---

<objective>
Implement the real bodies of the four small in-house Kotlin modules: `HumynPhoneStateModule` (pure `AudioManager.OnAudioFocusChangeListener` — NO telephony API, NO `READ_PHONE_STATE`, the corrected 04-RESEARCH D-LIFE-02 finding), `HumynBatteryModule` (`Intent.ACTION_BATTERY_CHANGED` sticky-broadcast receiver), `HumynScreenBrightnessModule` (per-window `screenBrightness` override + restore via `BRIGHTNESS_OVERRIDE_NONE`), and `HumynBeepModule` (`SoundPool` over two pre-baked `.wav` assets). Generate the two `.wav` assets.

Purpose: REC-08 (brightness 5% drop/restore), REC-10/REC-11 (battery alert + beep + end-segment-at-5%), REC-12/REC-13 (phone-call answered=stop / declined=continue via the audio-focus heuristic). `useRecordingLifecycle` (plan 04-08) subscribes to these event modules.
Output: four filled-in Kotlin modules + `battery_alert.wav` + `thermal_alert.wav`.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md
@CLAUDE.md
@engineering-handoff.md
@.planning/research/PITFALLS.md
@apps/mobile/android/app/src/main/java/ai/humynlabs/capture/phonestate/HumynPhoneStateModule.kt
@apps/mobile/android/app/src/main/java/ai/humynlabs/capture/battery/HumynBatteryModule.kt
@apps/mobile/android/app/src/main/java/ai/humynlabs/capture/screenbrightness/HumynScreenBrightnessModule.kt
@apps/mobile/android/app/src/main/java/ai/humynlabs/capture/beep/HumynBeepModule.kt
@apps/mobile/android/app/src/main/java/ai/humynlabs/capture/capture/HumynCaptureModule.kt

<interfaces>
<!-- RCTDeviceEventEmitter emit idiom (from HumynCaptureModule.kt) -->
reactApplicationContext.getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter::class.java).emit(name, Arguments.createMap().apply { putString("focus", ...) })
<!-- UI-thread window-mutation idiom (from HumynCaptureModule.applyKeepScreenOn) -->
val activity: Activity = reactApplicationContext.currentActivity ?: return; activity.runOnUiThread { activity.window?... }
<!-- AudioManager focus request (API 26+) -->
val am = reactApplicationContext.getSystemService(Context.AUDIO_SERVICE) as AudioManager
val req = AudioFocusRequest.Builder(AudioManager.AUDIOFOCUS_GAIN).setOnAudioFocusChangeListener(listener).build()
am.requestAudioFocus(req)  // ... am.abandonAudioFocusRequest(req) on stop
<!-- ACTION_BATTERY_CHANGED -->
val intent = reactApplicationContext.registerReceiver(receiver, IntentFilter(Intent.ACTION_BATTERY_CHANGED))
val level = intent.getIntExtra(BatteryManager.EXTRA_LEVEL, -1); val scale = intent.getIntExtra(BatteryManager.EXTRA_SCALE, -1)
val pct = if (level >= 0 && scale > 0) level.toDouble() / scale.toDouble() else -1.0
val status = intent.getIntExtra(BatteryManager.EXTRA_STATUS, -1); val isCharging = status == BatteryManager.BATTERY_STATUS_CHARGING || status == BatteryManager.BATTERY_STATUS_FULL
<!-- per-window brightness -->
val lp = activity.window.attributes; lp.screenBrightness = if (value < 0) WindowManager.LayoutParams.BRIGHTNESS_OVERRIDE_NONE else value.toFloat().coerceIn(0f, 1f); activity.window.attributes = lp
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HumynPhoneStateModule (AudioManager.OnAudioFocusChangeListener) + HumynBatteryModule (ACTION_BATTERY_CHANGED) bodies</name>
  <read_first>
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/phonestate/HumynPhoneStateModule.kt + battery/HumynBatteryModule.kt (the shells from plan 04-02 — you fill in the start()/stop() bodies and add the listener/receiver fields)
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/capture/HumynCaptureModule.kt (the `emitEvent()` RCTDeviceEventEmitter idiom + `Arguments.createMap()` composition; the `onCatalystInstanceDestroy`/`invalidate` cleanup pattern Phase 3 uses)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § "In-house Kotlin native modules" HumynPhoneState + HumynBattery rows + § Pitfall 2 (the READ_PHONE_STATE correction — AudioManager only) + § Pitfall 5 (listener/receiver leak — unregister on stop + invalidate) + § Pattern 3 (the answered-vs-declined heuristic lives JS-side; the Kotlin module just emits raw focus transitions)
    - CLAUDE.md "Do NOT Use" — NO TelephonyManager / PhoneStateListener / READ_PHONE_STATE; NO POST_NOTIFICATIONS; this module declares NO permissions
  </read_first>
  <action>
    **HumynPhoneStateModule.kt:**
    - Fields: `private var focusRequest: AudioFocusRequest? = null` (API 26+; minSdk is 26 so the modern API is always available), `private val focusListener = AudioManager.OnAudioFocusChangeListener { change -> emitFocus(change) }`.
    - `emitFocus(change: Int)` private fun → map `AudioManager.AUDIOFOCUS_GAIN` → `"gain"`, `AUDIOFOCUS_LOSS` → `"loss"`, `AUDIOFOCUS_LOSS_TRANSIENT` → `"transient_loss"`, `AUDIOFOCUS_LOSS_TRANSIENT_CAN_DUCK` → `"transient_loss_can_duck"`, anything else → return (no emit); then `reactApplicationContext.getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter::class.java).emit("onAudioFocusChanged", Arguments.createMap().apply { putString("focus", mapped) })`. (The "call answered vs declined" timing heuristic — start a ~6–8 s timer on `transient_loss`, GAIN-within-window → no-op, timer-fires-or-permanent-loss → stop — lives JS-side in `useRecordingLifecycle`, plan 04-08; the Kotlin module is a dumb pipe of raw focus transitions, and also covers alarms since an alarm ringing produces `AUDIOFOCUS_LOSS_TRANSIENT`. Add a comment noting this.)
    - `start(promise)`: build `AudioFocusRequest.Builder(AudioManager.AUDIOFOCUS_GAIN).setOnAudioFocusChangeListener(focusListener).setAudioAttributes(AudioAttributes.Builder().setUsage(AudioAttributes.USAGE_MEDIA).setContentType(AudioAttributes.CONTENT_TYPE_SPEECH).build()).build()`; `val am = reactApplicationContext.getSystemService(Context.AUDIO_SERVICE) as AudioManager`; `am.requestAudioFocus(focusRequest!!)`; `promise.resolve(null)`. (Requesting `AUDIOFOCUS_GAIN` is what makes the OS notify us on focus loss when a call/alarm comes in — no permission needed.)
    - `stop(promise)`: if `focusRequest != null`, `(reactApplicationContext.getSystemService(Context.AUDIO_SERVICE) as AudioManager).abandonAudioFocusRequest(focusRequest!!)`; `focusRequest = null`; `promise.resolve(null)`.
    - Override `invalidate()` (or `onCatalystInstanceDestroy()` per Phase-3 convention — re-read HumynCaptureModule to see which RN 0.83 uses) to abandon the focus request if still held (Pitfall 5).
    - Update the docstring: "REC-12/REC-13 — phone-call & alarm detection via AudioManager.OnAudioFocusChangeListener ONLY. NO TelephonyManager / PhoneStateListener / READ_PHONE_STATE (the call-state API requires that perm on API 31+ — 04-RESEARCH Pitfall 2; CLAUDE.md forbids it). Emits raw focus transitions; the answered-vs-declined heuristic is JS-side in useRecordingLifecycle."

    **HumynBatteryModule.kt:**
    - Fields: `private var receiver: BroadcastReceiver? = null`, `private var lastPct: Double = -1.0`, `private var lastCharging: Boolean = false`.
    - `start(promise)`: create a `BroadcastReceiver` whose `onReceive(_, intent)` reads `EXTRA_LEVEL` / `EXTRA_SCALE` → `pct = level/scale` (skip if either < 0/<=0), `EXTRA_STATUS` → `isCharging = status == BATTERY_STATUS_CHARGING || status == BATTERY_STATUS_FULL`; if `(pct != lastPct || isCharging != lastCharging)` then `emit("onBatteryChanged", Arguments.createMap().apply { putDouble("level", pct); putBoolean("isCharging", isCharging) })` and update `lastPct`/`lastCharging`. Register it: `val sticky = reactApplicationContext.registerReceiver(receiver, IntentFilter(Intent.ACTION_BATTERY_CHANGED))` — `registerReceiver` returns the last sticky broadcast immediately, so synthesize an initial emit from `sticky` too (so the JS side has a starting value). `promise.resolve(null)`.
    - `stop(promise)`: if `receiver != null`, `reactApplicationContext.unregisterReceiver(receiver)`; `receiver = null`; `lastPct = -1.0`; `promise.resolve(null)`.
    - Override `invalidate()`/`onCatalystInstanceDestroy()` to unregister if still registered (Pitfall 5).
    - Docstring: "REC-10/REC-11 — battery-level transitions via Intent.ACTION_BATTERY_CHANGED sticky broadcast (no permission — protected broadcast). Emits onBatteryChanged({level: 0..1, isCharging}); the JS side (useRecordingLifecycle) computes the ≤15% / ≤5% / <5%-start-guard threshold transitions + a 60s periodic cross-check (Pitfall 11 — some ROMs deliver coarse events)."

  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/native/HumynPhoneState.test.ts __tests__/native/HumynBattery.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/phonestate/HumynPhoneStateModule.kt` contains `OnAudioFocusChangeListener`, `AudioFocusRequest`, `AUDIOFOCUS_GAIN`, `abandonAudioFocusRequest`, and `onAudioFocusChanged`
    - that file does NOT contain `TelephonyManager`, `PhoneStateListener`, or `READ_PHONE_STATE`
    - `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/battery/HumynBatteryModule.kt` contains `ACTION_BATTERY_CHANGED`, `EXTRA_LEVEL`, `EXTRA_SCALE`, `unregisterReceiver`, and `onBatteryChanged`
    - the JS contract tests `npm --prefix apps/mobile test -- --run __tests__/native/HumynPhoneState.test.ts __tests__/native/HumynBattery.test.ts` exit 0 (the JS bindings from plan 04-02 are unchanged)
  </acceptance_criteria>
  <done>HumynPhoneState (AudioFocus only) + HumynBattery (ACTION_BATTERY_CHANGED) bodies landed with proper unregister-on-stop/invalidate; no forbidden APIs; JS contract tests green.</done>
</task>

<task type="auto">
  <name>Task 2: HumynScreenBrightnessModule (per-window brightness) + HumynBeepModule (SoundPool) bodies + generate the two .wav assets</name>
  <read_first>
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/screenbrightness/HumynScreenBrightnessModule.kt + beep/HumynBeepModule.kt (the shells from plan 04-02)
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/capture/HumynCaptureModule.kt `applyKeepScreenOn()` (the VERBATIM UI-thread window-mutation idiom HumynScreenBrightness reuses: `reactApplicationContext.currentActivity ?: return` + `activity.runOnUiThread { activity.window?... }`; note RN 0.83 deprecated the protected `currentActivity` accessor — use `reactApplicationContext.currentActivity` exactly as HumynCaptureModule does, see its comment)
    - engineering-handoff.md §6.1 (the beep spec: "pre-bake .wav at 44.1 kHz mono and ship as bundled assets to avoid AudioContext latency variance"; 520 Hz / 200 ms battery; descending 440→560→680 Hz thermal — verify exact tone sequence)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § Open Question 7 (the HumynBeep over SoundPool note) + § "Code Examples — HumynScreenBrightness"
    - CLAUDE.md "Do NOT Use" — NO Settings.System.SCREEN_BRIGHTNESS (needs WRITE_SETTINGS); NO react-native-sound / react-native-track-player
  </read_first>
  <action>
    **HumynScreenBrightnessModule.kt:**
    - `@ReactMethod fun set(value: Double, promise: Promise)`: `val activity: Activity = reactApplicationContext.currentActivity ?: run { promise.reject("NO_ACTIVITY", "no current activity"); return }`; `activity.runOnUiThread { try { val lp = activity.window.attributes; lp.screenBrightness = if (value < 0) WindowManager.LayoutParams.BRIGHTNESS_OVERRIDE_NONE else value.toFloat().coerceIn(0f, 1f); activity.window.attributes = lp; promise.resolve(null) } catch (e: Exception) { promise.reject("BRIGHTNESS_FAILED", e) } }`.
    - Docstring: "REC-08 — per-window screenBrightness override. value ∈ [0,1] sets the override; value < 0 (-1) → BRIGHTNESS_OVERRIDE_NONE (restore system default). Per-window only (NOT Settings.System.SCREEN_BRIGHTNESS — that needs WRITE_SETTINGS and persists OS-wide). The window override auto-resets when the activity is destroyed, but RecordingScreen also calls set(-1) explicitly on stop AND on unmount (Pitfall 6 — a force-navigation within the same activity won't auto-reset). Reuses HumynCaptureModule.applyKeepScreenOn's UI-thread idiom."
    - This file's body should NOT touch `Settings.System` or `android.permission.WRITE_SETTINGS`.

    **HumynBeepModule.kt:**
    - Fields: `private var soundPool: SoundPool? = null`, `private val soundIds = mutableMapOf<String, Int>()`.
    - Lazily build `SoundPool.Builder().setMaxStreams(2).setAudioAttributes(AudioAttributes.Builder().setUsage(AudioAttributes.USAGE_ASSISTANCE_SONIFICATION).setContentType(AudioAttributes.CONTENT_TYPE_SONIFICATION).build()).build()` on first `playTone`; load `assets/audio/${name}.wav` via `reactApplicationContext.assets.openFd("audio/${name}.wav")` → `soundPool.load(afd, 1)`, cache the sound id in `soundIds`. Then `soundPool.play(id, 1f, 1f, 1, 0, 1f)`. (SoundPool needs the sound loaded before `play` returns a valid id — for simplicity, load it lazily and play after `setOnLoadCompleteListener` fires the first time, OR pre-load both known names ("battery_alert", "thermal_alert") at module construction so `play` works immediately. Recommend pre-loading both at construction.)
    - `@ReactMethod fun playTone(name: String, promise: Promise)`: validate `name` is one of the known set (`"battery_alert"`, `"thermal_alert"`); if not, `promise.reject("UNKNOWN_TONE", name)`; else play the cached sound; `promise.resolve(null)`.
    - Override `invalidate()`/`onCatalystInstanceDestroy()` to `soundPool?.release()`.
    - Docstring: "REC-10 (520 Hz / 200 ms battery beep) + thermal-kill tones (descending 440→560→680 Hz) — engineering-handoff §6.1: pre-baked .wav at 44.1 kHz mono shipped as bundled assets. DO NOT use react-native-sound / react-native-track-player (CLAUDE.md). Plays via SoundPool."

    **Generate the .wav assets** at `apps/mobile/android/app/src/main/assets/audio/`:
    - `battery_alert.wav` — 44.1 kHz, mono, 16-bit PCM, a 520 Hz sine for 200 ms (with a tiny ~5 ms fade-in/out to avoid a click). Use `ffmpeg -y -f lavfi -i "sine=frequency=520:duration=0.2:sample_rate=44100" -af "afade=t=in:st=0:d=0.005,afade=t=out:st=0.195:d=0.005" -ac 1 -acodec pcm_s16le apps/mobile/android/app/src/main/assets/audio/battery_alert.wav` (if `ffmpeg` is unavailable, use `sox -n -r 44100 -c 1 -b 16 ... synth 0.2 sine 520` or a tiny Python script writing a WAV header + sine samples — any tool that produces a valid 44.1 kHz mono 16-bit PCM WAV).
    - `thermal_alert.wav` — 44.1 kHz, mono, 16-bit PCM, three descending tones 440 Hz → 560 Hz → 680 Hz... wait — engineering-handoff §6.1 says "descending 440→560→680" but that's ASCENDING in Hz; re-read §6.1 for the exact order and durations and reproduce VERBATIM (likely each tone ~150–200 ms, possibly with small gaps). Concat three sine segments with `ffmpeg`/`sox`. If §6.1 only gives the three frequencies without durations, use 200 ms each with a 50 ms gap.
    - Verify both files exist, are valid WAV (start with the `RIFF` magic), and are 44.1 kHz mono. (If no audio-synthesis tool is available on the machine at all, generate them with a 20-line Node/Python script that writes the WAV header + 16-bit PCM sine samples — do NOT skip this; the assets are required for REC-10.)

  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/native/HumynScreenBrightness.test.ts __tests__/native/HumynBeep.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/screenbrightness/HumynScreenBrightnessModule.kt` contains `screenBrightness`, `BRIGHTNESS_OVERRIDE_NONE`, `runOnUiThread`, and `coerceIn(0f, 1f)`; does NOT contain `Settings.System` or `WRITE_SETTINGS`
    - `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/beep/HumynBeepModule.kt` contains `SoundPool` and `battery_alert` and `thermal_alert`; does NOT contain `react-native-sound` or `MediaPlayer` (use SoundPool)
    - `apps/mobile/android/app/src/main/assets/audio/battery_alert.wav` exists and `head -c 4 apps/mobile/android/app/src/main/assets/audio/battery_alert.wav` is `RIFF`
    - `apps/mobile/android/app/src/main/assets/audio/thermal_alert.wav` exists and `head -c 4 apps/mobile/android/app/src/main/assets/audio/thermal_alert.wav` is `RIFF`
    - the JS contract tests `npm --prefix apps/mobile test -- --run __tests__/native/HumynScreenBrightness.test.ts __tests__/native/HumynBeep.test.ts` exit 0
    - the full suite `npm --prefix apps/mobile test -- --run` exits 0
  </acceptance_criteria>
  <done>HumynScreenBrightness (per-window override + restore) + HumynBeep (SoundPool over bundled .wav) bodies landed; both .wav assets generated; no forbidden APIs; JS contract tests + full suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                   | Description                                   |
| ---------------------------------------------------------- | --------------------------------------------- |
| JS → `screenBrightness.set(value)` / `beep.playTone(name)` | a number / a string cross from JS into Kotlin |
| OS audio focus / battery broadcasts → app                  | the app observes call/alarm/battery events    |
| bundled `.wav` assets                                      | read-only audio assets in the APK             |

## STRIDE Threat Register

| Threat ID | Category                                | Component                                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                 |
| --------- | --------------------------------------- | ----------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-4.5-01  | Elevation of privilege / Spec violation | `HumynPhoneStateModule.kt`                            | mitigate    | pure `AudioManager.OnAudioFocusChangeListener` — NO `TelephonyManager` / `PhoneStateListener` / `READ_PHONE_STATE`; declares NO permissions. Acceptance criterion greps the file for the forbidden symbols.                                                                                     |
| T-4.5-02  | Elevation of privilege                  | `HumynScreenBrightnessModule.kt`                      | mitigate    | per-window `WindowManager.LayoutParams.screenBrightness` override only — NOT `Settings.System.SCREEN_BRIGHTNESS` (needs `WRITE_SETTINGS`). Acceptance criterion greps for absence of `Settings.System` / `WRITE_SETTINGS`. Value clamped to `[0,1]` or the `BRIGHTNESS_OVERRIDE_NONE` sentinel. |
| T-4.5-03  | DoS (self-inflicted)                    | listener/receiver/audio-focus-request/SoundPool leaks | mitigate    | every module unregisters its receiver / abandons its focus request / releases its SoundPool on `stop()` AND on module `invalidate()`/`onCatalystInstanceDestroy()` (Pitfall 5).                                                                                                                 |
| T-4.5-04  | Input validation                        | `beep.playTone(name)`                                 | mitigate    | `name` validated against the known set (`battery_alert`, `thermal_alert`); unknown → `promise.reject("UNKNOWN_TONE", ...)` — no arbitrary asset path is opened from the JS string.                                                                                                              |
| T-4.5-05  | Tampering                               | `.wav` assets integrity                               | accept      | bundled read-only assets generated at build time from a known synthesis recipe; APK signing covers them.                                                                                                                                                                                        |
| T-4.5-06  | Spec violation                          | DND / notifications                                   | mitigate    | NONE of these modules touch `ACCESS_NOTIFICATION_POLICY`, DND, or notifications (REC-09) — they only listen to audio focus / battery and play SoundPool tones.                                                                                                                                  |

</threat_model>

<verification>
- `npm --prefix apps/mobile test -- --run` exits 0.
- `HumynPhoneStateModule.kt` grep-clean of `TelephonyManager` / `PhoneStateListener` / `READ_PHONE_STATE`.
- `HumynScreenBrightnessModule.kt` grep-clean of `Settings.System` / `WRITE_SETTINGS`; contains `BRIGHTNESS_OVERRIDE_NONE`.
- `battery_alert.wav` and `thermal_alert.wav` exist and start with `RIFF`.
</verification>

<success_criteria>

- HumynPhoneState (AudioFocus only), HumynBattery (ACTION_BATTERY_CHANGED), HumynScreenBrightness (per-window override + restore), HumynBeep (SoundPool over bundled .wav) all have their real bodies, with leak-clean unregister-on-stop/invalidate.
- The two pre-baked `.wav` assets generated.
- No forbidden APIs (READ_PHONE_STATE, ACCESS_NOTIFICATION_POLICY, WRITE_SETTINGS, react-native-sound).
- Full mobile test suite green.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-05-SUMMARY.md`
</output>
