---
phase: 04-handdetector-recording-ux-practice-tutorial
plan: 10
type: execute
wave: 6
depends_on: ['04-09']
files_modified:
  - apps/mobile/src/native/HumynCapture.ts
  - apps/mobile/src/native/HumynCapture.types.ts
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/capture/CaptureLaunchSweep.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainApplication.kt
  - apps/mobile/src/components/Toast.tsx
  - apps/mobile/src/boot/bootRecoveryListener.ts
  - apps/mobile/App.tsx
  - apps/mobile/__tests__/screens/recording/crashRecoveryToast.test.tsx
  - apps/mobile/__tests__/components/Toast.test.tsx
  - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [REC-12, ONB-04]
must_haves:
  truths:
    - "Phase 3's app-launch sweep emits a one-shot onCrashRecovery({recovered: string[]}) event when it re-finalizes orphan segments; HumynCapture.ts exposes onCrashRecovery(listener): EmitterSubscription mirroring the existing 5 event helpers"
    - "a boot listener (mounted in App.tsx after hydrate) subscribes once to onCrashRecovery, shows a Home toast 'Recording recovered after force-quit — uploading.' when recovered.length > 0, validates recovered is string[], and removes the subscription (one-shot per app launch)"
    - 'a Toast host/component exists (no toast primitive existed before Phase 4) — renders a transient bottom toast; bootRecoveryListener routes through it'
    - "04-MANUAL-SMOKE.md is a complete on-hardware runbook covering: practice E2E; non-practice 10-min recording via the __DEV__ dev affordance with gate-pass + 80ms vibrate + TTS + brightness drop + active substate + a 10-min auto-segment cut WITHOUT gate re-run + Stop + '{Hh Mm} added' toast; lifecycle edges (call-answered=stop, call-declined=continue, alarm=stop, rotation-out-of-landscape=stop, force-quit=recover-on-launch toast, battery 15% alert, battery 5% end, storage refuse); thermal injection (adb shell cmd thermalservice override-status 4); §5b the [BLOCKING] ±1ms drift re-measurement on the gate→record handoff; sign-off + re-walked-on stamp; the D-WAVE-09 amendments protocol"
  artifacts:
    - path: 'apps/mobile/src/native/HumynCapture.ts'
      provides: 'onCrashRecovery(listener): EmitterSubscription'
      contains: 'onCrashRecovery'
    - path: 'apps/mobile/src/boot/bootRecoveryListener.ts'
      provides: 'one-shot HumynCapture.onCrashRecovery → Home toast'
      exports: ['installBootRecoveryListener']
    - path: 'apps/mobile/src/components/Toast.tsx'
      provides: 'transient bottom-toast host/component'
      contains: 'Toast'
    - path: '.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md'
      provides: 'the on-hardware Phase 4 acceptance runbook (D-WAVE-04) incl. the [BLOCKING] drift gate'
      contains: 'imu_video_drift'
  key_links:
    - from: 'apps/mobile/android/app/src/main/java/ai/humynlabs/capture/capture/CaptureLaunchSweep.kt'
      to: 'RCTDeviceEventEmitter onCrashRecovery'
      via: 'after re-finalizing orphan segments, emit onCrashRecovery({recovered})'
      pattern: 'onCrashRecovery'
    - from: 'apps/mobile/App.tsx'
      to: 'bootRecoveryListener'
      via: 'installBootRecoveryListener() after hydrate()'
      pattern: 'installBootRecoveryListener'
---

<objective>
Wire the crash-recovery UX (D-LIFE-04): extend Phase 3's app-launch orphan-segment sweep to emit a one-shot `onCrashRecovery({recovered: string[]})` event, add the `onCrashRecovery` JS binding to `HumynCapture.ts`, build a Toast host/component (none existed), add the boot listener that shows the Home toast "Recording recovered after force-quit — uploading.", and author `04-MANUAL-SMOKE.md` — the on-hardware Phase 4 acceptance runbook (practice E2E + non-practice via dev affordance + lifecycle edges + thermal injection + the **`[BLOCKING]` ±1 ms drift re-measurement on the gate→record handoff**). This is also where Phase 3's seven pending hardware-UAT items effectively retire (D-WAVE-04).

Purpose: REC-12 (force-quit recovery on launch), ONB-04 (practice never uploaded — verified in the smoke walk), and the Phase-4 acceptance gate.
Output: `onCrashRecovery` event (Kotlin + JS) + Toast host + `bootRecoveryListener` + `04-MANUAL-SMOKE.md` + 2 tests + ROADMAP/STATE refresh.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-UI-SPEC.md
@CLAUDE.md
@.planning/phases/03-humyn-capture-native-module/03-CONTEXT.md
@.planning/phases/03-humyn-capture-native-module/03-MANUAL-SMOKE.md
@.planning/phases/02-mobile-shell-onboarding-permissions-compat-profile/02-MANUAL-SMOKE.md
@apps/mobile/src/native/HumynCapture.ts
@apps/mobile/src/native/HumynCapture.types.ts
@apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainApplication.kt
@apps/mobile/src/components/SoftUpgradeBanner.tsx
@apps/mobile/src/hooks/useForegroundUserRehydrate.ts
@apps/mobile/App.tsx
@apps/mobile/src/ui/tokens.ts

<interfaces>
<!-- HumynCapture.ts already has the lazy NativeEventEmitter + 5 on*(listener): EmitterSubscription helpers — add a 6th -->
export function onCrashRecovery(listener: (e: { recovered: string[] }) => void): EmitterSubscription {
  return emitter().addListener('onCrashRecovery', listener);   // caller MUST .remove() — one-shot per app launch
}
<!-- CaptureLaunchSweep.kt (Phase 3 D-FS-04) — currently re-finalizes orphan MP4s via per-segment .session.json sidecar; add the emit AT THE END -->
// after the sweep loop, if any orphans were re-finalized:
reactContext.getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter::class.java).emit("onCrashRecovery", Arguments.createMap().apply { putArray("recovered", Arguments.fromList(recoveredFilenameBases)) })
// NOTE: CaptureLaunchSweep is currently called from MainApplication.onCreate() as `CaptureLaunchSweep(filesDir).run()` — it may not have a ReactContext yet at that point.
// Re-read 03-CONTEXT D-FS-04 + the actual CaptureLaunchSweep.kt: the emit may need to be deferred until the ReactContext is ready (e.g. queued and flushed when the catalyst instance attaches), OR the sweep moved into HumynCapturePackage's module-init path which has a ReactApplicationContext. Pick the minimal correct approach; this is a SURGICAL edit to a Phase-3-owned file — keep the diff small + add a Kotlin test for the emit path if Phase-3 Kotlin test infra exists, else document smoke-only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: onCrashRecovery event (Kotlin sweep emit + JS binding) + Toast host + bootRecoveryListener + tests</name>
  <read_first>
    - .planning/phases/03-humyn-capture-native-module/03-CONTEXT.md D-FS-04 (the app-launch orphan-segment sweep — its location, semantics, the `.session.json` sidecar; D-LIFE-04 in 04-CONTEXT says Phase 4 "adds a small extension to HumynCapture's package init to emit" the event)
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/capture/CaptureLaunchSweep.kt (the actual sweep impl — does it have a ReactContext? It's currently invoked as `CaptureLaunchSweep(filesDir).run()` from MainApplication.onCreate() which is BEFORE the ReactContext attaches — re-read it; the emit may need to be deferred until the catalyst instance is ready, or the recovered list captured and flushed on the first onHostResume / when HumynCaptureModule constructs. Pick the minimal correct approach.)
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/capture/HumynCaptureModule.kt (the `emitEvent()` RCTDeviceEventEmitter idiom; whether the module can replay a "pending recovery" list on construction)
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/MainApplication.kt (where `CaptureLaunchSweep(filesDir).run()` is called — line ~67; the surgical edit anchors here; plan 04-02 already added the 5 new package registrations + the OrientationActivityLifecycle here — keep this edit minimal + additive)
    - apps/mobile/src/native/HumynCapture.ts (the lazy `NativeEventEmitter` + the 5 existing `on*(listener): EmitterSubscription` helpers + the "caller MUST .remove()" docstring — add a 6th `onCrashRecovery`) + HumynCapture.types.ts (add the `CrashRecoveryEvent` type `{ recovered: string[] }`)
    - apps/mobile/src/components/SoftUpgradeBanner.tsx (the closest transient-notification surface — Toast is a sibling pattern: a bottom-anchored auto-fading pill) + apps/mobile/src/ui/tokens.ts (`colors.recToastBg`/`rgba(26,26,26,.94)` from plan 04-07, or a light-surface toast color if the Home toast is on a light surface — UI-SPEC's recording toasts are dark; the Home recovery toast is on the light Home surface, so use a light-surface toast token — add `colors.toastBg` if missing)
    - apps/mobile/src/hooks/useForegroundUserRehydrate.ts (the "subscribe-once at boot, swallow errors" shape — bootRecoveryListener mirrors this) + apps/mobile/App.tsx (where `hydrate()` is called — mount the listener right after)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § Pattern 6 (the crash-recovery boot listener — the verbatim recipe; the surgical-edit warning) + § Open Question 4 (the exact event name + payload + emit site) + § Security "Crash-recovery payload trusted blindly" (validate `recovered` is `string[]`)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md § "src/boot/bootRecoveryListener.ts (NET-NEW — one-shot listener; toast host also net-new)"
  </read_first>
  <action>
    1. **Kotlin emit** — extend Phase 3's app-launch sweep (`CaptureLaunchSweep.kt`) to capture the list of re-finalized orphan-segment `filenameBase`s and, after the ReactContext is available, emit `onCrashRecovery` with `{ recovered: string[] }` via `RCTDeviceEventEmitter`. Because the sweep currently runs in `MainApplication.onCreate()` (before the catalyst instance attaches), the correct minimal approach is one of: (a) the sweep stores the recovered list in a static/companion holder, and `HumynCaptureModule`'s constructor (or its `onCatalystInstanceCreated`/first `onHostResume`) drains the holder and emits — OR (b) the sweep is given the `ReactApplicationContext` (via `HumynCapturePackage.createNativeModules` which has it) and emits when the bridge is ready. Re-read `CaptureLaunchSweep.kt` + `HumynCaptureModule.kt` + `HumynCapturePackage.kt` and pick whichever requires the smallest, most-additive diff to the Phase-3-owned files. KEEP the existing sweep semantics (re-finalize orphan MP4s via the `.session.json` sidecar; clean practice >24h) UNCHANGED — only ADD the recovered-list capture + the deferred emit. Document the chosen approach in the SUMMARY (this is a surgical edit to a Phase-3-owned module per D-LIFE-04 — annotate the diff with `// Phase 4 D-LIFE-04 — onCrashRecovery emit (plan 04-10)`).
    2. **JS binding** — in `apps/mobile/src/native/HumynCapture.ts`, add `export function onCrashRecovery(listener: (e: { recovered: string[] }) => void): EmitterSubscription { return emitter().addListener('onCrashRecovery', listener); }` with the "caller MUST .remove() on unmount (T-3.3-04 leak mitigation)" docstring, mirroring the existing 5 helpers. In `apps/mobile/src/native/HumynCapture.types.ts`, add `export interface CrashRecoveryEvent { recovered: string[] }` with a docstring: "D-LIFE-04 — emitted once on app launch when the Phase-3 app-launch sweep re-finalizes orphan segments after a force-quit/OS-evict; `recovered` lists the re-finalized filenameBases. The recovered segments go through the normal upload path (Phase 5)." Re-export `CrashRecoveryEvent` from `HumynCapture.ts`.
    3. **Toast host** — `apps/mobile/src/components/Toast.tsx` — NEW. A simple bottom-anchored auto-fading toast: a module-level imperative API `showToast(text: string, durationMs?: number)` + a `<ToastHost />` component mounted near the navigation root that renders the current toast (a pill with `colors.toastBg` background, white-ish text, `caption`/`body` variant, fades out after `durationMs ?? 2000`). Pattern: a tiny event-emitter or a Zustand slice (`appStore` already exists — you may add a `toast: { text: string; visible: boolean } | null` slice + `setToast`/`clearToast` actions, OR a standalone module-level subscribers list — pick the lighter; SoftUpgradeBanner is the closest existing transient-surface precedent). `<ToastHost />` is mounted in `App.tsx` (or inside `RootNativeStack` like `useForegroundUserRehydrate`) so any screen can `showToast(...)`. Add `colors.toastBg` (`'rgba(26,26,26,0.94)'`) to `tokens.ts` if not already there (it was added as `colors.recToastBg` in plan 04-07 — reuse that or add a generic `colors.toastBg` alias). `accessibilityLabel="toast"`. Also export the `showToast` function so `RecordingScreen.tsx`'s `setToast` callback (plan 04-09) and `bootRecoveryListener` both route through it — if RecordingScreen (plan 04-09) used a local `setToast` state, leave it; the Home recovery toast just needs this host. (If a `<ToastHost />` doesn't fit cleanly into the existing navigation root, mount it inside `App.tsx`'s render tree as a sibling of `<NavigationContainer>` so it floats over everything.)
    4. **bootRecoveryListener** — `apps/mobile/src/boot/bootRecoveryListener.ts` — NEW. Export `installBootRecoveryListener(): void` (or `(): () => void` returning a teardown): `const sub = HumynCapture.onCrashRecovery(({ recovered }) => { if (Array.isArray(recovered) && recovered.length > 0 && recovered.every(x => typeof x === 'string')) { showToast('Recording recovered after force-quit — uploading.'); } sub.remove(); /* one-shot per app launch */ });` — wrap in try/catch (if `HumynCapture` isn't registered in a test/JSDOM env, swallow). Top docstring: "D-LIFE-04 — one-shot at app boot. When Phase 3's app-launch sweep re-finalizes orphan segments after a force-quit, fire a Home toast. The recovered segments go through the normal upload path (Phase 5 — not Phase 4). RecordingScreen is NOT shown during recovery; the user sees the recording arrive in History (Phase 6). Validates `recovered` is `string[]` before use (Security — don't trust the payload blindly)." Mount it in `App.tsx` right after `hydrate()` (e.g. `import { installBootRecoveryListener } from './src/boot/bootRecoveryListener'; hydrate(); installBootRecoveryListener();`) — or inside `RootNativeStack` like `useForegroundUserRehydrate()` if a navigator-mounted hook is cleaner; pick one.
    5. **Tests** — `apps/mobile/__tests__/components/Toast.test.tsx` (render `<ToastHost />`, call `showToast('hi')`, assert it renders 'hi'; advance the fake clock past the duration, assert it's gone) + `apps/mobile/__tests__/screens/recording/crashRecoveryToast.test.tsx` (mock `react-native` to inject `NativeModules.HumynCapture` + a stub `NativeEventEmitter` whose `addListener` captures the listener; render the Toast host + call `installBootRecoveryListener()`; fire the captured `onCrashRecovery` listener with `{ recovered: ['20260511_120000_001'] }` → assert `showToast('Recording recovered after force-quit — uploading.')` was triggered (the toast renders the text) AND the subscription's `.remove()` was called (one-shot); fire it again with `{ recovered: [] }` → no new toast; fire with `{ recovered: 'not-an-array' }` → no toast (the `Array.isArray` guard). Mirror `HumynCapture.test.ts`'s `setupEmitterMock()` constructor-spy pattern for the event-emitter mock.)
  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/components/Toast.test.tsx __tests__/screens/recording/crashRecoveryToast.test.tsx __tests__/native/HumynCapture.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix apps/mobile test -- --run __tests__/components/Toast.test.tsx __tests__/screens/recording/crashRecoveryToast.test.tsx __tests__/native/HumynCapture.test.ts` exits 0
    - `apps/mobile/src/native/HumynCapture.ts` contains `onCrashRecovery` and exports it
    - `apps/mobile/src/native/HumynCapture.types.ts` contains `CrashRecoveryEvent`
    - `apps/mobile/src/components/Toast.tsx` exists and exports a `showToast` function and a `ToastHost` (or equivalent) component
    - `apps/mobile/src/boot/bootRecoveryListener.ts` exists, exports `installBootRecoveryListener`, contains the string `Recording recovered after force-quit — uploading.` and an `Array.isArray(recovered)` guard
    - `apps/mobile/App.tsx` contains `installBootRecoveryListener` (and `ToastHost` is mounted somewhere in the render tree or via RootNativeStack)
    - `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/capture/CaptureLaunchSweep.kt` OR `HumynCaptureModule.kt` contains `onCrashRecovery`
    - `crashRecoveryToast.test.tsx` asserts the subscription `.remove()` is called (one-shot) AND that a non-array `recovered` payload does NOT show a toast
    - the full suite `npm --prefix apps/mobile test -- --run` exits 0
  </acceptance_criteria>
  <done>onCrashRecovery event (Kotlin sweep emit + JS binding) + Toast host + bootRecoveryListener + tests landed; full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: 04-MANUAL-SMOKE.md on-hardware acceptance runbook (incl. the [BLOCKING] ±1ms drift gate) + ROADMAP/STATE refresh</name>
  <read_first>
    - .planning/phases/03-humyn-capture-native-module/03-MANUAL-SMOKE.md + 03-WAVE1-SMOKE.md + .planning/phases/02-mobile-shell-onboarding-permissions-compat-profile/02-MANUAL-SMOKE.md (the runbook shape — numbered checkbox sections + per-step Inputs/Assertions blocks + adb/curl commands inline + Pre-flight + Sign-off bookends + per-section Acceptance criteria + the D-WAVE-09 amendments protocol)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-CONTEXT.md `<specifics>` § "04-MANUAL-SMOKE.md runbook structure" (the §1–§7 outline) + D-WAVE-04 (the Phase-4 acceptance gate — the four things the operator must verify on Pixel 10a) + D-WAVE-02 (new cosmetic gaps → 04-COSMETIC-GAPS.md, NOT back into the frozen 02/03 gap files)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § "Validation Architecture — Wave 0" (the `04-MANUAL-SMOKE.md` bullet — §5b the ±1ms drift re-measurement is [BLOCKING]) + § "Manual-only verifications" table + § Pitfall 1 (the drift-on-handoff regression — read `onSegmentComplete.drift.{max,mean,p99}` for the first few segments of a gate-started recording; must not regress Phase 3 smoke 7's mean 0.594 / p99 0.728 ms) + STATE.md "Phase 3 hardware UAT pending (7 items)" (which retire here per D-WAVE-04)
    - CLAUDE.md (the audio-drop banner — drift invariant is non-negotiable; do NOT re-introduce audio) + .planning/STATE.md "Phase 3 hardware UAT pending"
  </read_first>
  <action>
    1. Create `.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md` — pattern-match `03-MANUAL-SMOKE.md` / `02-MANUAL-SMOKE.md` (numbered checkbox sections; per-step Inputs/Assertions; adb commands inline; Pre-flight + Sign-off bookends; per-section Acceptance criteria; the D-WAVE-09 amendments protocol pointing new cosmetic gaps at `04-COSMETIC-GAPS.md`, NEVER the frozen `02-COSMETIC-GAPS.md`/`03-W1-AMENDMENTS.md`). Sections:
       - **§1 Pre-flight** — device prep (Pixel 10a / 7a-class; debug build of the `apkRollout` flavor installed; DND off; plugged in; `adb devices` confirms; the dev API + backend reachable per the Phase-1 resume notes if a recording needs the upload seam — though Phase 5 owns upload, so a recording landing in `recordings/`/`practice/` is enough for Phase 4); confirm `__DEV__` is true in the debug build (so the TasksPlaceholder long-press affordance is present).
       - **§2 Practice E2E** — fresh install → Splash → Sign-up → Permissions → Compat → RigTutorial → tap Next → PracticeIntro ("One quick try") → tap "Start practice" → RecordingScreen: rotate to landscape → tap the 88×88 record button → gate substate (130×130 ring, "Mount the phone on your head and bring your hands in frame for 2 secs", Skip link visible) → bring 2 hands into frame on the rig → ring fills over ~5×400ms → gate-pass: 80ms vibrate + TTS "Recording started" (en-IN voice if installed) + screen dims to ~5% + active substate (32px mono timer counting, top minute-bar growing) → let it run to ~60s → auto-stops at exactly 60s (the JS-owned practice hard cap) → voice "Recording stopped" → PracticeCompleteScreen ("You got it.", confetti, [40,80,40]ms haptic) → tap Continue → Home (first-time hero variant). Assertions: each transition occurs; the recording landed in `filesDir/practice/` (`adb shell run-as ai.humynlabs.capture.apk ls files/practice/`); the practice recording is NOT in History and does NOT count toward contribution (ONB-04 — re-launch the app, check History/Home); restart the app → the tutorial does NOT re-run (ONB-08, the per-account flag persisted); the `cacheDir/hand-gate/` dir is empty after the gate (`adb shell run-as ... ls cache/hand-gate/` — the JPEGs were deleted on each gate-check resolve + the mount sweep).
       - **§3 Non-practice via the dev affordance** — long-press (>800ms) the "Tasks — coming in Phase 6." heading on the Tasks tab → RecordingScreen with `{ taskId: 'cooking_chop_vegetables', taskName: 'Practice — Chop vegetables', isPractice: false }` → gate-pass (80ms vibrate + TTS + brightness drop + active) → let it run ~10+ min → observe the 10-min auto-segment cut: a SILENT segment swap (no gate re-run, no voice cue — CAP-10) — confirm via `adb shell run-as ... ls -la files/recordings/` (two `.mp4`/`.csv`/`.json` triples with consecutive `YYYYMMDD_HHMMSS_NNN` names, ~0.5s gap) and `adb shell run-as ... cat files/recordings/<seg2>.json` (the `start_gate` block is IDENTICAL to seg1's — preserved across segments) → tap the 64×64 white Stop button → "Stop recording?" modal? No — the stop button stops directly; the X button shows the modal (test BOTH: X → "Stop recording?" modal with "Recordings under 1 minute are discarded." body + "Keep recording"/"Stop"; the floating stop button → stops directly) → voice "Recording stopped" + toast "{Hh Mm} added to your contribution." (e.g. "11m added to your contribution.") → returns to Home. Assertions: the segments are spec-compliant (this is where Phase 3 UAT #1/#2 retire — `ffprobe` the MP4: 1920×1080 / 30fps / HEVC Main / 8 Mbps CBR / GOP 30 / no B-frame at NAL level; the IMU CSV sustains ≥100 Hz); FGS type `camera|microphone|dataSync` + `KEEP_SCREEN_ON` during the recording (`adb shell dumpsys activity services | grep -i humyn` — Phase 3 UAT #5 retires); CAP-18 byte-for-byte SHA of the MP4/CSV matches the metadata JSON's `file_sha256`/`imu_sha256` (Phase 3 UAT #6 retires); the `onSessionStart`/`onSessionStop` upload-pause seam fires (Phase 3 UAT #7 — log only; Phase 5 wires the actual pause).
       - **§4 Lifecycle edges** — for each, start a recording then trigger the edge: (a) phone-call ANSWERED mid-record → recording stops (upload if ≥60s, discard if not) — REC-12; (b) phone-call DECLINED (let it ring, decline before answering) → recording CONTINUES — REC-13; (c) alarm rings mid-record → recording stops — REC-12; (d) rotate the phone out of landscape mid-record → recording stops + toast "Recording stopped — keep the phone in landscape." — REC-12; (e) force-quit the app mid-record (swipe away from recents) → relaunch → a one-shot Home toast "Recording recovered after force-quit — uploading." + the orphan segment was re-finalized (`adb shell run-as ... ls files/recordings/`) — REC-12/D-LIFE-04; (f) battery drops to ≤15% during a recording (or simulate — `adb shell dumpsys battery set level 15`) → alert pill "Battery 15%" + 520Hz beep + [100,50,100]ms haptic + voice "Battery low. Consider charging soon." + recording CONTINUES — REC-10; (g) battery drops to ≤5% mid-record → recording ends immediately — REC-11; (h) try to start a new recording with <5GB free → refused with a toast — REC-16; (i) try to start with battery <5% → refused with a toast — the battery start guard. (Reset battery with `adb shell dumpsys battery reset` after.) DND is NEVER toggled by the app — confirm DND state is unchanged before/after a recording (REC-09).
       - **§5 Thermal injection** — start a recording, then `adb shell cmd thermalservice override-status 4` (SEVERE) → the thermal alert pill "Phone too hot" + descending 440→560→680 Hz tones + 800ms continuous vibrate + voice "Phone too hot, stopping recording" → HC self-stops within ~2.5s → toast "Recording stopped — phone needs to cool." (Phase 3 UAT #4 — pre-record + mid-record thermal — retires here; also test pre-record: `adb shell cmd thermalservice override-status 3` then tap record → `HumynCapture.start()` rejects with `thermal_throttling` → voice "Phone too warm" + back to Ready substate.) Reset: `adb shell cmd thermalservice override-status 0`.
       - **§5b ±1 ms video↔IMU drift re-measurement on the gate→record handoff — [BLOCKING]** — record a NON-practice recording started THROUGH the hand-gate (so the camera was just released by VisionCamera before `HumynCapture.start()`); read the `onSegmentComplete.drift.{max,mean,p99}` for the first 2–3 segments (the metadata JSON's `imu_video_drift_{max,mean,p99}_ms` fields — `adb shell run-as ... cat files/recordings/<seg>.json | python -m json.tool | grep drift`); ALSO repeat for a recording that went through a lifecycle-recovery `start()` re-call (e.g. force-quit + relaunch path — though that path doesn't re-call `start()` on the same screen; if no lifecycle path re-calls `start()`, just the gate→record handoff suffices). **The drift figures MUST NOT regress past ±1 ms — specifically must not regress Phase 3 smoke 7's mean 0.594 ms / p99 0.728 ms.** If any segment exceeds ±1 ms: this is a Phase 4 BLOCKER (not a Phase 5 deferral) — increase the `SETTLE_MS` tunable in `RecordingScreen.tsx` (plan 04-09) and re-measure; if even that doesn't fix it, escalate a "HumynCapture.start() should poll for camera availability before opening Camera2" change to Phase 3 (surgical-stage protocol). Record the measured `{max,mean,p99}` figures in the runbook. (This is the section that satisfies the capture-quality invariant for the new Phase-4 camera handoff — Phase 3 UAT #3 retires here.)
       - **§6 Sign-off** — operator name + date + a `re-walked-on:` stamp + a per-section pass/fail checklist + the recorded §5b drift figures. The verifier accepts "module-ready + practice E2E passes + lifecycle edges manually verified + §5b drift figures within ±1 ms" per D-WAVE-04 — Phase 3's seven pending hardware-UAT items effectively retire here (the verifier should NOT separately re-block on them after Phase 4 closes).
       - **§7 Amendments protocol** — the Phase-3 D-WAVE-09 pattern: new cosmetic gaps surfaced during this smoke walk go into `.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-COSMETIC-GAPS.md` (created on first use; Phase 5 may pick up OR a Wave-5 fix-up before Phase 5 plan-phase) — NEVER back into the frozen `02-COSMETIC-GAPS.md` / `03-W1-AMENDMENTS.md`.
    2. **ROADMAP.md refresh** — update `.planning/ROADMAP.md` Phase 4 entry: change `**Plans**: TBD` to `**Plans:** 10 plans` and add the plan list under it (mirroring the Phase 2 entry format):
       ```
       Plans:
       **Wave 1** — 04-01 (RN deps + vitest mocks + orientation manifest), 04-02 (native module shells + MediaPipe gradle + hand_landmarker.task + JS bindings + registration), 04-03 (practiceDoneKey + jwtSub util + computeInitialRoute per-account gate + RigTutorial retarget)
       **Wave 2** — 04-04 (HumynHandDetector MediaPipe body), 04-05 (HumynPhoneState/Battery/ScreenBrightness/Beep bodies + .wav assets)
       **Wave 3** — 04-06 (PracticeIntro + PracticeComplete + OnboardingStack + practice tests + 2 visuals), 04-07 (recState machine + 5 recording components + RecordingScreen shell + RootNativeStack route + route-registry update + 8 visuals)
       **Wave 4** — 04-08 (useRecordingLifecycle §10 policy table + ttsVoice + durationFormat + __DEV__ dev affordance)
       **Wave 5** — 04-09 (RecordingScreen live wiring: VC mount + useHandGate + gate-pass transition + buildCaptureOpts + RemoteConfig gate + useRecordingLifecycle mount + §7h routing)
       **Wave 6** — 04-10 (onCrashRecovery + Toast host + bootRecoveryListener + 04-MANUAL-SMOKE.md + [BLOCKING] drift gate + ROADMAP/STATE refresh)
       ```
       Also flip the Phase 4 row in the `## Progress` table's "Plans Complete" cell from `0/TBD` to `0/10` (the orchestrator/execute-phase updates it to `N/10` as plans land).
    3. **STATE.md refresh** — update `.planning/STATE.md`: `stopped_at` → `Phase 4 planned (10 plans, 6 waves)`; `resume_file` → `.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-01-PLAN.md`; `progress.total_plans` += 10 → 56; add a "Roadmap Evolution" / "Accumulated Context > Decisions" line: "2026-05-11: Phase 4 planned — 10 plans across 6 plan-waves (conceptual D-WAVE-01 5-wave structure: wave-4 work [state machine + lifecycle] spans plan-waves 4-5 because RecordingScreen.tsx and useRecordingLifecycle.ts must not be touched by the same plan; wave-5 work [crash-recovery + smoke + Phase-3-UAT-retirement] is plan-wave 6). The [BLOCKING] ±1ms drift re-measurement on the gate→record camera handoff lives in 04-MANUAL-SMOKE.md §5b — a Phase-4 blocker if it regresses past Phase 3 smoke 7 (mean 0.594 / p99 0.728 ms). Phase 3's seven pending hardware-UAT items retire during the Phase 4 Wave-6 smoke walk (D-WAVE-04)." Update the "Phase 3 hardware UAT pending" note to point at 04-MANUAL-SMOKE.md §3/§5/§5b as the retirement site.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md && grep -q imu_video_drift .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md && grep -q BLOCKING .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md && grep -q "10 plans" .planning/ROADMAP.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md` exists and contains `imu_video_drift`, `BLOCKING`, `cmd thermalservice override-status 4`, `Recording recovered after force-quit`, and `re-walked-on:`
    - `.planning/ROADMAP.md` Phase 4 entry contains `**Plans:** 10 plans` (no longer `TBD`) and lists `04-01` through `04-10`
    - `.planning/ROADMAP.md` `## Progress` table Phase 4 row shows `0/10` (not `0/TBD`)
    - `.planning/STATE.md` `stopped_at` mentions `Phase 4 planned` and `resume_file` points at `04-01-PLAN.md` and `progress.total_plans` is `56`
    - the bash verify command above (`test -f ... && grep -q ... && echo ok`) prints `ok`
  </acceptance_criteria>
  <done>Crash-recovery wiring done; 04-MANUAL-SMOKE.md on-hardware acceptance runbook (with the [BLOCKING] §5b drift gate) authored; ROADMAP.md + STATE.md refreshed with the 10-plan / 6-wave Phase 4 structure.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                          | Description                                               |
| ------------------------------------------------- | --------------------------------------------------------- |
| Phase-3 app-launch sweep → JS boot listener       | the `onCrashRecovery` event crosses from the native sweep |
| `onCrashRecovery` payload → toast                 | the `recovered: string[]` payload is consumed at boot     |
| on-hardware smoke → the capture-quality invariant | §5b re-measures ±1ms drift on the new gate→record handoff |

## STRIDE Threat Register

| Threat ID | Category                                                 | Component                                                        | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                   |
| --------- | -------------------------------------------------------- | ---------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-4.10-01 | Capture-quality invariant (the project's non-negotiable) | the VC→HumynCapture camera handoff                               | mitigate    | §5b of `04-MANUAL-SMOKE.md` re-measures `onSegmentComplete.drift.{max,mean,p99}` on Pixel-class hardware for a gate-started recording — a `[BLOCKING]` gate; must not regress Phase 3 smoke 7 (mean 0.594 / p99 0.728 ms). If it regresses → bump `SETTLE_MS` (plan 04-09) or escalate to Phase 3. Audio is NOT re-introduced (CLAUDE.md banner). |
| T-4.10-02 | Tampering (low — app-internal)                           | the `onCrashRecovery({recovered})` payload                       | mitigate    | `bootRecoveryListener` validates `recovered` is `string[]` (`Array.isArray` + `every(typeof === 'string')`) before showing the toast; the recovered segments still go through Phase 5's verification path. The listener is one-shot per app launch (`.remove()` after the first fire).                                                            |
| T-4.10-03 | Spec violation                                           | the `MainApplication.kt` / `CaptureLaunchSweep.kt` surgical edit | accept      | minimal additive edit (capture the recovered list + a deferred emit when the ReactContext is ready); no permissions; the existing `__tests__/manifests/manifests.test.ts` invariant gate still passes; the sweep's existing semantics (re-finalize orphans, clean practice >24h) are unchanged.                                                   |
| T-4.10-04 | Info disclosure                                          | the recovery toast / smoke runbook                               | accept      | the toast carries a fixed string (no filenames, no PII); the smoke runbook's `adb` commands read app-internal files for operator verification, not exfiltration.                                                                                                                                                                                  |

</threat_model>

<verification>
- `npm --prefix apps/mobile test -- --run` exits 0 (Toast, crashRecoveryToast, HumynCapture, and the pre-existing suite green).
- `04-MANUAL-SMOKE.md` exists with the §5b `[BLOCKING]` drift gate + thermal injection + lifecycle edges + practice E2E + the Phase-3-UAT-retirement notes.
- `ROADMAP.md` Phase 4 entry shows `**Plans:** 10 plans` + the plan list; `## Progress` table shows `0/10`.
- `STATE.md` `stopped_at`/`resume_file`/`progress.total_plans` updated.
- `HumynCapture.ts` exports `onCrashRecovery`; `App.tsx` mounts `installBootRecoveryListener` + `ToastHost`.
</verification>

<success_criteria>

- `onCrashRecovery` event wired (Phase-3 sweep emit + JS binding) + a Toast host + a one-shot boot listener showing the Home recovery toast (REC-12 / D-LIFE-04), with the payload validated.
- `04-MANUAL-SMOKE.md` — the complete on-hardware Phase-4 acceptance runbook: practice E2E, non-practice via the `__DEV__` affordance (incl. the 10-min auto-segment cut without gate re-run), all `idea-brief.md §10` lifecycle edges, thermal injection, and the `[BLOCKING]` §5b ±1 ms drift re-measurement on the gate→record handoff. Phase 3's seven pending hardware-UAT items retire here (D-WAVE-04).
- `ROADMAP.md` + `STATE.md` refreshed with the 10-plan / 6-plan-wave Phase 4 structure.
- Full mobile test suite green.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-10-SUMMARY.md`
</output>
