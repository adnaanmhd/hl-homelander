---
phase: 04-handdetector-recording-ux-practice-tutorial
plan: 06
type: execute
wave: 3
depends_on: ['04-01', '04-03']
files_modified:
  - apps/mobile/src/screens/tutorial/PracticeIntroScreen.tsx
  - apps/mobile/src/screens/tutorial/PracticeCompleteScreen.tsx
  - apps/mobile/src/screens/tutorial/components/Confetti.tsx
  - apps/mobile/src/navigation/OnboardingStack.tsx
  - apps/mobile/src/util/analytics.ts
  - apps/mobile/__tests__/screens/PracticeIntroScreen.test.tsx
  - apps/mobile/__tests__/screens/PracticeCompleteScreen.test.tsx
  - apps/mobile/__tests__/screens/practiceFlow.test.tsx
  - apps/mobile/__tests__/visual/PracticeIntroScreen.visual.test.tsx
  - apps/mobile/__tests__/visual/PracticeCompleteScreen.visual.test.tsx
  - apps/mobile/__tests__/visual/__image_snapshots__/practice-intro.png
  - apps/mobile/__tests__/visual/__image_snapshots__/practice-complete-static.png
autonomous: true
requirements: [ONB-03, ONB-07, ONB-08]
must_haves:
  truths:
    - "PracticeIntroScreen renders the verbatim §6 copy and its Start practice CTA navigates to Recording with { taskId: '__practice__', taskName: 'Practice — 60 sec', isPractice: true }"
    - "PracticeCompleteScreen renders the success badge + scale-pop + confetti + fires Vibration.vibrate([0,40,80,40]) on enter, and its Continue CTA writes the per-account practice-done flag (appStore.setPracticeDone(sub)) then navigation.reset({routes:[{name:'MainTabs'}]})"
    - 'OnboardingStack registers PracticeIntro (after RigTutorial) and PracticeComplete'
    - 'RigTutorial → PracticeIntro → Recording → PracticeComplete → MainTabs is the wired chain (RigTutorial Next-CTA retarget already done in plan 04-03)'
  artifacts:
    - path: 'apps/mobile/src/screens/tutorial/PracticeIntroScreen.tsx'
      provides: "design-spec §6 verbatim — heading 'One quick try', body, muted line, btn-accent 'Start practice'"
      min_lines: 40
    - path: 'apps/mobile/src/screens/tutorial/PracticeCompleteScreen.tsx'
      provides: "design-spec §8 verbatim — 96×96 success badge, scale-pop 500ms, confetti, [40,80,40]ms haptic, btn-primary 'Continue'"
      min_lines: 50
    - path: 'apps/mobile/src/navigation/OnboardingStack.tsx'
      provides: 'PracticeIntro + PracticeComplete route registrations'
      contains: 'PracticeIntro'
  key_links:
    - from: 'apps/mobile/src/screens/tutorial/PracticeIntroScreen.tsx'
      to: 'Recording route'
      via: "navigation.replace('Recording', {taskId:'__practice__', isPractice:true})"
      pattern: 'isPractice'
    - from: 'apps/mobile/src/screens/tutorial/PracticeCompleteScreen.tsx'
      to: 'MMKV practiceDoneKey(sub)'
      via: 'appStore.setPracticeDone(decodeGoogleSubFromJwt(jwt)) then navigation.reset'
      pattern: 'setPracticeDone'
---

<objective>
Build the two light tutorial screens — `PracticeIntroScreen` (design-spec §6 verbatim, between RigTutorial and Recording) and `PracticeCompleteScreen` (design-spec §8 verbatim — confetti + scale-pop + `[40,80,40]` ms haptic, writes the ONB-08 per-account flag then resets to MainTabs) — register them in OnboardingStack, add the analytics events, and capture their 2 visual baselines. (The RigTutorial Next-CTA retarget to PracticeIntro and the `practiceDoneKey`/`setPracticeDone`/`decodeGoogleSubFromJwt` plumbing were landed in plan 04-03.)

Purpose: ONB-03 (the practice flow entry/exit), ONB-07 (Practice-complete screen), ONB-08 (the once-per-install gate write).
Output: 2 screens + a `Confetti` component + OnboardingStack additions + analytics events + 3 tests + 2 visual baselines.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-PATTERNS.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-UI-SPEC.md
@CLAUDE.md
@design-spec.md
@engineering-handoff.md
@prototype.html
@apps/mobile/src/screens/tutorial/RigTutorialScreen.tsx
@apps/mobile/src/screens/compat/CompatPassScreen.tsx
@apps/mobile/src/navigation/OnboardingStack.tsx
@apps/mobile/src/util/analytics.ts
@apps/mobile/src/lib/jwtSub.ts
@apps/mobile/src/state/appStore.ts
@apps/mobile/src/state/keys.ts
@apps/mobile/src/ui/tokens.ts
@apps/mobile/__tests__/visual/RigTutorialScreen.visual.test.tsx
@apps/mobile/__tests__/screens/RigTutorialScreen.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PracticeIntroScreen + OnboardingStack route + analytics event + test + visual baseline</name>
  <read_first>
    - design-spec.md §6 (Tutorial Practice intro `#tut-practice`) + UI-SPEC § "Copywriting Contract" (the verbatim copy: heading "One quick try", body "We'll walk you through one short recording — 60 seconds, just to get the feel.", muted line "This is a practice task — it does not count towards your contribution.", CTA "Start practice" `btn-accent`) + UI-SPEC § Typography (tutorialHeading 30/36 700, tutBody 17/25, bodyMuted 14/20, btn label 16/20 600) + UI-SPEC § Color (light surface; `--accent #FF6A2D` reserved for the "Start practice" CTA)
    - apps/mobile/src/screens/tutorial/RigTutorialScreen.tsx (near-perfect template — ScreenContainer + center stack + heading/body + bottom CTA + `logEvent` on mount; the `navigation.getParent()?.replace(...)` jump-out-of-OnboardingStack idiom)
    - apps/mobile/src/navigation/OnboardingStack.tsx (the `<Stack.Screen name="X" component={X} />` list — add PracticeIntro after RigTutorial)
    - apps/mobile/src/util/analytics.ts EVENT_NAMES (the frozen allowlist — add the new practice events)
    - apps/mobile/src/ui/primitives/Button.tsx (confirm whether a `btn-accent` / `variant="accent"` exists; if the Button primitive only has `primary`/`outline`, you need to add an `accent` variant whose fill is `colors.accent` — re-read Button.tsx and tokens.ts; UI-SPEC says `--accent #FF6A2D` is reserved for exactly this CTA)
    - apps/mobile/__tests__/visual/RigTutorialScreen.visual.test.tsx (the visual-snapshot template — `vi.mock('../../src/util/analytics')`, `render()`, `renderToImage(container)`, `toMatchImageSnapshot()`)
  </read_first>
  <action>
    1. `apps/mobile/src/util/analytics.ts` — add to `EVENT_NAMES`: `'practice_intro_shown'`, `'practice_started'`, `'practice_complete_shown'`, `'practice_complete_continued'`, plus the recording-funnel events `'recording_gate_started'`, `'recording_gate_passed'`, `'recording_gate_skipped'` (HAND-14 — used by plan 04-09), `'recording_gate_bypassed'`, `'recording_started'`, `'recording_stopped'`, `'recording_too_short'` — group them under an `// Onboarding — practice tutorial (Phase 4)` and `// Recording (Phase 4)` comment header. (Adding these here keeps the allowlist authoritative; plan 04-09 references the recording events.)
    2. If Button doesn't have an `accent` variant: add `variant="accent"` to `apps/mobile/src/ui/primitives/Button.tsx` — fill `colors.accent`, label `colors.surface` (white), same radius/padding as `primary`. Update the Button's type union. (UI-SPEC: `--accent` is the "go-do-the-recording" CTA color.) If it already exists, skip.
    3. `apps/mobile/src/screens/tutorial/PracticeIntroScreen.tsx` — NEW, template from RigTutorialScreen: `<ScreenContainer accessibilityLabel="PracticeIntro screen">` with a centered stack: `<Text variant="tutorialHeading" tone="primary">One quick try</Text>`, `<Text variant="tutBody" tone="secondary">We'll walk you through one short recording — 60 seconds, just to get the feel.</Text>`, `<Text variant="bodyMuted" tone="tertiary">This is a practice task — it does not count towards your contribution.</Text>` (use the exact variant names from UI-SPEC § Typography — if a `bodyMuted` variant doesn't exist use `caption` with `tone="tertiary"`), and a bottom `<Button variant="accent" label="Start practice" accessibilityLabel="Start practice" onPress={handleStart} />`. `useEffect(() => logEvent('practice_intro_shown'), [])`. `handleStart`: `logEvent('practice_started'); navigation.replace('Recording', { taskId: '__practice__', taskName: 'Practice — 60 sec', isPractice: true })` — `Recording` is a RootNativeStack route (registered by plan 04-07); use `navigation.getParent?.()` to leave OnboardingStack (`const parent = navigation.getParent?.(); if (parent && typeof parent.replace === 'function') parent.replace('Recording', {...}); else navigation.replace('Recording', {...});` — same idiom RigTutorialScreen uses). Spacing/gutter per UI-SPEC § Spacing (Practice-intro screen gutter = 28px / `spacing.xl`).
    4. `apps/mobile/src/navigation/OnboardingStack.tsx` — add `import PracticeIntroScreen from '../screens/tutorial/PracticeIntroScreen';` + `import PracticeCompleteScreen from '../screens/tutorial/PracticeCompleteScreen';` (the latter created in Task 2) and `<Stack.Screen name="PracticeIntro" component={PracticeIntroScreen} />` after `RigTutorial`, and `<Stack.Screen name="PracticeComplete" component={PracticeCompleteScreen} />` after that. Update the navigator's comment block: "Splash → Signup → Permissions → Compat → RigTutorial → PracticeIntro → (Recording, RootNativeStack route) → PracticeComplete → MainTabs (via navigation.reset). PracticeIntro + PracticeComplete added Phase 4 plan 04-06."
    5. `apps/mobile/__tests__/screens/PracticeIntroScreen.test.tsx` — mock navigation + `logEvent`; assert: renders the verbatim copy strings; `logEvent('practice_intro_shown')` fired on mount; tapping "Start practice" fires `logEvent('practice_started')` and navigates to `'Recording'` with `{ taskId: '__practice__', taskName: 'Practice — 60 sec', isPractice: true }`. Mirror `RigTutorialScreen.test.tsx`.
    6. `apps/mobile/__tests__/visual/PracticeIntroScreen.visual.test.tsx` — template from `RigTutorialScreen.visual.test.tsx`; baseline filename `practice-intro.png`. Run it once to generate the baseline (`npm --prefix apps/mobile test -- --run __tests__/visual/PracticeIntroScreen.visual.test.tsx` with `--update` if jest-image-snapshot requires it on first run; commit the generated PNG).
  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/screens/PracticeIntroScreen.test.tsx __tests__/visual/PracticeIntroScreen.visual.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix apps/mobile test -- --run __tests__/screens/PracticeIntroScreen.test.tsx __tests__/visual/PracticeIntroScreen.visual.test.tsx` exits 0
    - `apps/mobile/src/screens/tutorial/PracticeIntroScreen.tsx` exists, contains the strings `One quick try` and `Start practice` and `__practice__` and `isPractice`
    - `apps/mobile/src/navigation/OnboardingStack.tsx` contains `name="PracticeIntro"` and `name="PracticeComplete"`
    - `apps/mobile/src/util/analytics.ts` `EVENT_NAMES` contains `'practice_intro_shown'`, `'practice_started'`, `'recording_gate_skipped'`, `'recording_started'`, `'recording_stopped'`
    - `apps/mobile/__tests__/visual/__image_snapshots__/practice-intro.png` exists
    - the full suite `npm --prefix apps/mobile test -- --run` exits 0
  </acceptance_criteria>
  <done>PracticeIntroScreen + OnboardingStack route + analytics events + test + visual baseline landed; full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: PracticeCompleteScreen (confetti + scale-pop + [40,80,40]ms haptic + writes ONB-08 flag) + Confetti component + test + visual baseline + practiceFlow test</name>
  <read_first>
    - design-spec.md §8 (Practice complete `#practice-done` — 96×96 success badge, scale-pop 500ms, confetti 18 particles 800–1200ms rise "random hues from the accent palette", heading copy) + UI-SPEC § Copywriting (heading "You got it." `[confirm w/ PM]`, CTA "Continue" `btn-primary`) + UI-SPEC § Color (light surface; `--success #2EB872` badge fill only; CTA is `btn-primary` = `--text` fill, NOT accent) + UI-SPEC § Spacing (Practice-complete screen gutter = 32px / `spacing.2xl` ... `spacing.xxl`; major top inset 48px / `spacing.3xl`; bottom inset 24px / `spacing.xl`)
    - engineering-handoff.md §6.2 (practice-done haptic `[40, 80, 40]` ms — confirm the exact pattern and whether RN's `Vibration.vibrate([0, 40, 80, 40])` (leading 0 = no initial pause) is the right call)
    - apps/mobile/src/screens/compat/CompatPassScreen.tsx (the transient-success-screen + auto-advance flow pattern; the `Vibration.vibrate` import) + RigTutorialScreen.tsx (layout template)
    - apps/mobile/src/lib/jwtSub.ts (`decodeGoogleSubFromJwt` — REUSE, don't re-implement) + apps/mobile/src/state/appStore.ts (`setPracticeDone(sub)` action from plan 04-03; `jwt` selector) + apps/mobile/src/state/keys.ts (`practiceDoneKey`)
    - apps/mobile/src/components/LogoutModal.tsx lines ~41-53 (the `navigation.reset({ index: 0, routes: [{ name: 'X' }] })` idiom)
    - apps/mobile/vitest.setup.ts (react-native-reanimated is already mocked — confetti animation runs as a no-op under jsdom; the visual baseline must be captured pre-confetti / deterministic, so make the screen render the static layout immediately and start the confetti+scale-pop in a `useEffect` so the snapshot taken at render time is the pre-animation frame)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-RESEARCH.md § "Don't Hand-Roll — Confetti" (Reanimated 3.16.x + a ~30-LOC particle component, no new lib) + § Pattern 5 (the ONB-08 gate write)
  </read_first>
  <action>
    1. `apps/mobile/src/screens/tutorial/components/Confetti.tsx` — NEW, ~30-50 LOC. Renders 18 absolutely-positioned `Animated.View` particles (Reanimated 3.16.x) of small squares/circles, each with a random hue from the accent palette (e.g. a small array of warm tones around `#FF6A2D` — define them as a local const; UI-SPEC sanctions "random hues from the accent palette" for this decorative/transient use), a random x offset, rising 800–1200 ms (`withTiming` with a random duration in that range) with a fade-out. The component takes no props (or an optional `active` boolean). Under jsdom (reanimated mocked) it renders the static particle positions — that's fine for the visual baseline since we capture pre-confetti.
    2. `apps/mobile/src/screens/tutorial/PracticeCompleteScreen.tsx` — NEW. `<ScreenContainer accessibilityLabel="PracticeComplete screen">` with: a 96×96 `colors.success` rounded badge with a check glyph (Lucide `Check` icon or a Text checkmark) that does a scale-pop (`withSequence` 0 → 1.1 → 1.0 over 500ms via Reanimated) on mount; `<Confetti />` overlaid; `<Text variant="title28" tone="primary">You got it.</Text>` (use the `[confirm w/ PM]` recommended string verbatim; if PM has supplied an override during planning use that — otherwise "You got it."); a bottom `<Button variant="primary" label="Continue" accessibilityLabel="Continue" onPress={handleContinue} />`. `useEffect(() => { logEvent('practice_complete_shown'); Vibration.vibrate([0, 40, 80, 40]); }, [])` (leading 0 so the first 40 is the vibrate, then 80 off, then 40 on — confirm against engineering-handoff §6.2; if §6.2 means `[40,80,40]` literally as off/on/off then use that). `handContinue`: `const sub = decodeGoogleSubFromJwt(useAppStore.getState().jwt); useAppStore.getState().setPracticeDone(sub); logEvent('practice_complete_continued'); navigation.reset({ index: 0, routes: [{ name: 'MainTabs' }] });` — use `navigation.getParent?.()` if needed to reach the RootNativeStack-level `MainTabs` route (same idiom RigTutorial uses to reach the parent; PracticeComplete lives in OnboardingStack so `navigation.reset` on the parent resets to MainTabs). Spacing/gutters per UI-SPEC § Spacing.
    3. `apps/mobile/__tests__/screens/PracticeCompleteScreen.test.tsx` — mock navigation + `logEvent` + `appStore` (stub `getState` returning `{ jwt: '<fixture jwt with sub:abc>', setPracticeDone: vi.fn() }`) + `react-native`'s `Vibration` (assert `Vibration.vibrate` called with `[0, 40, 80, 40]` — or the exact pattern you chose); assert: renders "You got it." + "Continue"; `logEvent('practice_complete_shown')` on mount; `Vibration.vibrate(<pattern>)` on mount; tapping Continue calls `setPracticeDone('abc')`, fires `logEvent('practice_complete_continued')`, calls `navigation.reset({ index: 0, routes: [{ name: 'MainTabs' }] })`.
    4. `apps/mobile/__tests__/visual/PracticeCompleteScreen.visual.test.tsx` — template from RigTutorialScreen.visual.test.tsx; mock `appStore` (stub jwt + setPracticeDone) + `logEvent`; baseline `practice-complete-static.png` — capture at `render()` time (pre-confetti, pre-scale-pop — the animations start in `useEffect`, so the rendered DOM at `render()` is the static frame, which is deterministic). Generate + commit the PNG.
    5. `apps/mobile/__tests__/screens/practiceFlow.test.tsx` — an integration-ish test asserting the wired chain: (a) `RigTutorialScreen` Next → `navigation.replace('PracticeIntro')` (or `parent.replace('PracticeIntro')` — assert the route arg is `'PracticeIntro'`, NOT `'MainTabs'`) — re-render RigTutorialScreen with a mocked nav; (b) `PracticeIntroScreen` Start practice → `navigation.replace('Recording', { taskId: '__practice__', taskName: 'Practice — 60 sec', isPractice: true })`; (c) `PracticeCompleteScreen` Continue → `appStore.setPracticeDone(sub)` then `navigation.reset({ routes: [{ name: 'MainTabs' }] })` + `Vibration.vibrate(<pattern>)`; (d) `computeInitialRoute` returns `RigTutorial` when `practiceDoneKey(sub)` is unset and `MainTabs` when set (a thin re-assertion — the deep version lives in `__tests__/state/initialRoute.test.ts` from plan 04-03; here just confirm the screen-level write feeds the boot-level read by seeding the same key the screen wrote). Use the existing screen-test mocking conventions.
  </action>
  <verify>
    <automated>npm --prefix apps/mobile test -- --run __tests__/screens/PracticeCompleteScreen.test.tsx __tests__/screens/practiceFlow.test.tsx __tests__/visual/PracticeCompleteScreen.visual.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix apps/mobile test -- --run __tests__/screens/PracticeCompleteScreen.test.tsx __tests__/screens/practiceFlow.test.tsx __tests__/visual/PracticeCompleteScreen.visual.test.tsx` exits 0
    - `apps/mobile/src/screens/tutorial/PracticeCompleteScreen.tsx` exists, contains `You got it.`, `Continue`, `setPracticeDone`, `decodeGoogleSubFromJwt`, `Vibration.vibrate`, and `navigation.reset`
    - `apps/mobile/src/screens/tutorial/components/Confetti.tsx` exists
    - `apps/mobile/__tests__/visual/__image_snapshots__/practice-complete-static.png` exists
    - `apps/mobile/__tests__/screens/practiceFlow.test.tsx` asserts RigTutorial Next → `'PracticeIntro'` (not `'MainTabs'`)
    - the full suite `npm --prefix apps/mobile test -- --run` exits 0
  </acceptance_criteria>
  <done>PracticeCompleteScreen + Confetti + tests + visual baseline + the practiceFlow chain test landed; full suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                     |
| ---------------------------------------- | ------------------------------------------------------------------------------- |
| JWT (already verified) → local cache key | `decodeGoogleSubFromJwt` derives the MMKV key for the practice-done flag        |
| MMKV write → boot routing                | PracticeComplete's `setPracticeDone(sub)` controls whether the tutorial re-runs |

## STRIDE Threat Register

| Threat ID | Category                        | Component                                      | Disposition | Mitigation Plan                                                                                                                                                                                  |
| --------- | ------------------------------- | ---------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-4.6-01  | Business-logic bypass           | ONB-08 once-per-install gate                   | accept      | the gate is the per-account MMKV flag; reinstall legitimately re-runs the tutorial (intended ONB-08 semantics); same-device re-login with the same `sub` does not re-run. This is the spec.      |
| T-4.6-02  | Spoofing / unsigned-claim trust | `decodeGoogleSubFromJwt` used for the MMKV key | mitigate    | decode-without-verify is acceptable only because the JWT was verified at sign-in and `sub` is a local cache key, never an authz decision; the shared util (plan 04-03) carries this disposition. |
| T-4.6-03  | Information disclosure          | logging the JWT / `sub`                        | mitigate    | the screens MUST NOT log the JWT or `sub` (analytics props per the analytics.ts PII guard — `practice_complete_continued` carries no PII). No log statements with auth material.                 |

</threat_model>

<verification>
- `npm --prefix apps/mobile test -- --run` exits 0 (PracticeIntro/PracticeComplete tests, practiceFlow chain test, 2 visual baselines, and the pre-existing suite green).
- `practice-intro.png` and `practice-complete-static.png` committed.
- OnboardingStack registers `PracticeIntro` + `PracticeComplete`.
</verification>

<success_criteria>

- PracticeIntroScreen (§6 verbatim, Start practice → Recording w/ isPractice:true) + PracticeCompleteScreen (§8 verbatim — confetti + scale-pop + [40,80,40]ms haptic, writes ONB-08 flag, resets to MainTabs) exist and are wired into OnboardingStack.
- The RigTutorial → PracticeIntro → Recording → PracticeComplete → MainTabs chain is tested.
- Analytics events added; 2 visual baselines captured.
- Full mobile test suite green.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-06-SUMMARY.md`
</output>
