---
phase: 05-upload-pipeline-hash-verify-worker-anti-fraud
plan: 06
type: execute
wave: 3
depends_on: ['05-04']
files_modified:
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/ChunkUploader.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/NetworkMonitor.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt
  - apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/ChunkUploaderRetryTest.kt
  - apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadCoordinatorTest.kt
autonomous: false
requirements: [UP-01, UP-03, UP-04, UP-17, UP-19]
must_haves:
  truths:
    - "`ChunkUploader.putPart(client, presignedUrl, file, offset, length): String` streams the byte-range as the PUT body via an OkHttp `RequestBody` over a `RandomAccessFile.seek(offset)` + a 64 KB read loop (NEVER loads a multi-GB MP4 into memory), returns the `ETag` response header (throws if missing or the response isn't 2xx); each `putPart` is wrapped in: retry with the EXACT backoff sequence 2 / 4 / 8 / 16 / 32 / 64 s (six retries) → on the 7th failure mark the PART `FAILED` and the RECORDING `DEAD_LETTER` (`deadLetterReason` recorded); a successful part is recorded `DONE` + its `etag` persisted into the queue row immediately and is NEVER re-PUT (UP-04 — no whole-file restart); and a 30-second no-progress watchdog per part — if no bytes have moved for 30 s, cancel the OkHttp `Call`, close the socket, and retry the part on a FRESH `Call` (a new socket / new TCP handshake) — this is the PRIMARY UP-19 mitigation (the literal TCP_MAXSEG=1280 clamp is best-effort, see below)"
    - "UP-19 literal MSS clamp is best-effort + verified-on-device: a custom `OkHttpClient.socketFactory(...)` whose `createSocket()` returns a `Socket` subclass overriding `connect()` to attempt `android.system.Os.setsockoptInt(getFileDescriptor$(), OsConstants.IPPROTO_TCP, /* TCP_MAXSEG-equivalent */, 1280)` BEFORE `super.connect()`, wrapped in try/catch — if it throws `ErrnoException` or no-ops it's dropped silently and the 30s watchdog carries UP-19 alone; a code comment states this is best-effort and the watchdog is the reliable half (per 05-RESEARCH.md Pitfall 7); the SMOKE runbook gets a step 'verify whether the MSS clamp takes on-device — if it no-ops, drop it' (a manual-only check — it's in VALIDATION.md's Manual-Only table)"
    - "`UploadCoordinator` drains the queue (`UploadQueueStore.read()`): for each non-`VERIFIED`/non-`DEAD_LETTER` row whose `ownerUserId == currentSub` and that isn't paused — (1) `POST /recordings/init` (or, if the row's `qaStatus`-context says it's a re-upload, `POST /recordings/:id/reupload`) with `partsCount = partsCountFor(videoSizeBytes, chunkBytesForNetwork(NetworkMonitor.isCellular()))` decided ONCE here and pinned on the row, getting `partUrls`/`imuPartUrls`/`metadataUrl`/`uploadId`/`imuUploadId`; (2) PUT `metadata.json` to `metadataUrl` (one shot); (3) PUT each video part to its presigned URL and each IMU part to its presigned URL, with a bounded SEMAPHORE of 6 used as 3 video ∥ + 3 IMU ∥ (UP-03; the IMU CSV is tiny → it uses part 1 only, ignoring the surplus part-URLs — Pitfall 2), each via `ChunkUploader.putPart` + the retry/backoff/watchdog above, persisting `{etag, status}` into the queue row after each success (`UploadQueueStore.upsert`), emitting `HumynUploadModule.emitProgress(...)` DEBOUNCED to ≤ once per 5 s; (4) `POST /recordings/:id/finalize` with `{ videoParts: [{partNumber, etag}], imuParts: [...], imuUploadId }`; (5) mark the row `AWAITING_VERIFY` — it stays in the queue until a `verified`/`re-upload` event (Plan 05-08) clears it; on a non-retryable error mark `DEAD_LETTER`; the coordinator runs on the FGS thread (Plan 05-07 owns the FGS lifecycle), NOT the JS/main thread, and re-checks the paused flag (set by `HumynUploadModule.pause()` from `HumynCapture.start()`, UP-10) before each PUT"
    - '`NetworkMonitor` exposes `isCellular(): Boolean` via `ConnectivityManager.getNetworkCapabilities(activeNetwork).hasTransport(TRANSPORT_CELLULAR)` and registers a `registerDefaultNetworkCallback` (event-driven, NOT a timer poll) so a paused upload resumes when connectivity returns; cellular uploads proceed by default — there is NO Wi-Fi-only toggle anywhere (UP-17), the only cellular-specific behavior is the 5 MiB chunk size + the 30 s watchdog/MSS clamp'
    - "`HumynUploadModule` now owns a `UploadCoordinator` instance and a `paused: AtomicBoolean`: `pause()` sets it true + cancels any in-flight `Call`s (but does NOT discard parts/queue rows — they resume); `resume()` clears it + kicks `UploadCoordinator.drain()`; `enqueue()` (from Plan 05-04) now also kicks `drain()`; the coordinator drain runs on the FGS thread via a `startService(Intent(ACTION_SET_UPLOAD_ACTIVE, true))` to `HumynForegroundService` (the FGS's actual `startForeground`-with-`dataSync` + the thread it runs the drain on is Plan 05-07 — this plan just sends the intent + provides the `drain()` logic the FGS thread will call)"
  artifacts:
    - path: 'apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/ChunkUploader.kt'
      provides: 'the OkHttp streaming-PUT-per-part + ETag capture + retry/backoff(2/4/8/16/32/64s)/dead-letter + the 30s no-progress watchdog + the best-effort MSS clamp'
      min_lines: 90
    - path: 'apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt'
      provides: 'the queue drainer: /init→PUT(metadata+parts, 3∥×2∥ semaphore)→/finalize, persisting per-part state, debounced progress, pause-aware'
      min_lines: 90
    - path: 'apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/NetworkMonitor.kt'
      provides: 'isCellular() + the registerDefaultNetworkCallback resume trigger'
      contains: 'TRANSPORT_CELLULAR'
  key_links:
    - from: 'apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt'
      to: 'apps/api POST /recordings/init + PUT presigned URLs + POST /recordings/:id/finalize'
      via: 'the multipart client flow (the Phase-1 backend is the server side)'
      pattern: '/recordings/init'
    - from: 'apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt'
      to: 'apps/mobile/android/.../upload/ChunkUploader.kt'
      via: 'putPart(...) per part inside the 6-permit semaphore'
      pattern: 'putPart'
    - from: 'apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt'
      to: 'apps/mobile/android/.../upload/UploadQueueStore.kt'
      via: 'read()/upsert() — persist {etag,status} after each part; mark DEAD_LETTER / AWAITING_VERIFY'
      pattern: 'UploadQueueStore'
---

<objective>
The `HumynUpload` transfer engine on top of Plan 05-04's queue store + models: `ChunkUploader.kt` (the OkHttp streaming PUT-per-part + ETag capture + the exact 2/4/8/16/32/64 s retry/backoff → dead-letter + the 30 s no-progress abandon-and-retry-with-fresh-socket watchdog + the best-effort `TCP_MAXSEG=1280` clamp via a custom `SocketFactory`), `UploadCoordinator.kt` (the queue drainer: `/init`→PUT(metadata + parts, a 6-permit semaphore = 3 video ∥ + 3 IMU ∥)→`/finalize`, persisting per-part `{etag,status}` after each success, emitting debounced progress, pause-aware), `NetworkMonitor.kt` (`isCellular()` + the event-driven `registerDefaultNetworkCallback` resume trigger), the `HumynUploadModule.kt` wiring (the coordinator instance + the `paused` flag + `pause()`/`resume()`/`enqueue()` kicking `drain()`), and the JUnit scaffolds (`ChunkUploaderRetryTest.kt`, `UploadCoordinatorTest.kt`).

Purpose: UP-01 (the 3-file bundle uploads via S3 multipart with presigned URLs — `HumynUpload` is the client of the Phase-1 backend), UP-03 (3 chunks ∥ per file × 2 files ∥), UP-04 (per-part retry 2/4/8/16/32/64 s → dead-letter, no whole-file restart, a `DONE` part is never re-PUT), UP-17 (cellular by default, no Wi-Fi-only toggle), UP-19 (the 30 s no-progress watchdog as the reliable half + the best-effort MSS clamp). The FGS lifecycle (the actual `startForeground`-with-`dataSync` downgrade, `onTimeout`, the UIDT JobService) is Plan 05-07; the auto-start-on-stop / pause-on-record / logout / Pending-Uploads-UI / reconciliation wiring is Plan 05-08.
Output: `ChunkUploader.kt` + `UploadCoordinator.kt` + `NetworkMonitor.kt` + `HumynUploadModule.kt` + `UploadModels.kt` extensions + 2 JUnit test files.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-CONTEXT.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-PATTERNS.md

<interfaces>
<!-- From Plan 05-04's additions + the codebase. -->

From apps/mobile/android/.../upload/UploadModels.kt (Plan 05-04):

```kotlin
enum class UploadState { PENDING, UPLOADING, FINALIZING, AWAITING_VERIFY, VERIFIED, DEAD_LETTER }
enum class PartStatus { PENDING, DONE, FAILED }
data class PartState(val n: Int, var status: PartStatus = PartStatus.PENDING, var etag: String? = null, var retryCount: Int = 0)
data class UploadRow(recordingId, ownerUserId, mp4Path, csvPath, jsonPath, taskId, isPractice, var state, var uploadId, var imuUploadId, var partsCount, var chunkBytes, val videoParts, val imuParts, var metadataPut, val enqueuedAt, var lastProgressAt, var deadLetterReason)
const val WIFI_CHUNK_BYTES = 8L*1024*1024 ; const val CELLULAR_CHUNK_BYTES = 5L*1024*1024  // NOT 2 MiB — S3 5-MiB minimum
fun chunkBytesForNetwork(isCellular: Boolean): Long
fun partsCountFor(videoSizeBytes: Long, chunkBytes: Long): Int  // ceil, >=1
```

From apps/mobile/android/.../upload/UploadQueueStore.kt (Plan 05-04):

```kotlin
class UploadQueueStore(context: Context) {
  fun read(): MutableList<UploadRow>
  fun upsert(row: UploadRow)         // replace by recordingId, atomic write
  fun remove(recordingId: String)
  fun bootstrap(currentSub: String?): List<UploadRow>   // own-rows only, drops VERIFIED-with-no-files
  fun markVerifiedAndDeleteLocal(recordingId: String)
}
```

From apps/mobile/android/.../upload/HumynUploadModule.kt (Plan 05-04 — this plan extends it):

```kotlin
@ReactModule(name = HumynUploadModule.NAME) class HumynUploadModule(reactContext) : ReactContextBaseJavaModule(reactContext) {
  companion object { const val NAME = "HumynUpload" }
  private val bgExecutor = Executors.newSingleThreadExecutor()
  private val queueStore = UploadQueueStore(reactContext.applicationContext)
  @ReactMethod fun enqueue(...)  // Plan 05-06: also kicks UploadCoordinator.drain()
  @ReactMethod fun pause(promise) // Plan 05-06: paused.set(true) + cancel in-flight Calls
  @ReactMethod fun resume(promise)// Plan 05-06: paused.set(false) + UploadCoordinator.drain()
  @ReactMethod fun getQueue(promise) ; @ReactMethod fun clearVerified(recordingIds, promise)
  fun emitQueueChanged() ; fun emitProgress(recordingId, bytesUploaded, bytesTotal)  // debounce ≤1/5s
  override fun invalidate() { bgExecutor.shutdownNow(); super.invalidate() }
}
// HumynForegroundService.ACTION_SET_UPLOAD_ACTIVE / EXTRA_UPLOAD_ACTIVE — the existing seam (Plan 05-07 wires the actual startForeground)
```

From apps/mobile/android/.../updater/HumynUpdaterModule.kt#downloadAndVerifyApk (the streaming-over-file precedent — transport differs: ChunkUploader uses OkHttp, that uses HttpURLConnection):

```kotlin
// background executor; HttpURLConnection; 64 KB buffer read loop; MessageDigest.update(buf,0,n) in the loop
// → ChunkUploader: OkHttp RequestBody.writeTo(sink) over RandomAccessFile.seek(offset) + 64 KB loop; do NOT re-hash (the SHAs are already in metadata.json from HashStreamer.kt at capture time)
```

From apps/mobile/.../updater/HumynUpdaterModule.kt#launchInstaller (the startActivity-from-a-module + try/catch pattern — same shape Plan 05-07's BatteryOptimizationHelper uses):

```kotlin
val intent = Intent(...).addFlags(Intent.FLAG_ACTIVITY_NEW_TASK); try { reactApplicationContext.startActivity(intent) } catch (_: Exception) {}
```

The Phase-1 backend the coordinator calls (already shipped):

```
POST /recordings/init   → { partUrls[], imuPartUrls[], metadataUrl, uploadId, imuUploadId, expiresAt }   (15-min presign TTL)
PUT  <presignedUrl>     → 200 + ETag header (per part / per metadata.json)
POST /recordings/:id/parts/:n/complete  → optional state probe ("is the row still pending?")
POST /recordings/:id/finalize  → { videoParts:[{partNumber,etag}], imuParts:[...], imuUploadId } → server CompleteMultipartUpload + flips pending→uploaded + inserts recordings_to_verify
POST /recordings/:id/reupload  → same shape as /init (Plan 05-05 — for a hash-mismatch row)
// the authenticated bearer token comes from the app's auth store / Keychain — the coordinator must attach it to /init,/finalize,/reupload (NOT to the direct S3 PUTs — those are presigned)
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChunkUploader.kt — streaming PUT-per-part + ETag + retry/backoff/dead-letter + 30s watchdog + best-effort MSS clamp</name>
  <behavior>
    - `putPart` over a `RandomAccessFile`-ranged body: a 2xx response → returns the `ETag` header; the response body is streamed from the file, never the whole file in memory
    - retry: a part that fails (IOException / non-2xx) is retried with delays 2/4/8/16/32/64 s (six retries); the 7th failure → throws a `DeadLetterException` (or returns a sentinel) so the coordinator marks the recording `DEAD_LETTER` with `deadLetterReason`
    - no re-PUT of a done part: if a part already has `status == DONE` + an `etag`, `uploadPart` is a no-op (returns the cached etag)
    - watchdog: if a part transfer makes no progress (no `writeTo` bytes) for 30 s, the `Call` is cancelled and the part retried on a fresh `Call`; the retry counter still advances (so a permanently-stalled part still dead-letters after the backoff budget)
    - MSS clamp: the custom `SocketFactory` is installed on the OkHttp client; `createSocket()` returns a `Socket` whose `connect()` attempts the `setsockoptInt(...TCP_MAXSEG...,1280)` in a try/catch and proceeds regardless
  </behavior>
  <read_first>
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/updater/HumynUpdaterModule.kt — `downloadAndVerifyApk` (the streaming-over-file + 64 KB buffer + background-executor shape) — note: it uses `HttpURLConnection`; ChunkUploader uses OkHttp `RequestBody`
    - apps/mobile/android/app/build.gradle (or libs.versions.toml) — confirm OkHttp is available transitively (RN bundles it) so no new dep is needed; if a direct `implementation("com.squareup.okhttp3:okhttp")` is wanted, match the RN-bundled version
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt — `PartState`, `PartStatus`
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-RESEARCH.md — Code Examples § "Android: streaming chunk PUT + ETag capture" + Pitfall 7 (the TCP_MAXSEG-from-Kotlin reachability analysis + the watchdog-is-the-real-deliverable recommendation + the SocketFactory sketch) + Pitfall 2 (cellular chunk size)
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-PATTERNS.md — § ChunkUploader.kt
    - apps/mobile/android/app/src/test/java/ai/humynlabs/capture/capture/CaptureLaunchSweepTest.kt — the JUnit/temp-file conventions for `ChunkUploaderRetryTest.kt`
  </read_first>
  <action>
1. Create `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/ChunkUploader.kt`:
   - A `class ChunkUploader(private val baseClient: OkHttpClient)` (the coordinator passes in a shared client built with the custom `SocketFactory` — see step 2).
   - `private fun rangedBody(file: File, offset: Long, length: Long): RequestBody = object : RequestBody() { override fun contentType(): MediaType? = "application/octet-stream".toMediaTypeOrNull(); override fun contentLength(): Long = length; override fun writeTo(sink: BufferedSink) { RandomAccessFile(file, "r").use { raf -> raf.seek(offset); val buf = ByteArray(64 * 1024); var remaining = length; while (remaining > 0) { val n = raf.read(buf, 0, minOf(buf.size.toLong(), remaining).toInt()); if (n <= 0) break; sink.write(buf, 0, n); remaining -= n; onProgress(n.toLong()) /* feed the watchdog + the coordinator's debounced emitProgress */ } } } }`
   - `fun putPart(presignedUrl: String, file: File, offset: Long, length: Long, onProgress: (Long) -> Unit): String` — builds `Request.Builder().url(presignedUrl).put(rangedBody(...))`, executes `baseClient.newCall(req)` with a per-call **30 s no-progress watchdog**: a `ScheduledExecutorService` (or a `Handler`) that, if `onProgress` hasn't fired in 30 s, calls `call.cancel()`; on `resp` — `if (!resp.isSuccessful) throw IOException("part PUT ${resp.code}")`; `return resp.header("ETag") ?: throw IOException("no ETag on part response")`. Wrap the whole `putPart` in retry: `val delays = longArrayOf(2_000, 4_000, 8_000, 16_000, 32_000, 64_000); for (attempt in 0..delays.size) { try { return putPartOnce(...) } catch (e: Exception) { if (attempt == delays.size) throw DeadLetterException("part $n failed after 6 retries: ${e.message}", e); Thread.sleep(delays[attempt]) /* on a watchdog-cancel, the next iteration gets a FRESH Call → new socket → possibly new MSS negotiation */ } }`. Define `class DeadLetterException(msg: String, cause: Throwable?) : Exception(msg, cause)`.
   - `fun uploadPart(row: UploadRow, parts: MutableList<PartState>, n: Int, file: File, offset: Long, length: Long, presignedUrl: String, onProgress: (Long) -> Unit): String` — if `parts[n-1].status == PartStatus.DONE && parts[n-1].etag != null` → return the cached etag (NEVER re-PUT — UP-04); else `parts[n-1].retryCount++` per attempt, on success `parts[n-1].status = DONE; parts[n-1].etag = etag`, on `DeadLetterException` `parts[n-1].status = FAILED` and rethrow (the coordinator catches it → marks the row `DEAD_LETTER`).
2. The best-effort MSS clamp (UP-19, Pitfall 7): in `ChunkUploader.kt` (or a small `MssSocketFactory.kt` next to it) define `class MssSocketFactory(private val delegate: SocketFactory = SocketFactory.getDefault()) : SocketFactory() { override fun createSocket(): Socket = ClampedSocket(); /* override the other createSocket overloads to delegate + wrap */ }` and `private class ClampedSocket : Socket() { override fun connect(endpoint: SocketAddress, timeout: Int) { try { val fd = /* reflect getFileDescriptor$() or use Os.getsockoptInt path */; android.system.Os.setsockoptInt(fd, android.system.OsConstants.IPPROTO_TCP, /* TCP_MAXSEG: OsConstants doesn't expose it directly — use the numeric value 2 (IPPROTO_TCP TCP_MAXSEG on Linux) or skip if unavailable */ 2, 1280) } catch (_: Throwable) { /* best-effort — drop silently; the 30s watchdog is the reliable UP-19 mitigation */ }; super.connect(endpoint, timeout) } }`. Add a header comment: "UP-19 has two halves: (a) the 30 s no-progress abandon-and-retry-with-fresh-socket watchdog above — the RELIABLE, portable mitigation that actually defeats the Jio CGNAT / Vivo Brasil MTU-blackhole stall; (b) this MSS clamp — BEST-EFFORT only: java.net.Socket exposes no MSS setter and `setsockopt(TCP_MAXSEG)` on a client socket is pre-`connect()`-only + advisory on some kernels, so this may throw or no-op. If it no-ops on-device, drop it — (a) carries UP-19 alone. (per 05-RESEARCH.md Pitfall 7)." Build the shared `OkHttpClient` (in `UploadCoordinator` or a factory) with `.socketFactory(MssSocketFactory()).connectTimeout(30,SECONDS).readTimeout(0,SECONDS)/* the watchdog handles stalls */.callTimeout(0,SECONDS)`.
3. Create `apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/ChunkUploaderRetryTest.kt` — use OkHttp's `MockWebServer` (it's a standard test dep; add to `testImplementation` if not present): a part that the mock 500s 6 times then 200s → assert `putPart` retries 6 times with the right delays (use a controllable clock or just assert the call count, not real sleeps — inject the delay sequence so the test runs fast) and finally returns the ETag; a part that 500s 7 times → assert a `DeadLetterException`; a part whose response stalls with no body for >30 s → assert the `Call` is cancelled and a fresh `Call` retried (drive the watchdog with a small/injectable timeout in the test); a part already `DONE` with an `etag` → assert `uploadPart` returns the cached etag without a new request. Keep the test fast (inject short delays/timeouts; no real 2/4/8s sleeps).
  </action>
  <verify>
    <automated>cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests 'ai.humynlabs.capture.upload.ChunkUploaderRetryTest'</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/ChunkUploader.kt` exists (≥90 lines), uses an OkHttp `RequestBody` over `RandomAccessFile.seek` + a 64 KB loop (grep `RandomAccessFile` + `64 * 1024`), captures the `ETag` header, has the exact backoff sequence `2_000, 4_000, 8_000, 16_000, 32_000, 64_000` (grep), throws a `DeadLetterException` on the 7th failure, never re-PUTs a `DONE` part, and has a 30 s no-progress watchdog cancelling the `Call`
    - the file (or `MssSocketFactory.kt`) has the best-effort MSS-clamp `SocketFactory` with a comment stating it's best-effort and the watchdog is the reliable half
    - `apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/ChunkUploaderRetryTest.kt` covers: 6-retries-then-success, 7-failures→DeadLetterException, watchdog-cancel-then-fresh-retry, DONE-part-not-re-PUT
    - `./gradlew :app:testApkRolloutDebugUnitTest --tests 'ai.humynlabs.capture.upload.ChunkUploaderRetryTest'` exits 0
  </acceptance_criteria>
  <done>ChunkUploader streams parts, captures ETags, retries 2/4/8/16/32/64s→dead-letter, never re-PUTs a done part, has the 30s watchdog + the best-effort MSS clamp; ChunkUploaderRetryTest green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NetworkMonitor.kt + UploadCoordinator.kt + HumynUploadModule wiring</name>
  <behavior>
    - `NetworkMonitor.isCellular()` → true when the active network has `TRANSPORT_CELLULAR` and not `TRANSPORT_WIFI`; the default-network callback fires `onResume` when connectivity returns after a loss
    - `UploadCoordinator.drain()`: a `PENDING` row → `/init` called once with `partsCount = partsCountFor(videoSize, chunkBytesForNetwork(isCellular))`, the chunk size pinned on the row; `metadata.json` PUT; each video part PUT (≤3 concurrent) + the 1 IMU part PUT; per-part `{etag,status}` persisted as it goes; `/finalize` with the collected ETags; row → `AWAITING_VERIFY`, still in the queue
    - a row already `AWAITING_VERIFY` or `VERIFIED` or `DEAD_LETTER` is skipped by `drain()`
    - a row whose `ownerUserId != currentSub` is skipped (UP-13 — coordinator only drains own-rows)
    - the paused flag (set by `pause()`) short-circuits `drain()` before each PUT
    - a `DeadLetterException` from any part → the row's `state = DEAD_LETTER`, `deadLetterReason` set, `emitQueueChanged()` (so the Pending-Uploads UI shows `chip-failed`)
  </behavior>
  <read_first>
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadQueueStore.kt (Plan 05-04) — `read`/`upsert`/`remove`/`bootstrap`
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt (Plan 05-04) — `UploadRow`, `chunkBytesForNetwork`, `partsCountFor`, the enums
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt (Plan 05-04) — the stubbed `enqueue`/`pause`/`resume`/`getQueue`/`clearVerified` + `emitQueueChanged`/`emitProgress` + the `bgExecutor`
    - apps/mobile/src/services/api.ts (or wherever the authenticated API client + the bearer-token source lives) — how to call `/recordings/init` etc. with the JWT; the coordinator runs in Kotlin so it needs the token from the native side — check whether the JWT is in Keychain (`react-native-keychain`) reachable from Kotlin, or whether it must be passed across the bridge on `enqueue()` (RECOMMEND: pass the bearer token to `enqueue()` as an arg and store it on the row / a coordinator field, refreshed on `resume()`; OR have the coordinator post `/recordings/init` via an authenticated request whose token the JS side injects — pick whichever fits the existing auth plumbing; document the choice)
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/updater/HumynUpdaterModule.kt — the `Executors.newSingleThreadExecutor()` background-work pattern; `HumynForegroundService` constants (`ACTION_SET_UPLOAD_ACTIVE`, `EXTRA_UPLOAD_ACTIVE`)
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-RESEARCH.md — Pattern 1 (the full client flow), Pitfall 2 (partsCount pinned once; the IMU uses part 1 only), the "debounce progress to ≤1/5s" anti-pattern
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-PATTERNS.md — § UploadCoordinator.kt + § "Background work off the JS/main thread"
  </read_first>
  <action>
1. Create `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/NetworkMonitor.kt`: `class NetworkMonitor(private val context: Context, private val onConnectivityRegained: () -> Unit) { private val cm = context.getSystemService(Context.CONNECTIVITY_SERVICE) as ConnectivityManager; fun isCellular(): Boolean { val net = cm.activeNetwork ?: return false; val caps = cm.getNetworkCapabilities(net) ?: return false; return caps.hasTransport(NetworkCapabilities.TRANSPORT_CELLULAR) && !caps.hasTransport(NetworkCapabilities.TRANSPORT_WIFI) }; fun hasNetwork(): Boolean { ... }; private val callback = object : ConnectivityManager.NetworkCallback() { override fun onAvailable(network: Network) { onConnectivityRegained() } }; fun register() { cm.registerDefaultNetworkCallback(callback) }; fun unregister() { try { cm.unregisterNetworkCallback(callback) } catch (_: Throwable) {} } }` — event-driven, NO timer poll (anti-pattern). NOTE: there is intentionally no Wi-Fi-only gate — `isCellular()` only changes the chunk size; cellular uploads always proceed (UP-17).
2. Create `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt`:
   - `class UploadCoordinator(private val context: Context, private val queueStore: UploadQueueStore, private val networkMonitor: NetworkMonitor, private val emitProgress: (String, Long, Long) -> Unit, private val emitQueueChanged: () -> Unit, private val getBearerToken: () -> String?, private val getCurrentSub: () -> String?, private val isPaused: () -> Boolean)`.
   - `private val httpClient = OkHttpClient.Builder().socketFactory(MssSocketFactory()).connectTimeout(30, TimeUnit.SECONDS).readTimeout(0, TimeUnit.SECONDS).callTimeout(0, TimeUnit.SECONDS).build()` ; `private val chunkUploader = ChunkUploader(httpClient)` ; `private val partSemaphore = Semaphore(6)` (used as 3 video + 3 IMU — the IMU has 1 part so effectively 3 video + 1 IMU at a time) ; `private val drainExecutor = Executors.newSingleThreadExecutor()` for serializing `drain()` calls ; `private val partExecutor = Executors.newFixedThreadPool(6)` for the parallel PUTs.
   - `fun drain()` — `drainExecutor.execute { if (isPaused()) return@execute; val currentSub = getCurrentSub() ?: return@execute; if (!networkMonitor.hasNetwork()) return@execute; for (row in queueStore.read()) { if (isPaused()) break; if (row.ownerUserId != currentSub) continue; if (row.state == UploadState.AWAITING_VERIFY || row.state == UploadState.VERIFIED || row.state == UploadState.DEAD_LETTER) continue; try { uploadOne(row) } catch (e: DeadLetterException) { row.state = UploadState.DEAD_LETTER; row.deadLetterReason = e.message; queueStore.upsert(row); emitQueueChanged() } catch (e: Exception) { Log.w(TAG, "upload row ${row.recordingId} failed transiently: ${e.message}") /* leave it PENDING/UPLOADING — next drain retries */ } } }`.
   - `private fun uploadOne(row: UploadRow)` — the Pattern-1 flow:
     a. If `row.uploadId == null` (not yet `/init`-ed): `val isCellular = networkMonitor.isCellular(); val chunkBytes = chunkBytesForNetwork(isCellular); val videoSize = File(row.mp4Path).length(); val partsCount = partsCountFor(videoSize, chunkBytes); row.chunkBytes = chunkBytes; row.partsCount = partsCount;` — POST `/recordings/init` (Bearer `getBearerToken()`) with the body the Phase-1 backend expects (`{ recordingId, taskId: row.taskId, practice: row.isPractice, partsCount, durationMs, fileSha256, imuSha256, fileSizeBytes, imuSizeBytes, capturedAt }` — read those from `row.jsonPath`'s metadata JSON via `org.json` since they were produced by `MetadataComposer`) — parse the response into `partUrls[]`, `imuPartUrls[]`, `metadataUrl`, `uploadId`, `imuUploadId`, `expiresAt`; `row.uploadId = uploadId; row.imuUploadId = imuUploadId; row.videoParts = (1..partsCount).map { PartState(it) }.toMutableList(); row.imuParts = mutableListOf(PartState(1));` `row.state = UploadState.UPLOADING; queueStore.upsert(row);` — keep the `partUrls`/`imuPartUrls`/`metadataUrl` in memory for this drain (presigns expire in 15 min — if a presign 403s mid-upload, re-`/init` per Pitfall — but keep it simple: on a 403 throw a transient exception and let the next `drain()` re-`/init`). (NOTE: a row that's being re-uploaded after a `hash-mismatch` event — Plan 05-08 sets a flag/state — calls `/recordings/:id/reupload` instead of `/init`; gate on that.)
     b. `metadata.json`: if `row.metadataPut != DONE` → `PUT metadataUrl` with `rangedBody(File(row.jsonPath), 0, File(row.jsonPath).length())` (one shot) → `row.metadataPut = DONE; queueStore.upsert(row)`.
     c. Video parts: for each `PartState p` in `row.videoParts` where `p.status != DONE` — `partExecutor.submit { partSemaphore.acquire(); try { val offset = (p.n - 1L) * row.chunkBytes!!; val length = minOf(row.chunkBytes!!, File(row.mp4Path).length() - offset); val etag = chunkUploader.uploadPart(row, row.videoParts, p.n, File(row.mp4Path), offset, length, partUrls[p.n-1]) { bytes -> /* feed the debounced progress */ maybeEmitProgress(row) }; queueStore.upsert(row) } finally { partSemaphore.release() } }` — collect the futures; the semaphore caps total concurrency at 6 (≈3 video + the IMU). IMU part: same, one part (`row.imuParts[0]`, offset 0, length = whole CSV — it's tiny → 1 part regardless; ignore the surplus `imuPartUrls`). Wait for all futures; if any threw a `DeadLetterException`, rethrow it (→ the row dead-letters).
     d. `POST /recordings/:id/finalize` (Bearer) with `{ videoParts: row.videoParts.map { { partNumber: it.n, etag: it.etag!! } }, imuParts: row.imuParts.map { ... }, imuUploadId: row.imuUploadId!! }` → on success `row.state = UploadState.AWAITING_VERIFY; queueStore.upsert(row); emitQueueChanged()` — the row STAYS in the queue (it's deleted only on a `verified` event / the reconciliation sweep — Plan 05-08; UP-14).
   - `private fun maybeEmitProgress(row: UploadRow)` — debounce: only call `emitProgress(row.recordingId, doneBytes, totalBytes)` if ≥5 s since the last emit for this row (track a per-row `lastEmitMs`). Also bump `row.lastProgressAt = System.currentTimeMillis()`.
   - `companion object { private const val TAG = "HumynUploadCoord" }` ; a `fun shutdown()` closing the executors (called from `HumynUploadModule.invalidate()`).
3. `HumynUploadModule.kt` wiring (extend Plan 05-04's stub): add `private val paused = AtomicBoolean(false)` ; `private val networkMonitor by lazy { NetworkMonitor(reactContext.applicationContext, ::onConnectivityRegained) }` ; `private val coordinator by lazy { UploadCoordinator(reactContext.applicationContext, queueStore, networkMonitor, ::emitProgress, ::emitQueueChanged, ::currentBearerToken, ::currentSub, paused::get) }` ; `init { networkMonitor.register() }`. `enqueue(...)` now also `coordinator.drain()` after `queueStore.enqueue(row)` AND sends `Intent(reactApplicationContext, HumynForegroundService::class.java).setAction(HumynForegroundService.ACTION_SET_UPLOAD_ACTIVE).putExtra(HumynForegroundService.EXTRA_UPLOAD_ACTIVE, true)` via `reactApplicationContext.startService(...)` (the FGS's actual `startForeground`-with-`dataSync` is Plan 05-07 — this just signals it). `pause()` → `paused.set(true)` (the coordinator's per-PUT check stops new PUTs; in-flight `Call`s finish or are cancelled — cancel them by tracking the active `Call`s in the coordinator and adding a `coordinator.cancelInflight()`); `resume()` → `paused.set(false); coordinator.drain()`. `private fun onConnectivityRegained() { if (!paused.get()) coordinator.drain() }`. `private fun currentBearerToken(): String?` — read from Keychain or the bridge-passed token (whichever the auth plumbing uses — document). `private fun currentSub(): String?` — likewise (the `ownerUserId` on each row is the source of truth; `currentSub` is "who's signed in now" — if no one, `null`). `invalidate()` → also `coordinator.shutdown(); networkMonitor.unregister()`.
4. Create `apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadCoordinatorTest.kt` — with `MockWebServer` standing in for the API + S3: enqueue a row, drain → assert `/recordings/init` was called once, `metadata.json` PUT, the right number of video-part PUTs + 1 IMU PUT, `/recordings/:id/finalize` called with the collected ETags, the row ends `AWAITING_VERIFY` and is still in the queue store; a row whose `ownerUserId != currentSub` → `drain` skips it (no requests); the paused flag set → `drain` returns without PUTs; a part that the mock makes dead-letter → the row ends `DEAD_LETTER` with `deadLetterReason` and `emitQueueChanged` fired; `isCellular()` true → `partsCountFor` uses 5 MiB chunks. Keep it fast (inject short retry delays).
  </action>
  <verify>
    <automated>cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests 'ai.humynlabs.capture.upload.UploadCoordinatorTest' --tests 'ai.humynlabs.capture.upload.NetworkMonitorTest'</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/NetworkMonitor.kt` exists, uses `ConnectivityManager.getNetworkCapabilities(...).hasTransport(NetworkCapabilities.TRANSPORT_CELLULAR)` and `registerDefaultNetworkCallback` (grep — no `Handler.postDelayed`/timer poll), has no Wi-Fi-only gate
    - `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt` exists (≥90 lines), runs the `/init`→PUT(metadata+parts)→`/finalize` flow with a `Semaphore(6)`, persists per-part `{etag,status}` via `queueStore.upsert`, debounces `emitProgress` to ≤1/5s, checks `isPaused()` before each PUT, skips rows where `ownerUserId != currentSub`, and on a `DeadLetterException` sets `state = DEAD_LETTER` + `emitQueueChanged()`
    - `HumynUploadModule.kt` now has a `coordinator` + a `paused: AtomicBoolean`; `enqueue` kicks `drain()` + sends `ACTION_SET_UPLOAD_ACTIVE` true; `pause`/`resume` flip the flag; `invalidate` shuts down the coordinator + network monitor
    - `apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadCoordinatorTest.kt` covers: full flow → AWAITING_VERIFY-still-queued, owner-pin skip, paused short-circuit, dead-letter → DEAD_LETTER + emitQueueChanged, cellular chunk-size
    - `./gradlew :app:testApkRolloutDebugUnitTest --tests 'ai.humynlabs.capture.upload.UploadCoordinatorTest' --tests 'ai.humynlabs.capture.upload.NetworkMonitorTest'` exits 0
  </acceptance_criteria>
  <done>The queue drainer runs the multipart flow with bounded concurrency, persists per-part state, debounces progress, is pause/owner-aware, dead-letters cleanly; the module wires it; UploadCoordinatorTest + NetworkMonitorTest green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                 | Description                                                                                   |
| -------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| app → apps/api (`/init`, `/finalize`, `/reupload`)       | the coordinator attaches the user's bearer JWT; the API authz-checks ownership                |
| app → S3 (direct PUTs)                                   | presigned URLs (SigV4, 15-min TTL) — the bytes are the raw file, byte-for-byte (no re-encode) |
| device FS (queue store + recording files) ↔ coordinator | reads the local triple as PUT bodies; writes per-part state — app-private storage only        |
| network type detection                                   | `ConnectivityManager` — read-only, no input                                                   |

## STRIDE Threat Register

| Threat ID | Category                          | Component                                                                                    | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                           |
| --------- | --------------------------------- | -------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-5-06-01 | Tampering                         | re-encoding / re-muxing the MP4 before PUT                                                   | mitigate    | The PUT body is `rangedBody(File(mp4Path), offset, length)` — raw file bytes, no transcode/re-mux/strip (CLAUDE.md "Files never re-encoded"). The coordinator does NOT re-hash (the SHA-256s are already in `metadata.json` from `HashStreamer.kt` at capture time).                                                                                                      |
| T-5-06-02 | Information disclosure            | presigned URLs leaking into logcat / crash reports → S3 write access                         | mitigate    | Never `Log.*` the presigned URLs; log `recordingId` (ULID) + part numbers only (mirror `s3-client.ts`/`init.ts`'s posture); 15-min TTL bounds the window.                                                                                                                                                                                                                 |
| T-5-06-03 | Elevation of privilege / Spoofing | the coordinator draining a row under the wrong account's JWT on a shared phone               | mitigate    | `drain()` skips any row where `row.ownerUserId != getCurrentSub()`; `/init`/`/finalize`/`/reupload` are called with the _current_ signed-in user's bearer — a row owned by a logged-out user just waits (UP-13 / Pitfall 8).                                                                                                                                              |
| T-5-06-04 | Denial of Service                 | a 4 GB MP4 OOMing the device on PUT                                                          | mitigate    | `rangedBody`'s `writeTo` streams a 64 KB buffer over `RandomAccessFile.seek` — never `readBytes()`/loads the whole file; only `chunkBytes` (5–8 MiB) of range is touched per PUT, and even that is streamed not buffered.                                                                                                                                                 |
| T-5-06-05 | DoS                               | a stalled connection (Jio CGNAT / Vivo Brasil MTU blackhole) hanging the upload indefinitely | mitigate    | The 30 s no-progress watchdog cancels the `Call` and retries on a fresh socket (the reliable UP-19 mitigation); `readTimeout(0)`/`callTimeout(0)` on the client defers stall-handling to the watchdog (a fixed `readTimeout` would kill a slow-but-progressing transfer); the backoff budget (2/4/8/16/32/64 s) bounds total retry time → dead-letter, not infinite hang. |
| T-5-06-06 | Tampering                         | a malicious local `mp4Path`/`jsonPath` (path traversal)                                      | mitigate    | The paths come from the queue row, which `UploadQueueStore.enqueue()` (Plan 05-04) only accepts from `HumynCapture`'s finalize path under `filesDir/recordings/` (and refuses `files/practice/`) — never a server- or IPC-supplied path.                                                                                                                                  |

</threat_model>

<verification>
- `cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests 'ai.humynlabs.capture.upload.*'` exits 0 (ChunkUploaderRetryTest + UploadCoordinatorTest + NetworkMonitorTest + the Plan-05-04 UploadQueueStoreTest)
- `cd apps/mobile/android && ./gradlew :app:assembleApkRolloutDebug -x lint` exits 0
- `grep -rn 'Log\.' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/ChunkUploader.kt apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt | grep -i 'presigned\|http' ` returns nothing (no URL logging)
- `grep -rn '2_000, 4_000, 8_000, 16_000, 32_000, 64_000\|2_000L\|longArrayOf(2' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/ChunkUploader.kt` returns the backoff sequence
</verification>

<success_criteria>

- `ChunkUploader.kt` — streaming PUT + ETag + 2/4/8/16/32/64s retry→dead-letter + no re-PUT of done parts + 30s watchdog + best-effort MSS clamp; ChunkUploaderRetryTest green
- `UploadCoordinator.kt` — /init→PUT(metadata+parts, 6-permit semaphore = 3∥×2∥)→/finalize, per-part-state persistence, debounced progress, pause/owner-aware, dead-letter handling; UploadCoordinatorTest green
- `NetworkMonitor.kt` — isCellular() + event-driven resume; no Wi-Fi-only gate; NetworkMonitorTest green
- `HumynUploadModule.kt` wires the coordinator + paused flag + the ACTION_SET_UPLOAD_ACTIVE signal; APK assembles
  </success_criteria>

<output>
After completion, create `.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-06-SUMMARY.md`
</output>
