---
phase: 05-upload-pipeline-hash-verify-worker-anti-fraud
plan: 14
type: execute
wave: "1.5"
gap_closure: true
depends_on: []
files_modified:
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadQueueStore.kt
  - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt
  - apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadCoordinatorTest.kt
  - apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadQueueStoreTest.kt
  - apps/mobile/src/native/HumynUpload.ts
  - apps/mobile/src/services/uploadReconcile.ts
  - apps/mobile/src/components/Toast.tsx
  - apps/mobile/src/screens/recording/RecordingScreen.tsx
  - apps/mobile/src/screens/recording/useRecordingLifecycle.ts
  - apps/mobile/src/state/uploadToastBus.ts
  - apps/mobile/App.tsx
  - apps/mobile/src/screens/home/HomeSkeletonScreen.tsx
  - apps/mobile/src/screens/uploads/PendingUploadsScreen.tsx
  - apps/mobile/__tests__/services/uploadReconcile.test.tsx
  - apps/mobile/__tests__/screens/PendingUploadsScreen.test.tsx
  - apps/mobile/__tests__/screens/HomeSkeletonScreen.test.tsx
  - apps/api/src/plugins/logger.ts
  - apps/api/scripts/dev.sh
  - .planning/runbooks/05-upload-smoke.md
autonomous: true
requirements: [UP-01, UP-04, UP-10, UP-12, UP-13, UP-14, UP-15, UP-16, VERIFY-05, VERIFY-06]
must_haves:
  truths:
    - "Every API POST the upload coordinator makes carries its OWN per-route `Idempotency-Key` UUIDv4 — `/recordings/init` uses `row.initIdempotencyKey`, `/recordings/:id/parts` uses `row.partsIdempotencyKey`, `/recordings/:id/finalize` uses `row.finalizeIdempotencyKey`, `/recordings/:id/reupload` uses `row.reuploadIdempotencyKey`. Each is minted ONCE at row construction (UUIDv4), persisted to `queue.json` (toJson/fromJson), reused only across retries of ITS OWN route. The cross-route 409 (`/init` succeeds → `/finalize` with the same key + different body → 409 → DEAD_LETTER) seen on 2026-05-13 (recording `01KRFZ91Y3E315AJVG75KXJZE6`) cannot recur."
    - "`UploadRow.fromJson` migration: a legacy on-disk row missing ANY of the four per-route key fields (including a row written by commit `5c0b2d8`'s single-key shape) is migrated by minting UUIDv4(s) AND persisting them back to disk in-place via `UploadQueueStore.upsert(row)`, so a subsequent `read()` returns the SAME key (not a fresh-minted one). The per-`read()`-mints-fresh-UUID storm + the process-kill-between-/init-and-/parts edge are closed."
    - "`HumynUpload.reupload(recordingId)` distinguishes two dead-letter paths: a client-side `DEAD_LETTER` (server-side row is still `qa_status='pending'` with a valid `s3UploadId`) → LOCAL reset (`row.state = UPLOADING`, `row.deadLetterReason = null`, keep `uploadId`/`imuUploadId`/`videoParts`/`imuParts`/`metadataPut` UNCHANGED), `coordinator.drain()` — the drainer takes the `row.uploadId != null → postRePresign` branch (`/parts`), NOT `/reupload`. A worker-fired re-upload (server row in `qa_status='hash-mismatch'`) → existing full reset path (`row.reupload = true`, state PENDING, clear uploadId/imuUploadId/parts/etags, drain → `/reupload`). A VERIFIED row → no-op + resolve."
    - "Cold-start with a stale-on-disk queue auto-drains: `installUploadReconcile()`'s initial `reconcileOnce` calls `HumynUpload.drainNow()` (a new @ReactMethod bridge) if `getQueueSafe()` returns any row in {PENDING, UPLOADING}. No manual screen-bounce / enqueue / JWT change / Retry tap required."
    - "Live upload progress is visible on BOTH the Home tile (`pending-uploads-tile-row`) AND the standalone Pending Uploads screen (`pending-upload-row`): the chip text shows `\"Uploading… {N}%\"` (UploadStatusChip already supports the `percent` prop) AND a sibling determinate progress bar across the row card renders an `<View style={{width: pct%}}>` filled with `colors.chipProgressText` over a `colors.line` track (token-aligned, no new design tokens — D-10/D-10a). `onUploadProgress` cadence on-device is the 5s debounce already implemented in `maybeEmitProgress` — Item 4's deliverable is wiring the bar + verifying the chip percent renders on Home as well as Pending Uploads (it already renders on Pending Uploads via UploadStatusChip.percent)."
    - "The post-recording contribution toast (`\"{Hh Mm} added to your contribution.\"`) survives the RecordingScreen → Home transition for ≥5s. The Toast component is already `<ToastHost />`-as-navigator-sibling in `App.tsx:78` (it ALREADY survives screen transitions structurally — confirmed). The fix: pass through a 5-second `durationMs` argument when the contribution toast is shown, and route the call so the toast fires AFTER `navigation.navigate('Home')` resolves (use a single-shot `uploadToastBus` set right before `navigateToHome` + drained on Home mount; OR pass `5_000` as the `showToast` duration so the existing global ToastHost holds it through the transition). The chosen pattern: `uploadToastBus` (mirror `bootRecoveryListener`'s deliver-once-on-Home pattern) — set the pending message + duration before `navigateToHome`, drain it on Home mount via a one-shot effect that calls the global `showToast(text, 5_000)`."
    - "Home pending-uploads-row tap (D-10 — currently `navigation.navigate('PendingUploads')` opening an orphan screen with no back nav) routes instead to the History tab via `navigation.navigate('MainTabs', { screen: 'History' })`. The standalone `PendingUploads` screen route in `RootNativeStack.tsx` STAYS registered (for direct-link / deep-link use only — `humyn://pending-uploads` if/when added); the user-facing entry from Home tile is the History tab."
    - "`apps/api/src/plugins/logger.ts` in dev (NODE_ENV !== 'production') pins the pino-pretty transport to the parent process's stdout fd via `destination: 1`, so `tsx watch src/index.ts > /tmp/humyn-api.log 2>&1` actually captures live request log lines (Items 10). A `curl http://localhost:8080/healthz` adds a visible line to `/tmp/humyn-api.log`."
    - "`UploadCoordinator::drainNow` logs at every paused-checkpoint exit: `Log.d(TAG, \"drainNow paused at before-iteration\")` at line ~186 (before the per-row loop), and `Log.d(TAG, \"drainNow paused at after-metadata\")` at line ~309 (after metadata PUT). The two `if (isPaused())` checkpoints inside `uploadOne` (lines ~296, ~322/341, ~369) get an `at after-init` / `at part-submit` annotation. The `catch (e: DeadLetterException)` at line ~200 of `drainNow` logs `Log.w(TAG, \"row \${row.recordingId} DEAD_LETTER: \${e.message}\")` BEFORE the queueStore.upsert. (`row.recordingId` is a ULID — safe; `e.message` is the route + status code from `DeadLetterException(\"/recordings/init -> 409 ...\")`, never a presigned URL — T-5-06-02 stays satisfied.)"
    - "Runbook `.planning/runbooks/05-upload-smoke.md` §1 is consolidated to a single Tunnels bullet (`adb reverse tcp:8080 tcp:8080 && adb reverse tcp:8081 tcp:8081 && adb reverse tcp:4566 tcp:4566` — API / Metro / LocalStack), a Metro-must-be-running bullet (the `installApkRolloutDebug` offline-bundle gotcha), and the `pnpm --filter @humyn/api seed:dev-task` step. The `05-HUMAN-UAT.md` Current Test block stays `paused_pending_wave_1_5` until the on-device re-walk."
    - "JS Vitest + Kotlin Robolectric coverage: `UploadCoordinatorTest.kt` asserts each route's MockWebServer request carries its OWN UUIDv4 (init/parts/finalize/reupload — 4 distinct keys per row); each route's key is stable across retries; no cross-route key reuse. `UploadQueueStoreTest.kt` asserts `fromJson` on a legacy single-key row mints all 4 + persists back to disk (a re-read returns the same 4 keys). New JS Vitest `uploadReconcile.test.tsx` asserts cold-start with a queued PENDING/UPLOADING row calls `HumynUpload.drainNow()`. `PendingUploadsScreen.test.tsx` + `HomeSkeletonScreen.test.tsx` cover the progress bar render + the Home tap-→-History routing."
  artifacts:
    - path: 'apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt'
      provides: 'UploadRow.{init,parts,finalize,reupload}IdempotencyKey — four per-route UUIDv4 fields minted at construction, persisted via toJson/fromJson, migration persists back to disk via UploadQueueStore.upsert'
      contains: 'initIdempotencyKey'
    - path: 'apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt'
      provides: 'authedJsonRequest call sites threaded with the per-route key; drainNow paused-checkpoint logs; DEAD_LETTER logcat warn'
      contains: 'reuploadIdempotencyKey'
    - path: 'apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt'
      provides: 'reupload() branches on client-side DEAD_LETTER (LOCAL reset, no fields touched) vs worker-fired re-upload (full reset); new @ReactMethod drainNow() bridge'
      contains: 'drainNow'
    - path: 'apps/mobile/src/services/uploadReconcile.ts'
      provides: 'cold-start drainNow kick when a row in {PENDING,UPLOADING} is on disk'
      contains: 'drainNow'
    - path: 'apps/mobile/src/state/uploadToastBus.ts'
      provides: 'one-shot Home-mount-deliver bus for the post-recording contribution toast (≥5s)'
      min_lines: 15
    - path: 'apps/mobile/src/screens/uploads/PendingUploadsScreen.tsx'
      provides: 'live progress bar (sibling-of-chip determinate fill, token-aligned)'
      contains: 'progress-bar-fill'
    - path: 'apps/mobile/src/screens/home/HomeSkeletonScreen.tsx'
      provides: 'pending-uploads-tile-row chip percent + progress bar; tile-row tap → MainTabs/History'
      contains: 'MainTabs'
    - path: 'apps/api/src/plugins/logger.ts'
      provides: 'dev pino-pretty transport pinned to destination: 1 (parent stdout) so /tmp/humyn-api.log captures live request lines'
      contains: 'destination: 1'
    - path: '.planning/runbooks/05-upload-smoke.md'
      provides: 'consolidated §1 pre-flight (Tunnels + Metro + seed-dev-task) and the Wave-1.5 walk-log closing entry'
      contains: 'adb reverse tcp:4566'
  key_links:
    - from: 'apps/mobile/android/.../UploadCoordinator.kt::authedJsonRequest'
      to: 'apps/mobile/android/.../UploadModels.kt::UploadRow.{init,parts,finalize,reupload}IdempotencyKey'
      via: 'each of the four POST sites (postInit/postRePresign/postFinalize/postReupload) passes the matching row.{route}IdempotencyKey'
      pattern: 'row\\.(init|parts|finalize|reupload)IdempotencyKey'
    - from: 'apps/mobile/android/.../HumynUploadModule.kt::reupload'
      to: 'apps/mobile/android/.../UploadCoordinator.kt::drain'
      via: 'client-side DEAD_LETTER row → LOCAL reset (state=UPLOADING, deadLetterReason=null), preserve uploadId/parts/etags, coordinator.drain() → postRePresign branch (/parts)'
      pattern: 'state = UploadState\\.UPLOADING'
    - from: 'apps/mobile/src/services/uploadReconcile.ts::reconcileOnce'
      to: 'apps/mobile/src/native/HumynUpload.ts::drainNow'
      via: 'on boot, if getQueueSafe() returns a row in {pending,uploading} → HumynUpload.drainNow()'
      pattern: 'drainNow'
    - from: 'apps/mobile/src/screens/recording/RecordingScreen.tsx::handleStop (≥60s branch)'
      to: 'apps/mobile/src/state/uploadToastBus.ts'
      via: "uploadToastBus.set('contribution', text, 5_000) BEFORE navigateToHome(navigation); HomeSkeletonScreen mount drains it via showToast(text, 5_000)"
      pattern: 'uploadToastBus'
    - from: 'apps/mobile/src/screens/home/HomeSkeletonScreen.tsx (pending-uploads-tile-row onPress)'
      to: 'apps/mobile/src/navigation/MainTabs.tsx'
      via: "navigation.navigate('MainTabs', { screen: 'History' })"
      pattern: "screen: 'History'"
    - from: 'apps/api/src/plugins/logger.ts (dev branch)'
      to: 'pino-pretty worker thread'
      via: 'transport.options.destination = 1 (parent stdout fd) so the worker writes back to the launching process'
      pattern: 'destination: 1'
---

<objective>

| Plan item | COSMETIC-GAPS section    | Notes                              |
| --------- | ------------------------ | ---------------------------------- |
| Item 1    | Wave-1.5 / Functional    | Per-route Idempotency-Keys (4)     |
| Item 2    | Wave-1.5 / Functional    | Retry-on-client-side-DEAD_LETTER   |
| Item 3    | Wave-1.5 / Setup         | Runbook §1 consolidation           |
| Item 4    | UX                       | Live progress bar (Home + Pending) |
| Item 5    | UX                       | Contribution toast (≥5s, global)   |
| Item 6    | UX                       | Home tile → History routing        |
| Item 7    | Wave-1.5 / Cosmetic      | fromJson migration persist-back    |
| Item 8    | Wave-1.5 / Cosmetic      | Cold-start drainNow on stale queue |
| Item 9    | Wave-1.5 / Observability | drainNow paused-checkpoint logs    |
| Item 10   | Observability            | pino transport pinned to fd 1      |

Phase 5 Wave-1.5 batch-fix: close the 10 items surfaced during the 2026-05-13 on-device UAT walk on Pixel 10a (recording `01KRFZ91Y3E315AJVG75KXJZE6` → DEAD_LETTER from a cross-route `Idempotency-Key` reuse; Retry-on-client-side-dead-letter hitting `/reupload` → 409; cold-start drain not auto-firing on a stale queue; missing live progress; contribution toast killed by screen transition; Home pending-row tap landing on an orphan screen; pino worker-transport silently eating `/tmp/humyn-api.log`; drainNow paused-checkpoint and DEAD_LETTER logs missing; runbook §1 missing the LocalStack `adb reverse` + the consolidated pre-flight). The 13 existing Phase-5 plans (05-01..05-13) are untouched — they remain merged and verified on `main`. This single new plan delivers ONE coherent build that re-greens the §2 happy path.

Purpose: unblock the §2 re-walk on `5C161JEA304304` so Phase 5 can flip `05-HUMAN-UAT.md` Current Test from `paused_pending_wave_1_5` → `partial` (then ultimately `passed`).
Output: a single APK + a single backend restart that, when paired with the consolidated §1 pre-flight, runs §2 happy path end-to-end on the device.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-CONTEXT.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-RESEARCH.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-PATTERNS.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VERIFICATION.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-HUMAN-UAT.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-COSMETIC-GAPS.md
@.planning/runbooks/05-upload-smoke.md

<interfaces>
<!-- Extracted from the live codebase (commit `f0a8a08`). -->

From `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt`:
The single-key field today (`var idempotencyKey: String = UUID.randomUUID().toString()`, line 151) is REPLACED by four per-route fields. `toJson` writes them; `fromJson` reads them with migration. Constructor positional-arg order changes — call sites are `HumynUploadModule.kt::enqueue` (line ~131) and `UploadRow.fromJson` (line ~208). Both construct via named args (`recordingId = ..., ownerUserId = ...`) — adding four fields to the data class is a recompile, no call-site change. The `MODELS_TAG = "HumynUploadCoord"` (line 27) stays.

From `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt`:

- `authedJsonRequest(url, bodyJson, idempotencyKey)` (line 439). Four POST sites:
  - line ~481: `postInit(...)` → currently `row.idempotencyKey` → use `row.initIdempotencyKey`
  - line ~501: `postReupload(...)` → use `row.reuploadIdempotencyKey`
  - line ~522: `postRePresign(...)` (`/parts`) → use `row.partsIdempotencyKey`
  - line ~576: `postFinalize(...)` → use `row.finalizeIdempotencyKey`
- `drainNow()` `if (isPaused()) return` checkpoints:
  - line 186: `if (isPaused()) return` (before-iteration) → add `Log.d(TAG, "drainNow paused at before-iteration")`
  - line 190: `if (isPaused()) break` (per-row) → optional log; the before-iteration line covers the cold path
  - line 200: `catch (e: DeadLetterException)` → add `Log.w(TAG, "row ${row.recordingId} DEAD_LETTER: ${e.message}")` BEFORE the `row.state = DEAD_LETTER` assignment so logcat shows the transition even if upsert/emitter throws.
  - line 296 (inside `uploadOne`): `if (isPaused()) return` after `/init` + before metadata PUT → `Log.d(TAG, "drainNow paused at after-init")`
  - line 309 (inside `uploadOne`): `if (isPaused()) return` after metadata PUT → `Log.d(TAG, "drainNow paused at after-metadata")`
  - lines 322/341/369: the per-part `if (isPaused())` short-circuits — these are inside `partExecutor.submit` blocks, optional logs ok if cheap.
- `TAG = "HumynUploadCoord"` (line 582). Same TAG.

From `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt`:

- `reupload(recordingId: String, promise: Promise)` (line 235). Today it always does the full reset (state=PENDING, reupload=true, clear uploadId/imuUploadId/parts/etags). Must branch:
  - if `row.state == UploadState.DEAD_LETTER && row.uploadId != null && !row.reupload` → LOCAL reset (state=UPLOADING, deadLetterReason=null, KEEP uploadId/imuUploadId/parts/etags/metadataPut), drain.
  - else if state ∈ {PENDING, UPLOADING, FINALIZING, AWAITING_VERIFY} or (DEAD_LETTER && row.reupload == true) → existing full reset.
  - else (VERIFIED) → `promise.resolve(null)` no-op.
- New `@ReactMethod fun drainNow(promise)` — `bgExecutor.execute { try { coordinator.drain(); promise.resolve(null) } catch (t) { promise.reject(...) } }`. Add the typed bridge surface in `HumynUpload.ts` as `drainNow(): Promise<void>` + a `drainNowSafe` boot-safe variant.

From `apps/mobile/src/services/uploadReconcile.ts`:
`installUploadReconcile()` (line 125) calls `void reconcileOnce()` at boot. `reconcileOnce()` (line 74) currently only kicks `pushUploadContext()` + the `/recordings/verified-ids` sweep. After `pushUploadContext()` add: query `HumynUpload.getQueueSafe()`; if any row.state is `'pending'` or `'uploading'` → `await HumynUpload.drainNowSafe()`. Order matters: pushUploadContext FIRST (the drainer needs `getCurrentSub()`); drainNow AFTER. Wrap in try/catch (boot-safe, JSDOM-safe).

From `apps/mobile/src/screens/recording/RecordingScreen.tsx`:

- Line 200: `const showToast = useCallback((text: string) => { setToast({ text, visible: true }); ... setTimeout(..., 3000); }, [])` — this is the PER-SCREEN toast state (which is what dies on the transition). The lifecycle's `showToast` plumb-through (line 469) feeds back into this local state.
- Line 412-416: `showToast(\`${formatContributionDuration(durationMs)} added to your contribution.\`)`is the post-≥60s branch. **Fix:** before`navigateToHome(navigation)`(line 417), call`uploadToastBus.set(text, 5_000)`. The local screen `showToast` stays for the < 60s "Recording too short — discarded." case + the device-distress case — those are RecordingScreen-local toasts that genuinely don't need to survive a screen.
- Note line 195-203: the local `toast`/`showToast` uses `setTimeout(..., 3000)` — this is a SEPARATE in-screen toast component, NOT the global `<ToastHost />`. The Home toast surface IS `<ToastHost />` (App.tsx:78, sibling of NavigationContainer, already global). So the contribution toast just needs to be routed through the GLOBAL `showToast` from `components/Toast` with a 5_000ms duration, AND deferred until Home mounts so it fires after the navigate completes.

From `apps/mobile/src/components/Toast.tsx`:

- `showToast(text: string, durationMs?: number)` (line 63) — the global imperative API. Default ≤2.5s; pass `5_000` for the contribution case. `<ToastHost />` (line 105) is the global host already mounted in `App.tsx:78`.
- Already used by `bootRecoveryListener.ts::deliver` with `RECOVERY_TOAST_MS = 5_000` — the deliver-on-Home pattern is the template.

From `apps/mobile/src/screens/home/HomeSkeletonScreen.tsx`:

- Line 139-142: `<Pressable accessibilityLabel="pending-uploads-tile" onPress={() => navigation.navigate('PendingUploads')} ...>` — the OUTER tile press (entire card) navigates to the orphan `PendingUploads` screen.
- Line ~157: the per-row `<View accessibilityLabel="pending-uploads-tile-row">` is presentational (no onPress). The fix: change the outer tile press to `navigation.navigate('MainTabs', { screen: 'History' })`. The standalone `PendingUploads` route in `RootNativeStack.tsx:95` stays registered (deep-link only).
- Per-tile-row chip render at line ~169: `<UploadStatusChip variant={chipVariantFor(row)} />` — extend to pass `percent={progressById[row.recordingId]}` when the row is `'uploading'`; AND render a sibling `<View style={[styles.progressBarTrack, { width: '100%' }]}><View style={[styles.progressBarFill, { width: `${Math.max(0,Math.min(100,Math.round(pct)))}%` }]} /></View>` below the chip row (or as a thin underline at the bottom of the tile-row).

From `apps/mobile/src/screens/uploads/PendingUploadsScreen.tsx`:

- Already plumbs `onUploadProgress` → `progressById` + passes `percent={pct}` to UploadStatusChip (line 138-148, 177). Already renders the chip percent on this surface. **Fix:** ADD the sibling progress-bar `<View>` (lines ~176-188 area, after the chipRow) — same token pair (`colors.line` track + `colors.chipProgressText` fill), only on `isActive` rows.

From `apps/mobile/App.tsx`:

- Line 78: `<ToastHost />` is already a sibling of `<NavigationContainer>`. No change needed.

From `apps/mobile/__tests__/`:

- Existing Vitest convention — `apps/mobile/__tests__/components/Toast.test.tsx` is the template for screen-host renders; `apps/mobile/__tests__/services/` doesn't yet have an `uploadReconcile.test.tsx` (new file). The `HumynUpload` module is mocked via `jest.mock('../../src/native/HumynUpload', ...)` in existing tests.

From `apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadCoordinatorTest.kt`:

- Already captures `Idempotency-Key` per path via `idempotencyKeysByPath` (line 63, 88, 102, 108, 127). Tests `every API POST carries the row's stable Idempotency-Key UUIDv4 ...` (line 653) and `a re-drain via slash parts reuses the same Idempotency-Key as the original slash init` (line 678) — both currently assert `expectedKey = r.idempotencyKey` (the single key). **Update:** rewrite these to assert the per-route keys (`r.initIdempotencyKey == idempotencyKeysByPath["/recordings/init"]`, etc.), and add new tests for cross-route key distinctness + per-route retry stability.

From `apps/api/src/plugins/logger.ts`:
The current shape:

```ts
...(process.env.NODE_ENV !== 'production'
  ? {
      transport: {
        target: 'pino-pretty',
        options: { translateTime: 'HH:MM:ss.l', ignore: 'pid,hostname' },
      },
    }
  : {}),
```

**Fix:** add `destination: 1` to the options:

```ts
options: { translateTime: 'HH:MM:ss.l', ignore: 'pid,hostname', destination: 1 },
```

This pins the pino-pretty worker thread to the parent's stdout fd. (`destination: 1` is the documented pino-pretty option; the worker `process.stdout` of the worker thread is a private fd, so explicitly pointing at `1` makes it write back to the launching process's fd 1, which `>` redirection captures.)

From `.planning/runbooks/05-upload-smoke.md`:

- §1 line 13 already has `adb reverse tcp:8081 tcp:8081` + optional `tcp:8080`. **Fix:** consolidate into a single Tunnels bullet listing all three (`tcp:8080`, `tcp:8081`, `tcp:4566` — API / Metro / LocalStack) with the LocalStack annotation referencing the 2026-05-13 walk's recording `01KRFXGAWCMVQ89PJ2PBXSVAKK`. The Metro-must-be-running bullet stays (it's the `installApkRolloutDebug` ships-`--dev false` offline-bundle gotcha; restate succinctly). The `pnpm --filter @humyn/api seed:dev-task` step stays. Add a Wave-1.5 closing entry to §6 Walk log: "PASS — Wave-1.5 batch-fix landed; §2 re-walk green" (executor fills in date + finding).

</interfaces>
</context>

<tasks>

<!-- ========================================================================= -->
<!-- WAVE 1.5a — sub-divided into 1.5a-1 (Task 1 only — MUST land first) and    -->
<!--             1.5a-2 (Tasks 2, 3, 4 — parallel-safe once Task 1 is in).      -->
<!-- Sub-divided because Tasks 1 and 3 both modify UploadCoordinator.kt AND     -->
<!-- because Task 1's per-route-key rename breaks UploadCoordinatorTest.kt      -->
<!-- until Task 1's main+test edits land — Task 3's gradle test invocation     -->
<!-- (and Task 4's runbook docs are independent docs but live in the same      -->
<!-- wave for orchestration). Tasks 2 / 3 / 4 are parallel-safe to each other  -->
<!-- once 1.5a-1 is in.                                                         -->
<!-- Items 1 / 3 / 9 / 10                                                       -->
<!-- ========================================================================= -->

<task type="auto" tdd="true">
  <name>Task 1 (Wave 1.5a-1) — Per-route Idempotency-Key split (4 fields) + per-route MockWebServer assertions</name>
  <wave>1.5a</wave>
  <sub_wave>1.5a-1</sub_wave>
  <files>apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt, apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt, apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadCoordinatorTest.kt, apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadQueueStoreTest.kt</files>
  <read_first>
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt (FULL — see the `idempotencyKey` field at line 151, `toJson` at line 173, `fromJson` migration at line 188-204)
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt (the four `authedJsonRequest(...)` call sites at lines 481, 501, 522, 576)
    - apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadCoordinatorTest.kt (the `idempotencyKeysByPath` capture at lines 62-127; the two existing Idempotency-Key tests at lines 653-707)
    - apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadQueueStoreTest.kt (the three existing idempotencyKey tests at lines 230-280 — the patterns + the `uuidV4` regex)
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-COSMETIC-GAPS.md (Wave-1.5 Item 1 — the canonical 4-field design)
    - .planning/runbooks/05-upload-smoke.md §6 2026-05-13 walk log root cause (5) — the exact bug being closed
  </read_first>
  <behavior>
    - `UploadRow` REPLACES `var idempotencyKey: String = UUID.randomUUID().toString()` with FOUR data-class fields each minted independently at construction:
      ```kotlin
      var initIdempotencyKey: String = UUID.randomUUID().toString(),
      var partsIdempotencyKey: String = UUID.randomUUID().toString(),
      var finalizeIdempotencyKey: String = UUID.randomUUID().toString(),
      var reuploadIdempotencyKey: String = UUID.randomUUID().toString(),
      ```
    - `toJson` writes all four (`put("initIdempotencyKey", initIdempotencyKey)` etc.). Drop the `put("idempotencyKey", idempotencyKey)` line.
    - `fromJson` reads all four with the migration rule: if a field is missing or blank, mint a fresh UUIDv4 AND mark a `migrated` boolean. **Ownership boundary:** Task 1 delivers ONLY the four-field shape on `UploadRow` + the in-memory mint logic inside `fromJson` (mints fresh UUIDv4s for missing keys; marks the row via the `_migratedOnLoad` transient flag added by Task 5). The persist-back-to-disk on `read()` of a migrated row is Task 5's responsibility (Wave 1.5b).
    - The four POST sites in `UploadCoordinator.uploadOne`/`postInit`/`postRePresign`/`postFinalize`/`postReupload` use their matching field:
      - line ~481: `authedJsonRequest("$baseUrl/recordings/init", body, row.initIdempotencyKey)`
      - line ~501: `authedJsonRequest("$baseUrl/recordings/${row.recordingId}/reupload", body, row.reuploadIdempotencyKey)`
      - line ~522: `authedJsonRequest("$baseUrl/recordings/${row.recordingId}/parts", body, row.partsIdempotencyKey)`
      - line ~576: `authedJsonRequest("$baseUrl/recordings/${row.recordingId}/finalize", body, row.finalizeIdempotencyKey)`
    - Doc-comment for each field on `UploadRow`: "Stable UUIDv4 sent as `Idempotency-Key` on every retry of `POST /recordings/init`. Minted once at construction; reused only across retries of THIS route. Per-route split (not a single per-row key) because the server's global idempotency pre-handler caches by `(user_id, key)` and rejects on body mismatch — same key + different `(method,path,body)` ⇒ 409 idempotency-key-conflict; per-route keys make every (key,body) pair stable. Fix surfaces Wave-1.5 Item 1, see 05-COSMETIC-GAPS.md + the 2026-05-13 walk log."
    - `UploadCoordinatorTest.kt` updates:
      - Rewrite `every API POST carries the row's stable Idempotency-Key UUIDv4 (init+finalize on the first drain)` (line 653) to assert `idempotencyKeysByPath["/recordings/init"] == r.initIdempotencyKey` AND `idempotencyKeysByPath["/finalize"] == r.finalizeIdempotencyKey` AND those two keys are DISTINCT (`assertNotEquals(initKey, finalizeKey)`).
      - Rewrite `a re-drain via slash parts reuses the same Idempotency-Key as the original slash init` (line 678) to: `idempotencyKeysByPath["/parts"] == r.partsIdempotencyKey` AND `partsKey != r.initIdempotencyKey`.
      - Add: `each route's key is stable across retries` — drain a row, induce a synthetic 5xx (use the existing `MockResponse().setResponseCode(503)` dispatcher seam), drain again, assert the same `idempotencyKeysByPath["/recordings/init"]` value pre- and post-retry (capture the first key, clear `idempotencyKeysByPath["/recordings/init"]`, drain again, the second capture equals the first).
      - Add: `four distinct keys across init/parts/finalize/reupload` — drive a row through `/init` → `/finalize` (Wave 1) and a separate row through `/reupload` → `/finalize` (the dead-letter-retry path). Assert all four captured keys are distinct UUIDv4s.
    - `UploadQueueStoreTest.kt` updates:
      - Update `mint a fresh UUIDv4 idempotencyKey at construction` (line 230) to assert all FOUR fields are independent UUIDv4s and are pairwise distinct.
      - Update `idempotencyKey survives a round trip through queue json` (line 241) to assert all 4 round-trip.
      - Update `fromJson mints a fresh UUIDv4 when a legacy row on disk has no idempotencyKey` (line 252) to cover both shapes: (a) a row written by the pre-`5c0b2d8` shape (no key fields at all → all 4 minted); (b) a row written by commit `5c0b2d8`'s shape (only the legacy `idempotencyKey` field → all 4 still get minted independently; the legacy `idempotencyKey` field is IGNORED — do NOT propagate it into all four, because that re-introduces the cross-route 409 bug). Both cases: the minted keys satisfy `uuidV4.matches(...)`.
  </behavior>
  <action>
1. `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt`:
   - Delete line 151 (`var idempotencyKey: String = ...`).
   - Insert four data-class fields (each `var ... : String = UUID.randomUUID().toString()`) in this order: `initIdempotencyKey`, `partsIdempotencyKey`, `finalizeIdempotencyKey`, `reuploadIdempotencyKey`. Doc-comment per field per Behaviour.
   - Update `toJson` (line ~153): drop `put("idempotencyKey", idempotencyKey)`; add four `put("initIdempotencyKey", initIdempotencyKey)` / `put("partsIdempotencyKey", partsIdempotencyKey)` / `put("finalizeIdempotencyKey", finalizeIdempotencyKey)` / `put("reuploadIdempotencyKey", reuploadIdempotencyKey)` lines.
   - Update `fromJson` (line ~187): for EACH of the four field names, read `o.optString("{fieldName}", "")`; if blank → mint a UUIDv4 + `Log.w(MODELS_TAG, "row $recordingId missing $fieldName on load — minted (one-shot migration)")`. The legacy `idempotencyKey` field is IGNORED (do not copy it into the four). Construct `UploadRow(..., initIdempotencyKey = initKey, partsIdempotencyKey = partsKey, finalizeIdempotencyKey = finalizeKey, reuploadIdempotencyKey = reuploadKey)`.
2. `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt`:
   - Line ~481 (`postInit`): change `row.idempotencyKey` → `row.initIdempotencyKey`.
   - Line ~501 (`postReupload`): change `row.idempotencyKey` → `row.reuploadIdempotencyKey`.
   - Line ~522 (`postRePresign`): change `row.idempotencyKey` → `row.partsIdempotencyKey`.
   - Line ~576 (`postFinalize`): change `row.idempotencyKey` → `row.finalizeIdempotencyKey`.
   - Update the `authedJsonRequest` Kdoc (line 431): clarify "Per-route key — `row.initIdempotencyKey` for `/init`, `row.partsIdempotencyKey` for `/parts`, `row.finalizeIdempotencyKey` for `/finalize`, `row.reuploadIdempotencyKey` for `/reupload`. Per-route split closes Wave-1.5 Item 1 (the server caches by `(user_id, key)` + hashes `(method,path,body)` for equality — a single per-row key reused across routes hits a 409 on the second route)."
3. `apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadCoordinatorTest.kt`:
   - At the test at line 653: replace `val expectedKey = r.idempotencyKey` (line ~661) with separate captures for each path; assertions per Behaviour.
   - At the test at line 678: same — `expectedKey = r.partsIdempotencyKey`; assertion per Behaviour.
   - Add new tests `every API POST carries its OWN per-route Idempotency-Key UUIDv4 (4 distinct)`, `init Idempotency-Key is stable across a synthetic 5xx retry`, `the four routes use four distinct UUIDv4 keys (no cross-route reuse)`. Use the existing `MockWebServer` dispatcher + the `idempotencyKeysByPath` capture map.
4. `apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadQueueStoreTest.kt`:
   - At the test at line 230: update assertions to verify all four fields independently (`r.initIdempotencyKey`, `r.partsIdempotencyKey`, `r.finalizeIdempotencyKey`, `r.reuploadIdempotencyKey` each match `uuidV4` AND are pairwise distinct).
   - At the test at line 241: assert all 4 round-trip.
   - At the test at line 252: extend to assert (a) legacy-no-key shape mints all 4 distinct UUIDv4s; (b) legacy-single-`idempotencyKey` shape (commit `5c0b2d8`'s row layout) ALSO mints 4 NEW distinct UUIDv4s (the legacy field is discarded; the four are NOT copies of it).
  </action>
  <verify>
    <automated>cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests "ai.humynlabs.capture.upload.UploadCoordinatorTest" --tests "ai.humynlabs.capture.upload.UploadQueueStoreTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'initIdempotencyKey' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt` returns ≥3 (field decl, toJson, fromJson)
    - `grep -c 'partsIdempotencyKey' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt` returns ≥3
    - `grep -c 'finalizeIdempotencyKey' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt` returns ≥3
    - `grep -c 'reuploadIdempotencyKey' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt` returns ≥3
    - `grep -c 'row\.idempotencyKey' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt` returns 0 (single-key references gone)
    - `grep -c 'row\.\(init\|parts\|finalize\|reupload\)IdempotencyKey' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt` returns ≥4
    - `cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests "ai.humynlabs.capture.upload.UploadCoordinatorTest" --tests "ai.humynlabs.capture.upload.UploadQueueStoreTest"` exits 0
  </acceptance_criteria>
  <done>UploadRow has 4 per-route UUIDv4 fields; the four authedJsonRequest sites pass their matching field; toJson/fromJson round-trip all 4; legacy-row migration mints 4 fresh + IGNORES the old single key; Robolectric tests green.</done>
</task>

<task type="auto">
  <name>Task 2 (Wave 1.5a-2) — Runbook §1 consolidation (Tunnels + Metro + seed-dev-task)</name>
  <wave>1.5a</wave>
  <sub_wave>1.5a-2</sub_wave>
  <files>.planning/runbooks/05-upload-smoke.md</files>
  <read_first>
    - .planning/runbooks/05-upload-smoke.md §1 (lines 11-23) and §6 Walk log entry for 2026-05-13 (lines 102-112)
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-COSMETIC-GAPS.md "Setup / runbook gaps" section (lines 42-48)
  </read_first>
  <action>
1. In `.planning/runbooks/05-upload-smoke.md` §1 Pre-flight (lines 11-23):
   - Find the existing bullet on line 13 (currently mentions `adb reverse tcp:8081 tcp:8081` + the optional `tcp:8080`). REPLACE that single bullet with TWO consolidated bullets:
     - **Tunnels** — single fenced shell line: `adb reverse tcp:8080 tcp:8080 && adb reverse tcp:8081 tcp:8081 && adb reverse tcp:4566 tcp:4566`. Annotation: "API / Metro / LocalStack — the LocalStack tunnel was the missing piece on the 2026-05-13 walk (recording `01KRFXGAWCMVQ89PJ2PBXSVAKK` dead-lettered with 6 part-PUT retries against device-side `localhost:4566`)."
     - **Metro must be running in `apps/mobile`** — `pnpm start`. Annotation: "`installApkRolloutDebug` ships a `--dev false` offline JS bundle (the apkRollout flavor isn't in the RN gradle plugin's default `debuggableVariants` list, so `createBundleApkRolloutDebugJsAndAssets` runs and the offline bundle ships in the APK). Without Metro + `adb reverse tcp:8081` the app loads the offline bundle → `__DEV__ === false` → every `__DEV__`-gated affordance (including the Tasks-tab long-press the §2 walk uses to start a non-practice recording) is dead-code-eliminated."
   - The existing `pnpm --filter @humyn/api seed:dev-task` bullet (line 16) stays — confirm it reads as "Seed the canonical dev task — `pnpm --filter @humyn/api seed:dev-task`. Idempotent; inserts/refreshes the reserved task `id=01HVDEVSEEDTASK00000000000` (slug `dev-seed-chop-vegetables`). The `__DEV__` Tasks-tab long-press affordance (§2) sends this `taskId` on `POST /recordings/init`; the upload coordinator dead-letters on a missing-FK 400 without it."
2. In §6 Walk log, append a new dated bullet AFTER the 2026-05-13 paused entry (line 112 onwards), at the bottom of the existing log: `- **{YYYY-MM-DD} — Wave-1.5 batch-fix landed: §2 ready for re-walk.** Per-route Idempotency-Key split (4 fields), Retry-on-client-side-DEAD_LETTER LOCAL-reset, cold-start drainNow on stale queue, live progress chip+bar on Home tile + Pending Uploads, contribution toast on the global ToastHost (≥5s), Home pending-row → History tab, dev pino transport pinned to fd 1 (so `/tmp/humyn-api.log` captures live request lines), drainNow paused-checkpoint logs + DEAD_LETTER warn. APK rebuilt + installed; backend restarted. **§2 walk re-runnable.**` (The executor fills in the date — the planner doesn't pre-bake.)
3. No other section of the runbook is touched — §2 through §5 stay verbatim. The §7 amendments protocol stays.
  </action>
  <verify>
    <automated>grep -c 'adb reverse tcp:4566' .planning/runbooks/05-upload-smoke.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'adb reverse tcp:4566 tcp:4566' .planning/runbooks/05-upload-smoke.md` returns ≥1
    - `grep -c 'adb reverse tcp:8080 tcp:8080 && adb reverse tcp:8081 tcp:8081 && adb reverse tcp:4566 tcp:4566' .planning/runbooks/05-upload-smoke.md` returns ≥1 (the single-line Tunnels bullet)
    - `grep -c 'seed:dev-task' .planning/runbooks/05-upload-smoke.md` returns ≥1 (the bullet survives)
    - `grep -c 'Wave-1.5 batch-fix landed' .planning/runbooks/05-upload-smoke.md` returns ≥1 (the §6 closing entry)
  </acceptance_criteria>
  <done>§1 reads as a consolidated Tunnels + Metro + seed-dev-task pre-flight; §6 has a Wave-1.5 closing entry; rest of the runbook untouched.</done>
</task>

<task type="auto">
  <name>Task 3 (Wave 1.5a-2) — drainNow paused-checkpoint logs + DEAD_LETTER logcat warn</name>
  <wave>1.5a</wave>
  <sub_wave>1.5a-2</sub_wave>
  <depends_on_task>1</depends_on_task>
  <files>apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt</files>
  <read_first>
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt lines 180-215 (drainNow, the three `if (isPaused())` checkpoints, the `catch (e: DeadLetterException)` branch)
    - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt lines 290-370 (uploadOne, the per-step `if (isPaused())` checkpoints at lines 296, 309)
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-COSMETIC-GAPS.md Item 9 ("Observability nits — three more from this afternoon")
  </read_first>
  <action>
1. `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt`:
   - Line 186 (`if (isPaused()) return` inside drainNow before the per-row loop): change to `if (isPaused()) { Log.d(TAG, "drainNow paused at before-iteration"); return }`
   - Line 190 (`if (isPaused()) break` inside the per-row loop): change to `if (isPaused()) { Log.d(TAG, "drainNow paused at per-row checkpoint, row=${row.recordingId}"); break }`
   - Line ~200 (the `catch (e: DeadLetterException)` block): add `Log.w(TAG, "row ${row.recordingId} DEAD_LETTER: ${e.message}")` as the FIRST line of the catch body, BEFORE `row.state = UploadState.DEAD_LETTER`. (`row.recordingId` is a ULID — safe; `e.message` is the route + status code from `DeadLetterException("/recordings/init -> 409 ...")`, never a presigned URL — T-5-06-02 stays satisfied. Verify: the `DeadLetterException` constructor at line ~488/520 and the `parseInitResponse` IOException at line 558 are body-free.)
   - Line 296 (`if (isPaused()) return` inside uploadOne after /init): change to `if (isPaused()) { Log.d(TAG, "drainNow paused at after-init, row=${row.recordingId}"); return }`
   - Line 309 (`if (isPaused()) return` inside uploadOne after metadata PUT): change to `if (isPaused()) { Log.d(TAG, "drainNow paused at after-metadata, row=${row.recordingId}"); return }`
   - Line 369 (`if (isPaused()) return` after the part-PUT futures join): change to `if (isPaused()) { Log.d(TAG, "drainNow paused at after-parts, row=${row.recordingId}"); return }`
   - The inner `partExecutor.submit { ... if (isPaused()) return@submit ... }` at lines 322 + 341: leave unchanged (chatty + the outer checkpoints cover the diagnosis-tax case; a per-part log here would spam at 5MiB-per-part cadence).
2. Confirm no presigned URL or bearer token can reach logcat: `e.message` for `DeadLetterException` is constructed at lines ~488 (`/recordings/init -> ${resp.code} (recording not resumable...)`) and ~524 (`/recordings/${row.recordingId}/parts -> ${resp.code} (upload not resumable)`) — both contain only the route, the status code, and a static phrase. `parseInitResponse` re-throws a body-free `IOException` (line 558). The `row.recordingId` is a ULID (lowercase Crockford-base32, no PII). T-5-06-02 satisfied.
  </action>
  <verify>
    <automated>cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests "ai.humynlabs.capture.upload.UploadCoordinatorTest" && grep -c 'drainNow paused at' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'drainNow paused at' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt` returns ≥4 (before-iteration + after-init + after-metadata + after-parts; the per-row break is optional)
    - `grep -c 'DEAD_LETTER:' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt` returns ≥1
    - `cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests "ai.humynlabs.capture.upload.UploadCoordinatorTest"` exits 0 (no regression from the log calls)
    - no presigned URL string can appear in a log call — `grep -n 'Log\.[dwie](.*partUrls\|Log\.[dwie](.*X-Amz' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt` returns 0
  </acceptance_criteria>
  <done>Four paused-checkpoint logs + one DEAD_LETTER warn land in `UploadCoordinator.kt`; no presigned URL or bearer leaks; unit tests still pass.</done>
</task>

<task type="auto">
  <name>Task 4 (Wave 1.5a-2) — Pin pino-pretty transport to parent stdout (destination: 1) so /tmp/humyn-api.log captures live request lines</name>
  <wave>1.5a</wave>
  <sub_wave>1.5a-2</sub_wave>
  <files>apps/api/src/plugins/logger.ts, apps/api/scripts/dev.sh</files>
  <read_first>
    - apps/api/src/plugins/logger.ts (FULL — the current 31-line file; the dev-only `transport: { target: 'pino-pretty', ... }` block)
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-COSMETIC-GAPS.md Item 10 ("Dev API request logs")
    - .planning/runbooks/05-upload-smoke.md §1 (the `pnpm --filter @humyn/api dev` step)
  </read_first>
  <action>
1. `apps/api/src/plugins/logger.ts`:
   - In the `process.env.NODE_ENV !== 'production'` branch (line 14-21), add `destination: 1` to the `options` object:
     ```ts
     transport: {
       target: 'pino-pretty',
       options: {
         translateTime: 'HH:MM:ss.l',
         ignore: 'pid,hostname',
         // Wave-1.5 Item 10 — pin the pino-pretty worker-thread's output to the parent process's stdout fd
         // (fd 1). pino transports run in a worker_thread whose own `process.stdout` is a private fd, so
         // without this `destination: 1` line the `pnpm --filter @humyn/api dev > /tmp/humyn-api.log 2>&1`
         // redirect captures NO request log lines (the worker writes to its private fd, not fd 1). With it,
         // the worker writes back through fd 1 and the redirect captures everything. See pino-pretty
         // README "Programmatic Integration" → `destination`.
         destination: 1,
       },
     },
     ```
2. `apps/api/scripts/dev.sh` (NEW FILE — port-guard):
   ```sh
   #!/usr/bin/env bash
   # Wave-1.5 Item 10 (optional half) — guard against EADDRINUSE-tsx-watch-race seen on the 2026-05-13 walk.
   # Bail loudly if port 8080 is already taken (three concurrent `tsx watch src/index.ts` chains were observed,
   # two failing EADDRINUSE and ALL of them spamming /tmp/humyn-api.log).
   set -euo pipefail

PORT="${PORT:-8080}"
   if lsof -nP -i ":${PORT}" -sTCP:LISTEN -t > /dev/null 2>&1; then
echo "ERROR: port ${PORT} already in use. Run 'lsof -i :${PORT}' to find the owner, then 'kill -TERM <pid>'." >&2
exit 1
fi

exec pnpm tsx watch src/index.ts "$@"

````
- `chmod +x apps/api/scripts/dev.sh` (the executor must `git update-index --chmod=+x` it on commit so the file flag is preserved).
3. Do NOT alter `apps/api/package.json`'s `dev` script (that's a separate decision for a follow-on); the `dev.sh` script is an opt-in guard the operator can invoke directly (`./apps/api/scripts/dev.sh > /tmp/humyn-api.log 2>&1 &`).
</action>
<verify>
 <automated>grep -c 'destination: 1' apps/api/src/plugins/logger.ts && test -x apps/api/scripts/dev.sh</automated>
</verify>
<acceptance_criteria>
 - `grep -c 'destination: 1' apps/api/src/plugins/logger.ts` returns ≥1
 - `apps/api/scripts/dev.sh` exists and is executable (`test -x` returns 0)
 - `grep -c "lsof -nP -i" apps/api/scripts/dev.sh` returns ≥1 (the port guard)
 - on-device runbook verification (not automated): `pnpm --filter @humyn/api dev > /tmp/humyn-api.log 2>&1 &` + `curl http://localhost:8080/healthz` → `/tmp/humyn-api.log` contains a `"req":{"method":"GET","url":"/healthz"...}` line (or its pino-pretty rendering) within 1s
</acceptance_criteria>
<done>Dev pino transport pinned to fd 1; `/tmp/humyn-api.log` captures live request lines under `pnpm dev`-redirect; `dev.sh` port-guard available as opt-in.</done>
</task>

<!-- ========================================================================= -->
<!-- WAVE 1.5b — after 1.5a (same UploadRow / queue model)                      -->
<!-- Items 2 / 7 / 8                                                            -->
<!-- ========================================================================= -->

<task type="auto" tdd="true">
<name>Task 5 (Wave 1.5b) — UploadRow.fromJson migration persists back to disk (atomic write-back)</name>
<wave>1.5b</wave>
<files>apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt, apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadQueueStore.kt, apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadQueueStoreTest.kt</files>
<read_first>
 - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt (after Task 1's changes — the 4 per-route fields)
 - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadQueueStore.kt (FULL — the `read()` / `upsert(row)` / `bootstrap(currentSub)` API; the atomic-rename write pattern)
 - apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadQueueStoreTest.kt (the `fromJson` migration tests; the file-system helpers)
 - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-COSMETIC-GAPS.md Item 7 ("Idempotency-key migration mints a fresh UUID per `read()`, doesn't persist back")
</read_first>
<behavior>
 - `UploadRow.fromJson` (Task 1 already mints fresh UUIDv4s for missing per-route fields). Add an INTERNAL `migrated: Boolean` companion-level signal so `rowsFromJsonString` / the queue-store `read()` can detect it.
 - Option A (preferred — minimal API change): change `UploadRow.fromJson` to return `Pair<UploadRow, Boolean>` (the row + a `migrated` flag); update the two call sites (`rowsFromJsonString` in UploadModels.kt and any direct caller — there shouldn't be one) accordingly. `rowsFromJsonString` returns `List<UploadRow>` unchanged but EXPORTS a sibling `rowsFromJsonStringWithMigration(text): Pair<MutableList<UploadRow>, Boolean>` for `UploadQueueStore.read()`.
 - Option B (simpler — internal-state on the row): add `internal var _migratedOnLoad: Boolean = false` to `UploadRow` (not persisted to toJson — it's a transient signal); `fromJson` sets it to `true` whenever any of the 4 fields was minted from a missing value. `UploadQueueStore.read()` checks the flag and persists back via `upsert(row)` for each migrated row before returning. Clear `_migratedOnLoad = false` on the row after upsert.
 - **Choose Option B** (less API churn; keeps `fromJson` signature stable). Document the field as transient + `@Transient`-equivalent: do not serialize. Add a Kotlin comment: "Not persisted — a one-shot in-memory signal from `fromJson` to `UploadQueueStore.read()` that this row was migrated and must be written back to disk so subsequent reads return the same keys (Wave-1.5 Item 7)."
 - `UploadQueueStore.read()`: after parsing, iterate the rows; for any row with `_migratedOnLoad == true`, call `upsert(row)` (which is the existing atomic-rename writer), then set the flag to `false`. The upsert is a no-op for any unchanged row; for a migrated row, it writes the 4 minted keys to disk.
 - Test: write a `queue.json` with one row that has NO `*IdempotencyKey` fields (the pre-commit `5c0b2d8` shape), call `store.read()` twice, assert the second read returns the same 4 keys as the first (because the first read wrote them back). Also assert the `queue.json` file on disk now contains all 4 fields after the first read.
</behavior>
<action>
1. `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt`:
- Add to `UploadRow`: `internal var _migratedOnLoad: Boolean = false` (with Kotlin doc-comment per Behaviour). Do NOT include it in `toJson`. Do NOT mention it in equals/hashCode (it's transient).
- In `fromJson` (Task 1's reworked version), after computing each of the 4 keys: if ANY was minted (i.e. the source field was missing/blank), set `row._migratedOnLoad = true` BEFORE returning. (Use a local `var migrated = false` and OR in each branch.)
2. `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadQueueStore.kt`:
- In `read(): MutableList<UploadRow>` (or the equivalent method that returns the deserialized rows), after parsing: iterate the resulting list; for each row where `row._migratedOnLoad == true`, call `upsert(row)` to atomic-write the row back; then set `row._migratedOnLoad = false`. Log once per migrated row: `Log.i(TAG, "row ${row.recordingId} idempotency-key migration persisted (Wave-1.5 Item 7)")`. (If `read()` is called recursively from `upsert(row)` — review the file — guard against re-entry by setting the flag to false BEFORE the upsert call so the upserted row, when re-read, isn't re-migrated.)
- The atomic-write pattern (write to `queue.json.tmp` + rename) is already in `upsert` — do not introduce a new one. Confirm in the read.
3. `apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadQueueStoreTest.kt`:
- Add test `read() persists migrated idempotency keys back to disk so subsequent reads return the same keys`:
  - Write a `queue.json` containing one row with NO per-route key fields (legacy shape).
  - Call `store.read()` once, capture the 4 keys.
  - Re-read the on-disk `queue.json` (raw `JSONObject`) and assert all 4 `*IdempotencyKey` fields are now present with the captured values.
  - Call `store.read()` again, assert the same 4 keys are returned (no fresh mint).
- Add test `read() does NOT re-write a row that already has all 4 per-route keys (no migrated upsert)`: write a row with all 4 fields, call `read()`, assert no log line is emitted and the file mtime is preserved (mtime check is OS-dependent — instead assert via a wrapped/spied `upsert` that it's NOT called for this row).
</action>
<verify>
 <automated>cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests "ai.humynlabs.capture.upload.UploadQueueStoreTest"</automated>
</verify>
<acceptance_criteria>
 - `grep -c '_migratedOnLoad' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt` returns ≥2 (decl + assignment in fromJson)
 - `grep -c '_migratedOnLoad' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadQueueStore.kt` returns ≥1 (the check-and-upsert in `read()`)
 - `[ "$(grep -c '_migratedOnLoad' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt)" -ge 2 ]` — UploadModels.kt MUST reference `_migratedOnLoad` at least twice (the field + at least one set/read site)
 - `[ "$(grep -c '_migratedOnLoad' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt)" -eq 0 ]` — UploadCoordinator.kt MUST NOT reference `_migratedOnLoad` (it is a store-internal signal owned by UploadQueueStore.read())
 - `cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests "ai.humynlabs.capture.upload.UploadQueueStoreTest"` exits 0; the new `read() persists migrated ...` test passes
</acceptance_criteria>
<done>Migrated rows are written back to disk by the queue-store `read()`; subsequent reads return the same keys; no per-`read()` mint storm; tests green.</done>
</task>

<task type="auto" tdd="true">
<name>Task 6 (Wave 1.5b) — Retry on client-side DEAD_LETTER does LOCAL reset (no POST /reupload)</name>
<wave>1.5b</wave>
<files>apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt, apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadCoordinatorTest.kt</files>
<read_first>
 - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt lines 225-260 (the current `reupload(recordingId)` @ReactMethod — full reset path)
 - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt lines 252-294 (the `wasReupload = row.reupload` capture + the `when` branching to `postReupload` / `postRePresign` / `postInit`)
 - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt — the `UploadState` enum, `reupload: Boolean` field, `uploadId` nullable field
 - apps/api/src/routes/recordings/reupload.ts lines 125-138 (the server-side `if (rec.qaStatus !== 'hash-mismatch' || !canTransition(...)) { 409 Cannot re-upload from state ${rec.qaStatus} }` — the contract that REFUSES a pending row)
 - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-COSMETIC-GAPS.md Item 2 ("Retry affordance on a client-side dead-lettered row calls POST /recordings/:id/reupload → 409")
 - .planning/runbooks/05-upload-smoke.md §3 (the dead-letter Retry affordance line — both scenarios; needs the wording split note for Item 11 in Task 9 below)
</read_first>
<behavior>
 - The current `reupload(recordingId)` body (HumynUploadModule.kt:235-260) ALWAYS does the full reset (sets `row.reupload = true`, state PENDING, clears uploadId/imuUploadId/parts/etags/metadataPut/deadLetterReason, then drains). This is correct ONLY for the worker-fired re-upload path (server-side `qa_status='hash-mismatch'`).
 - Branch the handler on row.state + the existing-row signature:
   - **Client-side DEAD_LETTER** (`row.state == UploadState.DEAD_LETTER && row.uploadId != null && row.reupload == false`): LOCAL reset.
     - `row.state = UploadState.UPLOADING`
     - `row.deadLetterReason = null`
     - LEAVE `row.uploadId`, `row.imuUploadId`, `row.videoParts`, `row.imuParts`, `row.metadataPut` UNCHANGED (preserve DONE parts' ETags; UP-04 CR-02 idempotent re-presign path).
     - `queueStore.upsert(row)`; `emitQueueChanged()`; `coordinator.drain()`; `signalUploadActiveBestEffort()`.
     - The drainer's `when` (UploadCoordinator.kt:276-280) reads `row.uploadId != null && !row.reupload` → takes the `postRePresign` branch (`POST /recordings/:id/parts`) — re-presigns video+IMU against the existing `uploadId`/`imuUploadId`, every already-DONE part keeps its ETag.
   - **Worker-fired re-upload OR mid-flight retry** (state ∈ {PENDING, UPLOADING, FINALIZING, AWAITING_VERIFY}, OR DEAD_LETTER with row.reupload already true from a prior worker event): EXISTING full reset (state PENDING, reupload=true, clear uploadId/imuUploadId/parts/etags/metadataPut/deadLetterReason, drain). The server contract accepts this — it's hash-mismatch + canTransition('hash-mismatch','pending') = true.
   - **VERIFIED**: no-op (resolve null) — already done.
   - **(default — null row)**: `promise.resolve(null)` (existing).
 - Update the @ReactMethod's KDoc to explain the two branches + cite Wave-1.5 Item 2 + the server-side `qa_status` guard in `reupload.ts:125-138` + the 2026-05-13 walk observation (recording `01KRFXGAWCMVQ89PJ2PBXSVAKK`).
</behavior>
<action>
1. `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt`:
- Lines 235-260 (`@ReactMethod fun reupload(recordingId: String, promise: Promise)`):
  - Replace the body with the branching logic per Behaviour:
    ```kotlin
    val row = queueStore.read().find { it.recordingId == recordingId }
    if (row == null) {
      promise.resolve(null)
      return@execute
    }
    when {
      row.state == UploadState.VERIFIED -> {
        promise.resolve(null)
        return@execute
      }
      row.state == UploadState.DEAD_LETTER && row.uploadId != null && !row.reupload -> {
        // Wave-1.5 Item 2 — client-side dead-letter (server-side qa_status='pending' with a live s3UploadId).
        // POST /recordings/:id/reupload would 409 (server only accepts hash-mismatch rows). LOCAL reset:
        // state→UPLOADING, clear deadLetterReason, KEEP uploadId/imuUploadId/parts/etags/metadataPut so
        // the drainer takes the postRePresign branch (/parts) and re-presigns against the live upload —
        // already-DONE parts keep their ETags (UP-04, CR-02 idempotent re-presign).
        row.state = UploadState.UPLOADING
        row.deadLetterReason = null
      }
      else -> {
        // Worker-fired re-upload (server qa_status='hash-mismatch') OR retry of a non-dead-lettered row.
        // Full reset — the drainer takes the postReupload branch (POST /recordings/:id/reupload).
        row.reupload = true
        row.state = UploadState.PENDING
        row.uploadId = null
        row.imuUploadId = null
        row.metadataPut = PartStatus.PENDING
        row.deadLetterReason = null
        for (p in row.videoParts) { p.status = PartStatus.PENDING; p.etag = null; p.retryCount = 0 }
        for (p in row.imuParts) { p.status = PartStatus.PENDING; p.etag = null; p.retryCount = 0 }
      }
    }
    queueStore.upsert(row)
    emitQueueChanged()
    runCatching { coordinator.drain() }
    signalUploadActiveBestEffort()
    promise.resolve(null)
    ```
- Update the KDoc above the method (lines 225-233) to spell out the two branches.
2. `apps/mobile/android/app/src/test/java/ai/humynlabs/capture/upload/UploadCoordinatorTest.kt` (the same MockWebServer harness):
- Add test `reupload on a client-side DEAD_LETTER row does LOCAL reset and routes to /parts (not /reupload)`:
  - Seed a row with `state=DEAD_LETTER, uploadId="upload-abc", imuUploadId="upload-imu-xyz", reupload=false, deadLetterReason="some-transient"`; pre-mark every part DONE with an ETag.
  - Construct an instance of `HumynUploadModule` (or — simpler — drive the same logic by calling a refactored helper if the executor extracts the branch into a `UploadQueueStore.applyRetry(row)` for testability; otherwise use Robolectric's bridge harness).
  - Drive `reupload(recordingId)`; capture `idempotencyKeysByPath` after the drain settles.
  - Assert: `idempotencyKeysByPath["/parts"]` is non-null; `idempotencyKeysByPath["/reupload"]` is null (server `/reupload` was NEVER called); the row's `uploadId` and all parts' ETags are preserved on disk; `row.state` ends `AWAITING_VERIFY` after a successful drain (or `UPLOADING` mid-drain if the dispatcher delays).
- Add test `reupload on a worker-flagged DEAD_LETTER (row.reupload=true) calls /reupload`:
  - Seed a row with `state=DEAD_LETTER, uploadId="upload-abc", reupload=true`.
  - Drive `reupload(recordingId)`; assert `idempotencyKeysByPath["/reupload"]` is non-null AND uses `row.reuploadIdempotencyKey`; assert the row was full-reset (`uploadId=null` on entry to the drain — captured in the dispatcher's pre-init view; or `reupload=true` at the point of the /reupload POST).
- Add test `reupload on a VERIFIED row is a no-op`: seed VERIFIED; drive `reupload(...)`; assert no `idempotencyKeysByPath["*"]` was written.
</action>
<verify>
 <automated>cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests "ai.humynlabs.capture.upload.UploadCoordinatorTest"</automated>
</verify>
<acceptance_criteria>
 - `grep -c 'LOCAL reset' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt` returns ≥1
 - `grep -c 'row.state == UploadState.DEAD_LETTER && row.uploadId != null && !row.reupload' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt` returns ≥1
 - `grep -c 'row.state = UploadState.UPLOADING' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt` returns ≥1 (the LOCAL-reset assignment; existing full-reset path uses PENDING)
 - `cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests "ai.humynlabs.capture.upload.UploadCoordinatorTest"` exits 0; the three new tests pass
</acceptance_criteria>
<done>HumynUpload.reupload branches between LOCAL reset (client-side DEAD_LETTER) and full reset (worker-fired); LOCAL reset preserves uploadId+ETags and routes to /parts via the drainer; the 409 from `POST /reupload` on a pending row cannot recur; tests green.</done>
</task>

<task type="auto" tdd="true">
<name>Task 7 (Wave 1.5b) — Cold-start drain on stale queue (new HumynUpload.drainNow @ReactMethod bridge)</name>
<wave>1.5b</wave>
<files>apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt, apps/mobile/src/native/HumynUpload.ts, apps/mobile/src/services/uploadReconcile.ts, apps/mobile/__tests__/services/uploadReconcile.test.tsx</files>
<read_first>
 - apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt — full file (the @ReactMethod pattern; existing `resume()` at line 175 + `enqueue()` kicks `coordinator.drain()`)
 - apps/mobile/src/native/HumynUpload.ts — FULL file (the typed bridge interface, the `*Safe` boot-safe wrappers, the `ensure()` helper)
 - apps/mobile/src/services/uploadReconcile.ts — FULL file (the `reconcileOnce()` flow, the `pushUploadContext()` + `getQueueSafe()` pattern)
 - apps/mobile/__tests__/components/Toast.test.tsx — the existing Vitest-with-jest-mock-of-HumynUpload pattern (the `jest.mock('../../src/native/HumynUpload', ...)` template)
 - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-COSMETIC-GAPS.md Item 8 ("Cold-start drain on a stale-on-disk queue")
</read_first>
<behavior>
 - New native @ReactMethod `drainNow(promise: Promise)` on `HumynUploadModule`: identical to existing `resume()` but does NOT touch `UploadControlState.setPaused(false)` — purely kicks `coordinator.drain()`. Use `bgExecutor.execute { ... }` (same pattern). If the coordinator is paused, the drain is a no-op (the coordinator re-checks `isPaused()` at line 186) — correct: a pause is sticky until `resume()`, and a boot-time drain should not unpause.
 - New typed JS bridge surface `HumynUpload.drainNow()` + `HumynUpload.drainNowSafe()` (mirroring `getQueueSafe` / `setUploadContextSafe`). `drainNowSafe()` swallows on no-module-registered (JSDOM / a build without it).
 - `installUploadReconcile()`'s `reconcileOnce()` (line 74-116) — after `pushUploadContext()` (line 77), before the `apiClient.get('/recordings/verified-ids')` call (line 81): try to read `HumynUpload.getQueueSafe()`; if any row's state is `'pending'` or `'uploading'` → `await HumynUpload.drainNowSafe()`. Wrap in try/catch (boot-safe).
 - The kick has to come AFTER pushUploadContext (the drainer needs `getCurrentSub()`); a kick before that returns immediately via `getCurrentSub() == null`.
 - Test: a Vitest test that mocks `HumynUpload` to return a queue with a `pending` row, calls `reconcileOnce()`, asserts `drainNowSafe` was called. A second mock with only `awaiting-verify`/`verified` rows asserts `drainNowSafe` was NOT called.
</behavior>
<action>
1. `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt`:
- Add the new @ReactMethod just after `resume(promise)` (line 187):
  ```kotlin
  /**
   * Wave-1.5 Item 8 — cold-start drain kick. Used by `installUploadReconcile()` on boot:
   * if `getQueueSafe()` returns a row in {PENDING, UPLOADING}, JS calls `drainNow()` to wake
   * the drainer. Distinct from `resume()`: does NOT flip `UploadControlState.setPaused(false)`
   * — a pause is sticky and a boot-time drain should not silently unpause uploads. If the
   * coordinator is paused the drain is a no-op (UploadCoordinator.kt:186); otherwise it
   * iterates the queue exactly like an `enqueue()` kick would.
   */
  @ReactMethod
  fun drainNow(promise: Promise) {
    bgExecutor.execute {
      try {
        runCatching { coordinator.drain() }
        promise.resolve(null)
      } catch (t: Throwable) {
        promise.reject("UPLOAD_DRAIN_NOW_FAILED", t.message ?: "drainNow failed", t)
      }
    }
  }
  ```
2. `apps/mobile/src/native/HumynUpload.ts`:
- In `interface HumynUploadNativeModule` (around line 73), add:
  ```ts
  /** Wave-1.5 Item 8 — boot-time drain kick for installUploadReconcile (no unpause). */
  drainNow(): Promise<void>;
  ```
- In the `HumynUpload` const facade (around line 155, near `reupload`):
  ```ts
  /** Wave-1.5 Item 8 — kick the drainer (no unpause). Throws if the module is absent. */
  drainNow: (): Promise<void> => ensure().drainNow(),
  /** Boot-safe drainNow — never throws; no-op when the module is absent. */
  drainNowSafe: async (): Promise<void> => {
    try {
      await ensure().drainNow();
    } catch {
      /* no native module / JSDOM — non-fatal */
    }
  },
  ```
3. `apps/mobile/src/services/uploadReconcile.ts`:
- In `reconcileOnce()` (line 74), after `await pushUploadContext()` (line 77) and BEFORE the existing `try { const since = ... }` block (line 78), add:
  ```ts
  // Wave-1.5 Item 8 — cold-start drain on stale queue. installApk re-launch after a force-quit
  // leaves a row in {pending, uploading} on disk; nothing else kicks the drainer (enqueue / JWT
  // change / RecordingScreen.resume / Pending-Uploads Retry are all user-driven), so the row
  // sits forever. Kick the drainer if there's stale work. Wrapped in try/catch + boot-safe via
  // drainNowSafe.
  try {
    const queue = await HumynUpload.getQueueSafe();
    const hasStale = queue.some((r) => r.state === 'pending' || r.state === 'uploading');
    if (hasStale) {
      await HumynUpload.drainNowSafe();
    }
  } catch {
    /* boot-safe — never crash the reconcile sweep over a queue read */
  }
  ```
4. `apps/mobile/__tests__/services/uploadReconcile.test.tsx` (NEW FILE):
- Mock `HumynUpload` (the typed JS facade) with jest.mock. Spy on `drainNowSafe`.
- Test 1 — `reconcileOnce kicks drainNow when a pending row is on disk`: `HumynUpload.getQueueSafe.mockResolvedValue([{ recordingId:'r1', ownerUserId:'u', state:'pending', ... }])`; mock `apiClient.get` to return `{ ids:[], next_cursor:null }`; call `reconcileOnce()`; assert `drainNowSafe` was called.
- Test 2 — `reconcileOnce does NOT kick drainNow when only awaiting-verify/verified rows are queued`: getQueueSafe returns one `'awaiting-verify'` row; assert `drainNowSafe` was NOT called.
- Test 3 — `reconcileOnce is boot-safe — getQueueSafe throws does not crash`: getQueueSafe throws; assert `reconcileOnce()` resolves without throwing AND `drainNowSafe` was NOT called.
- Mirror the existing JSDOM-test setup in `apps/mobile/__tests__/components/Toast.test.tsx` (the React imports / jest.mock conventions). Use the same `apiClient` mock pattern if other reconcile-adjacent tests have one — else mock the `apiClient` import directly.
</action>
<verify>
 <automated>cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests "ai.humynlabs.capture.upload.*" && cd /Users/adnaan/Documents/hl-homelander/apps/mobile && pnpm vitest run __tests__/services/uploadReconcile.test.tsx</automated>
</verify>
<acceptance_criteria>
 - `grep -c 'fun drainNow' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt` returns ≥1
 - `grep -c 'drainNow(): Promise<void>' apps/mobile/src/native/HumynUpload.ts` returns ≥1 (the interface)
 - `grep -c 'drainNowSafe' apps/mobile/src/native/HumynUpload.ts` returns ≥1 (the boot-safe facade)
 - `grep -c 'drainNowSafe' apps/mobile/src/services/uploadReconcile.ts` returns ≥1 (the call site in reconcileOnce)
 - `cd apps/mobile && pnpm vitest run __tests__/services/uploadReconcile.test.tsx` exits 0
 - `cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests "ai.humynlabs.capture.upload.*"` exits 0
</acceptance_criteria>
<done>HumynUpload.drainNow() bridge exists; reconcileOnce kicks it when stale {pending,uploading} rows are on disk; tests cover the kick + the no-kick + the boot-safe paths.</done>
</task>

<!-- ========================================================================= -->
<!-- WAVE 1.5c — JS-side. Sub-divided into 1.5c-1 → 1.5c-2 → 1.5c-3 (SERIAL).   -->
<!-- All three Tasks 8 / 9 / 10 modify HomeSkeletonScreen.tsx; Tasks 8 + 10    -->
<!-- both modify HomeSkeletonScreen.test.tsx — NOT parallel-safe. Preserve     -->
<!-- one-commit-per-item: chore(05): progress / chore(05): toast /             -->
<!-- chore(05): routing — three serial commits, NOT one batched commit.        -->
<!-- Task 11 (runbook §3) stays parallel-safe (docs-only, different file).     -->
<!-- Items 4 / 5 / 6 / 11                                                       -->
<!-- ========================================================================= -->

<task type="auto" tdd="true">
<name>Task 8 (Wave 1.5c-1) — Live progress bar on Home tile + Pending Uploads (chip percent + sibling determinate fill)</name>
<wave>1.5c</wave>
<sub_wave>1.5c-1</sub_wave>
<files>apps/mobile/src/screens/uploads/PendingUploadsScreen.tsx, apps/mobile/src/screens/home/HomeSkeletonScreen.tsx, apps/mobile/__tests__/screens/PendingUploadsScreen.test.tsx, apps/mobile/__tests__/screens/HomeSkeletonScreen.test.tsx</files>
<read_first>
 - apps/mobile/src/screens/uploads/PendingUploadsScreen.tsx FULL — note lines 138-148 (onUploadProgress → progressById state), lines 156-200 (renderRow with chipRow), lines 222-296 (StyleSheet — the existing token palette: `colors.surface`, `colors.line`, `colors.chipProgressText` etc.)
 - apps/mobile/src/screens/home/HomeSkeletonScreen.tsx lines 130-175 (the pending-uploads-tile + per-tile-row layout; the existing `chipVariantFor` + `<UploadStatusChip variant={chipVariantFor(row)} />` render at line ~169 — currently no `percent` prop is passed)
 - apps/mobile/src/components/UploadStatusChip.tsx FULL — confirms `percent` prop already supported (line 64-68)
 - design-spec.md / prototype.html — no new tokens / no new colors (D-10/D-10a). Reuse `colors.line` (track) + `colors.chipProgressText` (fill).
 - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-COSMETIC-GAPS.md Item 4 ("Live progress bar while a video is uploading")
 - Recent commit `f0a8a08` ("add live progress bar (Home tile + Pending Uploads screen) to Wave-1.5") — already added a basic bar; this task EXTENDS it per the §2 spec wording and confirms on-device cadence wiring is complete.
</read_first>
<behavior>
 - PendingUploadsScreen.tsx (the standalone screen): chip already shows `Uploading… N%` via `<UploadStatusChip percent={pct} />` (line 177) — no change needed there. ADD a sibling determinate progress bar BELOW the chipRow when `isActive` AND `pct != null`:
   ```tsx
   {isActive && pct != null ? (
     <View accessibilityLabel="pending-upload-progress-track" style={styles.progressTrack}>
       <View
         accessibilityLabel="pending-upload-progress-fill"
         style={[styles.progressFill, { width: `${Math.max(0, Math.min(100, Math.round(pct)))}%` }]}
       />
     </View>
   ) : null}
   ```
 - StyleSheet additions in PendingUploadsScreen:
   ```ts
   progressTrack: {
     height: 3,
     backgroundColor: colors.line,
     borderRadius: 999,
     marginTop: spacing.s,
     overflow: 'hidden',
   },
   progressFill: {
     height: 3,
     backgroundColor: colors.chipProgressText,
     borderRadius: 999,
   },
   ```
 - HomeSkeletonScreen.tsx (the tile): mirror PendingUploads — confirm the tile-row's `<UploadStatusChip>` is passed `percent` (per the f0a8a08 commit; if the commit only added the bar without the percent prop, ADD the prop now) AND a sibling determinate fill, same accessibilityLabels (`pending-uploads-tile-progress-track` / `-fill`), same `colors.line` + `colors.chipProgressText` palette. The tile-row body's `<View>` layout already includes a name + meta line — add the track below the chip render at line ~169. Subscribe to `onUploadProgress` in the tile if not already done — check the existing tile's effect; if it doesn't subscribe, add a `useEffect` mirroring `PendingUploadsScreen`'s lines 124-148 (with cleanup `.remove()`).
 - Verify the native side ACTUALLY fires `onUploadProgress` at sensible cadence: this is the existing `maybeEmitProgress(row, totalBytes)` in `UploadCoordinator.kt:392-400`, debounced to ≤ once / 5 s (`PROGRESS_DEBOUNCE_MS = 5_000`). On-device verification is part of the §2 re-walk — not a Wave-1.5 plan task — but the executor SHOULD `adb logcat -s HumynUploadCoord:* RCTDeviceEventManager:* | grep -i 'onUploadProgress'` to confirm events reach the JS bridge during the post-build smoke run.
 - Vitest tests cover the render + the cadence shape.
</behavior>
<action>
1. `apps/mobile/src/screens/uploads/PendingUploadsScreen.tsx`:
- In `renderRow` (lines 156-199), after the existing `<View style={styles.chipRow}>...</View>` block (around line 188), insert the conditional progress track/fill `<View>` per Behaviour. Use `isActive` (already computed at line 159) AND `pct != null` (already computed at line 160) as the gate. Wrap the chipRow + the new track in a parent `<View style={styles.body}>` if not already there (line 169 already wraps in `<View style={styles.body}>` — keep that).
- StyleSheet (lines 222-296): add `progressTrack` + `progressFill` keys with the values from Behaviour. Place them above the `deadReason` style key (alphabetic-ish ordering ok; or after `retryLabel`).
2. `apps/mobile/src/screens/home/HomeSkeletonScreen.tsx`:
- Around line 169 (the per-tile-row `<UploadStatusChip variant={chipVariantFor(row)} />`):
  - Read the existing tile-row layout. Confirm whether commit `f0a8a08` already passes `percent` to the chip + renders a bar. If `percent` is NOT yet passed: change the render to `<UploadStatusChip variant={chipVariantFor(row)} {...(row.state === 'uploading' && progressById[row.recordingId] != null ? { percent: progressById[row.recordingId] } : {})} />` (mirror PendingUploads's line 177 pattern).
  - If the bar is NOT yet a sibling: add a `<View accessibilityLabel="pending-uploads-tile-progress-track">...<View ... /></View>` BELOW the chip, gated by `row.state === 'uploading' && progressById[row.recordingId] != null`. Same dimensions + colors as PendingUploads.
- At the Home screen state level (top of `HomeSkeletonScreen` function): if there's no existing `progressById` state + `onUploadProgress` subscription, add one — mirror PendingUploadsScreen lines 122-148, including the `.remove()` cleanup.
- StyleSheet: add the same `progressTrack` + `progressFill` keys (with the tile-appropriate dimensions if different — keep dimensions consistent with PendingUploads unless the tile's row size demands a different height; the §2 spec doesn't dictate a specific pixel — use the same `height: 3` for visual coherence).
3. `apps/mobile/__tests__/screens/PendingUploadsScreen.test.tsx` (NEW FILE or EXTEND existing — check first):
- `progress bar renders with the correct width when an uploading row has a progress event`: render with `__test_rows=[{...row, state:'uploading'}]`; fire a synthetic `onUploadProgress` event via the NativeEventEmitter mock at 47% (`bytesUploaded: 47, bytesTotal: 100`); assert `getByAccessibilityLabel('pending-upload-progress-fill').props.style[1].width === '47%'`.
- `progress bar does NOT render for awaiting-verify / verified / dead-letter rows`: render with one row in each state; assert `queryByAccessibilityLabel('pending-upload-progress-fill')` is null for each.
- `chip shows "Uploading… 47%" when progress is present`: same setup; assert the chip label text contains `47%`.
4. `apps/mobile/__tests__/screens/HomeSkeletonScreen.test.tsx` (NEW or EXTEND):
- Mirror the PendingUploads tests but against the tile: assert `pending-uploads-tile-progress-fill` width === `'47%'` when an `uploading` row has a progress event AND the tile's chip label contains `47%`.
</action>
<verify>
 <automated>cd apps/mobile && pnpm vitest run __tests__/screens/PendingUploadsScreen.test.tsx __tests__/screens/HomeSkeletonScreen.test.tsx</automated>
</verify>
<acceptance_criteria>
 - `grep -c 'pending-upload-progress-fill' apps/mobile/src/screens/uploads/PendingUploadsScreen.tsx` returns ≥1
 - `grep -c 'pending-uploads-tile-progress-fill\|pending-upload-progress-fill' apps/mobile/src/screens/home/HomeSkeletonScreen.tsx` returns ≥1
 - `grep -c 'percent={' apps/mobile/src/screens/home/HomeSkeletonScreen.tsx` returns ≥1 (chip percent prop passed)
 - `grep -c 'colors\.chipProgressText\|chipProgressText' apps/mobile/src/screens/uploads/PendingUploadsScreen.tsx` returns ≥1 (no new tokens introduced)
 - `cd apps/mobile && pnpm vitest run __tests__/screens/PendingUploadsScreen.test.tsx __tests__/screens/HomeSkeletonScreen.test.tsx` exits 0
</acceptance_criteria>
<done>
 - Both Home tile + Pending Uploads show the chip percent + sibling determinate bar for uploading rows; no new design tokens; tests green; on-device cadence is the existing 5s debounce (verified during §2 re-walk).
 - If commit `f0a8a08` ("chore(05): add live progress bar (Home tile + Pending Uploads screen) to Wave-1.5") already delivered the chip percent + the design-token-aligned bar with the testID `pending-upload-progress-fill`, Task 8 is a no-op CONFIRMATION (commit a thin `chore(05): wave-1.5 progress bar confirmation` to leave a trail). The grep-verifiable acceptance criteria above still apply regardless.
</done>
</task>

<task type="auto">
<name>Task 9 (Wave 1.5c-2) — Contribution toast → global ToastHost via uploadToastBus (≥5s lifetime, survives screen transition)</name>
<wave>1.5c</wave>
<sub_wave>1.5c-2</sub_wave>
<depends_on_task>8</depends_on_task>
<files>apps/mobile/src/state/uploadToastBus.ts, apps/mobile/src/screens/recording/RecordingScreen.tsx, apps/mobile/src/screens/home/HomeSkeletonScreen.tsx, apps/mobile/__tests__/state/uploadToastBus.test.ts</files>
<read_first>
 - apps/mobile/src/components/Toast.tsx — full file (the imperative `showToast(text, durationMs?)` API; the global `<ToastHost />` host already mounted in App.tsx:78)
 - apps/mobile/src/boot/bootRecoveryListener.ts — the deliver-once-on-Home pattern + the `RECOVERY_TOAST_MS = 5_000` template
 - apps/mobile/src/screens/recording/RecordingScreen.tsx lines 195-203 (the LOCAL `showToast` in-screen state — what was killing the toast on the transition) and lines 404-417 (the post-≥60s branch with `showToast(contributionText)` + `navigateToHome`)
 - apps/mobile/src/screens/home/HomeSkeletonScreen.tsx — find the existing Home `useEffect` block (mounts subscriptions); the bus drain will be a sibling effect there
 - apps/mobile/App.tsx line 78 — confirms `<ToastHost />` IS the global navigator-sibling host already
 - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-COSMETIC-GAPS.md Item 5 ("Contribution toast killed by RecordingScreen → Home transition")
</read_first>
<behavior>
 - **Structural finding:** `<ToastHost />` is ALREADY a global navigator-sibling host (App.tsx:78). The contribution toast dies on the transition because RecordingScreen has its OWN in-screen `<Toast>`-like component (RecordingScreen.tsx:195-203 — a local `useState` + `setTimeout(3000)` host distinct from the global one) AND because the duration is 3s. The fix is two-pronged:
   1. Route the contribution-toast call through the GLOBAL `showToast(text, 5_000)` from `components/Toast`, with a 5_000ms duration.
   2. Defer the call until AFTER `navigateToHome(navigation)` so the toast fires on Home (mirrors the bootRecoveryListener.ts deliver-on-Home pattern). Use a single-shot `uploadToastBus` module-level holder + a Home-mount one-shot effect.
 - New module `apps/mobile/src/state/uploadToastBus.ts`:
   ```ts
   // uploadToastBus — one-shot message holder for the post-recording contribution toast (Wave-1.5 Item 5).
   // Pattern mirrors bootRecoveryListener's deliver-on-Home pattern. Module-level state (not Zustand) —
   // we don't need re-renders, just a set/drain pair that's accessible from anywhere.

   type PendingToast = { text: string; durationMs: number };

   let pending: PendingToast | null = null;

   export function setPendingUploadToast(text: string, durationMs: number = 5_000): void {
     pending = { text, durationMs };
   }

   export function drainPendingUploadToast(): PendingToast | null {
     const taken = pending;
     pending = null;
     return taken;
   }

   // Test hatch.
   export function __test_resetUploadToastBus(): void {
     pending = null;
   }
   ```
 - `RecordingScreen.tsx` lines 404-417 (the ≥60s branch): right before `navigateToHome(navigation)` (line 417), call `setPendingUploadToast(toastText, 5_000)` instead of (or alongside) the local `showToast(toastText)`. The local `showToast` is a 3s in-screen visual; we replace it for THIS path because the screen is about to unmount. Keep the local `showToast` for the < 60s "Recording too short — discarded." case + the device-distress case (those are not Home-bound).
 - `HomeSkeletonScreen.tsx`: add a one-shot mount `useEffect`:
   ```ts
   useEffect(() => {
     const pending = drainPendingUploadToast();
     if (pending != null) {
       showToast(pending.text, pending.durationMs);
     }
   }, []);
   ```
 - The `<ToastHost />` global host (App.tsx:78) renders the toast over Home for the full 5s — confirmed by `Toast.tsx::showToast`'s implementation (the timer + `current` subscription model).
 - Test: a Vitest test that `setPendingUploadToast('foo', 5000)` then `drainPendingUploadToast()` returns the right shape, and a second drain returns null (one-shot).
</behavior>
<action>
1. NEW FILE `apps/mobile/src/state/uploadToastBus.ts` per Behaviour. Include the doc-comment up top citing Wave-1.5 Item 5 + the deliver-on-Home pattern.
2. `apps/mobile/src/screens/recording/RecordingScreen.tsx`:
- Add the import: `import { setPendingUploadToast } from '../../state/uploadToastBus';`
- In `handleStop` at the ≥60s branch (line 412-417), REPLACE the local `showToast(\`${formatContributionDuration(durationMs)} added to your contribution.\`)` call with:
  ```ts
  const text = stopErr
    ? 'Recording saved, but finalizing failed — it may not upload.'
    : `${formatContributionDuration(durationMs)} added to your contribution.`;
  setPendingUploadToast(text, 5_000);
  ```
  Then keep `navigateToHome(navigation)` (line 417) as-is. The local `showToast` here is removed for THIS branch only.
- Leave the local `showToast` call at line 439 (`'Recording stopped — your phone needs attention.'`) and line 446 (`'Recording too short — discarded.'`) UNCHANGED — those are RecordingScreen-local toasts that stay on the recording surface, not Home.
3. `apps/mobile/src/screens/home/HomeSkeletonScreen.tsx`:
- Add the imports: `import { drainPendingUploadToast } from '../../state/uploadToastBus';` and `import { showToast } from '../../components/Toast';`.
- Inside the `HomeSkeletonScreen` function, add a NEW mount-only `useEffect`:
  ```tsx
  useEffect(() => {
    const pending = drainPendingUploadToast();
    if (pending != null) {
      showToast(pending.text, pending.durationMs);
    }
  }, []);
  ```
  Place it near other mount effects.
4. `apps/mobile/__tests__/state/uploadToastBus.test.ts` (NEW FILE):
- `setPendingUploadToast / drainPendingUploadToast`:
  - Test 1: after `setPendingUploadToast('foo', 5000)`, `drainPendingUploadToast()` returns `{ text:'foo', durationMs:5000 }`.
  - Test 2: a second `drainPendingUploadToast()` returns `null` (one-shot).
  - Test 3: `setPendingUploadToast('foo')` with no duration → durationMs defaults to 5000.
  - Test 4: `__test_resetUploadToastBus()` clears any pending value.
  - Use `beforeEach(__test_resetUploadToastBus)` to keep tests isolated.
</action>
<verify>
 <automated>cd apps/mobile && pnpm vitest run __tests__/state/uploadToastBus.test.ts</automated>
</verify>
<acceptance_criteria>
 - `apps/mobile/src/state/uploadToastBus.ts` exists and exports `setPendingUploadToast`, `drainPendingUploadToast`, `__test_resetUploadToastBus`
 - `grep -c 'setPendingUploadToast' apps/mobile/src/screens/recording/RecordingScreen.tsx` returns ≥1 (the call site in the ≥60s branch)
 - `grep -c 'drainPendingUploadToast' apps/mobile/src/screens/home/HomeSkeletonScreen.tsx` returns ≥1 (the mount effect)
 - `grep -c 'showToast' apps/mobile/src/screens/home/HomeSkeletonScreen.tsx` returns ≥1 (the global host fire)
 - `cd apps/mobile && pnpm vitest run __tests__/state/uploadToastBus.test.ts` exits 0
</acceptance_criteria>
<done>Contribution toast survives RecordingScreen → Home for ≥5s via the uploadToastBus one-shot + the global ToastHost; the local in-screen toast stays for non-Home-bound paths; tests green.</done>
</task>

<task type="auto">
<name>Task 10 (Wave 1.5c-3) — Home pending-uploads-tile tap routes to History tab (not the orphan PendingUploads screen)</name>
<wave>1.5c</wave>
<sub_wave>1.5c-3</sub_wave>
<depends_on_task>9</depends_on_task>
<files>apps/mobile/src/screens/home/HomeSkeletonScreen.tsx, apps/mobile/__tests__/screens/HomeSkeletonScreen.test.tsx</files>
<read_first>
 - apps/mobile/src/screens/home/HomeSkeletonScreen.tsx lines 130-175 (the pending-uploads-tile + the outer `<Pressable onPress={() => navigation.navigate('PendingUploads')}>` at line 142)
 - apps/mobile/src/navigation/MainTabs.tsx — confirms `History` is a `<Tab.Screen>` registered at line 28
 - apps/mobile/src/navigation/RootNativeStack.tsx — confirms `PendingUploads` is a sibling Root screen at line 95 (stays registered for deep-link only)
 - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-COSMETIC-GAPS.md Item 6 ("Home → 'Pending Uploads' row tap routes to an orphan screen")
</read_first>
<behavior>
 - Home's pending-uploads-tile outer Pressable `onPress` (line 142) currently navigates to the standalone `PendingUploads` route at the Root-stack level — that route has no back nav and no tab bar (the user is stranded).
 - Owner preference: route the tap to the History tab (the natural home for the upload/contribution timeline) via React Navigation's nested-navigator API: `navigation.navigate('MainTabs', { screen: 'History' })`.
 - The standalone `PendingUploads` route STAYS registered in `RootNativeStack.tsx:95` (no deletion — it's available for future deep-link use `humyn://pending-uploads`); we just no longer route there from the Home tile.
 - Test: render Home with one pending row, fire `onPress` on `pending-uploads-tile`, assert `navigation.navigate` was called with `('MainTabs', { screen: 'History' })`.
</behavior>
<action>
1. `apps/mobile/src/screens/home/HomeSkeletonScreen.tsx`:
- Line 142: replace `onPress={() => navigation.navigate('PendingUploads')}` with:
  ```tsx
  onPress={() =>
    navigation.navigate('MainTabs', { screen: 'History' })
  }
  ```
- Update the top-of-file comment at line 11 (currently mentions "tapping the card navigates to PendingUploadsScreen ('PendingUploads')") to: "Tapping the card navigates to the History tab — the natural home for the upload/contribution timeline (Wave-1.5 Item 6). The standalone `PendingUploads` route stays registered (`RootNativeStack.tsx:95`) for deep-link use, but the Home-tile entry no longer routes there (it strands the user — no back nav, no tab bar)."
2. `apps/mobile/__tests__/screens/HomeSkeletonScreen.test.tsx`: add (or extend the Task-8 file if it's the same suite) the test `Home pending-uploads-tile tap routes to MainTabs → History`:
- Mock `navigation = { navigate: jest.fn() }`.
- Render Home with one pending row (`__test_rows` or seeded via the queue mock).
- Fire `fireEvent.press(getByAccessibilityLabel('pending-uploads-tile'))`.
- Assert `navigation.navigate` was called with `('MainTabs', { screen: 'History' })`.
3. No change to `RootNativeStack.tsx` (the PendingUploads route stays). No change to `MainTabs.tsx`.
</action>
<verify>
 <automated>cd apps/mobile && pnpm vitest run __tests__/screens/HomeSkeletonScreen.test.tsx</automated>
</verify>
<acceptance_criteria>
 - `grep -c "navigation.navigate('MainTabs', { screen: 'History' })" apps/mobile/src/screens/home/HomeSkeletonScreen.tsx` returns ≥1
 - `grep -c "navigation.navigate('PendingUploads')" apps/mobile/src/screens/home/HomeSkeletonScreen.tsx` returns 0 (the old route gone)
 - `grep -c 'PendingUploads' apps/mobile/src/navigation/RootNativeStack.tsx` returns ≥1 (the route stays registered — deep-link only)
 - `cd apps/mobile && pnpm vitest run __tests__/screens/HomeSkeletonScreen.test.tsx` exits 0
</acceptance_criteria>
<done>Home pending-uploads-tile tap routes to MainTabs/History; the standalone PendingUploads route stays registered as a deep-link target; tests green.</done>
</task>

<task type="auto">
<name>Task 11 (Wave 1.5c, parallel-safe) — Runbook §3 Retry-affordance wording split (worker-hash-mismatch path vs client-side dead-letter Retry path)</name>
<wave>1.5c</wave>
<sub_wave>1.5c-parallel</sub_wave>
<files>.planning/runbooks/05-upload-smoke.md</files>
<read_first>
 - .planning/runbooks/05-upload-smoke.md §3 line 59 (the current `(Sanity — the dead-letter "Retry" affordance hits the same path: ...)` bullet that conflates the two scenarios)
 - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-COSMETIC-GAPS.md Item 2 ("Plus: the runbook §3 wording needs to be split into two scenarios")
 - Task 6's branching contract in `HumynUploadModule.kt::reupload`
</read_first>
<action>
1. In `.planning/runbooks/05-upload-smoke.md` §3 (line 59 — the current `(Sanity — the dead-letter "Retry" affordance hits the same path: ...)` bullet), REPLACE that single bullet with TWO bullets:
- `- (Sanity — Path A — **worker-fired re-upload, after a server hash-mismatch**: if a row is in DEAD_LETTER because the worker flipped \`qa_status='hash-mismatch'\` and the \`re-upload\` event drove a full reset, the Pending Uploads row shows the \`chip-failed\` "Upload failed" chip + a "Retry" Pressable; tapping it calls \`HumynUpload.reupload(recordingId)\` — Path A — full reset (state→PENDING, reupload=true, clear uploadId/parts/etags), the drainer takes the \`postReupload\` branch (\`POST /recordings/:id/reupload\`, server accepts because qa_status='hash-mismatch'), re-upload from local copy.)`
- `- (Sanity — Path B — **client-side dead-letter, server-side qa_status='pending' still**: if a row is in DEAD_LETTER because of a transient PUT failure during the /init→parts phase (e.g. LocalStack tunnel missing — the 2026-05-13 walk pattern), the same chip-failed "Retry" Pressable calls \`HumynUpload.reupload(recordingId)\` — Path B — LOCAL reset (state→UPLOADING, deadLetterReason=null, KEEP uploadId/imuUploadId/parts/etags), the drainer takes the \`postRePresign\` branch (\`POST /recordings/:id/parts\` re-presigns video+IMU against the existing uploadId — already-DONE parts keep their ETags). The server's \`/reupload\` is NOT called (it would 409 — \`qa_status='pending'\` is non-resumable via that route). Wave-1.5 Item 2.)`
2. No other change to the runbook — Task 2 already handles §1 and §6.
</action>
<verify>
 <automated>grep -c 'Path A' .planning/runbooks/05-upload-smoke.md && grep -c 'Path B' .planning/runbooks/05-upload-smoke.md && grep -c 'worker-fired re-upload' .planning/runbooks/05-upload-smoke.md</automated>
</verify>
<acceptance_criteria>
 - `grep -c 'Path A' .planning/runbooks/05-upload-smoke.md` returns ≥1
 - `grep -c 'Path B' .planning/runbooks/05-upload-smoke.md` returns ≥1
 - `grep -c 'worker-fired re-upload' .planning/runbooks/05-upload-smoke.md` returns ≥1
 - `grep -c 'client-side dead-letter' .planning/runbooks/05-upload-smoke.md` returns ≥1
</acceptance_criteria>
<done>§3 spells out the two Retry-affordance scenarios separately, matching the Task-6 branching contract; no other runbook section touched.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                          | Description                                                                                                                                                                                              |
| ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client → API POST (`/init`, `/parts`, `/finalize`, `/reupload`)   | per-request `Idempotency-Key` UUIDv4; same key + different body ⇒ 409. The bug being closed: a single per-row key + 4 different request bodies hits the 409. Fix: 4 per-route keys, no cross-route reuse |
| `HumynUpload.reupload(recordingId)` JS→native bridge              | a client-side DEAD_LETTER row's `s3UploadId` + part ETags must NOT be reset by the LOCAL-reset path (UP-04 — preserves slow-cellular resume); `ownerUserId` invariant + `getCurrentSub` check stay       |
| `HumynUpload.drainNow()` JS→native bridge (NEW)                   | new @ReactMethod surface; the drain it kicks still goes through `UploadAuthContext::sub()` + `UploadCoordinator.getCurrentSub` UP-13 guard — no privilege escalation                                     |
| dev API stdout → `/tmp/humyn-api.log` (Wave-1.5 Item 10)          | dev-only observability surface; MUST NOT leak presigned URLs / bearer tokens (T-5-06-02 stays in force — the existing redact rule at logger.ts:5-12 redacts `authorization` + `x-idempotency-key`)        |
| logcat output (Wave-1.5 Item 9 drainNow paused + DEAD_LETTER logs) | the existing T-5-06-02 rule ("never log presigned URLs"): the new `Log.d/w` lines log only `row.recordingId` (a ULID) + `e.message` (route + status code, never a body, never a URL)                     |

## STRIDE Threat Register

| Threat ID  | Category                           | Component                                                                                                                                          | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-5-14-01  | DoS (stuck recording, Item 1)      | a single per-row `Idempotency-Key` reused across `/init`/`/parts`/`/finalize`/`/reupload` → server 409s on the 2nd route → 6 retries → DEAD_LETTER | mitigate    | Per-route key split (4 fields, each a stable UUIDv4 per route). Each route's key + body pair is stable across retries; cross-route key reuse impossible by construction. Captured by the MockWebServer test asserting 4 distinct keys per row.                                                                                                                                                                |
| T-5-14-02  | DoS (stuck row, Item 2)            | Retry on a client-side DEAD_LETTER row calling `POST /reupload` → 409 → row orphaned (drainer skips DEAD_LETTER state)                             | mitigate    | LOCAL-reset branch (`state == DEAD_LETTER && uploadId != null && !reupload`) keeps `uploadId`/parts/etags, sets state→UPLOADING, drains → `/parts` re-presigns against the live multipart upload (already-DONE parts keep their ETags — UP-04). Worker-fired path (server `qa_status='hash-mismatch'`) keeps the existing full-reset → `/reupload` flow.                                                       |
| T-5-14-03  | DoS / Data integrity (Item 7)      | per-`read()` UUID re-mint without write-back → process-kill between `/init` and `/parts` next-boot read mints a different key → 409                | mitigate    | `UploadRow.fromJson` marks `_migratedOnLoad=true` when it mints; `UploadQueueStore.read()` upserts migrated rows back to disk before returning; subsequent reads return the same keys. Tested.                                                                                                                                                                                                                  |
| T-5-14-04  | DoS (stale row, Item 8)            | cold-start with a row in {PENDING, UPLOADING} on disk: nothing kicks the drainer; user can't make progress without manual screen-bounce             | mitigate    | New `HumynUpload.drainNow()` @ReactMethod bridge + `installUploadReconcile()` boot kick. Drains do not unpause (`drainNow` !== `resume`); auth context is pushed first; coordinator's `isPaused()` + `getCurrentSub()` guards stay in force.                                                                                                                                                                  |
| T-5-14-05  | Information disclosure (Item 10)    | dev API request logs in `/tmp/humyn-api.log` leaking sensitive headers / bodies                                                                    | accept      | The existing redact rule at `apps/api/src/plugins/logger.ts:5-12` redacts `req.headers.authorization` + `req.headers["x-idempotency-key"]` + `res.headers["set-cookie"]`. Item 10 only changes the WHERE the worker writes (parent stdout fd vs the worker's private fd) — NOT the WHAT (the same redact rule applies regardless of destination). No new disclosure surface.                                  |
| T-5-14-06  | Information disclosure (Item 9)    | new logcat `Log.d` / `Log.w` calls in `UploadCoordinator::drainNow` leaking presigned URLs / bearer tokens                                         | mitigate    | The new logs use ONLY `row.recordingId` (a ULID — no PII) and `e.message` (route + status code from a body-free `DeadLetterException` / `IOException`). T-5-06-02 satisfied. Audit pattern: `grep -n 'Log\.[dwie](.*partUrls\|Log\.[dwie](.*X-Amz' UploadCoordinator.kt` must return 0.                                                                                                                       |
| T-5-14-07  | Tampering (Item 4 — progress fake) | a malicious JS-side `progressById` push could inflate the bar / chip percent                                                                       | accept      | The progress events are emitted natively (`UploadCoordinator::maybeEmitProgress`) — the JS side only DISPLAYS them, doesn't push them anywhere authoritative. A spoofed JS-side value affects only the local UI (the row is owned by the signed-in user, MMKV-isolated, no server consequence). No new surface.                                                                                              |
| T-5-14-08  | Tampering (Item 5 — toast bus)     | a malicious JS-side caller could push an arbitrary toast string via `setPendingUploadToast`                                                        | accept      | Same JS process; the toast string is rendered as `<Text>` in `<ToastHost />` (no HTML / no markdown) — RN flattens it; no XSS / no injection surface. The bus is intentionally trivial (1-shot, set/drain) — not a security boundary. No new surface.                                                                                                                                                          |
| T-5-14-09  | Navigation (Item 6 — Home → History) | a wrong nested-screen name could navigate to an unintended route                                                                                   | accept      | The names are static strings in source (`'MainTabs'`, `'History'`); RN typing catches typos at runtime (`getStateForRouteNamesChange` errors). No dynamic/user-supplied input on the navigation surface.                                                                                                                                                                                                       |
| T-5-14-10  | DoS (Item 10 dev.sh)               | `apps/api/scripts/dev.sh` racing other tsx processes; or the lsof check being TOCTOU                                                                | accept      | Dev-only opt-in script (operator runs it manually). The TOCTOU window between the `lsof` check and the `exec pnpm tsx` is microseconds; the worst case is a second EADDRINUSE that exits cleanly. No production surface.                                                                                                                                                                                      |

**Blockers:** none at HIGH severity. ASVS L1; block-on: high (per planning context). The threats are uniformly mitigation-tractable or accepted with rationale.

</threat_model>

<verification>

## Build cadence (per planning_context `<build_cadence>`)

After EACH Kotlin-touching task lands (Tasks 1, 3, 5, 6, 7), the executor MUST:
1. `cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests "ai.humynlabs.capture.upload.*"` → 0 exit
2. `cd apps/mobile/android && ./gradlew installApkRolloutDebug` → 0 exit + APK installed on the connected Pixel 10a (`5C161JEA304304`)

For JS-side tasks (Tasks 8, 9, 10): Metro hot-reload — no APK rebuild needed. Run `cd apps/mobile && pnpm vitest run` on the touched test files.

For docs-only / API-only tasks (Tasks 2, 4, 11): no build needed; for Task 4, the executor SHOULD verify `/tmp/humyn-api.log` captures a live request line after restarting the API and `curl http://localhost:8080/healthz`.

## Plan-completion checks

- `grep -c 'initIdempotencyKey\|partsIdempotencyKey\|finalizeIdempotencyKey\|reuploadIdempotencyKey' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt` returns ≥12 (4 fields × {decl, toJson, fromJson})
- `grep -c 'row\.idempotencyKey' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt` returns 0
- `grep -c 'fun drainNow' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt` returns ≥1
- `grep -c 'drainNowSafe' apps/mobile/src/services/uploadReconcile.ts` returns ≥1
- `grep -c 'LOCAL reset' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt` returns ≥1
- `grep -c '_migratedOnLoad' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadQueueStore.kt` returns ≥1
- `grep -c 'drainNow paused at' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt` returns ≥4
- `grep -c 'DEAD_LETTER:' apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadCoordinator.kt` returns ≥1
- `grep -c 'destination: 1' apps/api/src/plugins/logger.ts` returns ≥1
- `test -x apps/api/scripts/dev.sh` returns 0
- `grep -c 'pending-upload-progress-fill' apps/mobile/src/screens/uploads/PendingUploadsScreen.tsx` returns ≥1
- `grep -c 'pending-upload-progress-fill\|pending-uploads-tile-progress-fill' apps/mobile/src/screens/home/HomeSkeletonScreen.tsx` returns ≥1
- `grep -c "navigation.navigate('MainTabs', { screen: 'History' })" apps/mobile/src/screens/home/HomeSkeletonScreen.tsx` returns ≥1
- `grep -c "navigation.navigate('PendingUploads')" apps/mobile/src/screens/home/HomeSkeletonScreen.tsx` returns 0
- `grep -c 'setPendingUploadToast\|drainPendingUploadToast' apps/mobile/src/state/uploadToastBus.ts` returns ≥2
- `grep -c 'setPendingUploadToast' apps/mobile/src/screens/recording/RecordingScreen.tsx` returns ≥1
- `grep -c 'drainPendingUploadToast' apps/mobile/src/screens/home/HomeSkeletonScreen.tsx` returns ≥1
- `grep -c 'adb reverse tcp:4566' .planning/runbooks/05-upload-smoke.md` returns ≥1
- `grep -c 'Path A\|Path B' .planning/runbooks/05-upload-smoke.md` returns ≥2
- `cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests "ai.humynlabs.capture.upload.*"` exits 0
- `cd apps/mobile && pnpm vitest run __tests__/services/uploadReconcile.test.tsx __tests__/state/uploadToastBus.test.ts __tests__/screens/PendingUploadsScreen.test.tsx __tests__/screens/HomeSkeletonScreen.test.tsx` exits 0
- `cd apps/mobile/android && ./gradlew installApkRolloutDebug` exits 0 (APK installs on `5C161JEA304304`)
- The runbook §1 reads as the consolidated Tunnels + Metro + seed-dev-task block; §3 has the Path A/Path B split; §6 has the Wave-1.5 closing entry.

</verification>

<success_criteria>

- All 10 Wave-1.5 items committed atomically (one `fix(05):` or `chore(05):` commit per item, ending with the `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` trailer; the author/committer is the global git identity `Adnaan Mohammed <m.adnaan161@gmail.com>`)
- Kotlin unit tests green: `./gradlew :app:testApkRolloutDebugUnitTest --tests "ai.humynlabs.capture.upload.*"`
- JS Vitest tests green: `pnpm vitest run` on the new + extended files
- `./gradlew installApkRolloutDebug` produces an APK that installs on the Pixel 10a (`5C161JEA304304`)
- The API restarts clean with `pnpm --filter @humyn/api dev > /tmp/humyn-api.log 2>&1 &` and `/tmp/humyn-api.log` captures a live request line after a `curl http://localhost:8080/healthz`
- Runbook §1 reads as a single consolidated Tunnels + Metro + seed pre-flight block; §3 has the Path A/Path B split; §6 has the Wave-1.5 closing entry
- Phase 5 is ready for the §2 re-walk (`05-HUMAN-UAT.md` Current Test stays `paused_pending_wave_1_5` until the executor / owner flips it to `partial`/`passed` after the on-device re-walk)
- No regression of LOCKED constraints: drift gate metrics still RECORDED but NOT gated; audio still dropped; APK distribution only (no Play Store / iOS); en-US TTS owner deviation honored; designs unchanged from the LOCKED sources

</success_criteria>

<output>
After completion, create `.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-14-SUMMARY.md` covering: which Wave-1.5 items closed (1-10), the test verdicts (Kotlin + Vitest), the APK install verdict on `5C161JEA304304`, the `/tmp/humyn-api.log` capture verdict (Item 10), and a one-line readiness signal for the §2 re-walk.
</output>
````
