---
phase: 05-upload-pipeline-hash-verify-worker-anti-fraud
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - CLAUDE.md
  - .planning/research/STACK.md
  - deferred-decisions.md
  - design-spec.md
  - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-UI-SPEC.md
  - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md
  - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VALIDATION.md
  - .planning/runbooks/05-wave1-cleanup-smoke.md
autonomous: true
requirements: [FRAUD-05, FRAUD-06, UP-08]
must_haves:
  truths:
    - "FRAUD-05 (per-account daily upload-rate cap) and FRAUD-06 (pre-payout fraud monitoring dashboard) are MOVED to REQUIREMENTS.md §v2 (Anti-fraud) with a dated note 'Descoped to §v2 2026-05-12 — see CONTEXT.md D-04'; they no longer appear in the Phase-5 section of either REQUIREMENTS.md or ROADMAP.md; the ROADMAP Requirements Matrix rows for FRAUD-05/06 point at §v2, not Phase 5"
    - "The Phase-5 ROADMAP section (heading + the prose mention in the overview paragraph + the Phase-5 list-bullet at the top) is re-titled 'Upload Pipeline & Hash-Verify Worker' (the anti-fraud clause removed); the Phase-5 Requirements line drops FRAUD-05, FRAUD-06; Success Criterion #5 (the fraud-surface bullet) is deleted; Success Criterion #1 gains the dated note '(cellular S3 part size = 5 MiB — reconciled to S3's 5-MiB minimum non-final part size, 2026-05-12; idea-brief.md §7.1 still states 2 MB, not edited)'; the now-RESCINDED 'Phase 5's upload path should tolerate a crash-recovered segment's duration_seconds:0 + null drift' note is removed from the ROADMAP overview (D-03a). REQUIREMENTS.md UP-02 gets the same dated 5-MiB note inline. The directory slug stays 05-upload-pipeline-hash-verify-worker-anti-fraud."
    - "CLAUDE.md's 'Do NOT Use → Redis at MVP' line carries a one-line carve-out: the on-device MMKV upload queue is Postgres-free / on-device, but the hash-verify worker queue is BullMQ-on-Redis-on-ECS per VERIFY-01/07 + the ROADMAP; the backend tech-stack pins list bullmq@5.76.8 / ioredis@5.10.1 / @aws-sdk/client-sqs@3.1044.0 / Redis 7.x; STACK.md gets the same Redis/BullMQ pin if it's missing"
    - "04-MANUAL-SMOKE.md §2/§3 step text is refreshed (120ms not 80ms vibrate; en-US female not en-IN; 2×250ms gate dwell not 5×400ms; live camera preview from 'ready' onward; onSegmentStart/onSegmentComplete are the RN-bridge events not onSessionStart/onSessionStop logcat lines; no is_practice in the finalized metadata per D-08), and the shipped owner deviations are reflected into design-spec.md §6 / 04-UI-SPEC.md § Copywriting (PracticeIntro shortened copy commit eaaa1fe; en-US female cue voice; RigTutorial camera-framing tip)"
    - "A Wave-1 cleanup smoke runbook exists at .planning/runbooks/05-wave1-cleanup-smoke.md covering: D-03 force-quit-now-discards verification; D-05 device-distress→Home nav; D-06 alert-cue tones re-checked at FULL media volume (only chase a HumynBeep/SoundPool fix if still silent); D-09 RotatePrompt portrait-phone glyph eyeball; and a note that UP-08's iOS clause is a DOCUMENTED gap (iOS deferred to §v2 IOS-01..07, no HumynUploadIOS built this phase)"
    - 'UP-08 (iOS `URLSessionConfiguration.background` + `sessionSendsLaunchEvents=true` + the multipart-complete-POST-inside-`urlSessionDidFinishEvents` flow) is DOCUMENTED as a gap (not built): a note in the Wave-1 cleanup smoke runbook (§6) and a one-line note in `04-MANUAL-SMOKE.md` (or wherever appropriate) records that iOS is deferred to REQUIREMENTS.md §v2 (IOS-01..07) and no `HumynUploadIOS` / `URLSessionConfiguration.background` work ships this phase — this is how UP-08 is "covered": by formally recording the gap, not by implementing it'
  artifacts:
    - path: '.planning/REQUIREMENTS.md'
      provides: 'FRAUD-05/06 relocated to §v2 with the dated descope note'
      contains: 'Descoped to §v2 2026-05-12'
    - path: 'CLAUDE.md'
      provides: 'the Redis carve-out + the bullmq/ioredis/sqs/Redis backend pins'
      contains: 'BullMQ'
    - path: '.planning/runbooks/05-wave1-cleanup-smoke.md'
      provides: 'the Wave-1 cleanup on-hardware smoke runbook'
      min_lines: 30
  key_links:
    - from: '.planning/ROADMAP.md'
      to: '.planning/REQUIREMENTS.md §v2'
      via: "Requirements Matrix rows FRAUD-05/FRAUD-06 status → 'v2 (descoped 2026-05-12)'"
      pattern: 'FRAUD-05'
---

<objective>
Wave 1 — the documentation / housekeeping bucket from `04-COSMETIC-GAPS.md` D-09 + the FRAUD-05/06 retirement + the Phase-5 re-title + ROADMAP trims + the CLAUDE.md Redis carve-out + the STACK.md Redis pin + the Wave-1 cleanup smoke runbook + the VALIDATION.md per-task map seeding. No code in this plan — it's the planning-doc / project-rule / spec-doc / runbook layer that the rest of Phase 5 stands on. This is also how the descoped requirement IDs FRAUD-05 and FRAUD-06 get "covered": by formally retiring them to §v2, not by implementing them.

Purpose: D-04/D-04d (the anti-fraud descope housekeeping), D-09 (the doc-only polish bucket), the CLAUDE.md/STACK.md Redis-carve-out discretion item, D-03a (rescind the `duration_seconds:0` tolerance note), the Wave-1 smoke runbook (D-03/D-05/D-06/D-09 verification + the UP-08 iOS-gap note), and the Nyquist Dimension-8 per-task-verification-map fill.
Output: edits to REQUIREMENTS.md / ROADMAP.md / CLAUDE.md / STACK.md / deferred-decisions.md / design-spec.md / 04-UI-SPEC.md / 04-MANUAL-SMOKE.md / 05-VALIDATION.md + a new `.planning/runbooks/05-wave1-cleanup-smoke.md`.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-CONTEXT.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-COSMETIC-GAPS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire FRAUD-05/06 to §v2 + re-title Phase 5 + ROADMAP trims (D-04 / D-04d / D-03a)</name>
  <read_first>
    - .planning/REQUIREMENTS.md — the `### Anti-fraud` group under Phase-5 reqs (FRAUD-01/02 kept, FRAUD-05/06 lines ~240-241), the `## v2 Requirements → ### Anti-fraud` group (~line 291, currently FRAUD-03/04 + FRAUD-V2-01..04), and the Requirements Matrix rows for FRAUD-05/FRAUD-06 (~line 484+ area; search `| FRAUD-05`)
    - .planning/ROADMAP.md — line 5 (the overview paragraph with "plus the coarse server-side anti-fraud surface (per-account upload-rate cap + pre-payout fraud dashboard)" and the Wave-1 mention + the `duration_seconds: 0` tolerance sentence), line 20 (the Phase-5 list-bullet), lines 179-191 (the `### Phase 5: ...` section: heading, **Goal**, **Requirements** line, **Success Criteria** 1-5, **Plans: TBD**)
    - deferred-decisions.md — the Fraud & integrity section (where the deferred fraud designs live)
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-CONTEXT.md — D-04, D-04a, D-04b, D-04c, D-04d, D-03a
  </read_first>
  <action>
1. In `.planning/REQUIREMENTS.md`: remove the `- [ ] **FRAUD-05**: ...` and `- [ ] **FRAUD-06**: ...` lines from the Phase-5 `### Anti-fraud` group; update the prose note under that group so it reads "MVP anti-fraud is Play Integrity at sign-in (FRAUD-01/02) + the on-device one-shot hand gate. FRAUD-05 (per-account daily upload-rate cap) and FRAUD-06 (pre-payout fraud dashboard) were descoped to §v2 2026-05-12 — see CONTEXT.md D-04. The uploaded IMU CSV is collected but not analysed server-side at MVP." Add to `## v2 Requirements → ### Anti-fraud` (next to FRAUD-03/04): `- **FRAUD-05**: Per-account daily upload-rate cap enforced server-side as a coarse fraud heuristic. _Descoped to §v2 2026-05-12 — see CONTEXT.md D-04 (Phase 5). MVP upload path is fully uncapped per account (D-04a)._` and `- **FRAUD-06**: Pre-payout fraud monitoring dashboard tracks hash-mismatch rate, account-fingerprint clustering, and OEM/region anomalies (the liveness_score panel lands with FRAUD-03/04). _Descoped to §v2 2026-05-12 — see CONTEXT.md D-04 (Phase 5). Bull-Board for the worker queue is a separate Phase-7 observability item (OBS-04), unaffected._` In the Requirements Matrix, change the FRAUD-05 and FRAUD-06 rows' phase/status columns to `§v2` / `Deferred (2026-05-12)`. Also add a dated reconciliation note to **UP-02** in `.planning/REQUIREMENTS.md` (inline, after the existing UP-02 text): `(cellular S3 part size = 5 MiB — reconciled to S3's 5-MiB minimum non-final part size, 2026-05-12; idea-brief.md §7.1 still states 2 MB, not edited)`.
2. In `.planning/ROADMAP.md`: (a) line 5 — change "Phase 5 ships `HumynUpload` (...) plus the BullMQ hash-verify worker plus the coarse server-side anti-fraud surface (per-account upload-rate cap + pre-payout fraud dashboard)." to "Phase 5 ships `HumynUpload` (multipart S3 with the Android 14/15 foreground-service-type-downgrade + UIDT JobService architecture) plus the BullMQ hash-verify worker plus the server→client verified/re-upload event channel — anti-fraud beyond Play Integrity is descoped to §v2 (FRAUD-05, FRAUD-06)."; (b) line 5 — DELETE the sentence beginning "Also surface in the plan proper: the upload-bundle validation / hash-verify worker must tolerate a crash-recovered segment's `duration_seconds: 0` + null `imu_video_drift_*` / `imu_min_rate_*` ..." (D-03a — rescinded; CaptureLaunchSweep now discards crash-truncated fragments per Plan 05-01); (c) line 20 — change the Phase-5 list-bullet heading "Phase 5: Upload Pipeline, Hash-Verify Worker & Anti-fraud" to "Phase 5: Upload Pipeline & Hash-Verify Worker" and drop "per-account upload-rate cap + pre-payout fraud dashboard" from its description; (d) line 179 — change `### Phase 5: Upload Pipeline, Hash-Verify Worker & Anti-fraud` to `### Phase 5: Upload Pipeline & Hash-Verify Worker`; (e) the **Goal** line — remove "with a per-account daily upload-rate cap and the pre-payout fraud-monitoring dashboard standing up the coarse anti-fraud surface before payouts go live"; (f) the **Requirements** line — remove `, FRAUD-05, FRAUD-06`; (g) Success Criteria — DELETE criterion #5 entirely (the "backend enforces a per-account daily upload-rate cap ... fraud monitoring dashboard ..." bullet); (h) Success Criteria criterion #1 — append the same dated reconciliation note inline: `(cellular S3 part size = 5 MiB — reconciled to S3's 5-MiB minimum non-final part size, 2026-05-12; idea-brief.md §7.1 still states 2 MB, not edited)` (so the ROADMAP no longer says "2 MB on cellular" without the caveat); (i) leave **Plans: TBD** (the update-roadmap step at the end of planning will set it).
3. In `deferred-decisions.md`: under the Fraud & integrity section, add a line "**FRAUD-05 / FRAUD-06 descoped to §v2 (2026-05-12)** — per-account daily upload-rate cap + pre-payout fraud dashboard; the MVP upload path is fully uncapped per account; Bull-Board (worker-queue dashboard) is Phase 7 (OBS-04), separate. See CONTEXT.md D-04 + REQUIREMENTS.md §v2."
  </action>
  <verify>
    <automated>grep -c 'FRAUD-05\|FRAUD-06' .planning/ROADMAP.md && grep -n 'Upload Pipeline & Hash-Verify Worker' .planning/ROADMAP.md && grep -n 'Descoped to §v2 2026-05-12' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` does NOT contain `FRAUD-05` or `FRAUD-06` in the Phase-5 section or overview (grep for them returns only Requirements-Matrix rows that point at §v2, or nothing)
    - `.planning/ROADMAP.md` contains the heading `### Phase 5: Upload Pipeline & Hash-Verify Worker` and the Phase-5 Success Criteria list has exactly 4 numbered items (criterion #5 deleted)
    - `.planning/ROADMAP.md` does NOT contain the string `duration_seconds: 0` (the rescinded tolerance note is removed)
    - `.planning/REQUIREMENTS.md` UP-02 and `.planning/ROADMAP.md` Phase-5 Success Criterion #1 each carry the dated `cellular S3 part size = 5 MiB — reconciled ... 2026-05-12` note (so neither reads "2 MB on cellular" uncaveated)
    - `.planning/REQUIREMENTS.md` `## v2 Requirements → ### Anti-fraud` contains `**FRAUD-05**` and `**FRAUD-06**` with the text `Descoped to §v2 2026-05-12`; the Phase-5 `### Anti-fraud` group no longer lists them
    - `.planning/REQUIREMENTS.md` Requirements Matrix shows FRAUD-05 and FRAUD-06 as `§v2` / deferred, not `Phase 5`
  </acceptance_criteria>
  <done>FRAUD-05/06 are formally retired to §v2; Phase 5 is re-titled; the ROADMAP fraud-surface criterion + the rescinded duration_seconds:0 note are gone.</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md Redis carve-out + backend pins + STACK.md Redis pin</name>
  <read_first>
    - CLAUDE.md — the "Do NOT Use" section's "Redis at MVP — Postgres-only; queue lives on device" line; the "Tech Stack — Pins → Backend (Node 22 LTS / Jod)" block; the "Conventions" section
    - .planning/research/STACK.md — check whether bullmq / ioredis / Redis / SQS are pinned anywhere (they were added in 05-RESEARCH.md but may not be in STACK.md yet)
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-CONTEXT.md — the "Claude's Discretion" CLAUDE.md-Redis-tension item; 05-RESEARCH.md § "CLAUDE.md Redis carve-out" + § Standard Stack (the verified pins: bullmq 5.76.8, ioredis 5.10.1, @aws-sdk/client-sqs pinned to 3.1044.0 to match the existing S3 client minor, Redis 7.x)
  </read_first>
  <action>
1. In `CLAUDE.md`, change the "Do NOT Use" line `- **Redis at MVP** — Postgres-only; queue lives on device.` to `- **Redis for the on-device upload queue** — the upload queue lives on device (MMKV-backed, native-module-owned) per VERIFY-spec / the "queue lives on device" rule. The *hash-verify worker* queue, by contrast, IS BullMQ-on-Redis-on-ECS per VERIFY-01/07 + the ROADMAP — that's the one Redis carve-out at MVP. No other Redis usage.`
2. In `CLAUDE.md` under "Tech Stack — Pins → Backend (Node 22 LTS / Jod)", add a bullet: `- \`bullmq@5.76.8\`, \`ioredis@5.10.1\` — the hash-verify worker queue (Redis-backed; retries/backoff/concurrency/queue-depth metrics for ECS autoscaling). \`@aws-sdk/client-sqs@3.1044.0\` (pinned to the same minor as \`@aws-sdk/client-s3\` per the "always same minor" rule) — the thin SQS-poller in prod (S3→EventBridge→SQS→queue.add). Redis **7.x** (ElastiCache prod; \`redis:7-alpine\` container in dev).`
3. In `.planning/research/STACK.md`, if there is no Redis/BullMQ pin, add a "Hash-verify worker queue" subsection mirroring the CLAUDE.md bullet (bullmq 5.76.8 / ioredis 5.10.1 / @aws-sdk/client-sqs 3.1044.0 / Redis 7.x) with the rationale "ROADMAP locks BullMQ-on-Redis-on-ECS for VERIFY-01/07; switching to S3-EventBridge→Lambda is a §v2 concern. AWS SDK v3 modular, same minor as the S3 client. Redis 7+ is the BullMQ baseline." If STACK.md already has it, no-op (note that in the SUMMARY).
  </action>
  <verify>
    <automated>grep -n 'BullMQ' CLAUDE.md && grep -n 'bullmq@5.76.8' CLAUDE.md && grep -ci 'bullmq\|redis' .planning/research/STACK.md</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` "Do NOT Use" section's Redis line mentions "BullMQ" and "carve-out" (the hash-verify worker queue is explicitly excepted)
    - `CLAUDE.md` backend pins block contains `bullmq@5.76.8` and `ioredis@5.10.1` and `@aws-sdk/client-sqs@3.1044.0` and `Redis **7.x**`
    - `.planning/research/STACK.md` contains a Redis/BullMQ pin (grep `bullmq` or `Redis 7` returns a hit)
  </acceptance_criteria>
  <done>The CLAUDE.md Redis contradiction is resolved with a one-line carve-out; the backend pins + STACK.md carry the BullMQ/Redis/SQS versions.</done>
</task>

<task type="auto">
  <name>Task 3: Doc polish (D-09) — 04-MANUAL-SMOKE refresh + design-spec/UI-SPEC owner deviations + the Wave-1 cleanup smoke runbook + a VALIDATION-map re-verify</name>
  <read_first>
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md — §2 (the gate-pass step text, lines ~41-53), §3 (the `onSessionStart/onSessionStop` logcat line ~75, the non-practice flow), §6 WALK NOTES (the actual behavior), §1 pre-flight
    - design-spec.md — §6 (Tutorial — Practice intro, line ~268) and the "Voice synthesis" line (line ~729: "Indian English female voice (...) Rate 1.0, pitch 0.95, volume 0.85")
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-UI-SPEC.md — the "Copywriting Contract" section (line ~126) incl. the PracticeIntro row + the PracticeIntro heading/body/muted-line rows
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VALIDATION.md — the "Per-Task Verification Map" table (already filled during planning with one row per task across plans 05-01..05-08), the "Wave 0 Requirements" + "Manual-Only Verifications" sections, the frontmatter (`nyquist_compliant: true`, `wave_0_complete: false`)
    - .planning/runbooks/ — the existing operator-runbook conventions (if any runbooks exist; otherwise mirror the `04-MANUAL-SMOKE.md` §-numbered checklist style)
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-CONTEXT.md — D-06, D-08, D-09; CLAUDE.md banners (en-US female cue voice OWNER DEVIATION 2026-05-12; RigTutorialScreen camera-framing-tip deviation; PracticeIntro shortened copy commit eaaa1fe)
  </read_first>
  <action>
1. `04-MANUAL-SMOKE.md` §2/§3 refresh — change in the §2 gate-pass step (and the §3 equivalent): "80 ms vibrate" → "120 ms vibrate"; "Indian-English-female TTS / en-IN voice" → "en-US female-leaning TTS (owner deviation 2026-05-12 — `ttsVoice.ts`: en-US baseline → en-US female-ish → any en-US → first en-*)"; "5 × 400 ms (5 consecutive 2-hand detections)" → "2 × 250 ms gate dwell (2 consecutive 2-hand detections; the cadence/target/confidence are Remote Config keys with the `2 / 250 / 0.5` Android fallback)"; add to the §2 ready-substate description "a live Camera2-fed camera preview (`<HumynGateCameraView>`) is shown from the `'ready'` substate through the gate so the helper can check rig placement". In §3 change the line `**onSessionStart / onSessionStop upload-pause seam fires** ... adb logcat | grep -i "onSessionStart\|onSessionStop\|HumynCaptureModule"` to "**onSegmentStart / onSegmentComplete RN-bridge events fire** (the upload-pause seam — log-only at Phase 4; Phase 5 wires the actual pause) — these are RN-bridge events delivered to `RecordingScreen`'s `segMetaRef` subscription, NOT native `adb logcat` lines; verify via the dev console / a temporary `Log.i` if a trace is wanted." In §2 (and §6 if present) remove/replace the "confirm the metadata carries `is_practice: true`" assertion with "practice recordings are segregated by the `files/practice/` directory + `task_id: __practice__` — there is NO `is_practice` field in the finalized `{base}.json` (per D-08; the `.session.json` sidecar still carries `is_practice`). Confirm `files/practice/{base}.json` exists and `task_id` is `__practice__`."
2. design-spec.md §6 / "Voice synthesis" line: append to the §6 Practice-intro copy spec a note "OWNER DEVIATION (2026-05-12, commit eaaa1fe): the body is shortened to \"We'll walk you through one short recording — 60 secs, to get the feel\" and the muted line \"This is a practice task — it does not count towards your contribution.\" is removed."; change the "Voice synthesis" line's "**Indian English female voice** (...)" to "**en-US female-leaning voice** (OWNER DEVIATION 2026-05-12 from `idea-brief.md §13` which mandated en-IN female — on the test device the en-IN fallback sounded bad; `ttsVoice.ts` keeps the `EnIn` symbol names). Android: `Tts.setDefaultLanguage('en-US')` baseline → an en-US female-ish voice → any en-US → first en-*. Rate 1.0, pitch 0.95, volume 0.85." Add a note to the RigTutorial copy area (or §6's tutorial-chrome section) "OWNER DEVIATION (2026-05-12): `RigTutorialScreen.tsx` adds a one-line camera-framing tip to its otherwise-verbatim §5 copy." Make the matching edits in `04-UI-SPEC.md § Copywriting` (the PracticeIntro row/body/muted-line + a note on the cue voice + the RigTutorial tip).
3. Create `.planning/runbooks/05-wave1-cleanup-smoke.md` — a §-numbered on-hardware checklist with: §1 Pre-flight (install the Wave-1 build, full media volume); §2 D-03 force-quit-now-discards (record >30s, force-quit mid-record via `adb shell am force-stop`, relaunch → confirm NO "recovered — uploading" toast, confirm `adb shell run-as ai.humynlabs.capture.apk ls files/recordings/` shows no leftover fragment); §3 D-05 device-distress→Home (use the dev battery/thermal injection affordance to trigger a battery-≤5%/thermal mid-record stop → confirm the app lands on Home/MainTabs, not the RecordingScreen 'ready' substate; and a normal sub-60s manual discard still resets on-screen); §4 D-06 alert-cue tones at FULL media volume (trigger the battery-15% 520 Hz beep and the thermal-abort 440→560→680 Hz sequence with media volume maxed — confirm audible; ONLY chase a HumynBeep/SoundPool fix if STILL silent at full volume); §5 D-09 RotatePrompt glyph eyeball (open RecordingScreen in portrait → confirm the portrait-phone SVG tilt reads as "rotate your phone"); §6 NOTE — UP-08's iOS clause (`URLSessionConfiguration.background` etc.) is a DOCUMENTED gap, not built this phase (iOS deferred to REQUIREMENTS.md §v2 IOS-01..07; no `HumynUploadIOS` exists); §7 Sign-off (verdict YES/NO + any findings → `*-COSMETIC-GAPS.md` per the runbook §7 amendments protocol, never the FROZEN amendment files).
4. `05-VALIDATION.md` re-verify (the per-task map, the Test-Infrastructure rows, the Wave-0 list and `nyquist_compliant: true` / `wave_0_complete: false` were FILLED during planning — see the revision pass). This step is a low-cost re-verification: confirm the Per-Task Verification Map still matches the executed task set (after Plan 05-03's Task-2 split there are Tasks 05-03-01..05-03-05; if any other plan's tasks were renumbered/split during execution, sync the matching rows), confirm no `5-XX-XX` placeholder remains, and flip a row's `Status` to ✅ as its automated verify goes green during this wave's execution (the rest of the rows stay ⬜ until their plan runs). Do NOT re-derive the table from scratch — only reconcile drift. Leave `nyquist_compliant: true`.
  </action>
  <verify>
    <automated>test -f .planning/runbooks/05-wave1-cleanup-smoke.md && grep -ci 'force-quit\|D-03\|D-05\|D-06\|D-09\|iOS' .planning/runbooks/05-wave1-cleanup-smoke.md && grep -c 'is_practice' .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md && grep -q 'nyquist_compliant: true' .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VALIDATION.md && ! grep -q '5-XX-XX' .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VALIDATION.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/runbooks/05-wave1-cleanup-smoke.md` exists, is ≥30 lines, and mentions D-03 / D-05 / D-06 / D-09 and the iOS UP-08 gap
    - `04-MANUAL-SMOKE.md` §2/§3 no longer say "80 ms" or "en-IN" or "5 × 400 ms" for the gate-pass step (grep those strings in the §2/§3 region returns nothing) and the `onSessionStart/onSessionStop` logcat line is replaced with the `onSegmentStart/onSegmentComplete` RN-bridge wording
    - `design-spec.md` "Voice synthesis" line mentions "en-US female" + "OWNER DEVIATION" and §6 mentions the PracticeIntro shortened copy
    - `04-UI-SPEC.md § Copywriting` reflects the PracticeIntro shortened copy + the en-US female cue voice + the RigTutorial tip
    - `.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VALIDATION.md` frontmatter still has `nyquist_compliant: true`, the Per-Task Verification Map still has one row per task across plans 05-01..05-08 (no `5-XX-XX` placeholder), and its task IDs match the executed task set (this step only reconciles drift — the table was filled during planning)
  </acceptance_criteria>
  <done>Stale smoke-runbook text refreshed; owner deviations reflected into the design docs; the Wave-1 cleanup smoke runbook exists; the (pre-filled) VALIDATION.md per-task map re-verified against the executed task set, `nyquist_compliant: true` intact.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                                              |
| ------------------ | -------------------------------------------------------------------------------------------------------- |
| (none — docs only) | This plan modifies planning/spec/runbook markdown only; no code, no runtime surface, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category               | Component                  | Disposition | Mitigation Plan                                                                                                                                  |
| --------- | ---------------------- | -------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-5-02-01 | Information disclosure | runbook / docs             | accept      | The Wave-1 smoke runbook uses `adb shell run-as` only against the app's own debuggable build's private dir; no secrets are written into any doc. |
| T-5-02-02 | Tampering              | REQUIREMENTS/ROADMAP edits | accept      | Planning-doc edits are version-controlled; the descope trail is dated and cross-referenced (CONTEXT.md D-04). No security impact.                |

</threat_model>

<verification>
- `grep -n 'Upload Pipeline & Hash-Verify Worker' .planning/ROADMAP.md` returns a hit; `grep -c 'FRAUD-05\|FRAUD-06' .planning/ROADMAP.md` shows them only as §v2 matrix rows (or 0)
- `grep -n 'BullMQ' CLAUDE.md` and `grep -n 'bullmq@5.76.8' CLAUDE.md` return hits
- `test -f .planning/runbooks/05-wave1-cleanup-smoke.md` succeeds
- `grep -n 'nyquist_compliant: true' .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VALIDATION.md` returns a hit
</verification>

<success_criteria>

- FRAUD-05/06 retired to §v2 with the dated note; Phase 5 re-titled; ROADMAP fraud criterion + the rescinded duration_seconds:0 note removed
- CLAUDE.md Redis carve-out + backend BullMQ/Redis/SQS pins added; STACK.md carries the pin
- 04-MANUAL-SMOKE.md §2/§3 refreshed; owner deviations reflected into design-spec.md §6 + 04-UI-SPEC.md
- `.planning/runbooks/05-wave1-cleanup-smoke.md` created (D-03/D-05/D-06/D-09 + the UP-08 iOS-gap note)
- 05-VALIDATION.md per-task map filled; `nyquist_compliant: true`
  </success_criteria>

<output>
After completion, create `.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-02-SUMMARY.md`
</output>
