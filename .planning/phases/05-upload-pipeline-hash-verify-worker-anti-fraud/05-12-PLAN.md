---
phase: 05-upload-pipeline-hash-verify-worker-anti-fraud
plan: 12
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - apps/api/src/workers/sqs-poller.ts
  - apps/api/package.json
  - apps/api/test/workers/sqs-poller.test.ts
  - infra/terraform/modules/verify-queue/main.tf
autonomous: true
requirements: [VERIFY-01]
must_haves:
  truths:
    - '`apps/api/src/workers/sqs-poller.ts` exists — the prod SQS long-poll → `enqueueVerify(recordingId)` → `DeleteMessage` loop the Terraform worker task def references (`command = ["node","dist/workers/sqs-poller.js"]`). It mirrors `apps/api/src/workers/hash-verify.ts`''s entrypoint shape: standalone (does NOT `import buildApp`); a `pino(loggerOptions).child({ component: ''sqs-poller'' })` logger; reads `process.env.VERIFY_QUEUE_URL` (fails fast with a clear error if unset); uses `@aws-sdk/client-sqs` (`SQSClient`, `ReceiveMessageCommand`, `DeleteMessageCommand` — already pinned at `3.1044.0`); a `while (running)` loop doing `ReceiveMessageCommand({ QueueUrl, MaxNumberOfMessages: 10, WaitTimeSeconds: 20, VisibilityTimeout: 60 })` (long-poll), and for each message: parse the S3 EventBridge ''Object Created'' body, derive the `recordingId` from `detail.object.key`, `await enqueueVerify(recordingId)` (BullMQ, `jobId=recordingId` → idempotent collapse), then `DeleteMessageCommand({ QueueUrl, ReceiptHandle })`; `SIGTERM`/`SIGINT` → set `running = false` (graceful exit after the in-flight batch).'
    - "The S3-event-body parsing + `recordingId` derivation is DEFENSIVE and EXPORTED as `parseRecordingIdFromS3Event(messageBody: string): string | null`: the body is JSON-parsed in a try/catch (parse failure → `null`); the expected shape is the EventBridge envelope `{ source: 'aws.s3', 'detail-type': 'Object Created', detail: { bucket: { name }, object: { key } } }` OR the S3-direct `{ Records: [{ s3: { object: { key } } }] }` (handle both); the `key` (URL-decoded best-effort for `+`/`%`) MUST match `^recordings\\/[0-9A-HJKMNP-TV-Z]{26}\\/([0-9A-HJKMNP-TV-Z]{26})\\/(?:video\\.mp4|imu\\.csv|metadata\\.json)$` (Crockford-base32 ULIDs, the three locked suffixes per `recordingKeys()`) — the SECOND capture group is the `recordingId`; any non-match → `null`. In `pollOnce`: a `null` result that DID parse as JSON → `warn` + `DeleteMessage` (not ours / malformed — don't requeue garbage); a `null` result that did NOT parse → `warn` + leave it (it'll dead-letter after the queue's `maxReceiveCount`). The poller never trusts the key to point at a real recording — `enqueueVerify` just adds a BullMQ job; `verifyRecording` does the `SELECT` and is a no-op if the row doesn't exist or isn't `qa_status='uploaded'`, so an attacker who could put an object at `recordings/<ULID>/<ULID>/video.mp4` (only the app's presigned PUTs can) would at most cause one no-op `verifyRecording`."
    - '`apps/api/package.json` gains `"worker:sqs-poller": "node dist/workers/sqs-poller.js"` and `"worker:sqs-poller:dev": "tsx watch src/workers/sqs-poller.ts"` (mirroring the `worker:hash-verify` / `worker:hash-verify:dev` pair); `@aws-sdk/client-sqs@3.1044.0` is already in `dependencies` (verify — do not add/change).'
    - "`sqs-poller.ts`'s bootstrap (`void loop()`) is GUARDED so the module can be imported by a unit test without launching the polling loop (the parser is pure + exported): top-level `if (process.env.WORKER_BOOTSTRAP !== 'false') void loop();` — the test sets `process.env.WORKER_BOOTSTRAP = 'false'` before importing. The `VERIFY_QUEUE_URL` fail-fast check runs INSIDE `loop()` (so importing the module never `process.exit`s)."
    - '`infra/terraform/modules/verify-queue/main.tf`''s `sqs-poller` container comment is updated to drop the ''known stub'' caveat and reference `apps/api/src/workers/sqs-poller.ts` (Plan 05-12); the `command = ["node", "dist/workers/sqs-poller.js"]` is confirmed to match the new npm script''s `dist/` path (it does). No structural Terraform change — the env vars (`VERIFY_QUEUE_URL`/`REDIS_URL`/`AWS_REGION`) and the (unused-by-the-poller-but-harmless) `DATABASE_URL` secret stay.'
    - "Vitest coverage in `apps/api/test/workers/sqs-poller.test.ts` (no live SQS/Redis needed — `parseRecordingIdFromS3Event` is pure; the test sets `WORKER_BOOTSTRAP=false` before the import): a well-formed EventBridge 'Object Created' body for `recordings/<ULID>/<ULID>/video.mp4` → returns the recordingId; the `imu.csv` / `metadata.json` variants → same recordingId; a non-`recordings/` prefix → `null`; a non-ULID recordingId segment → `null`; a non-JSON body → `null`; a JSON body with no `detail.object.key` and no `Records` → `null`; an `{Records:[{s3:{object:{key}}}]}` S3-direct body → the recordingId; a `+`/`%`-encoded key → still resolves via the decode path."
  artifacts:
    - path: 'apps/api/src/workers/sqs-poller.ts'
      provides: 'the prod SQS long-poll → enqueueVerify → DeleteMessage loop (the file the Terraform worker task def references)'
      min_lines: 50
      exports: ['parseRecordingIdFromS3Event']
    - path: 'apps/api/package.json'
      provides: 'the worker:sqs-poller + worker:sqs-poller:dev npm scripts'
      contains: 'worker:sqs-poller'
  key_links:
    - from: 'apps/api/src/workers/sqs-poller.ts'
      to: 'apps/api/src/lib/queue.ts (enqueueVerify)'
      via: 'for each parsed S3 event → await enqueueVerify(recordingId) (jobId=recordingId, idempotent collapse)'
      pattern: 'enqueueVerify'
    - from: 'apps/api/src/workers/sqs-poller.ts'
      to: '@aws-sdk/client-sqs (ReceiveMessageCommand / DeleteMessageCommand)'
      via: 'long-poll ReceiveMessage (WaitTimeSeconds:20, MaxNumberOfMessages:10) → process → DeleteMessage'
      pattern: 'ReceiveMessageCommand'
    - from: 'infra/terraform/modules/verify-queue/main.tf'
      to: 'apps/api/src/workers/sqs-poller.ts (built to dist/workers/sqs-poller.js)'
      via: 'the sqs-poller container command = ["node","dist/workers/sqs-poller.js"]'
      pattern: 'dist/workers/sqs-poller.js'
---

<objective>
Gap closure for the missing prod `sqs-poller.ts` (VERIFY-01's EventBridge→SQS→BullMQ leg). The Terraform `verify-queue` module's worker task def has a second container `command = ["node","dist/workers/sqs-poller.js"]` — the thin SQS ReceiveMessage → `enqueueVerify(recordingId)` → DeleteMessage loop that feeds the BullMQ `verify` queue from `S3 → EventBridge → SQS` in prod — but `apps/api/src/workers/sqs-poller.ts` does NOT exist (only `hash-verify.ts` does). The prod worker container can't run as defined. (It's documented as a known stub with a backstop — `recordings_to_verify` + the `verify-sweep` cron converge without it, dev uses the `/finalize` shim — so it doesn't block the MVP *client* surface, but VERIFY-01's prod trigger leg is incomplete.) This plan adds `sqs-poller.ts` (mirroring `hash-verify.ts`'s standalone-entrypoint shape), the matching npm scripts, a defensive S3-event-key → `recordingId` parser, and cleans up the Terraform comment.

Purpose: VERIFY-01 (the hash-verify worker pipeline consumes S3 multipart-complete events via EventBridge → SQS — the prod trigger leg is now real, not a dangling reference).
Output: `apps/api/src/workers/sqs-poller.ts` + the `worker:sqs-poller` / `worker:sqs-poller:dev` scripts + the Vitest parser tests + the Terraform comment cleanup.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-CONTEXT.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-REVIEW.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VERIFICATION.md

<interfaces>
<!-- Extracted from the codebase. -->

From apps/api/src/workers/hash-verify.ts — the entrypoint shape to MIRROR:

```ts
import { Worker } from 'bullmq';
import pino from 'pino';
import { loggerOptions } from '../plugins/logger.js';
import { getRedisConnection } from '../lib/queue.js';
import { verifyRecording } from '../lib/verify-recording.js';
const log = pino(loggerOptions).child({ component: 'hash-verify-worker' });
const worker = new Worker<{ recordingId: string }>('verify', async (job) => { await verifyRecording(job.data.recordingId); }, { connection: getRedisConnection(), concurrency: 4 });
worker.on('completed', ...); worker.on('failed', ...); worker.on('error', ...);
async function shutdown(signal: string) { log.info({ signal }, '...'); try { await worker.close(); } finally { process.exit(0); } }
process.on('SIGTERM', () => void shutdown('SIGTERM')); process.on('SIGINT', () => void shutdown('SIGINT'));
log.info({ concurrency: 4 }, 'hash-verify worker started');
```

From apps/api/src/lib/queue.ts:

```ts
export function getRedisConnection(): Redis; // ioredis lazy singleton
export function getQueue(): Queue; // BullMQ 'verify' queue, lazy singleton
export async function enqueueVerify(recordingId: string): Promise<void>; // jobId = recordingId → idempotent collapse
```

From apps/api/src/lib/s3-client.ts — the key format the poller must parse:

```ts
export function recordingKeys(opts: { userId: string; recordingId: string }): {
  video: string;
  imu: string;
  metadata: string;
  prefix: string;
};
// recordings/{userId}/{recordingId}/{video.mp4|imu.csv|metadata.json} — userId & recordingId are 26-char Crockford-base32 ULIDs.
```

From apps/api/src/plugins/logger.ts: `export const loggerOptions: LoggerOptions` (pino options; pino-pretty in dev).

From infra/terraform/modules/verify-queue/main.tf — the relevant slice (the worker task def's 2nd container):

```hcl
{
  # Thin SQS long-poll → enqueueVerify(recordingId) → DeleteMessage. Kept as a 2nd container ...
  name      = "sqs-poller"
  image     = var.worker_image
  command   = ["node", "dist/workers/sqs-poller.js"]
  essential = false
  environment = [ { name = "NODE_ENV", ... }, { name = "AWS_REGION", ... }, { name = "REDIS_URL", ... }, { name = "VERIFY_QUEUE_URL", value = aws_sqs_queue.verify.id } ]
  secrets = [ { name = "DATABASE_URL", valueFrom = "${var.db_secret_arn}:url::" } ]
  logConfiguration = { ... awslogs-stream-prefix = "sqs-poller" }
}
```

The EventBridge rule `recordings_object_created` fires on `source: ["aws.s3"]`, `"detail-type": ["Object Created"]`, `detail.bucket.name == <recordings bucket>`, `detail.object.key` suffix `.mp4` / `.csv` / `metadata.json` → target = `aws_sqs_queue.verify`. So the SQS message body is the EventBridge event: `{ version, id, "detail-type": "Object Created", source: "aws.s3", account, time, region, resources, detail: { version, bucket: { name }, object: { key, size, etag, ... }, ... } }`.

From apps/api/test/workers/verify-recording.test.ts — the `describeIf(HAS_LOCALSTACK ...)` gating pattern (`const HAS_LOCALSTACK = !!process.env.AWS_ENDPOINT_URL`) for any LocalStack-touching test; the parser tests do NOT need it.

`@aws-sdk/client-sqs@3.1044.0` is already in `apps/api/package.json` `dependencies` (pinned to the same minor as `@aws-sdk/client-s3` per the CLAUDE.md rule).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: add apps/api/src/workers/sqs-poller.ts + the npm scripts + the parser Vitest tests + the Terraform comment cleanup</name>
  <files>apps/api/src/workers/sqs-poller.ts, apps/api/package.json, apps/api/test/workers/sqs-poller.test.ts, infra/terraform/modules/verify-queue/main.tf</files>
  <read_first>
    - apps/api/src/workers/hash-verify.ts — the FULL file (the standalone-entrypoint shape: imports, the pino child logger, the loop/worker, the SIGTERM/SIGINT shutdown, the startup log line)
    - apps/api/src/lib/queue.ts — `getRedisConnection`, `getQueue`, `enqueueVerify` (the poller calls `enqueueVerify`; it does NOT import `verifyRecording`)
    - apps/api/src/lib/s3-client.ts — `recordingKeys` (the key format the parser must validate against)
    - apps/api/src/plugins/logger.ts — `loggerOptions`
    - apps/api/package.json — the `scripts` block (where to add `worker:sqs-poller` / `worker:sqs-poller:dev`); confirm `@aws-sdk/client-sqs` is in `dependencies`
    - apps/api/test/workers/verify-recording.test.ts — the Vitest conventions (`describe`/`it`/`expect` from `vitest`; the `HAS_LOCALSTACK` gate for any SQS integration test)
    - infra/terraform/modules/verify-queue/main.tf — the `sqs-poller` container block (the `# Thin SQS long-poll …` comment) — update the comment only
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VERIFICATION.md — truth #4 artifact note (the exact missing-file diagnosis) ; .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-05-SUMMARY.md (the verify-queue Terraform module + the /finalize dev shim — context for why dev doesn't need the poller)
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-REVIEW.md / 05-VALIDATION.md — the VERIFY-01 expectations + Dimension 8 (keep the poller verifiable — hence the exported pure parser)
  </read_first>
  <behavior>
    - `sqs-poller.ts` structure (standalone, mirroring `hash-verify.ts`):
      - imports: `{ SQSClient, ReceiveMessageCommand, DeleteMessageCommand }` from `@aws-sdk/client-sqs`; `pino` + `{ loggerOptions }` from `../plugins/logger.js`; `{ enqueueVerify }` from `../lib/queue.js`.
      - `const log = pino(loggerOptions).child({ component: 'sqs-poller' });`
      - `const RECORDING_KEY_RE = /^recordings\/[0-9A-HJKMNP-TV-Z]{26}\/([0-9A-HJKMNP-TV-Z]{26})\/(?:video\.mp4|imu\.csv|metadata\.json)$/;`
      - `export function parseRecordingIdFromS3Event(messageBody: string): string | null` — JSON.parse in a try/catch (→ `null`); pull `detail.object.key` (EventBridge) or `Records[0].s3.object.key` (S3-direct); if not a string → `null`; best-effort `decodeURIComponent(key.replace(/\+/g, ' '))` (keep the raw key as a fallback if decode throws); `RECORDING_KEY_RE.exec(decoded) ?? RECORDING_KEY_RE.exec(rawKey)` → return capture group 1 or `null`.
      - `let running = true;`
      - `function makeSqs(): SQSClient { return new SQSClient({ region: process.env.AWS_REGION ?? 'ap-south-1', ...(process.env.AWS_ENDPOINT_URL ? { endpoint: process.env.AWS_ENDPOINT_URL } : {}) }); }` (lazy — built inside `loop()` so importing the module is side-effect-light).
      - `async function pollOnce(sqs: SQSClient, queueUrl: string): Promise<void>` — `ReceiveMessageCommand({ QueueUrl: queueUrl, MaxNumberOfMessages: 10, WaitTimeSeconds: 20, VisibilityTimeout: 60 })`; for each `msg`: `const recordingId = parseRecordingIdFromS3Event(msg.Body ?? '')`; if `null` → `try { JSON.parse(msg.Body ?? ''); log.warn(...,'S3 event key did not match a recording bundle — deleting'); await sqs.send(new DeleteMessageCommand({ QueueUrl: queueUrl, ReceiptHandle: msg.ReceiptHandle! })); } catch { log.warn(...,'unparseable SQS message body — leaving for the DLQ'); }`; else `try { await enqueueVerify(recordingId); await sqs.send(new DeleteMessageCommand({ QueueUrl: queueUrl, ReceiptHandle: msg.ReceiptHandle! })); log.info({ recordingId }, 'enqueued verify + deleted SQS message'); } catch (err) { log.error({ err, recordingId }, 'enqueueVerify failed — NOT deleting (will retry / DLQ)'); }`.
      - `async function loop(): Promise<void> { const queueUrl = process.env.VERIFY_QUEUE_URL; if (!queueUrl) { log.error('VERIFY_QUEUE_URL is not set — cannot run the SQS poller'); process.exit(1); } const sqs = makeSqs(); log.info({ queueUrl }, 'SQS poller started'); while (running) { try { await pollOnce(sqs, queueUrl); } catch (err) { log.error({ err }, 'SQS poll iteration failed — backing off 5s'); await new Promise((r) => setTimeout(r, 5000)); } } log.info('SQS poller loop exited'); process.exit(0); }`
      - `function shutdown(signal: string): void { log.info({ signal }, 'SQS poller shutting down'); running = false; }` ; `process.on('SIGTERM', () => shutdown('SIGTERM')); process.on('SIGINT', () => shutdown('SIGINT'));`
      - bootstrap (guarded for tests): `if (process.env.WORKER_BOOTSTRAP !== 'false') void loop();`
    - `enqueueVerify` is idempotent (`jobId = recordingId`) — the 3 events per bundle (mp4 + csv + metadata.json) collapse to one BullMQ job; no de-dup needed in the poller.
  </behavior>
  <action>
1. Create `apps/api/src/workers/sqs-poller.ts` per the structure above. Top-of-file comment: "SQS poller — the prod EventBridge→SQS→BullMQ trigger leg (VERIFY-01). Standalone ECS-container entrypoint: `node dist/workers/sqs-poller.js` (the 2nd container in the worker task def; see infra/terraform/modules/verify-queue/main.tf). Long-polls the `verify` SQS queue, derives `recordingId` from the S3 'Object Created' event key, `enqueueVerify`s the BullMQ job (jobId=recordingId → the 3 events per bundle collapse to one), DeleteMessages. Does NOT import buildApp or the DB — `verifyRecording` runs in `hash-verify.ts`. Dev uses the `/finalize` LocalStack shim instead; `recordings_to_verify` + the `verify-sweep` cron are the durable backstop either way. Bootstrap is gated on `WORKER_BOOTSTRAP !== 'false'` so unit tests can import the pure `parseRecordingIdFromS3Event` without launching the loop."
2. `apps/api/package.json`: add to `scripts` — `"worker:sqs-poller": "node dist/workers/sqs-poller.js"` and `"worker:sqs-poller:dev": "tsx watch src/workers/sqs-poller.ts"` (next to the `worker:hash-verify` / `worker:hash-verify:dev` lines). Confirm `@aws-sdk/client-sqs` is in `dependencies` (it is — `3.1044.0`); do NOT add/change it.
3. `apps/api/test/workers/sqs-poller.test.ts`: at the TOP of the file (before any import of `sqs-poller.ts`), `process.env.WORKER_BOOTSTRAP = 'false';` then `import { parseRecordingIdFromS3Event } from '../../src/workers/sqs-poller.js';` and `import { describe, it, expect } from 'vitest';`. Cases:
   - well-formed EventBridge body `{ source: 'aws.s3', 'detail-type': 'Object Created', detail: { bucket: { name: 'humyn-recordings-prod' }, object: { key: 'recordings/01HVABCDEFGHJKMNPQRSTVWXYZ/01HVZYXWVUTSRQPNMLKJHGFEDC/video.mp4' } } }` → `'01HVZYXWVUTSRQPNMLKJHGFEDC'` (use valid 26-char Crockford-base32 strings — no I/L/O/U).
   - the `imu.csv` and `metadata.json` variants → same recordingId.
   - non-`recordings/` prefix (`'other/x/y/video.mp4'`) → `null`.
   - non-base32 / wrong-length recordingId segment → `null`.
   - non-JSON body (`'<html>oops</html>'`) → `null`.
   - JSON body with neither `detail.object.key` nor `Records` → `null`.
   - S3-direct body `{ Records: [{ s3: { object: { key: 'recordings/<ULID>/<ULID>/imu.csv' } } }] }` → the recordingId.
   - a `+`/`%`-encoded key (`'recordings%2F<ULID>%2F<ULID>%2Fvideo.mp4'` or one with a `+`) → still resolves via the `decodeURIComponent` path.
   (Optional, gated on `process.env.AWS_ENDPOINT_URL` like `verify-recording.test.ts`: a LocalStack SQS integration test that sends a fake event, runs one `pollOnce`, asserts `enqueueVerify` was called + the message deleted — NOT required; the parser unit tests are the required coverage.)
4. `infra/terraform/modules/verify-queue/main.tf`: in the `sqs-poller` container block, replace the comment line(s) `# Thin SQS long-poll → enqueueVerify(recordingId) → DeleteMessage. Kept as a 2nd container in the same task (cheap; shares the worker's lifecycle). If it hiccups, the recordings_to_verify row + the verify-sweep cron re-enqueue.` with `# Thin SQS long-poll → enqueueVerify(recordingId) → DeleteMessage — the prod EventBridge→SQS→BullMQ trigger leg, implemented in apps/api/src/workers/sqs-poller.ts (Plan 05-12). 2nd container in the same task (cheap; shares the worker's lifecycle); if it hiccups, the recordings_to_verify row + the verify-sweep cron re-enqueue.` — keep `command = ["node", "dist/workers/sqs-poller.js"]` and the `environment` / `secrets` / `logConfiguration` unchanged. Run `terraform fmt infra/terraform/modules/verify-queue/main.tf` if `terraform` is available; otherwise leave formatting as-is.
  </action>
  <verify>
    <automated>cd apps/api && WORKER_BOOTSTRAP=false pnpm vitest run test/workers/sqs-poller.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/workers/sqs-poller.ts` exists (≥50 lines), exports `parseRecordingIdFromS3Event`, imports `{ SQSClient, ReceiveMessageCommand, DeleteMessageCommand }` from `@aws-sdk/client-sqs`, calls `enqueueVerify(recordingId)` then `DeleteMessageCommand`, long-polls (`WaitTimeSeconds: 20`), handles `SIGTERM`/`SIGINT` → `running = false`, and gates its bootstrap on `process.env.WORKER_BOOTSTRAP !== 'false'`
    - `grep -n 'worker:sqs-poller' apps/api/package.json` returns the two script lines; `grep -n '@aws-sdk/client-sqs' apps/api/package.json` returns the (pre-existing) dependency
    - `grep -n 'parseRecordingIdFromS3Event' apps/api/src/workers/sqs-poller.ts` returns the `export function` + the `RECORDING_KEY_RE.exec` usage
    - `infra/terraform/modules/verify-queue/main.tf` still has `command = ["node", "dist/workers/sqs-poller.js"]` and its comment now references `apps/api/src/workers/sqs-poller.ts` (no more "known stub")
    - `cd apps/api && WORKER_BOOTSTRAP=false pnpm vitest run test/workers/sqs-poller.test.ts` exits 0 (the 8 parser cases pass); `cd apps/api && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>`apps/api/src/workers/sqs-poller.ts` exists and is the real prod EventBridge→SQS→BullMQ leg; the npm scripts + the Vitest parser tests + the Terraform comment cleanup are in; tests green; `tsc` clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                                          | Description                                                                                                                                                                                                                    |
| --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| S3 → EventBridge → SQS → poller                                                   | the SQS message body is an AWS-emitted S3 'Object Created' event (the queue policy only allows EventBridge to `SendMessage`, scoped by `SourceArn` — threat T-5-05-06); the poller still validates the key shape before acting |
| poller → BullMQ `verify` queue                                                    | `enqueueVerify(recordingId)` adds a job keyed `jobId=recordingId`; the worker (`hash-verify.ts`) re-`SELECT`s the row — a job for a non-existent / non-`uploaded` recording is a no-op                                         |
| an object landing at `recordings/<ULID>/<ULID>/{video.mp4,imu.csv,metadata.json}` | only the app's presigned PUTs (from `/init` / `/parts` / `/reupload`) write under that prefix — the bucket has no public write; the EventBridge rule + the poller's key regex bound what can be enqueued                       |

## STRIDE Threat Register

| Threat ID | Category                                      | Component                                                                                                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                   |
| --------- | --------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-5-12-01 | Spoofing / Tampering                          | a forged SQS message with an attacker-chosen `recordingId` causing `enqueueVerify` to queue arbitrary work | mitigate    | The SQS queue policy only accepts `SendMessage` from EventBridge scoped by `SourceArn` (existing Terraform — T-5-05-06); the poller's `parseRecordingIdFromS3Event` only accepts keys matching `recordings/<ULID>/<ULID>/{video.mp4                                                                                                                                               | imu.csv       | metadata.json}`(a forged key shape →`null`→ message deleted, not enqueued); and even a "valid-looking"`recordingId`that doesn't correspond to a real`qa_status='uploaded'`row makes`verifyRecording`a no-op (it`SELECT`s + early-returns). The blast radius of a forged message is one no-op DB read.                                                                             |
| T-5-12-02 | DoS                                           | a malformed-message storm filling the queue / spinning the poller                                          | mitigate    | Unparseable messages are NOT deleted by the poller → they redeliver up to `maxReceiveCount` then move to the DLQ (existing redrive policy in the verify-queue module) — they don't loop forever; key-mismatch messages ARE deleted immediately (no requeue of garbage); a poll-iteration exception backs off 5 s; long-poll (`WaitTimeSeconds: 20`) keeps the receive call cheap. |
| T-5-12-03 | Information disclosure                        | the poller logging full S3 event bodies (could contain object keys = `recordings/<userId>/...`)            | mitigate    | The poller logs `recordingId` + `msgId` on success and a generic `'... did not match a recording bundle'` / `'... unparseable ...'` on the bad paths — it never logs the raw `msg.Body` or the full key. `userId` is not in `recordingId`, so a leaked log line carries no user identifier.                                                                                       |
| T-5-12-04 | Elevation of privilege                        | the `sqs-poller` container having more IAM than it needs                                                   | accept      | The worker task role grants `sqs:ReceiveMessage                                                                                                                                                                                                                                                                                                                                   | DeleteMessage | GetQueueAttributes`on the`verify`queue (existing`ConsumeVerifyQueue`statement) + Redis network access — exactly what the poller needs. The`DATABASE_URL`secret is injected (same image as`hash-verify`) but the poller doesn't use the DB; leaving it is harmless (the secret is already scoped to the worker task role, not widened). Documented; not worth a separate task def. |
| T-5-12-05 | Tampering (visibility-timeout double-process) | a message processed by two poller instances if `VisibilityTimeout` expires mid-`enqueueVerify`             | mitigate    | `enqueueVerify` is idempotent (`jobId=recordingId`) — a double-enqueue collapses to one BullMQ job; `verifyRecording` is idempotent (`qaStatus !== 'uploaded'` early-return); `VisibilityTimeout: 60` >> the `enqueueVerify` latency. No correctness impact.                                                                                                                      |

</threat_model>

<verification>
- `cd apps/api && WORKER_BOOTSTRAP=false pnpm vitest run test/workers/sqs-poller.test.ts` exits 0
- `cd apps/api && pnpm tsc --noEmit` exits 0
- `test -f apps/api/src/workers/sqs-poller.ts` succeeds
- `grep -n 'worker:sqs-poller' apps/api/package.json` returns the two script lines
- `grep -n 'dist/workers/sqs-poller.js' infra/terraform/modules/verify-queue/main.tf` returns a hit; `grep -ci 'known stub' infra/terraform/modules/verify-queue/main.tf` (filtering comments out is unnecessary — there should simply be no "known stub" mention any more) returns 0
- `grep -n 'parseRecordingIdFromS3Event' apps/api/src/workers/sqs-poller.ts` returns the `export function`
</verification>

<success_criteria>

- `apps/api/src/workers/sqs-poller.ts` exists — the prod SQS long-poll → `enqueueVerify` → `DeleteMessage` loop the Terraform references; standalone (no `buildApp`), `@aws-sdk/client-sqs`, long-poll, graceful shutdown, bootstrap gated for tests
- `parseRecordingIdFromS3Event` is exported + pure + validates the `recordings/<ULID>/<ULID>/{video.mp4|imu.csv|metadata.json}` key shape; Vitest covers EventBridge + S3-direct + bad-key + non-JSON cases
- `apps/api/package.json` has `worker:sqs-poller` + `worker:sqs-poller:dev`; `@aws-sdk/client-sqs` already present
- `infra/terraform/modules/verify-queue/main.tf` comment updated (no "known stub"); `command` unchanged
- Vitest green; `tsc --noEmit` clean
  </success_criteria>

<output>
After completion, create `.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-12-SUMMARY.md`
</output>
