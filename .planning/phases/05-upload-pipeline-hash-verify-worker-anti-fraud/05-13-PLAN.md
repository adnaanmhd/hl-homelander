---
phase: 05-upload-pipeline-hash-verify-worker-anti-fraud
plan: 13
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - apps/api/src/routes/recordings/finalize.ts
  - apps/api/src/lib/verify-recording.ts
  - apps/api/src/workers/hash-verify.ts
  - apps/api/src/plugins/events-outbox.ts
  - apps/api/src/routes/recordings/verified-ids.ts
  - apps/api/test/routes/recordings-finalize.test.ts
  - apps/api/test/workers/verify-recording.test.ts
  - apps/api/test/plugins/events-outbox.test.ts
  - apps/api/test/routes/recordings/verified-ids.test.ts
  - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VALIDATION.md
autonomous: true
requirements: [UP-01, VERIFY-03, VERIFY-04, VERIFY-05]
must_haves:
  truths:
    - "`POST /recordings/:id/finalize` is retry-safe across the two `CompleteMultipartUpload` calls (WR-01): each `CompleteMultipartUploadCommand` (video, then IMU) is wrapped so a `NoSuchUpload` / `NoSuchMultipartUpload` (i.e. 'this multipart upload was already completed') is SWALLOWED — the route then `HeadObject`s the corresponding key (`keys.video` / `keys.imu`) and treats 'the object exists' as success (idempotent); only if BOTH objects are confirmed present does the route flip the row to `qa_status='uploaded'` + insert the `recordings_to_verify` row. A genuine S3 error that is neither 'already completed' nor a present object → still propagates (the coordinator retries; or dead-letters). So a `/finalize` retry after the video `CompleteMultipartUpload` already consumed the video upload (the WR-01 stuck-recording scenario) now completes the IMU side and finalizes — instead of 500ing forever. A row already in `qa_status='uploaded'` short-circuits to a `200` (the prior `/finalize`'s response dropped); a row in `verified`/`hash-mismatch`/`rejected`/`takedown` still `409`s."
    - "The hash-verify worker's `qa_status` flip is TOCTOU-safe (WR-02 — VERIFY-03/04): both `UPDATE recordings SET qa_status=...` statements in `verifyRecording` carry an `AND qa_status = 'uploaded'` predicate (so an ops action — takedown / re-upload — that flipped the row during the multi-second S3 re-hash window is NOT silently resurrected), and the outbox-event append + the `recordings_to_verify` row delete happen ONLY if the UPDATE actually affected a row (`rowCount === 1`); if the UPDATE affected 0 rows, `verifyRecording` logs at `info` ('row moved during re-hash; skipping') and returns without writing an outbox event or deleting the queue row (the cron / a fresh enqueue reconverges if appropriate). The dead `canTransition(rec.qaStatus, ...)` check (always `'uploaded'` there because `rec` is a stale read) is replaced by the `AND qa_status='uploaded'` SQL predicate as the actual guard."
    - "The `events-outbox` `onSend` hook does NOT mutate `application/problem+json` / non-2xx responses (WR-03 — VERIFY-05): the hook returns `payload` unchanged unless the response status is 2xx AND the `content-type` is `application/json` (not `application/problem+json`, not non-JSON) — so RFC-7807 error bodies never carry a `_events` key and pending outbox events are NEVER marked delivered on a response the client treats as a hard failure. Concretely: at the top of the hook, `if (reply.statusCode < 200 || reply.statusCode >= 300) return payload;` and `const ct = reply.getHeader('content-type'); if (typeof ct !== 'string' || !ct.startsWith('application/json')) return payload;` — only then JSON-parse, attach `_events`, `markDelivered`, re-stringify. (The hook still fires on the normal 2xx JSON carrier routes — `/me`, `/recordings` list, `/recordings/verified-ids`, `/recordings/:id/reupload` — exactly as today.)"
    - "`GET /recordings/verified-ids`'s cursor-resolution SELECT is user-gated (IN-05): `SELECT verified_at, id FROM recordings WHERE id = since AND user_id = <caller>` — an out-of-scope `since` id resolves nothing (closing the faint existence oracle); the result set was already user-gated, so this is a defence-in-depth one-liner with no behaviour change for a legitimate cursor."
    - "Vitest coverage: `recordings-finalize.test.ts` — a `/finalize` retry where the video `CompleteMultipartUpload` returns `NoSuchUpload` but the video object exists (and the IMU completes normally) → `200` + `qa_status='uploaded'` + a `recordings_to_verify` row (idempotent retry path); a `/finalize` where the video `CompleteMultipartUpload` errors with something that's neither 'already completed' nor a present object → the error propagates (≥500 / the existing failure behaviour) and the row stays `pending`; a `/finalize` on an already-`uploaded` row → `200`, no `CompleteMultipartUpload` call; a `/finalize` on a `verified` row → `409`. `verify-recording.test.ts` — a recording flipped to `takedown` AFTER the `rec` SELECT but BEFORE the UPDATE (simulate by updating the row mid-test) → `verifyRecording` does NOT set it back to `verified`, writes NO outbox event, leaves the `recordings_to_verify` row (or deletes it — match the chosen behaviour) — the row stays `takedown`. The EXISTING `apps/api/test/plugins/events-outbox.test.ts` (created in Plan 05-05) is EXTENDED (its current 4 tests stay): a `404` `application/problem+json` response carries NO `_events` key and any pending outbox row for that user is STILL undelivered afterwards; a `200` `application/json` response still carries `_events` and marks them delivered (regression guard). `apps/api/test/routes/recordings/verified-ids.test.ts` (existing, Plan 05-05) gains one IN-05 case: seed two users' verified recordings; `GET /recordings/verified-ids?since=<the OTHER user's recording id>` behaves identically to `?since=<an unknown id>` — no cursor predicate is added, the response is the same set/order as no-`since`."
    - "`05-VALIDATION.md`'s Per-Task Verification Map is reconciled (MINOR 7): rows are appended for the gap-closure task IDs `05-09-01`, `05-09-02`, `05-10-01`, `05-11-01`, `05-12-01`, `05-13-01`, `05-13-02`, `05-13-03` (matching the task IDs actually used), each citing the task's `<automated>` verify command + its requirement(s) + threat ref + the secure-behavior line — the map's column format is kept; `nyquist_compliant: true` stays true."
  artifacts:
    - path: 'apps/api/src/routes/recordings/finalize.ts'
      provides: 'retry-safe /finalize — NoSuchUpload-tolerant CompleteMultipartUpload + HeadObject confirmation before the state flip (WR-01)'
      contains: 'HeadObjectCommand'
    - path: 'apps/api/src/lib/verify-recording.ts'
      provides: "TOCTOU-safe qa_status flip — AND qa_status='uploaded' predicate + rowCount-gated outbox append (WR-02)"
      contains: "qaStatus, 'uploaded'"
    - path: 'apps/api/src/plugins/events-outbox.ts'
      provides: 'onSend hook that only touches 2xx application/json responses (WR-03)'
      contains: 'statusCode'
  key_links:
    - from: 'apps/api/src/routes/recordings/finalize.ts'
      to: '@aws-sdk/client-s3 (HeadObjectCommand)'
      via: 'on a NoSuchUpload from CompleteMultipartUpload → HeadObject the key; "exists" = success'
      pattern: 'HeadObjectCommand'
    - from: 'apps/api/src/lib/verify-recording.ts'
      to: 'apps/api/src/db/schema.ts (recordings)'
      via: "UPDATE ... WHERE id = recordingId AND qa_status = 'uploaded' — only append the outbox event if rowCount === 1"
      pattern: "and\\([\\s\\S]*qaStatus[\\s\\S]*'uploaded'"
    - from: 'apps/api/src/plugins/events-outbox.ts'
      to: 'apps/api/src/lib/recording-events.ts (drainOutbox / markDelivered)'
      via: 'only after the 2xx + application/json guard passes'
      pattern: 'drainOutbox'
---

<objective>
Gap closure for the backend warnings the verifier flagged inside the otherwise-wired hash-verify pipeline: WR-01 (`/finalize` is not atomic across the two `CompleteMultipartUpload` calls — an IMU complete that fails after the video complete already consumed the video multipart leaves the row `pending` and un-finalizable on retry), WR-02 (`verifyRecording`'s `qa_status` UPDATEs have no `AND qa_status='uploaded'` predicate, so an ops takedown/re-upload during the multi-second S3 re-hash gets silently resurrected — the `canTransition` check inside the transaction is dead because `rec` is a stale read), WR-03 (`events-outbox` onSend fires on `application/problem+json` ≥400 responses too — it mutates RFC-7807 bodies and marks pending events delivered on a response the client treats as a hard failure), plus IN-05 (the `verified-ids` cursor SELECT lacks a `user_id` predicate — a faint existence oracle, no data leak — one-liner, folded in). It also reconciles `05-VALIDATION.md`'s Per-Task Verification Map with the new gap-closure task IDs (MINOR 7 — a doc-only Task 3).

Purpose: UP-01 (a `/finalize` retry always converges instead of 500ing forever), VERIFY-03/04 (the worker's state flip is correct under concurrent ops actions), VERIFY-05 (the `_events` channel doesn't lose events on error responses / pollute problem-detail bodies).
Output: a retry-safe `finalize.ts` + a TOCTOU-safe `verify-recording.ts` + a 2xx-only `events-outbox.ts` + the user-gated `verified-ids.ts` cursor + the extended/added Vitest specs + the reconciled `05-VALIDATION.md` map.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-CONTEXT.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-REVIEW.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VERIFICATION.md

<interfaces>
<!-- Extracted from the codebase. -->

From apps/api/src/routes/recordings/finalize.ts — the current flow: `SELECT row → 404/403/409 guards (canTransition pending→uploaded, s3UploadId present) → s3.send(CompleteMultipartUploadCommand{ Bucket, Key: keys.video, UploadId: rec.s3UploadId, MultipartUpload: { Parts: body.videoParts } }) → s3.send(CompleteMultipartUploadCommand{ Bucket, Key: keys.imu, UploadId: body.imuUploadId, MultipartUpload: { Parts: body.imuParts } }) → db.transaction(tx => { tx.update(recordings).set({ qaStatus:'uploaded', uploadCompletedAt: new Date() }).where(eq(id, rec.id)); tx.insert(recordingsToVerify).values({ recordingId: rec.id }).onConflictDoNothing(); return after }) → (dev shim: if AWS_ENDPOINT_URL → enqueueVerify) → reply.send(toRecordingResponse(updated))`. Imports: `{ CompleteMultipartUploadCommand } from '@aws-sdk/client-s3'`; `{ getS3Client, RECORDINGS_BUCKET, recordingKeys } from '../../lib/s3-client.js'`.

From apps/api/src/lib/verify-recording.ts — the current flow: `[rec] = SELECT recordings WHERE id = recordingId LIMIT 1; if (!rec) return; if (rec.qaStatus !== 'uploaded') return; [videoSha, imuSha] = await Promise.all([sha256OfS3Object(keys.video), sha256OfS3Object(keys.imu)]); match = videoSha === rec.fileSha256 && imuSha === rec.imuSha256; await db.transaction(tx => { if (match && canTransition(rec.qaStatus,'verified')) { tx.update(recordings).set({ qaStatus:'verified', verifiedAt: new Date() }).where(eq(id, recordingId)); appendOutboxEvent(tx, { userId: rec.userId, recordingId, eventType:'verified' }); } else { tx.update(recordings).set({ qaStatus:'hash-mismatch' }).where(eq(id, recordingId)); appendOutboxEvent(tx, { userId: rec.userId, recordingId, eventType:'re-upload' }); } tx.delete(recordingsToVerify).where(eq(recordingId, recordingId)); })`. Imports `{ eq } from 'drizzle-orm'`, `{ canTransition } from './recording-state.js'`, `{ appendOutboxEvent } from './recording-events.js'`. **drizzle's `tx.update(...).where(...)` returns a result with `.rowCount` (pg driver) — use it.** `and` is available from `drizzle-orm`.

From apps/api/src/plugins/events-outbox.ts — the current onSend hook: `app.addHook('onSend', async (req, _reply, payload) => { const sub = (req.user as {sub?:string}|undefined)?.sub; if (!sub) return payload; if (typeof payload !== 'string') return payload; let body; try { body = JSON.parse(payload); } catch { return payload; } if (body == null || typeof body !== 'object' || Array.isArray(body)) return payload; const rows = await drainOutbox(sub); if (rows.length === 0) return payload; (body as Record<string,unknown>)._events = rows.map(...); await markDelivered(rows.map(r => r.id)); return JSON.stringify(body); })`. **`_reply` is the third callback param (currently unused — rename to `reply` and use `reply.statusCode` + `reply.getHeader('content-type')`).** `fp(..., { name: 'events-outbox', dependencies: ['auth'] })`.

From apps/api/src/routes/recordings/verified-ids.ts — the cursor block: `if (since) { const [c] = await db.select({verifiedAt, id}).from(recordings).where(eq(recordings.id, since)).limit(1); if (c?.verifiedAt) { where.push(sql\`(verified_at, id) < (...)\`); } }`— **add`and(eq(recordings.id, since), eq(recordings.userId, userId))` to that SELECT** (`and` is already imported in this file).

From apps/api/src/lib/recording-state.ts: `canTransition(from, to)` — `uploaded → verified|hash-mismatch|rejected|takedown`. (Still used as the `match`-branch's belt-and-braces check; the real guard becomes the SQL predicate.)

From apps/api/src/lib/problem-detail.ts: `buildProblemDetail` + `PROBLEM_SLUGS`; `application/problem+json` is the content-type the route sets via `reply.type(PROBLEM_CT)`. From apps/api/src/lib/s3-client.ts: `recordingKeys`, `RECORDINGS_BUCKET`, `getS3Client`. `@aws-sdk/client-s3` exposes `HeadObjectCommand` (already a dependency).

Tests: `apps/api/test/routes/recordings-finalize.test.ts` (existing — extended here), `apps/api/test/workers/verify-recording.test.ts` (existing — `describeIf(HAS_LOCALSTACK)`, extended here), `apps/api/test/plugins/events-outbox.test.ts` (**already exists** — created in Plan 05-05 with 4 tests: drains+marks delivered, no cross-user leak, unauthed untouched, strict-schema route still serializes — this plan EXTENDS it with the problem+json-vs-2xx cases, it does NOT replace the suite), `apps/api/test/routes/recordings/verified-ids.test.ts` (**already exists** — created in Plan 05-05 with 4 tests — this plan adds one IN-05 cursor-user-gate case).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-01 — make POST /recordings/:id/finalize retry-safe (NoSuchUpload-tolerant CompleteMultipartUpload + HeadObject confirmation before the state flip)</name>
  <files>apps/api/src/routes/recordings/finalize.ts, apps/api/test/routes/recordings-finalize.test.ts</files>
  <read_first>
    - apps/api/src/routes/recordings/finalize.ts — the FULL file (the two `CompleteMultipartUploadCommand` calls, the `db.transaction` state flip, the dev shim, `toRecordingResponse`)
    - apps/api/src/lib/s3-client.ts — `recordingKeys`, `RECORDINGS_BUCKET`, `getS3Client`
    - apps/api/src/lib/recording-state.ts — `canTransition` (the `pending → uploaded` guard stays)
    - apps/api/test/routes/recordings-finalize.test.ts — the existing finalize-test conventions (Fastify `inject`, the S3 mocking / LocalStack gating, the auth header helper)
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-REVIEW.md — WR-01 (the exact scenario + the prescribed fix)
  </read_first>
  <behavior>
    - Add a helper `async function completeOrConfirm(s3, bucket, key, uploadId, parts): Promise<void>` — `try { await s3.send(new CompleteMultipartUploadCommand({ Bucket: bucket, Key: key, UploadId: uploadId, MultipartUpload: { Parts: parts } })); } catch (err) { const name = (err as { name?: string }).name; if (name === 'NoSuchUpload' || name === 'NoSuchMultipartUpload' || name === 'NoSuchUploadException') { // the multipart upload is gone — either we already completed it on a prior /finalize attempt, or it expired. If the object exists, treat as success (idempotent retry). await s3.send(new HeadObjectCommand({ Bucket: bucket, Key: key })); /* HeadObject throws NotFound/404 if the object doesn't exist → propagate (genuine failure) */ return; } throw err; }`.
    - In the handler, replace the two raw `s3.send(new CompleteMultipartUploadCommand(...))` calls with `await completeOrConfirm(s3, bucket, keys.video, rec.s3UploadId, req.body.videoParts.map(p => ({ PartNumber: p.partNumber, ETag: p.etag })))` and `await completeOrConfirm(s3, bucket, keys.imu, req.body.imuUploadId, req.body.imuParts.map(p => ({ PartNumber: p.partNumber, ETag: p.etag })))`.
    - After both `completeOrConfirm` calls succeed, before the `db.transaction`, do `await s3.send(new HeadObjectCommand({ Bucket: bucket, Key: keys.video }))` and the IMU one — if either throws NotFound, the row stays `pending` (don't flip). This makes the state flip conditional on "both objects confirmed present" (cheap; an extra `HeadObject` per finalize).
    - The `db.transaction` state flip + `recordingsToVerify` insert + the dev shim + `reply.send(toRecordingResponse(updated))` are unchanged. The `canTransition(rec.qaStatus, 'uploaded')` 409 guard at the top stays — but note: a retry where the row is ALREADY `uploaded` (a prior `/finalize` flipped it but the response dropped) currently 409s. **Fold in: also treat a row already in `uploaded` as a successful idempotent finalize — if `rec.qaStatus === 'uploaded'`, skip the `CompleteMultipartUpload` calls (the objects are there) and return `toRecordingResponse(rec)` directly (200), so a `/finalize` retry on an already-finalized row succeeds.** (A row in `verified` / `hash-mismatch` / `rejected` / `takedown` still 409s — those are post-finalize states the client shouldn't be re-finalizing.)
  </behavior>
  <action>
1. In `apps/api/src/routes/recordings/finalize.ts`:
   a. Import `HeadObjectCommand` alongside `CompleteMultipartUploadCommand` from `@aws-sdk/client-s3`.
   b. At the top of the handler, after the `404`/`403` guards: if `rec.qaStatus === 'uploaded'` → `return reply.send(toRecordingResponse(rec))` (idempotent — already finalized; the response just dropped). Then the existing `if (!canTransition(rec.qaStatus, 'uploaded')) → 409` (now only `pending` reaches the complete path; `verified`/`hash-mismatch`/`rejected`/`takedown` still 409); then the `if (!rec.s3UploadId) → 409`.
   c. Add the `completeOrConfirm(s3, bucket, key, uploadId, parts)` helper (module-level function or an inline closure) per the behaviour above.
   d. Replace the two `s3.send(new CompleteMultipartUploadCommand(...))` calls with `completeOrConfirm(...)` calls (video then IMU).
   e. After both succeed, before the `db.transaction`: `await s3.send(new HeadObjectCommand({ Bucket: bucket, Key: keys.video })); await s3.send(new HeadObjectCommand({ Bucket: bucket, Key: keys.imu }));` (these throw if an object isn't actually there → the row stays `pending`, the client retries). Keep the rest as-is.
   f. Update the route's top comment: "Retry-safe: a `/finalize` retry where a `CompleteMultipartUpload` returns `NoSuchUpload` (already completed) `HeadObject`s the key and treats 'present' as success; a row already in `qa_status='uploaded'` short-circuits to a 200 (the prior finalize's response dropped). The row is only flipped to `uploaded` once BOTH objects are confirmed present."
2. `apps/api/test/routes/recordings-finalize.test.ts`: add cases (mirror the existing finalize tests' S3-mocking style — if they use a mock S3 or `aws-sdk-client-mock`, follow it; if they hit LocalStack via `describeIf(HAS_LOCALSTACK)`, gate the new cases the same way):
   - "retry after the video multipart was already completed": stub the video `CompleteMultipartUpload` to throw `{ name: 'NoSuchUpload' }` and `HeadObject(video)` to resolve (object exists); the IMU `CompleteMultipartUpload` resolves; call `/finalize` → `200`, `body.qaStatus === 'uploaded'`, a `recordings_to_verify` row exists.
   - "retry where the video object is also gone": video `CompleteMultipartUpload` throws `{ name: 'NoSuchUpload' }` AND `HeadObject(video)` throws `{ name: 'NotFound' }` (or 404) → the route propagates (the `inject` response is ≥500 / the existing error shape) and the row stays `pending`.
   - "/finalize on an already-uploaded row": pre-set the row to `qaStatus='uploaded'`, call `/finalize` → `200`, no `CompleteMultipartUpload` call was made (assert the mock), `body.qaStatus === 'uploaded'`.
   - "/finalize on a verified row → 409": pre-set `qaStatus='verified'`, call `/finalize` → `409`.
   - keep / re-run the existing happy-path test.
  </action>
  <verify>
    <automated>cd apps/api && pnpm vitest run test/routes/recordings-finalize.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/routes/recordings/finalize.ts` imports `HeadObjectCommand`; has a `completeOrConfirm`-style helper that on `NoSuchUpload`/`NoSuchMultipartUpload` `HeadObject`s the key and returns success if present; calls it for both video + IMU instead of raw `CompleteMultipartUploadCommand`; `HeadObject`s both keys before the `db.transaction`; short-circuits a `qaStatus === 'uploaded'` row to a `200`
    - `grep -n 'HeadObjectCommand\|NoSuchUpload\|NoSuchMultipartUpload' apps/api/src/routes/recordings/finalize.ts` returns hits
    - `cd apps/api && pnpm vitest run test/routes/recordings-finalize.test.ts` exits 0; the new cases (retry-after-already-completed → 200 + verify-queue row; both-gone → propagates + row stays pending; already-uploaded → 200, no Complete call; verified → 409) are present and pass
  </acceptance_criteria>
  <done>`/finalize` is retry-safe — a retry after a `CompleteMultipartUpload` already consumed an upload converges; an already-finalized row 200s; the state flip only happens once both objects are confirmed; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-02 (verify-recording TOCTOU) + WR-03 (events-outbox problem+json) + IN-05 (verified-ids cursor user gate)</name>
  <files>apps/api/src/lib/verify-recording.ts, apps/api/src/workers/hash-verify.ts, apps/api/src/plugins/events-outbox.ts, apps/api/src/routes/recordings/verified-ids.ts, apps/api/test/workers/verify-recording.test.ts, apps/api/test/plugins/events-outbox.test.ts, apps/api/test/routes/recordings/verified-ids.test.ts</files>
  <read_first>
    - apps/api/src/lib/verify-recording.ts — the FULL file (the `rec` SELECT, the re-hash, the `db.transaction` with the two `tx.update(...).set(...).where(eq(id, recordingId))` + `appendOutboxEvent` + `tx.delete(recordingsToVerify)`)
    - apps/api/src/lib/recording-events.ts — `appendOutboxEvent(tx, { userId, recordingId, eventType })` (so the rowCount-gated append is clear)
    - apps/api/src/plugins/events-outbox.ts — the FULL onSend hook (the `_reply` param to rename + use; the parse → attach → `markDelivered` → re-stringify flow)
    - apps/api/src/routes/recordings/verified-ids.ts — the cursor-resolution SELECT (`where(eq(recordings.id, since))` → add the `userId` predicate; `and` is already imported)
    - apps/api/src/lib/recording-state.ts — `canTransition` (kept as the belt-and-braces in the match branch)
    - apps/api/test/workers/verify-recording.test.ts — the existing `describeIf(HAS_LOCALSTACK)` tests + the `stub-bundle.js` fixtures (`STUB_VIDEO_BYTES`, `STUB_VIDEO_SHA256`, etc.) + the `putBundle` helper
    - apps/api/test/plugins/events-outbox.test.ts — the EXISTING suite (4 tests, Plan 05-05) — its `buildApp`/`app.inject` conventions, how it seeds an undelivered `recording_events_outbox` row, the auth-header helper; the new cases are ADDED alongside these, the file is not rewritten
    - apps/api/test/routes/recordings/verified-ids.test.ts — the EXISTING suite (4 tests, Plan 05-05) — its seeding of verified recordings + the `?since=` cursor case; the IN-05 case is ADDED alongside these
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-REVIEW.md — WR-02, WR-03, IN-05 (the exact diagnoses + the prescribed fixes)
  </read_first>
  <behavior>
    - WR-02: in `verifyRecording`, the `match` branch becomes `const res = await tx.update(schema.recordings).set({ qaStatus: 'verified', verifiedAt: new Date() }).where(and(eq(schema.recordings.id, recordingId), eq(schema.recordings.qaStatus, 'uploaded'))); if ((res.rowCount ?? 0) === 1) { await appendOutboxEvent(tx, { userId: rec.userId, recordingId, eventType: 'verified' }); await tx.delete(schema.recordingsToVerify).where(eq(schema.recordingsToVerify.recordingId, recordingId)); } else { /* the row moved during the re-hash — leave it; don't emit, don't delete the queue row (the cron / a fresh enqueue reconverges if it ever goes back to 'uploaded', which it won't from takedown/rejected). Log at info. */ }`. The mismatch branch: `const res = await tx.update(schema.recordings).set({ qaStatus: 'hash-mismatch' }).where(and(eq(schema.recordings.id, recordingId), eq(schema.recordings.qaStatus, 'uploaded'))); if ((res.rowCount ?? 0) === 1) { await appendOutboxEvent(tx, { userId: rec.userId, recordingId, eventType: 're-upload' }); await tx.delete(schema.recordingsToVerify).where(eq(schema.recordingsToVerify.recordingId, recordingId)); } else { /* moved during re-hash — leave it; log at info */ }`. The dead `canTransition(rec.qaStatus, ...)` check is no longer the guard (the SQL predicate is) — keep `canTransition(rec.qaStatus, 'verified')` only as an additional belt-and-braces in the JS `if` if you like, but it's optional now; the must-have is the `AND qa_status='uploaded'` predicate. Thread the worker's logger through `verifyRecording(recordingId, log?)` — `hash-verify.ts`'s worker callback is `async (job) => { await verifyRecording(job.data.recordingId) }`, change it to `async (job) => { await verifyRecording(job.data.recordingId, log) }` (the `log` there is the pino child) and add an optional second arg `log?: { info: (o: unknown, m?: string) => void }` to `verifyRecording`.
    - WR-03: in `events-outbox.ts`'s `onSend` hook, rename the third param `_reply` → `reply`; immediately after the `sub` check, add `if (reply.statusCode < 200 || reply.statusCode >= 300) return payload;` and `const ct = reply.getHeader('content-type'); if (typeof ct !== 'string' || !ct.startsWith('application/json')) return payload;` — only then do the existing `typeof payload !== 'string'` / JSON.parse / object-check / `drainOutbox` / `_events` attach / `markDelivered` / re-stringify. (Note: `reply.getHeader` returns the header set so far; routes that `reply.type('application/problem+json')` will have `ct` start with `application/problem+json` → skipped. The normal carrier routes send `application/json; charset=utf-8` → `ct.startsWith('application/json')` is true → handled.) Update the plugin's top comment to note the 2xx-application/json gate.
    - IN-05: in `verified-ids.ts`, the cursor SELECT `where(eq(schema.recordings.id, since))` → `where(and(eq(schema.recordings.id, since), eq(schema.recordings.userId, userId)))`. One line. Update the inline comment ("A cursor pointing at another user's row resolves nothing — the SELECT is user-gated, not just the result set").
  </behavior>
  <action>
1. `apps/api/src/lib/verify-recording.ts`: import `and` from `drizzle-orm` (alongside `eq`). Replace the `db.transaction` body with the rowCount-gated version above (both branches `AND qa_status='uploaded'`; `appendOutboxEvent` + `tx.delete(recordingsToVerify)` only when `(res.rowCount ?? 0) === 1`; an `else` that does nothing but log at `info`). Add an optional `log?: { info: (o: unknown, m?: string) => void }` second parameter; update `apps/api/src/workers/hash-verify.ts`'s worker callback to `async (job) => { await verifyRecording(job.data.recordingId, log); }` (the `log` there is the pino child). Update `verify-recording.ts`'s top comment: "TOCTOU-safe: the `qa_status` flip carries `AND qa_status='uploaded'` and only writes the outbox event / deletes the queue row if the UPDATE affected a row — an ops takedown / re-upload during the multi-second S3 re-hash is not silently resurrected (WR-02 / VERIFY-03/04)."
2. `apps/api/src/plugins/events-outbox.ts`: rename `_reply` → `reply` in the `onSend` callback signature; add the `statusCode` (2xx) + `content-type` (`application/json`) guards right after the `sub` check, before everything else. Update the top comment.
3. `apps/api/src/routes/recordings/verified-ids.ts`: add `eq(schema.recordings.userId, userId)` to the cursor-resolution SELECT's `where` (wrap in `and(...)`). Update the inline comment.
4. `apps/api/test/workers/verify-recording.test.ts`: add (inside the `describeIf(HAS_LOCALSTACK)` block, using the `putBundle` helper + `stub-bundle.js` fixtures): "row moved to takedown during the re-hash window" — seed a recording in `qaStatus='uploaded'` with a good bundle in S3, then in the test (before calling `verifyRecording`) flip the DB row to `qaStatus='takedown'` directly (`db.update(...).set({ qaStatus:'takedown' }).where(eq(id, recId))`), then call `verifyRecording(recId)` → assert the row is STILL `takedown` (not `verified`), there is NO `recording_events_outbox` row for it, and the `recordings_to_verify` row is LEFT in place (the cron will reap it after MAX_ATTEMPTS — document this in the test comment). Also add a non-LocalStack-needed assertion if feasible (e.g. unit-test the rowCount path with a mocked `tx` — optional; the LocalStack test is the required one if LocalStack is the only way to drive `verifyRecording`).
5. `apps/api/test/plugins/events-outbox.test.ts` (**extend the existing suite — its 4 tests stay, add cases alongside them**): pre-insert an undelivered `recording_events_outbox` row for the test user; `inject` an authed request to a route that responds `404` `application/problem+json` (use `buildApp()` + an existing 404-producing route like `GET /recordings/<bogus-26-char-id>`, or a tiny stub route that `reply.status(404).type('application/problem+json').send({ type:'about:blank', title:'x', status:404 })`) → assert the response body has NO `_events` key AND the outbox row is still `deliveredAt === null`; then `inject` an authed request to a `200` `application/json` route (e.g. `GET /me`, or the existing happy-path route the suite already uses) → assert the response body HAS `_events` with the row AND the outbox row is now `deliveredAt !== null` (this last one is the regression guard for the existing behaviour).
6. `apps/api/test/routes/recordings/verified-ids.test.ts` (**add one IN-05 case to the existing suite**): seed two users — user A and user B — each with a verified recording (so user A has ≥1 verified row); as user A, call `GET /recordings/verified-ids?since=<user B's verified recording id>` → assert the response equals what `GET /recordings/verified-ids` (no `since`) returns for user A — i.e. the out-of-scope `since` resolved nothing, no `(verified_at, id) < (...)` predicate was added; the response is the same id list/order. (Contrast the existing `?since=<user A's own row>` case which DOES bound the result.)
  </action>
  <verify>
    <automated>cd apps/api && pnpm vitest run test/workers/verify-recording.test.ts test/plugins/events-outbox.test.ts test/routes/recordings/verified-ids.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/lib/verify-recording.ts`'s two `UPDATE`s include `AND qa_status = 'uploaded'` (drizzle `and(eq(id,...), eq(qaStatus,'uploaded'))`) and the `appendOutboxEvent` + `tx.delete(recordingsToVerify)` are inside an `if ((res.rowCount ?? 0) === 1)` — `grep -n "qaStatus, 'uploaded'" apps/api/src/lib/verify-recording.ts` returns ≥2 hits; `grep -n 'rowCount' apps/api/src/lib/verify-recording.ts` returns a hit
    - `apps/api/src/plugins/events-outbox.ts` returns `payload` unchanged when `reply.statusCode` is not 2xx or `content-type` doesn't start with `application/json` — `grep -n 'statusCode' apps/api/src/plugins/events-outbox.ts` and `grep -n "application/json" apps/api/src/plugins/events-outbox.ts` both return hits; the hook no longer takes `_reply` (it's `reply`)
    - `apps/api/src/routes/recordings/verified-ids.ts`'s cursor SELECT `where` includes `eq(schema.recordings.userId, userId)` — `grep -n 'eq(schema.recordings.userId, userId)' apps/api/src/routes/recordings/verified-ids.ts` returns ≥2 hits (the main query + the cursor query)
    - `apps/api/src/workers/hash-verify.ts` passes its `log` to `verifyRecording` (`verifyRecording(job.data.recordingId, log)`)
    - `apps/api/test/plugins/events-outbox.test.ts` still has its original 4 tests AND the new problem+json (no `_events`, row stays undelivered) + 2xx-json (regression: `_events` present, row marked delivered) cases; `apps/api/test/routes/recordings/verified-ids.test.ts` has the new IN-05 cross-user `?since=` case
    - `cd apps/api && pnpm vitest run test/workers/verify-recording.test.ts test/plugins/events-outbox.test.ts test/routes/recordings/verified-ids.test.ts` exits 0; `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The worker's `qa_status` flip is TOCTOU-safe (rowCount-gated, `AND qa_status='uploaded'`); the `events-outbox` hook only touches 2xx `application/json` responses; the `verified-ids` cursor is user-gated; the existing `events-outbox.test.ts` is extended (not replaced) and `verified-ids.test.ts` gains the IN-05 case; tests green; `tsc` clean.</done>
</task>

<task type="auto">
  <name>Task 3: reconcile 05-VALIDATION.md's Per-Task Verification Map with the gap-closure task IDs (MINOR 7 — doc only)</name>
  <files>.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VALIDATION.md</files>
  <read_first>
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VALIDATION.md — the "## Per-Task Verification Map" table (the column order: `Task ID | Plan | Wave | Requirement(s) | Threat Ref | Secure Behavior verified | Test Type | Automated Command | File Exists | Status`) and the note at the bottom about renumbered tasks being synced here
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-09-PLAN.md, 05-10-PLAN.md, 05-11-PLAN.md, 05-12-PLAN.md, 05-13-PLAN.md — to copy each task's `<automated>` verify command, `requirements`, and a one-line secure-behavior summary (from the task's `<done>` / `<threat_model>`)
  </read_first>
  <action>
1. In `.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VALIDATION.md`, append rows to the "## Per-Task Verification Map" table (keep the existing rows; keep the exact column format) for each gap-closure task — use the task IDs as they actually shipped (`05-09-01`, `05-09-02`, `05-10-01`, `05-11-01`, `05-12-01`, `05-13-01`, `05-13-02`, `05-13-03`; if any plan's tasks were split/renumbered during execution, use the real IDs):
   - `05-09-01` | 05-09 | 1 | UP-01, UP-04 | T-5-09-01..05 | `/recordings/init` idempotent — SELECT-first; a duplicate for a pending row re-presigns video against the stored s3UploadId + fresh IMU CreateMultipartUpload only → 200 same uploadId; 403 leaks no row fields | integration | `cd apps/api && pnpm vitest run test/routes/recordings/init.test.ts`
   - `05-09-02` | 05-09 | 1 | UP-04 | T-5-09-02,03 | `POST /recordings/:id/parts` re-presigns video+IMU against existing ids, no CreateMultipartUpload/state-change; 403 no row fields; partsCount ≤ MAX | integration | `cd apps/api && pnpm vitest run test/routes/recordings/parts.test.ts && pnpm tsc --noEmit`
   - `05-10-01` | 05-10 | 2 | UP-01, UP-04 | T-5-10-01..05 | re-drain → /recordings/:id/parts (DONE parts' ETags preserved); row.reupload cleared after postReupload; 409/404 from /parts and 409/403 from /init → DeadLetter; parseInitResponse never logs presigned URLs | unit | `cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests 'ai.humynlabs.capture.upload.UploadCoordinatorTest'`
   - `05-11-01` | 05-11 | 1 | UP-06, UP-01, UP-04 | T-5-11-01..04 | drainNow() serialised by ReentrantLock.tryLock() (a 2nd concurrent drain is a no-op); bogus synchronized(queueStore) wrappers removed; DEF-5-01 (Robolectric module compiles) confirmed | unit | `cd apps/mobile/android && ./gradlew :app:testApkRolloutDebugUnitTest --tests 'ai.humynlabs.capture.upload.UploadCoordinatorTest'`
   - `05-12-01` | 05-12 | 1 | VERIFY-01 | T-5-12-01..05 | apps/api/src/workers/sqs-poller.ts is the real prod EventBridge→SQS→BullMQ leg; parseRecordingIdFromS3Event validates the recordings/<ULID>/<ULID>/{video.mp4|imu.csv|metadata.json} key shape (bad key → null → message deleted, never enqueued); bootstrap gated for tests | unit | `cd apps/api && WORKER_BOOTSTRAP=false pnpm vitest run test/workers/sqs-poller.test.ts && pnpm tsc --noEmit`
   - `05-13-01` | 05-13 | 1 | UP-01 | T-5-13-01,05 | /finalize retry-safe — NoSuchUpload-tolerant CompleteMultipartUpload + HeadObject confirmation before the state flip; already-uploaded row → 200; verified row → 409 | integration | `cd apps/api && pnpm vitest run test/routes/recordings-finalize.test.ts`
   - `05-13-02` | 05-13 | 1 | VERIFY-03, VERIFY-04, VERIFY-05 | T-5-13-02,03,04 | verifyRecording qa_status flip TOCTOU-safe (AND qa_status='uploaded' + rowCount-gated outbox/cleanup); events-outbox onSend only touches 2xx application/json (no _events on problem+json, no events lost on errors); verified-ids cursor SELECT user-gated | integration | `cd apps/api && pnpm vitest run test/workers/verify-recording.test.ts test/plugins/events-outbox.test.ts test/routes/recordings/verified-ids.test.ts && pnpm tsc --noEmit`
   - `05-13-03` | 05-13 | 1 | (doc reconciliation) | — | N/A (doc-only — appends the gap-closure rows to this map) | doc / grep | `grep -c '05-09-01\|05-10-01\|05-11-01\|05-12-01\|05-13-01\|05-13-02' .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VALIDATION.md`
   ("File Exists" column: `✅ W0` for the rows whose plan creates its own test scaffold (05-09-02 creates `parts.test.ts`; 05-12-01 creates `sqs-poller.test.ts`); `✅` for the rows extending an existing test (05-09-01, 05-10-01, 05-11-01, 05-13-01, 05-13-02); `N/A` for 05-13-03. "Status" column: `⬜ pending` for all of them.)
2. Do NOT change any other section of `05-VALIDATION.md`; do NOT flip `nyquist_compliant` (it stays `true`); do NOT touch `wave_0_complete` (it's set to `true` during execution by the wave that completes the harness, not here).
3. Update the bottom note in `05-VALIDATION.md` (the one about "If any other plan's tasks are renumbered/split during execution, sync the matching rows here") to add: "Gap-closure plans 05-09…05-13 (commit `af20f00` + the revision pass) are reflected above (rows `05-09-01`…`05-13-03`)."
  </action>
  <verify>
    <automated>grep -c '05-09-01\|05-09-02\|05-10-01\|05-11-01\|05-12-01\|05-13-01\|05-13-02\|05-13-03' .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '05-09-01\|05-09-02\|05-10-01\|05-11-01\|05-12-01\|05-13-01\|05-13-02\|05-13-03' .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VALIDATION.md` returns ≥8 (one hit per appended task-ID row, plus any in the bottom note)
    - the appended rows keep the existing 10-column format (`Task ID | Plan | Wave | Requirement(s) | Threat Ref | Secure Behavior verified | Test Type | Automated Command | File Exists | Status`); each cites the matching task's `<automated>` command verbatim
    - `grep -n 'nyquist_compliant: true' .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VALIDATION.md` still returns a hit (unchanged)
  </acceptance_criteria>
  <done>`05-VALIDATION.md`'s Per-Task Verification Map has rows for the gap-closure task IDs (05-09-01…05-13-03), each citing its `<automated>` verify command; the rest of the doc is untouched; `nyquist_compliant` stays true.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                                                                                                                                                                       |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client → `POST /recordings/:id/finalize` (retry)   | a retried `/finalize` (the coordinator retries on a transient failure) — must converge, not corrupt; the parts/ETags are client-supplied but already validated against the multipart upload by S3 |
| ops action ↔ hash-verify worker                   | an ops `takedown` / a client `/reupload` can flip `qa_status` while the worker is mid-re-hash (multi-second S3 read) — the worker must NOT overwrite a moved row                                  |
| API response → client (`_events` envelope)         | the hook attaches outbox events only to 2xx `application/json` carrier responses — never to RFC-7807 error bodies (which the client treats as a hard failure and doesn't parse for `_events`)     |
| client → `GET /recordings/verified-ids?since=<id>` | `since` is a client-supplied 26-char id — the cursor resolution must be user-gated so it isn't an existence oracle for arbitrary recording ids                                                    |

## STRIDE Threat Register

| Threat ID | Category                           | Component                                                                                                                                                        | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                         |
| --------- | ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-5-13-01 | DoS (stuck recording)              | a `/finalize` that fails between the two `CompleteMultipartUpload`s leaving the row permanently un-finalizable (WR-01)                                           | mitigate    | `completeOrConfirm` swallows `NoSuchUpload`/`NoSuchMultipartUpload` and `HeadObject`s the key — a retry after the video upload was already consumed completes the IMU side and finalizes; a row already in `qa_status='uploaded'` short-circuits to a `200`; the state flip only happens once BOTH objects are confirmed present. No infinite-500 loop. |
| T-5-13-02 | Tampering (state resurrection)     | the worker silently flipping a `takedown`/`rejected`/re-uploaded row back to `verified` (WR-02 — a legal/ops takedown getting undone)                            | mitigate    | Both `UPDATE`s carry `AND qa_status = 'uploaded'` — a row that moved during the re-hash window matches 0 rows; the outbox event + the `recordings_to_verify` delete only happen on `rowCount === 1`; a 0-row update logs at `info` and returns without side effects. A `takedown` stays `takedown`.                                                     |
| T-5-13-03 | Information disclosure / Tampering | RFC-7807 error bodies carrying a non-standard `_events` key, and outbox events lost (marked delivered) on a response the client treats as a hard failure (WR-03) | mitigate    | The `onSend` hook returns `payload` untouched unless `reply.statusCode` ∈ [200,300) AND `content-type` starts with `application/json` — `application/problem+json` and every non-2xx response is skipped; pending events stay undelivered and are picked up on the next 2xx carrier response (and the reconcile sweep backstops `verified` either way). |
| T-5-13-04 | Information disclosure             | the `verified-ids` cursor SELECT as an existence oracle for arbitrary recording ids (IN-05)                                                                      | mitigate    | The cursor-resolution `SELECT` adds `AND user_id = <caller>` — an out-of-scope `since` resolves nothing (no extra predicate added, no observable difference between "id exists for someone else" and "id doesn't exist"). No cross-tenant data was ever leaked; this closes the timing/shape oracle.                                                    |
| T-5-13-05 | Tampering                          | a malicious `videoParts`/`imuParts` array in `/finalize` (no upper bound — WR-05, related)                                                                       | accept      | Out of scope for this gap-closure plan (WR-05 is a `warning`, not a blocker, and not in the verifier's gap list). Noted: `RecordingFinalizeSchema` is unbounded; AWS rejects > 10 000 parts; a follow-up could add `.max(1000)` + a `videoParts.length === rec.partsCount` check. Not done here.                                                        |

</threat_model>

<verification>
- `cd apps/api && pnpm vitest run test/routes/recordings-finalize.test.ts test/workers/verify-recording.test.ts test/plugins/events-outbox.test.ts test/routes/recordings/verified-ids.test.ts` exits 0
- `cd apps/api && pnpm tsc --noEmit` exits 0
- `grep -n 'HeadObjectCommand' apps/api/src/routes/recordings/finalize.ts` returns a hit
- `grep -n "qaStatus, 'uploaded'" apps/api/src/lib/verify-recording.ts` returns ≥2 hits; `grep -n 'rowCount' apps/api/src/lib/verify-recording.ts` returns a hit
- `grep -n 'statusCode' apps/api/src/plugins/events-outbox.ts` returns a hit; `grep -cn '_reply' apps/api/src/plugins/events-outbox.ts` returns 0
- `grep -n 'eq(schema.recordings.userId, userId)' apps/api/src/routes/recordings/verified-ids.ts` returns ≥2 hits
- `grep -c '05-09-01\|05-10-01\|05-11-01\|05-12-01\|05-13-01\|05-13-02\|05-13-03' .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-VALIDATION.md` returns ≥7
</verification>

<success_criteria>

- `/finalize` is retry-safe — `NoSuchUpload`-tolerant `CompleteMultipartUpload` + `HeadObject` confirmation + an already-`uploaded` row short-circuits to 200; the state flip only happens once both objects are confirmed (WR-01)
- the hash-verify worker's `qa_status` flip carries `AND qa_status='uploaded'` and only writes the outbox event / deletes the queue row on `rowCount === 1` (WR-02 / VERIFY-03/04)
- the `events-outbox` `onSend` hook only touches 2xx `application/json` responses — no `_events` on problem+json, no events lost on error responses (WR-03 / VERIFY-05)
- the `verified-ids` cursor SELECT is user-gated (IN-05)
- the existing `events-outbox.test.ts` is extended (not replaced) and `verified-ids.test.ts` gains the IN-05 case; Vitest green for `recordings-finalize.test.ts` + `verify-recording.test.ts` + `events-outbox.test.ts` + `verified-ids.test.ts`; `tsc --noEmit` clean
- `05-VALIDATION.md`'s Per-Task Verification Map has rows for the gap-closure task IDs; `nyquist_compliant` stays true
  </success_criteria>

<output>
After completion, create `.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-13-SUMMARY.md`
</output>
