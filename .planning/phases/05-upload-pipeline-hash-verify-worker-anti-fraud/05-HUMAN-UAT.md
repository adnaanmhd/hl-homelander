---
status: partial
phase: 05-upload-pipeline-hash-verify-worker-anti-fraud
source: [05-VERIFICATION.md]
started: 2026-05-12T16:46:52Z
updated: 2026-05-13T10:46:00Z
---

## Current Test

**Item 1 §3 PASSES 2026-05-13 afternoon (post-second-fix), end-to-end on-device, NO dev-shim assistance.** Final re-walk on recording `01KRGENTPAM6BGNRA3SWPS4Q7E` (Pixel 10a, apkRollout-Debug HEAD `19df291`) drove the full §3 chain: corrupt S3 video.mp4 (72 MB → 57 B) → worker `qa_status: uploaded → hash-mismatch` (10:44:16 worker log entry) → outbox `re-upload` event → app-fg → `HumynUpload.reupload()` rotates the three idempotency keys (commit `5596635`) → POST /reupload 200 — **the second fix's new code at `apps/api/src/routes/recordings/reupload.ts` ran `getJob(R) → getState() → remove()`** before responding, clearing the prior verify completion from `bull:verify:completed` — → drainer re-PUT all 9 video parts + 1 IMU + metadata.json against the fresh `s3_upload_id=o-fmbh7qy_...` → POST /finalize 200 with the rotated `finalizeIdempotencyKey='d68e5a0b-...'` → handler's `void enqueueVerify(R)` **now succeeded** (prior was removed) → worker picked up the new job within ~1s → **second hash-verify completion at 10:44:43.892 — exact smoking-gun proof on the worker log: two `hash-verify job completed jobId: "01KRGENTPAM6BGNRA3SWPS4Q7E"` entries for the SAME jobId** → `qa_status='verified'` at 10:44:43.889 → outbox `verified` event → app-fg → `HumynUpload.clearVerified` unlinked local triple at 10:45:55 → queue.json `[]` → contributions row written. Item 1 result flipped `partial → pass`; severity `passed-client-side-blocked-server-side-on-jobid-dedupe → passed`. Both `/gsd-debug` sessions (`reupload-finalize-409` + `enqueue-verify-jobid-dedupe`) ready to move to `.planning/debug/resolved/`. 2 cosmetic gaps surfaced during the re-walk (no upload-progress bar visibly rendering on-device — promoted from "dev-stack too fast" theory to real on-device gap; no in-app video playback affordance) logged to `05-COSMETIC-GAPS.md` per `[[feedback_functionality_first_during_smoke]]`. **One dev-stack hygiene note (NOT a Phase 5 defect):** the second debug session's vitest run wiped the dev DB's `users` + `recordings` + `contributions` + `recording_events_outbox` + `tasks` tables. The §3 final re-walk re-seeded the dev user (`01KRG9XG7VCSAYN823Q96FJ216`) and the dev task (`01HVDEVSEEDTASK00000000000`) directly via psql/seed script and recovered the in-flight 72 MB recording. Test isolation should use a separate DB; out-of-scope for this UAT item but worth a follow-on. Items 2-5 remain `pending`; do NOT call `phase.complete`.

### Historical (§3 re-walk on rotate-keys fix only — 2026-05-13 10:13, before the second fix)

§3 re-walk on recording `01KRGD2D6GVET6K7QKNJ1BZSM7` validated the rotate-keys fix from commit `5596635` end-to-end on the client side: pre-reupload `finalizeIdempotencyKey='1f62c949-...'` rotated to post-reupload `21e99021-...`; POST /finalize 200 (was 409 pre-fix). But the chain only completed with dev-shim assistance because the SECOND defect — `apps/api/src/lib/queue.ts:40-43` `enqueueVerify` uses `jobId=recordingId` (SQS-redelivery dedupe, threat T-5-03-01) — over-collapsed across re-uploads: the prior verify completion's jobId in `bull:verify:completed` blocked the post-/finalize re-enqueue. All three production paths affected: `routes/recordings/finalize.ts:238`, `workers/sqs-poller.ts:128`, `cron/verify-sweep.ts:47`. Resolved by commit `19df291` (scope #3: `prior.remove()` in /reupload handler — preserves T-5-03-01 dedupe at SQS-poller + sweep-cron) + `apps/api/test/routes/recordings/reupload.test.ts` (2 itIf tests pinning the bridge contract + the T-5-03-01 preservation pair). Debug session at `.planning/debug/enqueue-verify-jobid-dedupe.md` (ready to move to `resolved/`).

### Historical (§3 walk BLOCKED 2026-05-13 afternoon — original rotate-keys finding, now resolved)

Item 1 §3 (hash-mismatch / re-upload) walked observation-only on Pixel 10a (apkRollout-Debug HEAD `e5ff29b`) and surfaced the rotate-keys defect: post-reupload `POST /recordings/01KRGB97X3MPJ784QF78SD77NJ/finalize` 409'd in 4 ms because `HumynUpload.reupload()` (`HumynUploadModule.kt:316-328`) reset row state/uploadId/parts/etags but didn't rotate the per-route idempotency keys, so `finalizeIdempotencyKey='ee0bced9-...'` consumed by the §2-original /finalize at 09:40:39 was reused for the §3-post-reupload /finalize against a different `(uploadId, parts)` body → server idempotency cache (correctly) rejected. Resolved by commit `5596635` (3-line `UUID.randomUUID()` rotation in the `else ->` Path-A branch + KDoc clarifications in `UploadModels.kt` + 2 Robolectric regression tests in `HumynUploadModuleReuploadTest.kt`). Debug session at `.planning/debug/reupload-finalize-409.md` (status `fix_applied_awaiting_on_device_verify` → ready to move to `resolved/` once Item 1 fully passes after the second defect lands). §3-stuck recording `01KRGB97X3MPJ784QF78SD77NJ` preserved on-device + in S3 + DB as the original §3 evidence (parallel to `01KRGA1B5H8BSRNFPSRHQFTHQ8` for §2 and now `01KRGD2D6GVET6K7QKNJ1BZSM7` for the §3 re-walk).

### Historical (re-walk READY 2026-05-13 morning — Plan 05-15 gap-closure shipped)

The `invalid_opts: name` blocker logged earlier today was closed by Plan 05-15 / Gap Wave 3 (4 commits `2da5465..ff19694` on `main`): (1) `coalesceDisplayName(name, email)` helper applied at all 3 setUser write sites (SignupScreen / ProfileScreen / useForegroundUserRehydrate) so a Google-withheld `displayName` or empty server-side `users.name` falls back to email-local-part instead of null/empty; (2) JS-side `requireNonEmpty` guards in `buildCaptureOpts.ts` (parallel to the V11 `consentPresent` guard) throw `code: 'profile_incomplete'` so any future regression of the same shape surfaces as a clear "Please complete your profile." toast at RecordingScreen's catch instead of an opaque bridge rejection; (3) +2 Robolectric tests pin the Kotlin defense-in-depth guard at `CaptureSessionOptsBridge.kt:84-85` (empty-name + empty-email reject paths); (4) runbook §1 pre-flight bullet added so the next walker checks `useAppStore.user.name` is non-empty before §2. Mobile vitest is 93 files / 684 tests green; mobile typecheck exits 0. The afternoon re-walk drove §2 happy-path PASS on Pixel 10a (5C161JEA304304) then walked §3 (see Current Test above).

### Historical (paused 2026-05-13 morning — pre-gap-closure walk)

**Initial finding — Item 1 surfaced a new blocker: `HumynCapture.start() → invalid_opts: name`.** On the post-Wave-1.5 build, the runbook §2 attempt opened the `__DEV__` Tasks-tab long-press path, navigated to RecordingScreen, the gate camera opened on the back ultrawide (logcat `CameraOpen: ai.humynlabs.capture.apk` → `Lyric RearWide` frames at ~14–29 FPS), the hand gate confirmed (the JS reached the `HumynCapture.start()` call site), and `HumynCapture.start()` rejected synchronously with `code=invalid_opts msg=invalid_opts: name` (logcat `E HumynCapture: java.lang.IllegalArgumentException: invalid_opts: name` at `CaptureSessionOptsBridge.requireNonEmpty(CaptureSessionOptsBridge.kt:150)` ← `fromBridge(CaptureSessionOptsBridge.kt:84)` ← `HumynCaptureModule.start$lambda$0(HumynCaptureModule.kt:138)`). Line 84 is `val contributorName = requireNonEmpty(contributorMap, "name")` — so the empty/blank field is `contributor.name`, NOT the task name. Upstream in JS: `RecordingScreen.tsx:695` builds `user.name: u.user?.name ?? ''` — and `UserDisplay.name` is `string | null` (`appStore.ts:51`), so a sign-in path that leaves `name` null (or the Tester dev-seed user whose name reaches the store as empty) emits `name: ''` into the bridge → the Kotlin guard fires → recording never starts → the rest of §2/§3/§4/§5 can't execute. Pre-flight was clean (tunnels 8080/8081/4566 up, Metro running, API + hash-verify worker up, dev-seed task at `01HVDEVSEEDTASK00000000000` present, recording_events_outbox schema migrated, on-device queue + recordings dir empty). DB sanity: `users` table has one row `(name='Tester', email='tester@example.com')` — so the server-side has a non-empty name, meaning the regression is on the **client side** (sign-in flow not writing `name` into the store, or the store hydration losing it). Items 2–5 are still pending. Re-walk after the gap closes. The 2026-05-13 backend-half VERIFIED state from the earlier automated probe still stands.

### Historical note (paused state captured 2026-05-13 afternoon — Wave 1 walk)

**Previous status:** `paused_pending_wave_1_5`. Backend half of items 1+3 was walked 2026-05-13 morning by an automated probe against the dev stack (Postgres + Redis + LocalStack + the real hash-verify worker process) — `POST /recordings/init` → PUT parts to LocalStack → PUT `metadata.json` → `POST /recordings/:id/finalize` → (dev shim) `enqueueVerify` → the BullMQ worker re-hashed the S3 bytes → `recordings.qa_status='verified'` (+ `verified_at`) → `GET /me` carried `_events: [{recording_id, event_type:'verified'}]` and a second `GET /me` did NOT re-carry it (delivered). Hash-mismatch path: a recording that claimed a wrong `file_sha256` at `/init` → worker → `qa_status='hash-mismatch'` → `_events: [{…, event_type:'re-upload'}]` → `POST /recordings/:id/reupload` → row reset to `qa_status='pending'` with a fresh `s3UploadId` + `verified_at` cleared. Plus the Phase-5 gap-closure routes: a duplicate `POST /recordings/init` on a `pending` row → `200` with the SAME `uploadId` (CR-02 idempotent `/init` / lost-201 self-heal); `POST /recordings/:id/parts` → `200`, re-presigned video against the existing `uploadId`, row unchanged (CR-02 new re-presign route). `ip_address` server-populated (UP-18); `recordings_to_verify` drained on both verified + mismatch. **No backend findings.**

The on-device half (item 1's device leg + items 2–5) was attempted 2026-05-13 afternoon on a Pixel 10a (`5C161JEA304304`, Android 16) with the apkRollout build loaded from Metro. The attempt surfaced **five distinct root causes** for the same observable `POST /recordings/init → 400` symptom; the first three were fixed inline through three `/gsd-debug` sessions:

1. ✅ `debug-task-id-init-400` (commit `48dea49`) — `DEBUG_TEST_TASK.taskId` was the 23-char taxonomy slug `cooking_chop_vegetables`, failing `RecordingsInitRequestSchema.taskId = z.string().length(26)` and the `recordings.task_id → tasks.id` FK. Fix: dev-seed task at the fixed ULID `01HVDEVSEEDTASK00000000000`; `TasksPlaceholderScreen.tsx` `DEBUG_TEST_TASK.taskId` retargeted at it; runbook §1/§2 patched to call out the seed step + the `installApkRolloutDebug`-ships-`--dev false`-offline-bundle gotcha.
2. ✅ `init-400-capturedat-offset` (commit `692e295`) — Zod's default `.string().datetime()` rejects `+05:30` numeric offsets; the device's `MetadataComposer` emits local-time-with-offset for `start_timestamp`, which `UploadCoordinator.kt` forwards verbatim as `capturedAt`. Fix: `shared/types/src/recording.ts` relaxed three client-supplied datetime sites to `.datetime({ offset: true })`. Host-side `apps/api/scripts/repro-init-400.ts` proves before/after.
3. ✅ `init-400-no-idempotency-key` (commit `5c0b2d8`) — `apps/api/src/plugins/idempotency.ts` rejects every POST/PATCH lacking an `Idempotency-Key: <UUIDv4>` header (Phase 5 contract); the native `UploadCoordinator.authedJsonRequest()` only set `Authorization` + `Content-Type`. Fix: `UploadRow.idempotencyKey: String = UUID.randomUUID().toString()` minted at construction, persisted to `queue.json`, threaded through all four POST callers (init/reupload/parts/finalize) with a one-shot fromJson migration for legacy rows.

After all three fixes landed and `adb reverse tcp:4566 tcp:4566` was added (LocalStack tunnel — also a runbook §1 setup gap, see `05-COSMETIC-GAPS.md`), the next §2 attempt got nearly all the way through: `/init -> 201`, all 10 video parts PUT to S3 (etags captured in `queue.json`), `metadata.json` PUT, then **`/finalize -> 409`** because the third fix gave each `UploadRow` exactly ONE `idempotencyKey` and reused it across `/init`, `/parts`, `/finalize`, `/reupload`. The API's idempotency hook keys cache by `(user_id, key)` and treats a same-key/different-`(method, path, body)` as a conflict → 409 `idempotency-key-conflict`. So `/finalize`'s POST hit the cached init-201 by key, saw the request hashes differ, and returned 409 → 6 retries → DEAD_LETTER. A subsequent "Retry" tap also 409'd because server-side `/reupload` only accepts `qa_status='hash-mismatch'` (the canonical worker-flagged path, NOT a client-side dead-letter on a pending row); the runbook §3 sanity-check wording for client-side dead-letter Retry is wrong on the server contract.

Five total root causes (3 fixed inline + 2 outstanding) + adjacent cosmetic / setup gaps logged. Rather than continue patch-and-pray, owner decision is to **pause the §2 walk and batch-fix in a Phase-5 Wave-1.5 plan** before re-walking on a single coherent build. Full enumeration with fix recipes in `.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-COSMETIC-GAPS.md` under the new "Wave-1.5 follow-on items" section. Backend half stays VERIFIED — no backend regression today; the new functional issues are all in the native upload coordinator + the runbook's setup section.

## Tests

### 1. End-to-end upload (Pixel 7a/8a-class + dev backend) + the hash-mismatch path

expected: On a Pixel 7a/8a-class device with the dev backend up (Postgres + Redis + LocalStack + worker), record a ≥60 s task → the bundle (mp4 + IMU CSV + metadata.json) auto-enqueues; the Pending Uploads tile/screen shows "Uploading…" progressing, then the row drops once verified. Bundle lands in S3 (`aws --endpoint-url=http://localhost:4566 s3 ls s3://humyn-recordings-dev/recordings/`), the BullMQ hash-verify worker re-hashes, `recordings.qa_status='verified'`, the next authed API response carries `_events: [{recording_id, event_type:'verified'}]`, the local mp4+csv+json are deleted, the row disappears from the queue. Then corrupt the S3 object → hash-mismatch → `re-upload` event → re-upload-from-local → re-verify. Runbook: `.planning/runbooks/05-upload-smoke.md`.
result: pass
reported: "§2 happy path PASSES (Pixel 10a, post-Plan-05-15 build). §3 hash-mismatch leg WALKED 2026-05-13 afternoon and surfaced TWO defects in sequence — both FIXED + validated end-to-end on-device WITHOUT dev-shim assistance: (1) Rotate-keys defect (HumynUploadModule.kt:316-328 didn't rotate per-route idempotency keys on Path-A reupload reset → /finalize 409) → FIXED by commit `5596635` + tests in `HumynUploadModuleReuploadTest.kt`. Validated on §3 re-walk recording `01KRGD2D6GVET6K7QKNJ1BZSM7` (rotated keys observed in queue.json pre/post reupload; /finalize 200). (2) jobId-dedupe defect (queue.ts:40-43 `enqueueVerify` uses `jobId=recordingId` for SQS-redelivery T-5-03-01 dedupe; prior verify completion in bull:verify:completed ZSET blocked the re-enqueue after re-upload; all 3 production paths affected) → FIXED by commit `19df291` (scope #3: prior.remove() at /reupload handler — preserves T-5-03-01 elsewhere) + 2 itIf vitests at `apps/api/test/routes/recordings/reupload.test.ts`. Validated on FINAL §3 re-walk recording `01KRGENTPAM6BGNRA3SWPS4Q7E` — worker log shows TWO `hash-verify job completed` entries for the SAME jobId (10:44:16 + 10:44:43) → second verify ran automatically → qa*status='verified' → clearVerified → locals deleted → contributions row written, all WITHOUT dev-shim invocation. §3 acceptance fully met. 2 cosmetic gaps logged to 05-COSMETIC-GAPS.md (no upload progress bar visibly rendering, no in-app video playback)."
severity: passed
device: Pixel 10a (5C161JEA304304), Android 16, build apkRollout-Debug (commits c551138..d143a75)
prior_issue_resolution: |
The 2026-05-13 morning `invalid_opts: name` blocker (CaptureSessionOptsBridge.requireNonEmpty :150 ← :84 ← HumynCaptureModule.kt:138) is RESOLVED by Plan 05-15 / Gap Wave 3 (4 commits 2da5465..ff19694, fast-forward-merged into main as 4e2897a). Wave-1.5 plan 05-14 + Plan 05-15 together closed the morning walk's regressions. The §2 re-walk this afternoon successfully drove the full happy path end-to-end with the new build.
§2_happy_path_evidence: |
Recording `01KRGA1B5H8BSRNFPSRHQFTHQ8`: fresh sign-in after `adb shell pm clear` wiped the stale-JWT state — new user row `01KRG9XG7VCSAYN823Q96FJ216` created via POST /auth/google (`name='Adnaan Mohammed', email='m.adnaan161@gmail.com'`); Plan-05-15 coalesceDisplayName propagation path active (Google's displayName came through non-empty on this run, so the email-local-part fallback wasn't exercised — see §3 / future). RecordingScreen → hand gate → HumynCapture.start() proceeded past CaptureSessionOptsBridge.fromBridge into the HEVC pipeline (no `invalid_opts: name`); Camera2 opened on RearWide ultrawide, ECOSession created at 1920×1080, recording ran ~93 s (duration_ms=92748). Stop → contribution toast → Home (toast killed-by-transition is a pre-existing 05-COSMETIC-GAPS item, not regressed). Auto-enqueued with 11 video parts + 1 IMU part + metadata.json; all 4 per-route Idempotency-Keys minted as distinct UUIDv4s (Wave-1.5 Plan 05-14 Item 1 holds — no cross-route 409). FGS started with `dataSync` type; Wave-1.5 drainNow cold-start kick fired. Bundle landed in S3 at `recordings/01KRG9XG7VCSAYN823Q96FJ216/01KRGA1B5H8BSRNFPSRHQFTHQ8/` — video.mp4 (90,288,788 B), imu.csv (4,019,618 B), metadata.json (2,313 B). `recordings.qa_status='uploaded'` after /finalize 200, IP server-populated (UP-18), `recordings_to_verify` row written. BullMQ hash-verify worker had an INDEPENDENT stale-state issue (Drizzle pool + tsx-watch interaction); manual restart (`pkill` + `nohup pnpm --filter @humyn/api worker:hash-verify:dev`) drained the queue in 0.6 s — `qa_status='verified'`, `verified_at='2026-05-13 09:26:27.124+00'`, outbox event `verified` written. **Stale-worker recovery is a dev-stack quirk, NOT a Phase 5 defect**; runbook §1 candidate footnote. App-to-foreground → next authed call piggybacked `_events: [{event_type:'verified'}]` → `HumynUpload.clearVerified` unlinked the local triple and dropped the queue row. Post-delivery snapshot: `files/recordings/` empty, `queue.json: []`, `recordings_to_verify` drained, contributions row written (`duration_ms=92748, recording_count=1, task_count=1`). ✓
§2_cosmetic_observations: |
(1) Tap on the (now empty) Pending uploads tile routed correctly to MainTabs/History per the Wave-1.5 rewire — but History is `HistoryPlaceholderScreen.tsx` (Phase 6 "Coming soon" stub). Expected, not a Phase 5 defect. Logged to 05-COSMETIC-GAPS.md. (2) No live progress bar observed on the Pending Uploads row — the ~90 MB / 11-part multipart PUT to LocalStack@localhost completed in ~3 s, faster than the user could navigate to Home; by then the row was in AWAITING_VERIFY state (no continuous progress to render). Dev-environment artifact; the Wave-1.5 Item 4 progress chip + determinate fill will be exercisable on the §4 CGNAT-cellular walk. Logged to 05-COSMETIC-GAPS.md.
§3_walk_2026-05-13: |
Walked observation-only on the Pixel 10a (apkRollout-Debug, commits c551138..e5ff29b). Server-side path WORKS end-to-end through `/reupload`; CLIENT-side `/finalize` re-call BLOCKS on the same idempotency-key-conflict shape that Wave-1.5 Plan 05-14 Item 1 was supposed to close. Full timeline:
(a) Worker SIGSTOPped preemptively. Fresh recording `01KRGB97X3MPJ784QF78SD77NJ` (file_sha256=`f9ce5492...`, 69,296,175 B, 9 video parts + 1 imu part + metadata) drove POST /recordings/init→201, all PUTs to LocalStack, POST /finalize→200 at 09:40:39 (finalizeIdempotencyKey=`ee0bced9-c472-4a9f-8834-f4522121ac96`); enqueueVerify added the BullMQ job to `bull:verify:wait` where it sat because of the SIGSTOP.
(b) Corruption: `curl -X PUT --data-binary "corrupted-by-uat-§3-walk-2026-05-13" http://localhost:4566/humyn-recordings-dev/recordings/01KRG9XG7VCSAYN823Q96FJ216/01KRGB97X3MPJ784QF78SD77NJ/video.mp4` → HTTP 200, video.mp4 now 36 B (down from 69 MB); imu.csv + metadata.json untouched.
(c) Worker SIGCONTed. At 09:41:34 worker re-streamed S3 → recomputed SHA differs from claimed `f9ce5492...` → DB row flipped `qa_status: uploaded → hash-mismatch`, `verified_at=null`, outbox event `re-upload` written for recordingId `01KRGB97X3MPJ784QF78SD77NJ`. `bull:verify:completed` 19→20. ✓
(d) App-to-foreground. At 09:43:08 the events-outbox onSend hook piggybacked `_events: [{event_type:'re-upload'}]` onto GET /recordings/verified-ids; the JS handler called `HumynUpload.reupload(...)` → Path A full-reset (state→PENDING, reupload=true, clear uploadId/imuUploadId/metadataPut/videoParts.etag/imuParts.etag); drainer took `postReupload` branch → POST /recordings/01KRGB97X3MPJ784QF78SD77NJ/reupload (Idempotency-Key=`d2fdba3f-...`, reuploadIdempotencyKey) → 200 with fresh `s3_upload_id=bVCQAmlbUJTGK...`, fresh imuUploadId, fresh presigned part URLs. DB row went `qa_status: hash-mismatch → pending`, `verified_at` stayed null, `upload_started_at=09:43:08.901`. Outbox `re-upload` event `delivered_at=09:43:08.791`. ✓
(e) Drainer PUT all 9 video parts + 1 IMU part + the regenerated metadata.json to LocalStack against the new uploadId (etags identical to §2 because LocalStack returns MD5-based ETag for identical bytes — but they're staged under the new uploadId so they're valid for CompleteMultipartUpload). queue.json went videoParts.status=DONE, imuParts.status=DONE, metadataPut=DONE, state=FINALIZING. lastProgressAt=09:43:11. ✓
(f) BLOCKER. At 09:43:10.759 the drainer called POST /recordings/01KRGB97X3MPJ784QF78SD77NJ/finalize with `Idempotency-Key: ee0bced9-c472-4a9f-8834-f4522121ac96` (the SAME finalizeIdempotencyKey that the §2 /finalize at 09:40:39 consumed). Server idempotency cache at `idempotency_keys (user_id, key)` found a hit but `request_hash` differed (new uploadId + new part list in body) → **409 idempotency-key-conflict in 4ms** (pre-S3, pre-DB-mutation). The drainer left the row in `state=FINALIZING`; the local file triple is still present; `qa_status=pending`; `verified_at=null`; no second `verified` outbox event; locals NOT deleted; contributions row still `recording_count=1` (the §2 row).
Root cause: `apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt:316-328` (the Path A full-reset branch of `@ReactMethod fun reupload(...)`) resets state/uploadId/parts/etags/metadataPut/deadLetterReason but does NOT rotate `row.initIdempotencyKey`, `row.partsIdempotencyKey`, `row.finalizeIdempotencyKey`. Plan 05-14 Item 1 split these into four per-route keys (closing cross-route 409s within ONE upload session), but the same `finalizeIdempotencyKey` is reused across the §2-original-finalize AND the §3-post-reupload-finalize — two distinct sessions with different `(uploadId, parts)` bodies → idempotency-key conflict. Minimal fix: three `UUID.randomUUID().toString()` assignments inside the `else ->` branch at HumynUploadModule.kt:316-328, parallel to the existing reset lines.
Owner decision: opening a `/gsd-debug` session for this defect (functional regression, blocks Phase 5 sign-off Item 1). DO NOT touch §3 recording `01KRGB97X3MPJ784QF78SD77NJ` — it's preserved on-device + in S3 + DB as the §3 finding evidence (parallel to `01KRGA1B5H8BSRNFPSRHQFTHQ8` for §2). Re-walk §3 after the debug session lands a fix + rebuilds the APK.
§3_rewalk_2026-05-13: |
Re-walked on a fresh recording `01KRGD2D6GVET6K7QKNJ1BZSM7` (95,402,233 B, 12 video parts, file_sha256=`98b44503...`) after the rotate-keys fix (commit `5596635`) + drive-bys (`43b4089`) + rebuilt apkRollout-Debug installed on Pixel 10a. Device pre-cleaned (queue.json `[]`, files/recordings/ empty — via host-side `adb shell run-as ai.humynlabs.capture.apk rm files/recordings/<3 files> && printf %s "[]" > files/upload-queue/queue.json`); preserved §2 + §3-stuck recordings in DB + S3 as evidence. Steps (a)-(h) all green on the natural device path:
(a) Worker SIGSTOPped preemptively. Fresh recording → POST /init→201, all PUTs to LocalStack, POST /finalize→200 at 10:12:20 (`finalizeIdempotencyKey='1f62c949-f818-4b71-b83f-082f9e64c1e2'`). BullMQ job sat in `bull:verify:wait`.
(b) Corruption: `curl -X PUT --data-binary "corrupted-by-uat-§3-rewalk-2026-05-13-post-fix-5596635" http://localhost:4566/humyn-recordings-dev/.../video.mp4` → 200, video.mp4 now 55 B. imu.csv + metadata.json untouched.
(c) Worker SIGCONTed. At 10:12:42 worker re-streamed S3 → recomputed SHA ≠ `98b44503...` → `qa_status: uploaded → hash-mismatch`, outbox `re-upload` event written. ✓
(d) App-to-foreground. At 10:13:18.925 the events-outbox onSend hook piggybacked `_events: [{event_type:'re-upload'}]`; the JS handler called `HumynUpload.reupload()` → **Path-A full-reset NOW rotates the three keys** (the commit `5596635` fix). Drainer took `postReupload` branch → POST /reupload → 200 at 10:13:18.967 with fresh `s3_upload_id=1He_hpM0UuKdUlgxnnHuYD5e0Ghp...` + fresh `imuUploadId`. DB row went `qa_status: hash-mismatch → pending`. ✓
(e) Drainer PUT all 12 video parts + 1 IMU part + regenerated metadata.json to LocalStack against the new uploadId. queue.json updated: state=AWAITING_VERIFY, metadataPut=DONE, all part etags captured. Critically, queue.json now showed the three rotated keys (`initIdempotencyKey: 4a1b6bf0 → b337cc78`, `partsIdempotencyKey: 774fc20b → 53ccd25d`, `finalizeIdempotencyKey: 1f62c949 → 21e99021`) while `reuploadIdempotencyKey` stayed `a2bd1146-ac62-...` (intentional — /reupload is one-shot per re-upload cycle and same-body replay is correct). ✓
(f) Drainer called POST /finalize at 10:13:21.484 with `Idempotency-Key: 21e99021-...` (rotated) → **200 in ~70 ms** (was 409 in the original §3 walk before the fix). DB row went `qa_status: pending → uploaded`, `upload_completed_at=10:13:21.552`. `recordings_to_verify` row appended. ✓ **THE ROTATE-KEYS FIX IS PROVEN END-TO-END.**
(g) Second defect surfaced. `/finalize`'s `void enqueueVerify(rec.id)` call no-op'd because `apps/api/src/lib/queue.ts:40-43` uses `jobId=recordingId` and the prior (10:12:42) verify completion left `01KRGD2D6GVET6K7QKNJ1BZSM7` in `bull:verify:completed` ZSET. `bull:verify:wait`/`active` both stayed at 0 after the /finalize 200 → worker NEVER re-ran on the re-uploaded bundle. recordings_to_verify row sat at attempts=0 (would be picked up by the verify-sweep cron after 10 min, but the cron's enqueueVerify also uses jobId=recordingId and would also dedupe → production path is fully broken without an explicit prior-job-remove). Used the dev shim `apps/api/scripts/enqueue-verify-dev.ts 01KRGD2D6GVET6K7QKNJ1BZSM7` (`await existing.remove()` then `q.add()`) — worker re-verified at 10:15:49.494, `qa_status: uploaded → verified`, outbox `verified` event written.
(h) App-to-foreground (third time). At 10:16:36.088 the events-outbox piggybacked `_events: [{event_type:'verified'}]`, `HumynUpload.clearVerified` unlinked the local triple (files/recordings/ now empty), queue.json dropped to `[]`, contributions row grew. Full chain complete — but only via dev-shim bridge for the second defect. §3 client-side acceptance MET; §3 production-path acceptance BLOCKED on the jobId-dedupe defect (next `/gsd-debug` session).
§3_final_rewalk_2026-05-13: |
After commit `19df291` (`/reupload` handler now calls `prior.remove()` to clear the dedupe entry at the re-upload boundary), the FINAL §3 re-walk on recording `01KRGENTPAM6BGNRA3SWPS4Q7E` (72,278,254 B, 9 video parts, file_sha256=`79559ba2...`) PASSES end-to-end on Pixel 10a WITHOUT dev-shim assistance:
(a) Worker SIGSTOPped (PID 99929 in T state). Fresh recording → POST /init → 201, all PUTs to LocalStack, POST /finalize → 200 with rotated finalizeIdempotencyKey `d68e5a0b-8252-468e-a0f8-0c71811813b9`. NOTE: the prior vitest run during the second debug session had wiped users + tasks + recordings + contributions + outbox tables, so /init initially 500'd on FK violation; restored by re-inserting user `01KRG9XG7VCSAYN823Q96FJ216` + re-running `seed-dev-task.ts` for `01HVDEVSEEDTASK00000000000`; drainer's retry succeeded on app-foreground at 10:43:34. (Test-isolation dev-stack hygiene — out-of-scope for this UAT item; flagged in the Current Test section for follow-on.) BullMQ job sat in `bull:verify:wait`.
(b) Corruption: `curl -X PUT --data-binary "corrupted-by-uat-§3-final-rewalk-2026-05-13-post-19df291" http://localhost:4566/humyn-recordings-dev/.../video.mp4` → 200, video.mp4 now 57 B.
(c) Worker SIGCONTed. At 10:44:16.644 worker first-verify completed (uploaded → hash-mismatch); outbox re-upload event written. `bull:verify:completed` ZSET added entry `01KRGENTPAM6BGNRA3SWPS4Q7E` at score 1778669056644 (the precondition that previously blocked the post-/reupload re-enqueue). ✓
(d) App-fg → events delivered → `HumynUpload.reupload()` Path-A rotates the 3 keys (per commit `5596635`) → POST /reupload to the dev API. **THE NEW CODE PATH FROM COMMIT 19df291 NOW RAN:** the /reupload handler at `apps/api/src/routes/recordings/reupload.ts` (post row reset, pre response) called `getQueue().getJob('01KRGENTPAM6BGNRA3SWPS4Q7E')` → `getState() = 'completed'` → `remove()`, clearing the dedupe entry. ✓ Response: 200 with fresh `s3_upload_id=o-fmbh7qy_z0S*-nI2FoFbYzNphHq8CC6r1pdIYoM0TNdNzX3TBKCNxxjA_qvD9AXupCQA9BgiktbfdCFXjn4P7Q8SDisSqA761v-TWW3PpqH4xHItc21Dn2A8SuJ8ht`+ fresh imuUploadId + fresh presigned URLs.
(e) Drainer re-PUT all 9 video parts + 1 IMU part + regenerated metadata.json against the new uploadId. queue.json updated with the rotated keys:`initIdempotencyKey='afffcfec-ad56-437a-8914-af43de7a1d88'`, `partsIdempotencyKey='a6643501-0b54-4249-94c9-40394989776c'`, `finalizeIdempotencyKey='d68e5a0b-8252-468e-a0f8-0c71811813b9'`, `reuploadIdempotencyKey='88417df5-d7aa-4435-ae03-d91319849b86'`(unchanged through Path-A as designed).
(f) Drainer called POST /finalize at 10:44:43.442 → **200** with the rotated finalizeIdempotencyKey. DB row went`qa_status: pending → uploaded`, `upload_completed_at=10:44:43.442`. The handler's `void enqueueVerify('01KRGENTPAM6BGNRA3SWPS4Q7E')`SUCCEEDED — because the prior completed entry had been removed in step (d) — and added a NEW job to`bull:verify:wait`. **CRITICAL: this is the exact bridge step the second fix unblocked.**
(g) Worker auto-picked up the new job within ~1s. At 10:44:43.892 the worker logged a SECOND `hash-verify job completed`entry for the SAME`jobId: "01KRGENTPAM6BGNRA3SWPS4Q7E"`— the worker log entry pair (10:44:16 + 10:44:43) is the smoking-gun proof that the same jobId was re-enqueued + re-processed, which BullMQ's default add-with-jobId-dedupe forbids — only possible because the /reupload handler explicitly removed the prior completion. The completed ZSET's score for`01KRGENTPAM6BGNRA3SWPS4Q7E` updated from 1778669056644 to 1778669083891 (`ZSCORE`directly confirms). DB row:`qa_status='verified', verified_at=2026-05-13 10:44:43.889+00`. Outbox `verified`event written. ✓
(h) App-fg → events-outbox piggybacked`\_events: [{event_type:'verified'}]`at 10:45:55.179 →`HumynUpload.clearVerified`unlinked the local triple → queue.json dropped to`[]` → contributions row written (recording_count=1, duration_ms=74254). **All AUTOMATIC. NO dev shim invoked anywhere in the (a)-(h) chain.** §3 acceptance MET in production-path form.

### 2. Force-quit / OS-evict recovery + Android-14 FGS type downgrade + Android-15 UIDT onTimeout handoff

expected: Force-quit / OS-evict the app mid-upload on Android, then relaunch → the upload resumes from the persisted per-part state via `POST /recordings/:id/parts` (re-presign against the existing `uploadId`; already-DONE parts keep their ETags, not re-PUT) and eventually completes + verifies. Background the app for >5 min → the FGS type downgrades `camera|microphone|dataSync` → `dataSync` → stops after 5 min idle; the FGS notification flips from "Recording in progress" (camera/mic privacy indicators) to "Uploading recordings…" (indicators gone) then disappears after 5 min idle. On Android 15, the `dataSync` 6-h `onTimeout` hands off to the UIDT `UploadJobService` which picks up true-background work past the 6-h cap. (The CR-01/CR-03 defects that previously made a mid-upload process-kill leave the row stuck are now fixed in code — this test confirms the fix holds on-device.)
result: [pending]

### 3. OEM battery-optimization deep-links (Xiaomi/Oppo/Vivo/Samsung) + the AOSP fallback

expected: On a Xiaomi (MIUI) / Oppo (ColorOS) / Vivo (FunTouch) / Samsung (OneUI) device — or the AOSP fallback on a Pixel — trigger the first-upload `BatteryOptimizationScreen`. The "Open Autostart settings" button appears only when an OEM component resolves and launches it; the AOSP `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` exemption button always works; the standalone fallback line is always shown; the per-vendor steps match `help-center-content.md`.
result: [pending]

### 4. CGNAT-cellular MSS clamp + the 30 s no-progress watchdog

expected: On a CGNAT cellular link (Jio / Vivo Brasil), upload a multi-hundred-MB MP4. A stalled part transfer is abandoned within ~30 s (the no-progress watchdog cancels the stalled OkHttp Call) and retried on a fresh socket without restarting the whole file; the upload eventually completes over the bad link. Check logcat for whether the `TCP_MAXSEG=1280` `MssSocketFactory` clamp takes or no-ops.
result: [pending]

### 5. Wave-1 cleanup on hardware (force-quit fragment discard, device-distress→Home nav, alert-cue audibility, RotatePrompt glyph legibility)

expected: Per `.planning/runbooks/05-wave1-cleanup-smoke.md` (authored, not yet walked) — D-03: a force-quit mid-record leaves only crash-truncated fragments and `CaptureLaunchSweep` discards ALL of them (no re-finalized stub recording appears in History). D-05: a mid-record device-distress stop (battery ≤5% / thermal abort) navigates to Home, not the RecordingScreen "ready" substate. D-06: the recording alert cues are audible on the device speaker at the en-US female-leaning voice. D-09: the RotatePrompt portrait-phone glyph is legible. The crash-recovery toast shows for 5 s (not the 15 s workaround).
result: [pending]

## Summary

total: 5
passed: 1
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps

- truth: "HumynCapture.start() succeeds on the **DEV** Tasks-tab long-press path when a Google-signed-in user (or the dev tester@example.com) hits the active substate after the hand gate passes — the contributor.name in CaptureSessionOpts is a non-empty string."
  status: failed
  reason: "User reported: recording could not start. Logcat: `E HumynCapture: start() failed — code=invalid_opts msg=invalid_opts: name`. Stack at CaptureSessionOptsBridge.kt:150 ← :84 (`requireNonEmpty(contributorMap, \"name\")`) ← HumynCaptureModule.kt:138. JS upstream: RecordingScreen.tsx:695 passes `user.name: u.user?.name ?? ''`; UserDisplay.name is `string | null` (appStore.ts:51). When the store's `user.name` is null/empty the bridge throws and recording never starts."
  severity: blocker
  test: 1
  artifacts:

  - path: "apps/mobile/src/screens/recording/RecordingScreen.tsx"
    line: 695
    issue: "Defaults missing `user.name` to `''` instead of bailing with a clear UX — silently delegates the failure to the Kotlin bridge."
  - path: "apps/mobile/src/lib/buildCaptureOpts.ts"
    line: 75
    issue: "`name: args.user.name` is forwarded without a non-empty guard parallel to the `consentPresent` guard above it. Either reject empty/null here with a clear JS-side error (mirroring the V11-style guard) OR rely on the screen having already coalesced it to a real value."
  - path: "apps/mobile/src/state/appStore.ts"
    line: 51
    issue: "`UserDisplay.name: string | null` — null is a valid state; nothing downstream of sign-in / `/me` guarantees a non-empty name reaches RecordingScreen."
  - path: "apps/mobile/android/app/src/main/java/ai/humynlabs/capture/capture/CaptureSessionOptsBridge.kt"
    line: 84
    issue: "Kotlin guard fires correctly (defense-in-depth across the JS↔Kotlin trust boundary, T-3.3-01). The fix needs to land on the JS side so the bridge guard is the last-line check, not the first observable failure."
    missing:
  - "Populate `useAppStore.user.name` reliably after Google Sign-In / `/me` — either fall back to email-local-part when the Google displayName is absent, or fail the sign-in flow with a 'display name required' UX (NOT just default to empty downstream)."
  - "Add a JS-side guard in buildCaptureOpts that throws if `args.user.name` is empty / whitespace, with a message that triggers a clear UX in RecordingScreen (today the bridge promise rejection lands in the screen's catch as CAPTURE_START_FAILED with no user-facing diagnostic of WHICH field was empty)."
  - "Decide whether to allow the `__DEV__` Tasks-tab long-press path to fall back to a hardcoded test contributor when the store's name is empty (parallel to the DEBUG_TEST_TASK id/name/category/setting hardcodes already in TasksPlaceholderScreen), or to require a real signed-in user with a populated name for the dev shortcut."
  - "Audit whether other CaptureSessionOpts fields are similarly null-vulnerable (email follows the same `?? ''` pattern at RecordingScreen.tsx:696 and would fail at CaptureSessionOptsBridge.kt:85; appVersion at :126 has a SEMVER regex but no length floor)."
    debug_session: ""
    device: "Pixel 10a (5C161JEA304304), Android 16"
    build: "apkRollout-Debug + Metro --reset-cache"

- truth: "Item 1 §3 hash-mismatch / re-upload acceptance — corrupting the S3 object → worker `qa_status='hash-mismatch'` → outbox `re-upload` event → app re-uploads from local copy via `POST /recordings/:id/reupload` → re-verify → `qa_status='verified'` → locals deleted (per `.planning/runbooks/05-upload-smoke.md` §3 acceptance)."
  status: resolved
  reason: "§3 walked 2026-05-13 on Pixel 10a (apkRollout-Debug, HEAD `e5ff29b`). Steps (a)-(e) all green: corruption → worker mismatch → outbox `re-upload` → app `/reupload` 200 with fresh `s3_upload_id=bVCQAmlbUJTGK...` → all 9 video parts + 1 IMU part + metadata.json re-PUT to LocalStack. Step (f) BLOCKS: `POST /recordings/01KRGB97X3MPJ784QF78SD77NJ/finalize` 409 `idempotency-key-conflict` in 4 ms because the client re-used the §2-original `finalizeIdempotencyKey='ee0bced9-c472-4a9f-8834-f4522121ac96'` against a different `(uploadId, parts)` body. Server's `idempotency_keys (user_id, key)` cache correctly rejected the mismatch. Row stuck in `state=FINALIZING` on device; DB `qa_status=pending, verified_at=null`; locals still present; no follow-on `verified` outbox event; contributions row still at the §2 baseline (`duration_ms=92748, recording_count=1, task_count=1`). Wave-1.5 Plan 05-14 Item 1 split the keys per-route within one upload session but did not rotate them across the hash-mismatch boundary."
  severity: blocker
  test: 1
  artifacts:

  - path: "apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/HumynUploadModule.kt"
    line: 316
    issue: "The `else ->` Path-A full-reset branch of `@ReactMethod fun reupload(recordingId, promise)` resets `row.state / row.uploadId / row.imuUploadId / row.metadataPut / row.videoParts.{status,etag,retryCount} / row.imuParts.{status,etag,retryCount} / row.deadLetterReason / row.reupload` — but does NOT rotate the three reusable per-route idempotency keys (`row.initIdempotencyKey`, `row.partsIdempotencyKey`, `row.finalizeIdempotencyKey`). The post-`/reupload` `/finalize` re-uses the §2 finalize key against a new `(uploadId, parts)` body → 409. Minimal fix: three `UUID.randomUUID().toString()` assignments inside the `else ->` branch, parallel to the existing reset lines (around lines 320-327)."
  - path: "apps/mobile/android/app/src/main/java/ai/humynlabs/capture/upload/UploadModels.kt"
    line: 159
    issue: "The doc comments on `initIdempotencyKey`, `partsIdempotencyKey`, `finalizeIdempotencyKey` say 'Minted once at construction; reused only across retries of <route> itself within the SAME upload session' — but they don't define what 'same session' means after a hash-mismatch re-upload (which is logically a NEW session for /init/parts/finalize but a CONTINUATION of /reupload's idempotent replay). The fix in HumynUploadModule.kt:316-328 should be paired with a clarifying doc-comment update here (or, alternatively, lift the rotation into a small `UploadRow.resetForReupload()` helper invoked from the reupload @ReactMethod)."
    missing:
  - "Rotate `initIdempotencyKey`, `partsIdempotencyKey`, `finalizeIdempotencyKey` in `HumynUploadModule.reupload()` Path-A full-reset branch (the worker-fired hash-mismatch re-upload case). `reuploadIdempotencyKey` is NOT rotated — /reupload itself is one-shot per re-upload cycle and the same key+body is a valid idempotent replay."
  - "Add a re-upload-side native unit / Robolectric test that asserts the three keys differ between pre- and post-`HumynUpload.reupload()` (Path A) on a row that has already consumed a /finalize against the original keys."
  - "Add a backend `vitest` test that hits the same shape: same `Idempotency-Key` against `/recordings/:id/finalize` with two different request bodies → 409 — pin the server contract that catches future client-side rotation regressions."
  - "Re-walk Item 1 §3 on the next apkRollout-Debug build after the fix lands; commit the §3 PASS evidence + flip `result: partial → pass` + `severity: partial-§3-blocked-by-defect → passed`."
    debug_session: "reupload-finalize-409 (status fix_applied_awaiting_on_device_verify; commit 5596635 + 43b4089 land the 3-line rotate-keys fix + Robolectric regression tests; PROVEN end-to-end on the §3 re-walk recording 01KRGD2D6GVET6K7QKNJ1BZSM7 — pre-reupload finalizeIdempotencyKey 1f62c949-... rotated to post-reupload 21e99021-...; /finalize 200 was 409. Defer move-to-resolved/ until Item 1 fully passes after the second-defect fix.)"
    device: "Pixel 10a (5C161JEA304304), Android 16"
    build: "apkRollout-Debug HEAD e5ff29b → 43b4089 (post-fix rebuild)"
    observed_at: "2026-05-13T09:43:10Z"

- truth: "Item 1 §3 production-path acceptance — after a hash-mismatch + /reupload + re-PUT parts + /finalize 200, the server-side `enqueueVerify(rec.id)` call MUST re-add a BullMQ verify job so the worker re-hashes the re-uploaded bundle and flips `qa_status: uploaded → verified` without dev-shim assistance."
  status: resolved
  reason: "§3 re-walk on fresh recording 01KRGD2D6GVET6K7QKNJ1BZSM7 (Pixel 10a, apkRollout-Debug HEAD 43b4089) proved the rotate-keys fix (commit 5596635) works — POST /finalize after /reupload returns 200 with the rotated finalizeIdempotencyKey 21e99021-... — but the post-/finalize automatic `void enqueueVerify(rec.id)` no-op'd because `apps/api/src/lib/queue.ts:40-43` uses `jobId=recordingId` for SQS-redelivery dedupe (threat T-5-03-01). The prior (10:12:42) verify completion's jobId stayed in `bull:verify:completed` ZSET (capped at 1000 via `removeOnComplete: 1000` in queue.ts:33), so BullMQ silently dedupes the re-enqueue. `bull:verify:wait` + `bull:verify:active` both stayed at 0 after the 10:13:21 /finalize 200 — worker NEVER picked up the re-uploaded bundle. All three production paths affected: `apps/api/src/routes/recordings/finalize.ts:238` (the dev-shim `void enqueueVerify(rec.id)`), `apps/api/src/workers/sqs-poller.ts:128` (production SQS-poller → enqueueVerify path), `apps/api/src/cron/verify-sweep.ts:47` (10-min stale-row sweep — also dedupes). The §3 chain completed only via host-side dev shim `apps/api/scripts/enqueue-verify-dev.ts` (`await existing.remove()` + `q.add()` — exactly the missing step in production). In a real deployment a re-uploaded recording would sit at qa_status='uploaded' forever after the worker's first hash-mismatch run."
  severity: blocker
  test: 1
  artifacts:
  - path: "apps/api/src/lib/queue.ts"
    line: 40
    issue: "`enqueueVerify(recordingId)` uses `jobId = recordingId` to dedupe SQS redelivery (T-5-03-01) — correct for that case, wrong for re-uploads. After a prior verify completes with the same jobId, the entry sits in `bull:verify:completed` (capped at 1000) and BullMQ's `add({ jobId })` silently no-ops on duplicate jobId. Doc comment at queue.ts:9-11 only acknowledges the SQS-redelivery/sweep-cron collapse case."
  - path: "apps/api/src/routes/recordings/finalize.ts"
    line: 238
    issue: "After /finalize sets qa_status='uploaded' + inserts into recordings_to_verify, calls `void enqueueVerify(rec.id)` (dev-shim — prod path is the SQS poller). Per comment at finalize.ts:231-236 the recordings_to_verify row + verify-sweep cron are the durable backstop — but BOTH flow through enqueueVerify → both subject to the same dedupe. A re-uploaded row stays at uploaded → enqueueVerify no-ops → sweep cron picks up stale row after 10 min → enqueueVerify no-ops again → attempts increments to MAX_ATTEMPTS (8 sweeps ≈ 80 min) → operator-investigate, recording stuck forever."
  - path: "apps/api/src/workers/sqs-poller.ts"
    line: 128
    issue: "Production hot path. The S3-created event for the re-uploaded bundle hits SQS → poller calls `enqueueVerify(recordingId)` → BullMQ dedupes against prior completed job → worker never runs. The recordings_to_verify + sweep-cron backstop is also broken (see above)."
  - path: "apps/api/src/cron/verify-sweep.ts"
    line: 47
    issue: "5-min sweep on >10-min-stale rows calls `enqueueVerify(id)` — same dedupe applies. Increments `attempts` even though the job never enters BullMQ wait queue → attempts → MAX_ATTEMPTS (8) → row left for operator triage; recording stuck at qa_status='uploaded'."
  - path: "apps/api/scripts/enqueue-verify-dev.ts"
    line: 18
    issue: "Reference implementation of the workaround. `const existing = await q.getJob(rid); if (existing) { await existing.remove(); } await q.add(...)` is exactly what's missing from the production /reupload handler. Dev shim's header comment explicitly says it's used by the Phase-5 UAT re-walk to bypass this case — production path was never patched."
    missing:
  - "Pick one of the three fix shapes (recommend shape #3 — scope `prior.remove()` to /reupload handler; alternatives: shape #1 tag-jobId per verify-attempt; shape #2 server-side remove+add in enqueueVerify with a `force` option)."
  - "Add backend vitest in `apps/api/src/__tests__/` (or wherever queue/finalize tests live) pinning the post-/reupload re-enqueue contract: insert a completed BullMQ job for recordingId, call enqueueVerify (or the /reupload handler) for the same recordingId, assert a NEW BullMQ job enters `wait` AND the worker would pick it up. Pair test: non-reupload double-enqueue still dedupes (T-5-03-01 preserved)."
  - "Re-walk Item 1 §3 once more on the post-fix build WITHOUT dev-shim assistance — corrupt → mismatch → reupload → /finalize 200 → worker re-verifies AUTOMATICALLY → qa_status='verified' → locals deleted. Flip Item 1 to `result: pass / severity: passed` only after this is green."
  - "Update queue.ts:9-11 doc comment to acknowledge the re-upload boundary (or, if shape #3 lands, point at the /reupload handler as the place that bridges it)."
    debug_session: "enqueue-verify-jobid-dedupe-on-reupload (opening)"
    device: "Pixel 10a (5C161JEA304304), Android 16"
    build: "apkRollout-Debug HEAD 43b4089"
    observed_at: "2026-05-13T10:13:21Z"
