---
phase: 05-upload-pipeline-hash-verify-worker-anti-fraud
verified: 2026-05-12T16:42:46Z
status: human_needed
score: 4/4 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 1/4
  gaps_closed:
    - 'CR-01/CR-02 — re-/init orphaned ETags + non-idempotent /init → permanently un-finalizable rows. Closed: init.ts is SELECT-first idempotent (.onConflictDoNothing() removed, re-presign path returns 200 with the SAME uploadId); new POST /recordings/:id/parts re-presign route (no CreateMultipartUpload/INSERT/UPDATE); UploadCoordinator.uploadOne() branches reupload→/reupload, uploadId≠null→/parts, else→/init; 409/403 from /init and 404/409 from /parts → DeadLetterException (no infinite loop).'
    - 'CR-03 — drainNow() public + called off 3 threads with no mutual exclusion. Closed: drainNow() acquires drainLock.tryLock() at the top, releases in finally; the bogus synchronized(queueStore) wrapper removed; test "drainNow is serialised" confirms the loser returns promptly.'
    - 'Missing prod sqs-poller.ts + WR-02 TOCTOU in verify-recording. Closed: apps/api/src/workers/sqs-poller.ts now exists (SQS long-poll → enqueueVerify(recordingId, jobId=recordingId) → DeleteMessage; defensive S3-event key parser; WORKER_BOOTSTRAP-gated bootstrap); Terraform verify-queue worker task references it correctly. verify-recording.ts qa_status UPDATEs now carry AND qa_status=\'uploaded\' + only emit the outbox event / delete the recordings_to_verify row when rowCount===1.'
    - 'WR-01/WR-03/IN-05 — events-outbox mutated RFC-7807 bodies + consumed events on error responses; /finalize not retry-safe; verified-ids cursor SELECT not user-gated. Closed: events-outbox.ts gates on 2xx + content-type starts-with application/json; finalize.ts completeOrConfirm tolerates NoSuchUpload via HeadObject + short-circuits an already-uploaded row to 200; verified-ids.ts cursor-resolution SELECT is user-gated.'
  gaps_remaining: []
  regressions: []
gaps: []
deferred: []
human_verification:
  - test: "On a Pixel 7a/8a-class device with the dev backend up (Postgres + Redis + LocalStack + worker), record a ≥60 s task → confirm the bundle (mp4 + IMU CSV + metadata.json) auto-enqueues, the Pending Uploads tile/screen shows 'Uploading…' progressing, then the row drops once verified; then corrupt the S3 object and confirm hash-mismatch → 're-upload' event → re-upload-from-local → re-verify"
    expected: "Bundle lands in S3 (aws --endpoint-url=http://localhost:4566 s3 ls s3://humyn-recordings-dev/recordings/), the BullMQ hash-verify worker re-hashes, recordings.qa_status='verified', the next authed API response carries `_events: [{recording_id, event_type:'verified'}]`, the local mp4+csv+json are deleted, the row disappears from the queue; the hash-mismatch path emits a `re-upload` event, re-uploads from the still-present local copy, and re-verifies"
    why_human: 'End-to-end upload over a real device + LocalStack/AWS; requires running the app, the BullMQ worker (or the /finalize dev shim), Redis, and S3 — cannot be exercised in the verifier environment. Runbook: .planning/runbooks/05-upload-smoke.md (authored, not yet walked).'
  - test: 'Force-quit / OS-evict the app mid-upload on Android, then relaunch; also background the app for >5 min and confirm the FGS type downgrades camera|microphone|dataSync → dataSync → stops after 5 min idle; on Android 15 confirm the dataSync 6-h onTimeout hands off to the UIDT UploadJobService'
    expected: "The upload resumes from the persisted per-part state via POST /recordings/:id/parts (re-presign against the existing uploadId; already-DONE parts keep their ETags, not re-PUT) and eventually completes + verifies; the FGS notification flips from 'Recording in progress' (camera/mic privacy indicators) to 'Uploading recordings…' (indicators gone) then disappears after 5 min idle; on Android 15 the UIDT job picks up true-background work past the 6-h dataSync cap. (The CR-01/CR-03 defects that previously made a mid-upload process-kill leave the row stuck are now fixed in code — this test confirms the fix holds on-device.)"
    why_human: 'FGS service-type strict mode, Doze, force-quit recovery, and the Android-15 onTimeout cap are device-only behaviors not reproducible in Robolectric or the verifier environment.'
  - test: 'On a Xiaomi (MIUI) / Oppo (ColorOS) / Vivo (FunTouch) / Samsung (OneUI) device — or the AOSP fallback on a Pixel — trigger the first-upload BatteryOptimizationScreen and confirm the OEM autostart deep-link resolves (or the AOSP REQUEST_IGNORE_BATTERY_OPTIMIZATIONS intent fires) and the per-vendor walkthrough copy matches help-center-content.md'
    expected: "The 'Open Autostart settings' button appears only when an OEM component resolves and launches it; the AOSP exemption button always works; the standalone fallback line is always shown; the per-vendor steps match help-center-content.md"
    why_human: "OEM ROM deep-links can't be resolved off-device; resolveActivity-gating and the per-vendor ComponentName list need a real ROM (or the Pixel AOSP fallback)."
  - test: 'On a CGNAT cellular link (Jio / Vivo Brasil), upload a multi-hundred-MB MP4 and confirm the 30 s no-progress watchdog cancels the stalled OkHttp Call and retries on a fresh socket; check logcat for whether the TCP_MAXSEG=1280 MssSocketFactory clamp takes or no-ops'
    expected: 'A stalled part transfer is abandoned within ~30 s and retried on a new socket without restarting the whole file; the upload eventually completes over the bad link'
    why_human: "Requires real cellular / MTU-blackhole conditions; the MSS clamp's effectiveness is platform-dependent and can only be confirmed on-device (VALIDATION.md Manual-Only)."
  - test: "Wave-1 cleanup checks on hardware: force-quit-mid-record now discards the crash-truncated fragment (no degenerate {base}.{mp4,csv,json} triple reaches the queue); a device-distress (battery ≤5% / thermal) mid-record stop navigates to Home (practice → PracticeComplete); alert-cue tones (520 Hz / 440→560→680 Hz) audible at full media volume; RotatePrompt portrait-phone glyph reads as 'rotate your phone'"
    expected: 'No {base}.{mp4,csv,json} triple after a force-quit; device-distress stop lands on MainTabs (Home), normal sub-60s discard stays on the recording screen (RESET_FOR_FRESH); tones audible; glyph legible'
    why_human: 'Force-quit timing, device-distress simulation, audio audibility, and the SVG glyph are device-only. Runbook: .planning/runbooks/05-wave1-cleanup-smoke.md (authored, not yet walked).'
---

# Phase 5: Upload Pipeline & Hash-Verify Worker — Verification Report

**Phase Goal:** A finished recording uploads automatically as a three-file bundle (MP4 + IMU CSV + metadata JSON), survives backgrounding / force-quit / Android 14-15 service-type restrictions / OEM battery managers, gets re-hashed server-side and either confirmed via a `verified` event or queued for re-upload. (FRAUD-03/04 server-side IMU-liveness descoped to §v2 2026-05-11; FRAUD-05/06 — per-account upload-rate cap + pre-payout fraud dashboard — descoped to §v2 2026-05-12 per CONTEXT.md D-04; iOS native-module analogues + Play-Store rollout deferred to a follow-on milestone — MVP is Android-only via signed APK.)
**Verified:** 2026-05-12T16:42:46Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (plans 05-09..05-13 executed; the prior `gaps_found` items + code-review CR-01/CR-02/CR-03 + WR-01/02/03/06 + the missing prod `sqs-poller.ts` + DEF-5-01 + IN-05 closed; a fresh `05-REVIEW.md` found 0 BLOCKER / 5 WARNING / 7 INFO)

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Wave-1 cleanup: CaptureLaunchSweep discards ALL crash-truncated fragments (no degenerate bundle reaches the upload queue); device-distress mid-record stop → Home (practice → PracticeComplete; normal sub-60s discard → RESET_FOR_FRESH); RECOVERY_TOAST_MS = 5_000 with the D-07/D-03 annotation; FRAUD-05/06 retired to §v2; Phase 5 re-titled; CLAUDE.md Redis carve-out + pins; runbooks authored                                                                                                                                | ✓ VERIFIED | CaptureLaunchSweep.kt deletes mp4/csv/sidecar; RecordingScreen.tsx handleStop branches on `battery_critical`/`thermal` → navigateToHome / practice → PracticeComplete; bootRecoveryListener.ts `RECOVERY_TOAST_MS = 5_000` + annotation; REQUIREMENTS.md FRAUD-05/06 in §v2 (dated note + matrix rows); ROADMAP Phase 5 re-titled; CLAUDE.md carries the Redis carve-out + bullmq/ioredis/sqs pins; `.planning/runbooks/05-wave1-cleanup-smoke.md` + `05-upload-smoke.md` exist. Mobile JS test suite 649/649 pass (incl. RecordingScreen.test.tsx, bootRecoveryListener.test.ts); CaptureLaunchSweepTest exists. (Unchanged from the prior verification — still VERIFIED.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2   | On recording stop the bundle uploads via S3 multipart presigned URLs, 8 MiB Wi-Fi / 5 MiB cellular chunks, 3∥×2∥, exponential backoff 2/4/8/16/32/64 s → dead-letter, no manual cancel, NO whole-file restart on chunk fail, 30 s no-progress watchdog + best-effort MSS clamp                                                                                                                                                                                                                                                        | ✓ VERIFIED | Machinery present + unit-tested (ChunkUploader.kt: streaming PUT over RandomAccessFile, ETag capture, exact 2/4/8/16/32/64 s backoff → DeadLetterException on the 7th, 30 s no-progress watchdog cancels the Call → fresh socket, MssSocketFactory; UploadCoordinator.kt: /init→metadata PUT→6-permit semaphore 3∥×2∥→/finalize→AWAITING_VERIFY, per-part {etag,status} persisted, debounced progress; UploadModels.kt 8 MiB/5 MiB; HumynUploadModule has no cancel method — UP-11). **Prior gap CLOSED**: `init.ts` is now SELECT-first idempotent — a duplicate `/init` for a `pending` row owned by the caller returns the SAME `uploadId` (re-presign path returns 200, not 201; `.onConflictDoNothing()` removed) + a fresh IMU `CreateMultipartUpload` only; new `POST /recordings/:id/parts` re-presign route (no `CreateMultipartUpload`/INSERT/UPDATE/state-change) preserves DONE parts' ETags; `UploadCoordinator.uploadOne()` branches `row.reupload`→`/reupload`, `row.uploadId != null`→`/parts`, else→`/init`; 409/403 from `/init` and 404/409 from `/parts` → `DeadLetterException` (no infinite loop). UploadCoordinatorTest covers `reDrain uses parts route not init - DONE part not re-PUT, uploadId unchanged`, `a 409 from the parts route dead-letters the row, no infinite loop`, `a 409 from the init route dead-letters the row, no infinite loop`, `cellular network makes init request 5 MiB-chunk partsCount`. api Vitest 179/179 pass; init.test.ts + parts.test.ts present. (Residual WR-02: a 400 from `/recordings/init` on a malformed metadata bundle still loops as transient — WARNING, not a blocker; D-03 already discards crash-truncated fragments so the metadata is well-formed in practice.)                                                                                                                                                                                                                                                                                                                                                                                    |
| 3   | Uploads survive backgrounding / force-quit on Android — FGS type-downgrade camera\|microphone\|dataSync → dataSync → 5-min idle stop; UIDT JobService for the Android-15 6-h cap; pause during recording / resume on stop; logout cancels in-flight + preserves the queue; same-user re-login resumes. (iOS URLSession bg = documented gap, deferred §v2)                                                                                                                                                                             | ✓ VERIFIED | Structural pieces present + manifest/FGS/JobService tests pass (HumynForegroundService.kt: FGS_TYPE_RECORDING superset unchanged + second startForeground DATA_SYNC-only on ACTION_SET_UPLOAD_ACTIVE + onRecordingFinalized seam + onTimeout(int,int)/onTimeout(int) → UploadJobService.scheduleUidt + IDLE_STOP_MS 5 min; UploadJobService.kt: setUserInitiated(true), NETWORK_TYPE_ANY, onStartJob→drainNow→jobFinished(params, queueHasWork); AndroidManifest: RUN_USER_INITIATED_JOBS + REQUEST_IGNORE_BATTERY_OPTIMIZATIONS + UploadJobService <service>; pause/resume via useRecordingLifecycle + RecordingScreen handleStop + uploadReconcile appStore.jwt watcher; UploadQueueStore.bootstrap(currentSub) owner-pin — logout preserves rows). **Prior gap CLOSED (CR-03)**: `drainNow()` now acquires `drainLock.tryLock()` at the top of the body and releases it in a `finally` — a second concurrent drain off the FGS HandlerThread / the UIDT JobService Thread / the drainExecutor returns immediately ("drainNow skipped — a drain is already running"); the bogus `synchronized(queueStore)` wrapper was removed. UploadCoordinatorTest `drainNow is serialised - two concurrent drains, only one does the upload work (CR-03)` confirms the loser returns < 300 ms while the winner is parked in a 400 ms `/init`. On-hardware survival (Doze / force-quit / the Android-15 onTimeout handoff / OEM battery managers) not exercisable here — see human-verification items; the prior "process-kill leaves the row stuck" caveat is removed now that CR-01/CR-03 are fixed in code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 4   | The hash-verify worker pipeline (BullMQ + Redis + ECS, queue-depth scaled) consumes S3 multipart-complete events via EventBridge → SQS, re-hashes both MP4 + IMU CSV, flips qa_status to 'verified' (emit `verified`) on match / 'hash-mismatch' (emit `re-upload`) on mismatch; events piggy-back on the next API response; app deletes local mp4+csv+json only on `verified`; re-uploads from the still-present local copy on `re-upload`; an app-launch reconciliation sweep cleans local files for any verified-but-undeleted set | ✓ VERIFIED | Dev/MVP path fully wired + tested (lib/queue.ts BullMQ+ioredis singletons + enqueueVerify jobId=recordingId; workers/hash-verify.ts standalone BullMQ Worker + `worker:hash-verify` script; lib/sha256-stream.ts streamed GetObject re-hash, no buffer-collect; lib/verify-recording.ts re-hash → qa_status flip → appendOutboxEvent — idempotent on qaStatus!=='uploaded'; recording-state.ts hash-mismatch→pending edge; cron/verify-sweep.ts stale-row re-enqueue; finalize.ts LocalStack dev shim → enqueueVerify; migration 0006 recording_events_outbox + recordingEventTypeEnum + partial index; plugins/events-outbox.ts onSend hook draining `_events`; routes/recordings/reupload.ts (UPDATE not INSERT, fresh CreateMultipartUpload, 404/403/409 guards); routes/recordings/verified-ids.ts cursor pagination; mobile services/recordingEvents.ts (`${id}:${type}`-idempotent) + uploadReconcile.ts (GET /verified-ids → intersection → clearVerified, cold-start + AppState→active) + api.ts interceptEvents on every JSON response; PendingUploadsScreen.tsx + the 'Paused — no Wi-Fi' chip; Terraform verify-queue module: SQS+DLQ, EventBridge rule, worker ECS task + queue-depth autoscaling + least-privilege IAM, wired into envs/prod/main.tf). **Prior gap CLOSED**: `apps/api/src/workers/sqs-poller.ts` now exists — the prod EventBridge→SQS→BullMQ trigger leg (`ReceiveMessageCommand` long-poll → `parseRecordingIdFromS3Event` → `enqueueVerify(recordingId)` → `DeleteMessageCommand`; bootstrap gated on `WORKER_BOOTSTRAP !== 'false'`; `worker:sqs-poller` npm script); the Terraform `verify-queue` worker task def now declares both containers (`node dist/workers/hash-verify.js` + `node dist/workers/sqs-poller.js`). **Prior gap CLOSED (WR-02)**: `verify-recording.ts`'s `qa_status` UPDATEs now carry `AND qa_status='uploaded'` and the outbox event + the `recordings_to_verify` delete only happen on `rowCount === 1` — a mid-re-hash ops action is no longer silently resurrected. api Vitest 179/179 pass (incl. sqs-poller.test.ts parser tests, verify-recording.test.ts). |

**Score:** 4/4 truths verified

### Deferred Items

None — no later-phase Phase-5 work; Phase 6 = Tasks/History/Home, Phase 7 = observability/APK distribution. The residual code-review WARNINGs (below) are advisory follow-ups, not deferred phase work.

### Required Artifacts

| Artifact                                                                                                                                                                                                                    | Expected                                                                                                                                                                                                          | Status                      | Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `apps/api/src/routes/recordings/init.ts`                                                                                                                                                                                    | idempotent POST /recordings/init — SELECT-first; a duplicate /init for a pending row re-presigns video against the stored s3UploadId + fresh IMU CreateMultipartUpload only + 200; .onConflictDoNothing() removed | ✓ VERIFIED                  | Present; `select().from(schema.recordings).where(eq(...id...)).limit(1)` runs before any video CreateMultipartUpload; existing-pending-owned → re-presign video + presignImuStream + 200 with `uploadId: existing.s3UploadId`; existing wrong-owner → 403 (no row fields); existing non-pending → 409 `Cannot re-init from state <qaStatus>`; new-row → CreateMultipartUpload(video) + presignImuStream + INSERT (no onConflictDoNothing) + 201. Imports `eq` from drizzle-orm.                                                                                                                                                                                                                                                                                                                                                                   |
| `apps/api/src/routes/recordings/parts.ts`                                                                                                                                                                                   | POST /recordings/:id/parts — re-presign video + IMU part URLs against the existing uploads; no CreateMultipartUpload/INSERT/UPDATE/state-change                                                                   | ✓ VERIFIED                  | Present (~174 lines); body `{ partsCount, imuUploadId }`, params `{ id: length(26) }`, `preHandler: [app.requireAuth]`, per-user rate-limit; guards in order partsCount>MAX→400, !rec→404, owner mismatch→403 (no row fields), qaStatus!=='pending'→409, !s3UploadId→409; on success presignVideoParts(video, rec.s3UploadId) + presignVideoParts(imu, body.imuUploadId) + presignMetadata + 200; registered in index.ts after the literal routes / alongside completePartRoute / before recordingsGetRoute.                                                                                                                                                                                                                                                                                                                                      |
| `apps/api/src/workers/sqs-poller.ts`                                                                                                                                                                                        | the prod EventBridge→SQS→BullMQ trigger leg the Terraform references                                                                                                                                              | ✓ VERIFIED                  | Present (~171 lines); `ReceiveMessageCommand` long-poll (MaxNumberOfMessages 10, WaitTimeSeconds 20, VisibilityTimeout 60) → `parseRecordingIdFromS3Event` (EventBridge + S3-direct envelope shapes, URL-decode, `recordings/<ULID>/<ULID>/{video.mp4                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | imu.csv | metadata.json}`regex) →`enqueueVerify(recordingId)`→`DeleteMessageCommand`; non-matching-but-JSON message → delete; non-JSON → leave for the DLQ; enqueueVerify failure → don't delete; SIGTERM/SIGINT graceful drain; bootstrap gated on `WORKER_BOOTSTRAP !== 'false'`. `worker:sqs-poller`+`worker:sqs-poller:dev` npm scripts present. |
| `apps/api/src/lib/verify-recording.ts`                                                                                                                                                                                      | re-hash → qa_status flip → outbox event, TOCTOU-safe                                                                                                                                                              | ✓ VERIFIED                  | Present; early-returns if !rec or qaStatus!=='uploaded'; streamed re-hash via sha256OfS3Object; transaction UPDATEs carry `and(eq(id), eq(qaStatus,'uploaded'))`; appendOutboxEvent + recordingsToVerify delete ONLY on `rowCount===1` (else `info`-log "row moved during re-hash; skipping").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `apps/api/src/plugins/events-outbox.ts`                                                                                                                                                                                     | onSend hook draining `_events`, 2xx + application/json only                                                                                                                                                       | ✓ VERIFIED                  | Present; `fp({name:'events-outbox', dependencies:['auth']})`; returns payload unchanged unless `200 ≤ statusCode < 300` AND `content-type` starts-with `application/json` AND payload is a string AND parses to an object envelope; then `drainOutbox(sub)` → attach `_events` → `markDelivered`. No longer touches `application/problem+json` or non-2xx responses (WR-03 closed).                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `apps/api/src/routes/recordings/finalize.ts`                                                                                                                                                                                | CompleteMultipartUpload(video+IMU) → qa_status pending→uploaded → recordings_to_verify enqueue, retry-safe                                                                                                        | ✓ VERIFIED (⚠ WR-04)       | Present; an already-`uploaded` row short-circuits to 200; `completeOrConfirm` catches NoSuchUpload/NoSuchMultipartUpload → `HeadObjectCommand` → treat present-object as success; flips to 'uploaded' only after BOTH HeadObjects confirm present; dev shim enqueueVerify only when `AWS_ENDPOINT_URL` set. Caveat: the qa_status pending→uploaded UPDATE has no `AND qa_status='pending'` predicate (pre-existing TOCTOU vs an ops takedown between the stale SELECT and the transaction — WR-04, WARNING).                                                                                                                                                                                                                                                                                                                                      |
| `apps/api/src/routes/recordings/verified-ids.ts`                                                                                                                                                                            | GET /recordings/verified-ids cursor pagination, user-gated cursor                                                                                                                                                 | ✓ VERIFIED                  | Present; `eq(userId)` + `eq(qaStatus,'verified')` filter, `(verified_at DESC, id DESC)`, `LIMIT N+1` cursor; the cursor-resolution SELECT is now `and(eq(id, since), eq(userId, userId))` — a cursor pointing at another user's row resolves nothing (IN-05 closed).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `apps/api/src/lib/queue.ts` + `apps/api/src/workers/hash-verify.ts` + `apps/api/src/lib/sha256-stream.ts` + `apps/api/src/cron/verify-sweep.ts`                                                                             | BullMQ+ioredis singletons; standalone Worker entrypoint; streamed S3 re-hash; stale-row re-enqueue cron                                                                                                           | ✓ VERIFIED                  | All present (unchanged from the prior verification — still VERIFIED). queue.ts: getRedisConnection (maxRetriesPerRequest:null), getQueue('verify') attempts:5 exp-5000, enqueueVerify(jobId=recordingId). hash-verify.ts: `new Worker('verify', verifyRecording, {concurrency:4})`, does not import buildApp, `worker:hash-verify` npm script. sha256-stream.ts: pipeline(GetObject().Body, createHash('sha256')) — no buffer-collect. verify-sweep.ts: findStaleVerifyRows + startVerifySweep/stopVerifySweep + `_timer.unref()` + wired into app.ts (skipped in test).                                                                                                                                                                                                                                                                          |
| `apps/api/src/db/schema.ts` recordingEventsOutbox + recordingEventTypeEnum + migration 0006                                                                                                                                 | table + enum + partial index                                                                                                                                                                                      | ✓ VERIFIED                  | `recordingEventTypeEnum = pgEnum('recording_event_type', ['verified','re-upload'])`; `recordingEventsOutbox` pgTable with `user_id`/`recording_id`/`event_type`/`created_at`/`delivered_at` + `recording_events_outbox_user_created_idx`; migration `0006_recording_events_outbox.sql` present.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `infra/terraform/modules/verify-queue/main.tf`                                                                                                                                                                              | SQS+DLQ + EventBridge rule + worker ECS task (both containers) + autoscaling + IAM                                                                                                                                | ✓ VERIFIED                  | Present + wired into envs/prod/main.tf; SQS+DLQ, EventBridge rule on `.mp4`/`.csv`/`metadata.json` suffixes, `humyn-worker` ECS task with TWO containers — `hash-verify` (`node dist/workers/hash-verify.js`) AND `sqs-poller` (`node dist/workers/sqs-poller.js`) — appautoscaling on backlog-per-task, least-privilege IAM. (Both referenced files exist; the prior STUB status is cleared.) Minor IN-04/IN-05 nits remain — INFO.                                                                                                                                                                                                                                                                                                                                                                                                              |
| `apps/mobile/.../upload/UploadCoordinator.kt`                                                                                                                                                                               | queue drainer: drainLock-serialised; /init (idempotent) / /reupload / /parts (re-drain) branching; per-part persist; debounced progress; pause/owner-aware                                                        | ✓ VERIFIED                  | Present (~600 lines); `drainNow()` acquires `drainLock.tryLock()` then `try { ... } finally { drainLock.unlock() }`; `drainExecutor.execute { drainNow() }` is the JS-bridge entry; `uploadOne()` `when { row.reupload→postReupload; row.uploadId != null→postRePresign; else→postInit }`; `postRePresign` → `POST /recordings/:id/parts { partsCount, imuUploadId }`, 404/409 → DeadLetterException; `postInit` 409/403 → DeadLetterException; `postReupload` clears `row.reupload` right after the fresh ids are persisted; `parseInitResponse` re-throws a body-free IOException on a non-JSON body (no presigned-URL leak — WR-06). 11 MockWebServer tests pass per the 05-10/05-11 SUMMARYs (the test file covers the drain-lock, re-drain-via-/parts, 409 dead-letter on both routes, the non-JSON-leak case, cellular 5-MiB chunk sizing). |
| `apps/mobile/.../upload/ChunkUploader.kt` / `UploadQueueStore.kt` / `NetworkMonitor.kt` / `HumynUploadModule.kt` / `HumynUploadPackage.kt`                                                                                  | streaming PUT + ETag + backoff→dead-letter + watchdog + MSS clamp; native-owned atomic JSON queue + owner-pin + practice filter + markVerifiedAndDeleteLocal; isCellular + default-network callback; RN bridge    | ✓ VERIFIED                  | All present (unchanged from the prior verification — still VERIFIED). UploadQueueStore.markVerifiedAndDeleteLocal finds the row by id regardless of state → a `verified` event clears even a DEAD_LETTER row (closes the WR-01 spurious-dead-letter UX wart via the reconcile sweep).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `apps/mobile/.../fgs/HumynForegroundService.kt` / `apps/mobile/.../upload/UploadJobService.kt` / `apps/mobile/.../upload/BatteryOptimizationHelper.kt` / `apps/mobile/src/screens/onboarding/BatteryOptimizationScreen.tsx` | type-downgrade lifecycle + onTimeout→UIDT handoff; UIDT JobService; AOSP-first + resolveActivity-gated OEM deep-links; first-upload walkthrough                                                                   | ✓ VERIFIED                  | All present (unchanged — still VERIFIED). The drain off the FGS HandlerThread / the UIDT JobService Thread now both go through `drainNow()`'s `drainLock.tryLock()` (CR-03 fix), so the "only one drain at a time" invariant the design relies on is enforced.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `apps/mobile/src/screens/uploads/PendingUploadsScreen.tsx` / `services/recordingEvents.ts` / `services/uploadReconcile.ts` / `services/api.ts` interceptEvents                                                              | History-row reuse + chip variants + no cancel + getQueueSafe + events; `_events` consumer; app-launch + AppState→active sweep; every response → processRecordingEvents                                            | ✓ VERIFIED                  | All present (unchanged — still VERIFIED). interceptEvents wraps post/postNoBody/getJson/patch/delete/postMultipart; recordingEvents idempotent on `${id}:${type}`; uploadReconcile cold-start + AppState→active + appStore.jwt watcher.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `.planning/runbooks/05-wave1-cleanup-smoke.md` + `.planning/runbooks/05-upload-smoke.md`                                                                                                                                    | the two on-hardware smoke runbooks                                                                                                                                                                                | ✓ VERIFIED (not yet walked) | Both present; cover D-03/D-05/D-06/D-09 + the UP-08 iOS-gap note (wave1) and the end-to-end upload + hash-mismatch + manual-only checks + the amendments protocol (upload). Neither has been walked on hardware yet — see human-verification items.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

### Key Link Verification

| From                                         | To                                                    | Via                                                                                                                        | Status  | Details                                                                                                                                                                                                |
| -------------------------------------------- | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| UploadCoordinator.uploadOne()                | apps/api POST /recordings/:id/parts                   | `row.uploadId != null` → postRePresign → `POST /recordings/${recordingId}/parts { partsCount, imuUploadId }`               | ✓ WIRED | Confirmed by reading UploadCoordinator.kt lines 276-279, 508-519; parts.ts route present + registered in index.ts. The re-drain branch keeps already-DONE parts' ETags (no whole-file restart, UP-04). |
| apps/api init.ts                             | apps/api/src/db/schema.ts (recordings)                | `select().from(schema.recordings).where(eq(...id...)).limit(1)` BEFORE any video CreateMultipartUpload — idempotency guard | ✓ WIRED | Confirmed by reading init.ts lines 165-241.                                                                                                                                                            |
| apps/api/src/workers/sqs-poller.ts           | apps/api/src/lib/queue.ts                             | `enqueueVerify(recordingId)` (jobId=recordingId → the 3 events per bundle collapse to one job)                             | ✓ WIRED | Confirmed; queue.ts enqueueVerify uses `{ jobId: recordingId }`.                                                                                                                                       |
| infra/terraform/modules/verify-queue/main.tf | apps/api/src/workers/{hash-verify,sqs-poller}.ts      | worker ECS task → 2 containers `node dist/workers/hash-verify.js` + `node dist/workers/sqs-poller.js`                      | ✓ WIRED | Confirmed; both source files exist (the prior dangling reference is resolved).                                                                                                                         |
| workers/hash-verify.ts                       | lib/verify-recording.ts                               | `new Worker('verify', verifyRecording)` → `verifyRecording(job.data.recordingId)`                                          | ✓ WIRED | Confirmed (unchanged from prior verification).                                                                                                                                                         |
| lib/verify-recording.ts                      | schema recordingEventsOutbox                          | `appendOutboxEvent(tx, ...)` inside the qa_status-flip transaction, only on `rowCount===1`                                 | ✓ WIRED | Confirmed by reading verify-recording.ts lines 51-91.                                                                                                                                                  |
| plugins/events-outbox.ts                     | lib/recording-events.ts (drainOutbox / markDelivered) | onSend → drainOutbox(sub) → attach `_events` → markDelivered, gated on 2xx + application/json                              | ✓ WIRED | Confirmed by reading events-outbox.ts.                                                                                                                                                                 |
| apps/mobile services/api.ts interceptEvents  | services/recordingEvents.ts processRecordingEvents    | every JSON-object response body → `processRecordingEvents(body._events)`                                                   | ✓ WIRED | Confirmed; api.ts line 30 imports processRecordingEvents, interceptEvents wraps the response helpers.                                                                                                  |
| services/uploadReconcile.ts                  | apps/api GET /recordings/verified-ids                 | `reconcileOnce` → GET /recordings/verified-ids?since=cursor → intersection with HumynUpload.getQueueSafe() → clearVerified | ✓ WIRED | Confirmed (unchanged from prior verification).                                                                                                                                                         |

### Data-Flow Trace (Level 4)

| Artifact                        | Data Variable         | Source                                                                                                         | Produces Real Data                                                                                                                                   | Status    |
| ------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| PendingUploadsScreen.tsx        | upload-queue rows     | `HumynUpload.getQueueSafe()` + `onUploadQueueChanged` / `onUploadProgress` native events                       | Yes — UploadQueueStore reads the on-device JSON queue (real recordings the user captured), filtered to the current sub                               | ✓ FLOWING |
| `_events` channel               | `body._events`        | recording_events_outbox rows written by `verifyRecording` (the hash-verify worker), drained by the onSend hook | Yes — outbox rows are written on a real qa_status flip; drained on the next authed 2xx JSON response; client de-dups on `(recording_id, event_type)` | ✓ FLOWING |
| HomeSkeletonScreen pending tile | real pending rows     | same `HumynUpload.getQueueSafe()` queue                                                                        | Yes (the `count>0` visibility / pull-to-refresh / offline banner are explicitly Phase 6 per CONTEXT.md D-10 — out of scope)                          | ✓ FLOWING |
| verify-recording.ts re-hash     | `videoSha` / `imuSha` | `sha256OfS3Object(keys.video/keys.imu)` — streamed GetObject                                                   | Yes — the worker reads the actual stored bytes (the byte-fidelity carve-out) and compares to `rec.fileSha256` / `rec.imuSha256`                      | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior                                                  | Command                                                         | Result                                                                                                                                                                                                                                                                                         | Status                                                              |
| --------------------------------------------------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| api Vitest suite (incl. all Phase-5 routes/workers tests) | `cd apps/api && set -a && . ./.env && set +a && npx vitest run` | `Test Files  40 passed (40)` / `Tests  179 passed (179)` (1 non-fatal "error" warning — sqs-poller bootstrap `process.exit(1)` fires during the test run because `WORKER_BOOTSTRAP` isn't `'false'` at module-eval time in some file ordering; suite still green — INFO, matches review IN-03) | ✓ PASS                                                              |
| mobile JS/TS Vitest suite                                 | `cd apps/mobile && npx vitest run`                              | `Test Files  91 passed (91)` / `Tests  649 passed (649)`                                                                                                                                                                                                                                       | ✓ PASS                                                              |
| api TypeScript build                                      | `cd apps/api && npx tsc --noEmit`                               | exit 0                                                                                                                                                                                                                                                                                         | ✓ PASS                                                              |
| mobile TypeScript build                                   | `cd apps/mobile && npx tsc --noEmit`                            | exit 0                                                                                                                                                                                                                                                                                         | ✓ PASS                                                              |
| shared-types TypeScript build                             | `cd shared/types && npx tsc --noEmit`                           | exit 0                                                                                                                                                                                                                                                                                         | ✓ PASS                                                              |
| shared-types re-presign schemas                           | `grep RecordingRePresign shared/types/src/recording.ts`         | `RecordingRePresignRequestSchema` / `RecordingRePresignRequest` / `RecordingRePresignResponseSchema = RecordingsInitResponseSchema` / `RecordingRePresignResponse` present + exported                                                                                                          | ✓ PASS                                                              |
| mobile Kotlin / Robolectric suite                         | not run here (full Gradle build is too slow for the verifier)   | claimed passing per the 05-10/05-11 SUMMARYs; UploadCoordinatorTest covers the drain-lock + re-drain-via-/parts + 409 dead-letter + non-JSON-leak + cellular-chunk tests                                                                                                                       | ? SKIP — routed to the on-hardware human items + the SUMMARY claims |
| APK assembly                                              | not run here                                                    | claimed assembling per the prompt                                                                                                                                                                                                                                                              | ? SKIP                                                              |

### Requirements Coverage

| Requirement | Source Plan                       | Description                                                                               | Status                            | Evidence                                                                                                                                                             |
| ----------- | --------------------------------- | ----------------------------------------------------------------------------------------- | --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| UP-01       | 05-06, 05-09, 05-10, 05-11, 05-13 | three-file bundle via S3 multipart presigned URLs                                         | ✓ SATISFIED                       | UploadCoordinator /init→PUT→/finalize; idempotent /init + /parts re-drain; /finalize NoSuchUpload-tolerant                                                           |
| UP-02       | 05-04                             | 8 MiB Wi-Fi / 5 MiB cellular chunk size                                                   | ✓ SATISFIED                       | UploadModels.kt; NetworkMonitor.isCellular() picks the size                                                                                                          |
| UP-03       | 05-06                             | 3∥ chunks/file × 2 files∥                                                                 | ✓ SATISFIED                       | 6-permit semaphore in UploadCoordinator                                                                                                                              |
| UP-04       | 05-06, 05-09, 05-10, 05-11        | independent chunk retry 2/4/8/16/32/64 s → dead-letter; no whole-file restart             | ✓ SATISFIED                       | ChunkUploader exact 6-step backoff → DeadLetterException; re-drain via /parts keeps DONE parts' ETags                                                                |
| UP-05       | 05-04, 05-08                      | uploads start automatically on stop                                                       | ✓ SATISFIED                       | useRecordingLifecycle + RecordingScreen handleStop → enqueue + drain                                                                                                 |
| UP-06       | 05-07, 05-11                      | FGS survives backgrounding / force-quit; Android-14 type downgrade → 5-min idle stop      | ✓ SATISFIED                       | HumynForegroundService.kt; drainNow drain-lock fix                                                                                                                   |
| UP-07       | 05-07                             | Android-15 UIDT JobService for the 6-h dataSync cap                                       | ✓ SATISFIED                       | UploadJobService.kt setUserInitiated(true); onTimeout handoff                                                                                                        |
| UP-08       | 05-02                             | iOS URLSession background config                                                          | ✓ SATISFIED (documented deferral) | iOS native-module analogues deferred to §v2 IOS-03; CONTEXT.md D + the 05-upload-smoke runbook document the gap; REQUIREMENTS.md marks the iOS-parity workstream §v2 |
| UP-09       | 05-07                             | first-upload OEM battery-optimization walkthrough                                         | ✓ SATISFIED                       | BatteryOptimizationHelper.kt + BatteryOptimizationScreen.tsx                                                                                                         |
| UP-10       | 05-07, 05-08                      | uploads pause during recording / resume on stop                                           | ✓ SATISFIED                       | pause/resume @ReactMethods + useRecordingLifecycle wiring                                                                                                            |
| UP-11       | 05-04, 05-08                      | user cannot manually cancel                                                               | ✓ SATISFIED                       | HumynUploadModule has no cancel method; PendingUploadsScreen has only Retry, no cancel                                                                               |
| UP-12       | 05-08                             | per-file progress visible from the Pending Uploads tile → upload-queue screen             | ✓ SATISFIED                       | PendingUploadsScreen.tsx (History-row reuse, chip variants); Home tile renders real pending rows                                                                     |
| UP-13       | 05-04, 05-08                      | logout cancels in-flight + preserves the queue; same-user re-login resumes                | ✓ SATISFIED                       | UploadQueueStore.bootstrap(currentSub) owner-pin; cancelInflight on logout; uploadReconcile appStore.jwt watcher                                                     |
| UP-14       | 05-08                             | local files NEVER deleted before `verified`                                               | ✓ SATISFIED                       | markVerifiedAndDeleteLocal called only by clearVerified ← `verified` event                                                                                           |
| UP-15       | 05-08                             | on `verified`, delete local mp4+csv+json                                                  | ✓ SATISFIED                       | markVerifiedAndDeleteLocal unlinks all three                                                                                                                         |
| UP-16       | 05-05, 05-08                      | on `re-upload`, re-upload from the local copy                                             | ✓ SATISFIED                       | recordingEvents `re-upload` → HumynUpload.reupload → POST /recordings/:id/reupload (UPDATE not INSERT)                                                               |
| UP-17       | 05-04, 05-06                      | cellular allowed by default, no Wi-Fi-only toggle                                         | ✓ SATISFIED                       | NetworkMonitor only picks chunk size; no toggle anywhere                                                                                                             |
| UP-18       | 05-05                             | client sends `null` ip_address; server populates                                          | ✓ SATISFIED                       | init.ts `ipAddress: req.ip`; finalize.ts returns `r.ipAddress`                                                                                                       |
| UP-19       | 05-06                             | TCP_MAXSEG=1280 + 30 s no-progress abandon-and-retry-with-fresh-socket on cellular        | ✓ SATISFIED                       | ChunkUploader 30 s watchdog + MssSocketFactory (best-effort)                                                                                                         |
| VERIFY-01   | 05-03, 05-05, 05-12               | worker subscribes to S3 multipart-complete events via EventBridge → SQS → BullMQ on Redis | ✓ SATISFIED                       | sqs-poller.ts (EventBridge→SQS→enqueueVerify→BullMQ); Terraform verify-queue module wires it; dev uses the /finalize shim + the verify-sweep cron backstop           |
| VERIFY-02   | 05-03                             | re-hashes both MP4 + IMU CSV from S3, compares to manifest hashes                         | ✓ SATISFIED                       | verify-recording.ts sha256OfS3Object(video) + sha256OfS3Object(imu) vs rec.fileSha256/rec.imuSha256                                                                  |
| VERIFY-03   | 05-03, 05-13                      | on match → qa_status='verified' + emit `verified`                                         | ✓ SATISFIED                       | verify-recording.ts (TOCTOU-safe; rowCount-gated)                                                                                                                    |
| VERIFY-04   | 05-03, 05-13                      | on mismatch → qa_status='hash-mismatch' + emit `re-upload`                                | ✓ SATISFIED                       | verify-recording.ts mismatch branch                                                                                                                                  |
| VERIFY-05   | 05-05, 05-13                      | events delivered piggy-backed on every API response                                       | ✓ SATISFIED                       | plugins/events-outbox.ts onSend hook (2xx + application/json gate)                                                                                                   |
| VERIFY-06   | 05-05, 05-08                      | app-launch reconciliation sweep                                                           | ✓ SATISFIED                       | GET /recordings/verified-ids + services/uploadReconcile.ts                                                                                                           |
| VERIFY-07   | 05-03, 05-05                      | worker scales on queue depth (BullMQ + ECS at MVP)                                        | ✓ SATISFIED                       | Terraform verify-queue: appautoscaling on backlog-per-task                                                                                                           |
| FRAUD-05    | 05-02                             | per-account daily upload-rate cap                                                         | ✓ DESCOPED §v2                    | REQUIREMENTS.md §v2 line 293 (D-04 / D-04a — MVP upload path fully uncapped per account)                                                                             |
| FRAUD-06    | 05-02                             | pre-payout fraud monitoring dashboard                                                     | ✓ DESCOPED §v2                    | REQUIREMENTS.md §v2 line 294 (D-04 / D-04b — Bull-Board is a separate Phase-7 OBS-04 item)                                                                           |

All 26 phase-binding requirement IDs accounted for: UP-01..UP-19 satisfied (UP-08 via a documented iOS deferral); VERIFY-01..VERIFY-07 satisfied; FRAUD-05/FRAUD-06 explicitly descoped to §v2 with dated notes + traceability-matrix rows. No orphaned requirements.

### Anti-Patterns Found

| File                                           | Line                                           | Pattern                                                                                                                                                                                                            | Severity   | Impact                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/api/src/workers/sqs-poller.ts`           | 146, 170                                       | `process.exit(1)` in the bootstrap `loop()` fires during the api Vitest run (a different file-eval order leaves `WORKER_BOOTSTRAP` unset at module-eval time) → "1 error" warning in the test summary              | ℹ️ Info    | Suite still green (179/179). Matches review IN-03 (sqs-poller `pollOnce`/`loop` untested). Harden the test or the bootstrap guard.                                                                                                                                                                                                                   |
| `apps/api/src/routes/recordings/finalize.ts`   | 211-219                                        | qa_status pending→uploaded UPDATE has no `AND qa_status='pending'` predicate; the route's stale-`rec` SELECT short-circuit is the only guard                                                                       | ⚠️ Warning | A concurrent ops takedown between the SELECT and the transaction is silently clobbered back to 'uploaded' → the worker then re-flips to 'verified' (review WR-04). Edge case (ops action mid-finalize); ops takedowns are rare. Mirror the WR-02 `verifyRecording` hardening.                                                                        |
| `apps/mobile/.../upload/UploadCoordinator.kt`  | 457-485 (`postInit`), 566-568 (`postFinalize`) | a 400 from `/recordings/init` (malformed metadata) and a 409 from `/finalize` (row moved server-side) fall through to `IOException` → treated as transient → infinite retry instead of dead-lettering / re-routing | ⚠️ Warning | Review WR-02 / WR-03. Edge-of-edge: D-03 already discards crash-truncated fragments so the metadata bundle is well-formed in practice; a 409 from /finalize needs an ops takedown / a stale hash-mismatch the client hasn't reconciled. Converges eventually via the verified-ids reconcile sweep + `_events`. Treat 400/409 as DeadLetterException. |
| `apps/mobile/.../upload/UploadCoordinator.kt`  | 281-294                                        | `row.reupload === true` is persisted together with the fresh `uploadId` before being cleared in a second `upsert`                                                                                                  | ⚠️ Warning | Review WR-05. A process-kill in the ~1-line window between the two writes makes the next drain re-call `/reupload` (fresh ids → orphaned ETags) — but it self-heals on a fresh re-upload drain (all parts still PENDING). Clear the flag in the same `upsert`.                                                                                       |
| `apps/mobile/.../upload/UploadCoordinator.kt`  | 311, 403                                       | `row.chunkBytes ?: chunkBytesForNetwork(false)` dead fallback — `chunkBytes` is unconditionally set at line 253-255 first                                                                                          | ℹ️ Info    | Review IN-07. Harmless; misleading. Use `row.chunkBytes!!`.                                                                                                                                                                                                                                                                                          |
| `infra/terraform/modules/verify-queue/main.tf` | 45-47, 70-86                                   | `visibility_timeout_seconds = 900` is dead config (the poller overrides to 60 s); the EventBridge rule's `.mp4`/`.csv` suffix filter is bucket-wide, not scoped to `recordings/`                                   | ℹ️ Info    | Review IN-04 / IN-05. Wasteful (extra SQS traffic the poller's regex rejects), not incorrect. Add a `prefix = "recordings/"` constraint; drop/correct the queue visibility timeout.                                                                                                                                                                  |
| `apps/api/src/workers/sqs-poller.ts`           | 24-28                                          | comment says "the SECOND capture group is the recordingId" but the regex has one capture group                                                                                                                     | ℹ️ Info    | Review IN-01. Stale comment; the code reads `m[1]` correctly.                                                                                                                                                                                                                                                                                        |

No blocker-class anti-patterns. The 5 WARNINGs are all narrow edge cases that converge via the reconcile sweep / `_events` channel; none undermine the phase goal (auto-upload as a 3-file bundle, survives lifecycle, re-hashed server-side, verified/re-upload events).

### Human Verification Required

The phase goal is observably true in the codebase — all 4 success criteria are VERIFIED, the 3 prior gaps are closed (idempotent `/init` + `/parts` re-drain route; `drainLock.tryLock()` drain serialisation; prod `sqs-poller.ts` + TOCTOU-safe `verify-recording`), the fresh code review found 0 BLOCKER, and api 179/179 + mobile JS 649/649 + all TypeScript builds pass. But Phase 5's substantive behavior is on-hardware / over-the-network and cannot be exercised in the verifier environment. The following must be walked before the phase is operationally signed off:

1. **End-to-end upload (Pixel 7a/8a-class + dev backend) + the hash-mismatch path.** See `human_verification[0]` above. Runbook: `.planning/runbooks/05-upload-smoke.md` (authored, not yet walked).
2. **Force-quit / OS-evict recovery + the Android-14 FGS type downgrade + the Android-15 UIDT onTimeout handoff.** See `human_verification[1]`. The CR-01/CR-03 fixes mean the resume path should now work — this test confirms it on-device.
3. **OEM battery-optimization deep-links (Xiaomi/Oppo/Vivo/Samsung) + the AOSP fallback.** See `human_verification[2]`.
4. **CGNAT-cellular MSS clamp + the 30 s no-progress watchdog.** See `human_verification[3]`.
5. **Wave-1 cleanup on hardware** (force-quit fragment discard, device-distress→Home nav, alert-cue audibility, RotatePrompt glyph legibility). See `human_verification[4]`. Runbook: `.planning/runbooks/05-wave1-cleanup-smoke.md` (authored, not yet walked).

Also recommended (advisory, not blocking): the mobile Kotlin / Robolectric suite was not re-run in the verifier (the full Gradle build is too slow here); confirm it stays green on the merged state (claimed passing per the 05-10/05-11 SUMMARYs — UploadCoordinatorTest covers the drain-lock + re-drain-via-/parts + 409 dead-letter + non-JSON-leak + cellular-chunk tests).

### Gaps Summary

No code-level gaps. The prior verification's 3 `partial` truths (CR-01/CR-02 re-/init ETag-orphaning + non-idempotent `/init`; CR-03 unserialised `drainNow()`; missing prod `sqs-poller.ts` + WR-02 TOCTOU in `verify-recording`) are all closed in the codebase — verified by reading `init.ts`, `parts.ts`, `UploadCoordinator.kt`, `verify-recording.ts`, `events-outbox.ts`, `finalize.ts`, `verified-ids.ts`, `sqs-poller.ts`, and the Terraform module, plus the matching test files and a green api/mobile test run. The fresh `05-REVIEW.md` found 0 BLOCKER, 5 WARNING, 7 INFO; the WARNINGs are advisory follow-ups (a 400/409 mobile-side that loops as transient instead of dead-lettering — converges via the reconcile sweep; a pre-existing `/finalize` TOCTOU vs an ops takedown; a 1-line `reupload`-flag-persistence window — self-heals) and do not undermine the phase goal. Status is `human_needed` (not `passed`) solely because the phase's on-hardware behavior (FGS lifecycle through Doze / force-quit, OEM ROMs, real CGNAT cellular, the full device→S3→worker→client loop) is non-empty and cannot be exercised here — per the verification gate rules, a non-empty human-verification section forces `human_needed` even when all truths are VERIFIED.

---

_Verified: 2026-05-12T16:42:46Z_
_Verifier: Claude (gsd-verifier)_
