---
phase: 06-tasks-history-home-tiles-lexical-search
plan: 12
type: execute
wave: 8
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11]
files_modified:
  - apps/mobile/src/screens/tasks/TasksScreen.tsx
  - apps/mobile/src/screens/tasks/SendRequestSheet.tsx
  - apps/mobile/src/screens/history/HistoryScreen.tsx
  - apps/api/package.json
  - .planning/phases/06-tasks-history-home-tiles-lexical-search/06-COSMETIC-GAPS.md
  - .planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "06-COSMETIC-GAPS.md findings 1 + 10 + 11 + 12 + 13 each get FIX status (no longer 'Defer')."
    - 'Findings 2, 3, 5, 6, 7, 9 stay deferred per their existing dispositions.'
    - "Finding 8 ('View only' footer) holds for owner copy/interaction decision — NOT in this plan."
    - 'TasksScreen FlatList wires a RefreshControl that calls fetchTasks on pull-down — mirrors HistoryScreen / HomeScreen pattern from Plan 06-09.'
    - "SendRequestSheet hides the 'Upload Sample' tile entirely at MVP — no non-functional control visible."
    - "HistoryScreen FilterChip renders exactly one trailing chevron — no duplicate icon or duplicate '▾' literal."
    - "HistoryScreen HIST-04 empty state renders 'Pick a task and try one.' on its own line below 'You haven't recorded anything yet.'"
    - 'apps/api/package.json gains a posttest hook that re-seeds (`pnpm seed:tasks && pnpm seed:dev-task`) after the test suite runs.'
  artifacts:
    - path: apps/mobile/src/screens/tasks/TasksScreen.tsx
      provides: 'Tasks tab now supports pull-to-refresh'
      contains: 'RefreshControl'
    - path: apps/mobile/src/screens/tasks/SendRequestSheet.tsx
      provides: 'Upload Sample sub-component hidden at MVP'
      contains: 'Upload Sample'
    - path: apps/mobile/src/screens/history/HistoryScreen.tsx
      provides: 'Single-chevron FilterChip + multi-line empty-state body'
      contains: 'chevron'
    - path: apps/api/package.json
      provides: 'Post-test re-seed hook'
      contains: 'posttest'
  key_links:
    - from: TasksScreen RefreshControl
      to: HistoryScreen / HomeScreen Plan 06-09 RefreshControl pattern
      via: same useState + onRefresh + fetchX dance
      pattern: 'RefreshControl'
    - from: SendRequestSheet hide
      to: Plan 06-07 D-sample-video (picker NOT wired at MVP)
      via: owner-directive 2026-05-14 makes the OPTIONAL field invisible at MVP
      pattern: 'Upload Sample'
    - from: HistoryScreen single chevron
      to: 06-UI-SPEC.md §13 FilterChip
      via: design-spec single trailing ▾
      pattern: 'FilterChip'
    - from: HistoryScreen empty-state line break
      to: HIST-04 body copy
      via: owner-directive 2026-05-14 splits the two sentences
      pattern: 'Pick a task'
    - from: apps/api posttest hook
      to: feedback_api_tests_wipe_dev_db memory + 06-COSMETIC-GAPS Finding 13
      via: re-seed tasks after api tests so dev DB stays usable
      pattern: 'posttest'
---

<objective>
Wave 8 — Phase 6 cosmetic cleanup.

Closes findings 1, 10, 11, 12, and 13 from `06-COSMETIC-GAPS.md`. Each is a
small, isolated change; the wave is wholly autonomous (no design / spec
decisions). Findings 2, 3, 5, 6, 7, 8, 9 stay deferred per their existing
dispositions (Finding 8 specifically waits on the owner's toast-vs-footer
copy decision; Finding 9 is now deferred to Phase 8).

Output: 5 small fixes + 1 `06-MANUAL-SMOKE.md §7` flip from PARTIAL → YES
once the device-side spot-check passes.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-tasks-history-home-tiles-lexical-search/06-COSMETIC-GAPS.md
@.planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md
@.planning/phases/06-tasks-history-home-tiles-lexical-search/06-UI-SPEC.md
@.planning/phases/06-tasks-history-home-tiles-lexical-search/06-07-PLAN.md
@.planning/phases/06-tasks-history-home-tiles-lexical-search/06-08-PLAN.md
@.planning/phases/06-tasks-history-home-tiles-lexical-search/06-09-PLAN.md

<interfaces>
<!-- RefreshControl pattern (Plan 06-09 HistoryScreen + HomeScreen) -->
- `import { RefreshControl } from 'react-native'`
- `const [refreshing, setRefreshing] = useState(false)`
- `const onRefresh = useCallback(async () => { setRefreshing(true); await fetchTasks(); setRefreshing(false); }, [...])`
- `<FlatList ... refreshControl={<RefreshControl refreshing={refreshing} onRefresh={onRefresh} />}>`

<!-- FilterChip — single chevron contract -->

- The chip is a labelled pressable: `{label} ▾` OR `{label}` + `<ChevronDown />`. Pick ONE. If the inspection shows both, drop the duplicate.

<!-- HIST-04 empty-state body (06-UI-SPEC §13 State 3) -->

- Heading: "Your recordings will live here." (sheetTitle style)
- Body: "You haven't recorded anything yet.\nPick a task and try one." ← line break between sentences (owner directive 2026-05-14).
- "Pick a task" is an accent link → MainTabs / Tasks.

<!-- Upload Sample hide (TASK-08 SendRequestSheet) -->

- Plan 06-07 D-sample-video left the picker unwired. At MVP, the field should NOT render at all (not just disabled). Wrap the sub-component or its parent block in a feature-flag-style boolean (`const SHOW_UPLOAD_SAMPLE = false;`) so it's a one-line flip when post-MVP picker wiring lands.

<!-- posttest hook (apps/api/package.json) -->

- Existing scripts have `test: "vitest run"`.
- Add `"posttest": "pnpm seed:tasks && pnpm seed:dev-task"`. npm/pnpm auto-fires `posttest` after `test`. Memory `feedback_api_tests_wipe_dev_db` documents the wipe; this is the smallest-possible permanent guard. (Full TEST_DATABASE_URL isolation is OUT of scope here.)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: TasksScreen pull-to-refresh</name>

Scope: Add a `RefreshControl` to the TasksScreen FlatList — mirror the Plan 06-09 HistoryScreen / HomeScreen pattern.

Steps:

1. Read `apps/mobile/src/screens/tasks/TasksScreen.tsx` and locate the `<FlatList>` (or equivalent list primitive).
2. Read `apps/mobile/src/screens/history/HistoryScreen.tsx` for the RefreshControl wiring shape (refreshing state + onRefresh callback + import).
3. Wire the same shape in TasksScreen: `useState`, `useCallback`-wrapped `onRefresh` that calls the existing `fetchTasks` (or equivalent), pass `refreshControl={<RefreshControl ... />}` to the FlatList.
4. Run `cd apps/mobile && npm test` — confirm 811/811 still passes (the test suite mocks RefreshControl as a passthrough — should not break).
5. Update `06-COSMETIC-GAPS.md` Finding 1 status to "✅ Fixed in Plan 06-12 / commit `<hash>`".

Acceptance: pull-down on the Tasks tab refreshes the cards; vitest 811/811 stays green.
</task>

<task type="auto">
  <name>Task 2: SendRequestSheet hide Upload Sample at MVP</name>

Scope: Hide the Upload Sample tile from the Send-Request form for the MVP — Plan 06-07 D-sample-video left the picker unwired; owner wants the entire field invisible.

Steps:

1. Read `apps/mobile/src/screens/tasks/SendRequestSheet.tsx` and locate the Upload Sample render block.
2. Introduce a top-of-file feature flag: `const SHOW_UPLOAD_SAMPLE = false;`. Wrap the render block in `{SHOW_UPLOAD_SAMPLE && (...)}`.
3. Confirm the form's tab order, validation logic, and Send button still work without the sample field (it was OPTIONAL).
4. Update any existing tests for SendRequestSheet that assert on the Upload Sample tile — either guard them by the flag or skip them.
5. Re-run `npm test` — confirm green.
6. Update `06-COSMETIC-GAPS.md` Finding 12 status to "✅ Fixed in Plan 06-12 / commit `<hash>`".

Acceptance: SendRequestSheet renders without the Upload Sample tile; form still submits.
</task>

<task type="auto">
  <name>Task 3: HistoryScreen FilterChip single-chevron</name>

Scope: HistoryScreen's filter pill renders two chevrons — drop the duplicate.

Steps:

1. Read `apps/mobile/src/screens/history/HistoryScreen.tsx` for the FilterChip consumer.
2. Locate `FilterChip.tsx` (likely `apps/mobile/src/components/FilterChip.tsx` or under `screens/shared/`).
3. Inspect: is the chevron added (a) by the component itself, (b) by the label string (e.g. `"All time ▾"`), or (c) both? Confirm "both" via grep — that's the bug.
4. Drop ONE of them. Recommended: keep the icon-component approach (e.g. `<ChevronDown />`) and remove `▾` from labels. Or vice versa, whichever matches the codebase convention.
5. Re-run `npm test` — confirm green (snapshot tests may need re-baselining if any).
6. Update `06-COSMETIC-GAPS.md` Finding 10 status to "✅ Fixed in Plan 06-12 / commit `<hash>`".

Acceptance: HistoryScreen FilterChip shows exactly one chevron.
</task>

<task type="auto">
  <name>Task 4: HistoryScreen HIST-04 empty-state line break</name>

Scope: The HIST-04 empty-state body "You haven't recorded anything yet. Pick a task and try one." should render as two lines.

Steps:

1. Read `apps/mobile/src/screens/history/HistoryScreen.tsx` and locate `renderEmpty` (or wherever HIST-04 copy lives).
2. Split the body into two `<Text>` blocks OR insert `\n` in the body string so "Pick a task and try one." starts on its own line.
3. Preserve the "Pick a task" accent link target (`navigation.navigate('MainTabs', { screen: 'Tasks' })`).
4. Re-run `npm test` — if a snapshot/string assertion fires on the body text, update it.
5. Update `06-COSMETIC-GAPS.md` Finding 11 status to "✅ Fixed in Plan 06-12 / commit `<hash>`".

Acceptance: HIST-04 empty state shows "Pick a task and try one." on its own line; link still routes.
</task>

<task type="auto">
  <name>Task 5: apps/api posttest re-seed hook</name>

Scope: Add a `posttest` script in `apps/api/package.json` that re-seeds the dev DB after the test suite runs.

Steps:

1. Read `apps/api/package.json`.
2. Add to the `scripts` block: `"posttest": "pnpm seed:tasks && pnpm seed:dev-task"`.
3. Verify: run `pnpm --filter @humyn/api test` (env-sourced per `feedback_post_merge_test_env`: `set -a && source apps/api/.env && set +a`). The tests run, posttest fires automatically, tasks count returns to 66 (65 seed + 1 dev-task).
4. Verify count: `docker exec humyn-postgres psql -U humyn -d humyn_dev -t -c "SELECT COUNT(*) FROM tasks;"` returns 66.
5. Update `06-COSMETIC-GAPS.md` Finding 13 status to "✅ Fixed in Plan 06-12 / commit `<hash>`".
6. Update memory `feedback_api_tests_wipe_dev_db.md` with a note that the posttest hook now re-seeds (the memory's rule still holds for cross-checks, but the auto-reseed reduces the manual recovery burden).

Acceptance: Running api tests leaves the dev tasks table at 66; the Tasks tab keeps showing cards even after a `pnpm test` round-trip.
</task>

<task type="auto">
  <name>Task 6: Device verification + flip §7 sign-off</name>

Scope: Rebuild apkRolloutDebug, owner spot-checks the 5 surfaces on Pixel 10a, flip `06-MANUAL-SMOKE.md §7` from PARTIAL → YES.

Steps:

1. `cd apps/mobile/android && ./gradlew :app:installApkRolloutDebug`. Force-stop + relaunch the app on the device.
2. Owner spot-checks:
   - Tasks tab: pull-down to refresh fires (visible spinner + refetch).
   - SendRequestSheet: open via "Send Request" footer link → Upload Sample tile is NOT visible.
   - History tab: filter pill shows exactly one ▾ chevron.
   - History tab: empty state (run `adb shell pm clear ai.humynlabs.capture.apk` first or use a fresh sign-in) — "Pick a task and try one." is on its own line.
   - Tasks tab: 65+ task cards still load after a fresh `pnpm --filter @humyn/api test` round-trip (posttest restored them).
3. Update `06-MANUAL-SMOKE.md §7`:
   - Flip `**Phase 6 sign-off:** PARTIAL` → `YES`.
   - Update `Walked-on` date and `Commit` hash.
   - Add a one-line note: "Plan 06-12 cosmetic cleanup landed (commit `<hash>`); findings 1, 10, 11, 12, 13 closed. Findings 2, 3, 5, 6, 7, 9 deferred per dispositions; Finding 8 awaits owner copy decision."
4. Final commit on `main`. Phase 6 closes.

Acceptance: On-device spot-check passes all 5 surfaces; §7 sign-off updated; final close commit lands.
</task>

</tasks>
