---
phase: 06-tasks-history-home-tiles-lexical-search
plan: 07
type: execute
wave: 4
depends_on: [05]
files_modified:
  - apps/mobile/src/screens/tasks/TasksScreen.tsx
  - apps/mobile/src/screens/tasks/TaskDetailsSheet.tsx
  - apps/mobile/src/screens/tasks/SendRequestSheet.tsx
  - apps/mobile/src/components/TaskCard.tsx
  - apps/mobile/src/components/TaskCategoryPills.tsx
  - apps/mobile/src/components/SearchInput.tsx
  - apps/mobile/src/components/UniversalRulesBlock.tsx
  - apps/mobile/__tests__/screens/tasks/TasksScreen.test.tsx
  - apps/mobile/__tests__/screens/tasks/TaskDetailsSheet.test.tsx
  - apps/mobile/__tests__/screens/tasks/SendRequestSheet.test.tsx
  - apps/mobile/__tests__/components/TaskCard.test.tsx
autonomous: true
requirements:
  [TASK-01, TASK-02, TASK-03, TASK-04, TASK-05, TASK-06, TASK-07, TASK-08, TASK-09, TASK-10]
must_haves:
  truths:
    - 'TasksScreen renders 65 task cards across 10 categories (sourced from GET /tasks paginated)'
    - 'Category pills filter the grid (11 pills: All + 10 categories per UI-SPEC)'
    - 'Always-visible debounced 200ms server-side lexical search via useTaskSearch (TASK-03)'
    - 'Task card shows lucide icon at 28px stroke 1.75, name, category eyebrow, 1-2 line description'
    - 'Tapping a card opens TaskDetailsSheet with chips, name, description, Universal-rules block (4 hardcoded rows), per-task instructions (max 3), Start Recording CTA'
    - 'Universal-rules block renders 4 equal-weight rules with verbatim icons (front_hand, videocam, lightbulb, apps) and labels'
    - 'Send Request form validates name 3-80, description 10-240, category required, setting Indoor/Outdoor, optional sample video ≤30s ≤50MB'
    - 'On submit success: success toast and sheet closes; on error: in-sheet banner with Retry link'
    - 'TASK-09 — submitted-request status is NOT surfaced anywhere'
    - "No-results state renders SearchX icon + 'No tasks match. Try clearing filters or [send a request](#).' (TASK-10)"
    - '__DEV__ long-press affordance on TasksScreen header preserves the Phase 4 RecordingScreen dev entry'
    - 'TASK-07 — per-task instructions already enforced at Phase 1 seed time; no Phase 6 server work; rendered up to 3 bullets verbatim'
  artifacts:
    - path: apps/mobile/src/screens/tasks/TasksScreen.tsx
      provides: 'Tasks screen with search + pills + grid + sheets'
      contains: 'useTaskSearch'
    - path: apps/mobile/src/screens/tasks/TaskDetailsSheet.tsx
      provides: 'Bottom sheet with Universal rules + per-task instructions + Start Recording'
      contains: 'UniversalRulesBlock'
    - path: apps/mobile/src/screens/tasks/SendRequestSheet.tsx
      provides: 'Multipart form sheet with validation + Toast/banner'
      contains: 'submitTaskRequest'
    - path: apps/mobile/src/components/TaskCard.tsx
      provides: 'TaskIcon + name + category eyebrow + description card'
      contains: 'TaskIcon'
    - path: apps/mobile/src/components/TaskCategoryPills.tsx
      provides: 'Horizontal 11-pill row'
      contains: 'Cooking'
    - path: apps/mobile/src/components/SearchInput.tsx
      provides: 'Debounced 200ms TextInput with clear-X + SearchX-on-empty'
      contains: '200'
    - path: apps/mobile/src/components/UniversalRulesBlock.tsx
      provides: '4-row hardcoded Universal rules block'
      contains: 'front_hand'
  key_links:
    - from: TasksScreen onCardTap
      to: TaskDetailsSheet
      via: setSelectedTask + visible state
      pattern: 'TaskDetailsSheet'
    - from: TaskDetailsSheet Start Recording CTA
      to: navigation.navigate('Recording')
      via: 200ms close delay then navigate
      pattern: 'navigation.navigate.*Recording'
    - from: SendRequestSheet submit
      to: services/taskRequestService.submitTaskRequest
      via: multipart POST
      pattern: 'submitTaskRequest'
---

<objective>
Wave 4 — Tasks tab + its two sheets.

Purpose: Closes TASK-01..TASK-10 (the entire Tasks-tab surface). The 65-task catalog + per-category pills + 200ms-debounced lexical search + Task details sheet (with Universal-rules block + per-task instructions + Start Recording) + Send Request form sheet (multipart submit + validation + success/error states).

This plan is one of three parallel Wave 4 plans (06-07 Tasks / 06-08 Home / 06-09 History). The three share NO file modifications — see the `files_modified` audit at plan-level.

Output: 7 new TSX files (1 screen + 2 sheets + 4 components) + 1 navigation tab swap + 4 Vitest test files.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-tasks-history-home-tiles-lexical-search/06-CONTEXT.md
@.planning/phases/06-tasks-history-home-tiles-lexical-search/06-PATTERNS.md
@.planning/phases/06-tasks-history-home-tiles-lexical-search/06-UI-SPEC.md
@.planning/phases/06-tasks-history-home-tiles-lexical-search/06-RESEARCH.md
@apps/mobile/src/screens/tasks/TasksPlaceholderScreen.tsx
@apps/mobile/src/screens/home/HomeSkeletonScreen.tsx
@apps/mobile/src/components/ReportProblemSheet.tsx
@apps/mobile/src/screens/tutorial/RigTutorialScreen.tsx
@apps/mobile/src/navigation/MainTabs.tsx
@apps/mobile/src/services/tasksApi.ts
@apps/mobile/src/services/taskRequestService.ts
@apps/mobile/src/ui/tokens.ts
@apps/mobile/src/ui/primitives/Sheet.tsx
@apps/mobile/src/ui/primitives/Button.tsx
@apps/mobile/src/components/Toast.tsx
@apps/mobile/src/components/TopBar.tsx
@apps/mobile/src/hooks/useTabTopBarProps.ts
@design-system/task-icons/TaskIcon.native.tsx
@design-system/task-icons/mapping.ts
@task-taxonomy.md

<interfaces>
<!-- Existing TopBar + useTabTopBarProps pattern (Phase 2 Plan 02-16 / 03-11) -->
```typescript
const topBarProps = useTabTopBarProps();
return (
  <ScreenContainer accessibilityLabel="...">
    <TopBar {...topBarProps} />
    {/* body */}
  </ScreenContainer>
);
```

<!-- Sheet primitive interface -->

```typescript
<Sheet visible={visible} onDismiss={onDismiss} accessibilityLabel="...">{children}</Sheet>
```

<!-- Universal rules — hardcoded from task-taxonomy.md header -->

```typescript
const UNIVERSAL_RULES = [
  { icon: 'front_hand', label: 'Keep your hands in frame' },
  { icon: 'videocam', label: 'Mount the device firmly on the rig' },
  { icon: 'lightbulb', label: 'Make sure your space is well-lit' },
  { icon: 'apps', label: 'Close all other apps before you start' },
] as const;
```

<!-- Existing __DEV__ debug entry from TasksPlaceholderScreen.tsx — preserve -->

```typescript
// TasksPlaceholderScreen.tsx ~lines 72-90 had a __DEV__-gated long-press affordance:
// onLongPress={() => navigation.navigate('Recording', { taskId: 'dev-task', taskName: 'Dev Task', taskCategory: 'Cooking', taskSetting: 'indoor', isPractice: false })}
// Preserve this on the new TasksScreen TopBar long-press (or on a hidden corner pressable).
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TaskCard, TaskCategoryPills, SearchInput, UniversalRulesBlock components</name>
  <files>apps/mobile/src/components/TaskCard.tsx, apps/mobile/src/components/TaskCategoryPills.tsx, apps/mobile/src/components/SearchInput.tsx, apps/mobile/src/components/UniversalRulesBlock.tsx</files>
  <read_first>
    - .planning/phases/06-tasks-history-home-tiles-lexical-search/06-UI-SPEC.md "Spacing Scale" + "Typography" + "Color" + "Tasks (§10)" sections — exact px / font / color values
    - .planning/phases/06-tasks-history-home-tiles-lexical-search/06-PATTERNS.md "apps/mobile/src/components/TaskCard.tsx + TaskCategoryPills.tsx + SearchInput.tsx" sections
    - apps/mobile/src/screens/home/HomeSkeletonScreen.tsx (CURRENT — `styles.card` block as a card-shape analog)
    - apps/mobile/src/components/ReportProblemSheet.tsx (CURRENT — `FEEDBACK_CATEGORIES` chip row as a pills analog)
    - design-system/task-icons/TaskIcon.native.tsx (will exist after 06-05 — verify import path)
    - apps/mobile/src/ui/tokens.ts (with the 06-05 additions: `taskCardName`, `taskCardDesc`, `ruleLabel`)
    - task-taxonomy.md (CURRENT — confirm the 10 category names exactly: Cooking, Dishwashing, Kitchen, Cleaning, Tidying, Laundry, Gardening, Pet Care, Home Maintenance, Hobby)
  </read_first>
  <action>
    Four components, each a pure presentational unit:

    (a) `apps/mobile/src/components/TaskCard.tsx` — render a single task card:
    - Container: `surface` bg, 18px radius (radii.tile), 16px padding, aspect 1:1.05, 1.5px line border.
    - Top: 28px `<TaskIcon task={slug} strokeWidth={1.75} color={colors.accent} />`.
    - Below icon: category eyebrow (`typography.formLabel`, UPPERCASE, secondary tone — `text2`).
    - Name: `typography.taskCardName` (15/19/600, primary tone `text`).
    - Description: `typography.taskCardDesc` (12/16/400, secondary tone, max 2 lines via `numberOfLines={2}`).
    - Pressable scale-to-0.97 on press (existing pattern from `Pressable` primitive).
    - Props: `{ slug, name, category, description, onPress }`.

    (b) `apps/mobile/src/components/TaskCategoryPills.tsx` — horizontal pill row:
    - ScrollView horizontal, 8px inter-pill gap (`spacing.m`), 9/16 padding, 999px radius, 1.5px `line` border, transparent fill.
    - Active pill: `text` fill (#1A1A1A), white label, no border.
    - Right-edge fade gradient hint when content overflows (use a `LinearGradient`-free fallback: a 40px right-side overlay with `colors.bg` to-transparent — implement via a positioned `View` with `pointerEvents="none"`; gradient via `react-native-svg`'s `<LinearGradient>` element is optional).
    - 11 pills in order: All, Cooking, Dishwashing, Kitchen, Cleaning, Tidying, Laundry, Gardening, Pet Care, Home Maintenance, Hobby (TASK-02 + UI-SPEC).
    - Props: `{ selected: string | 'all'; onSelect: (cat: string | 'all') => void }`.

    (c) `apps/mobile/src/components/SearchInput.tsx` — debounced 200ms TextInput:
    - Container: 14px radius, 12/14 padding, 44px left padding for icon, 1px `line` border.
    - Leading icon: lucide `Search` 18px in `text2`.
    - Trailing X-clear icon when value non-empty: lucide `X` 18px in `text2`, tap clears the value.
    - Focus ring: 1.5px border in `accent`.
    - Placeholder: `text3`.
    - Props: `{ value: string; onChangeText: (s: string) => void; placeholder?: string; debounceMs?: number; onChangeDebounced?: (s: string) => void; analyticsDebounceMs?: number; onAnalyticsDebounced?: (s: string) => void }`.
    - Internal `useEffect` debounces both `onChangeDebounced` (200ms per TASK-03) AND `onAnalyticsDebounced` (400ms per UI-SPEC §Motion analytics throttle). Clear timers on next change.

    (d) `apps/mobile/src/components/UniversalRulesBlock.tsx` — 4-row hardcoded block:
    - Container: `universalRulesBg` background (FFF7F0), 16px radius, 16/16 padding, 14px header-to-list gap, 12px row-to-row gap.
    - Header: `'Always'` UPPERCASE in `accent` (`typography.formLabel` style + accent color).
    - 4 rows: 32px white circle (icon well) + 18px Material icon glyph in `accent` + 14/20/500 label (`typography.ruleLabel`).
    - The 4 (icon, label) tuples are hardcoded per TASK-06:
      ```typescript
      const UNIVERSAL_RULES = [
        { icon: 'front_hand', label: 'Keep your hands in frame' },
        { icon: 'videocam',   label: 'Mount the device firmly on the rig' },
        { icon: 'lightbulb',  label: 'Make sure your space is well-lit' },
        { icon: 'apps',       label: 'Close all other apps before you start' },
      ] as const;
      ```
    - The icon glyphs are **Material Symbols Outlined**, NOT lucide. The repo's existing `<Icon name="..." />` primitive at `apps/mobile/src/ui/primitives/Icon.tsx` may support Material strings; if not, render the icon as a `<Text>` with the Material font (Phase 2 + 4 conventions). Verify the existing convention via Read of `Icon.tsx` before implementing.

    All four components MUST import from `apps/mobile/src/ui/tokens.ts` — NO hex literals in component bodies (the existing no-hex-literals lint gate from Phase 2 Plan 02-22 enforces this).

  </action>
  <verify>
    <automated>cd apps/mobile &amp;&amp; npm run typecheck &amp;&amp; npx vitest run --testPathPattern='ui/no-hex-literals'</automated>
  </verify>
  <acceptance_criteria>
    - All 4 component files exist
    - `grep -c "TaskIcon" apps/mobile/src/components/TaskCard.tsx` >= 1
    - `grep -c "taskCardName\|taskCardDesc" apps/mobile/src/components/TaskCard.tsx` >= 2
    - `grep -c "Cooking.*Dishwashing.*Kitchen\|'Cooking',.*'Dishwashing'" apps/mobile/src/components/TaskCategoryPills.tsx` >= 1 OR each name listed individually (regex grep allows newlines:) `grep -czl "Cooking\|Dishwashing\|Kitchen\|Cleaning\|Tidying\|Laundry\|Gardening\|Pet Care\|Home Maintenance\|Hobby" apps/mobile/src/components/TaskCategoryPills.tsx` >= 10 distinct matches
    - `grep -c "debounceMs.*200\|setTimeout.*200" apps/mobile/src/components/SearchInput.tsx` >= 1
    - `grep -c "front_hand.*videocam\|UNIVERSAL_RULES" apps/mobile/src/components/UniversalRulesBlock.tsx` >= 1
    - `grep -c "Keep your hands in frame\|Mount the device firmly on the rig\|Make sure your space is well-lit\|Close all other apps before you start" apps/mobile/src/components/UniversalRulesBlock.tsx` == 4
    - `cd apps/mobile && npm run typecheck` exits 0
    - The no-hex-literals lint gate passes: `cd apps/mobile && npx vitest run --testPathPattern='ui/no-hex-literals'` exits 0
  </acceptance_criteria>
  <done>4 components ship; tokens-only, no hex literals in component bodies.</done>
</task>

<task type="auto">
  <name>Task 2: Create TasksScreen + TaskDetailsSheet + SendRequestSheet</name>
  <files>apps/mobile/src/screens/tasks/TasksScreen.tsx, apps/mobile/src/screens/tasks/TaskDetailsSheet.tsx, apps/mobile/src/screens/tasks/SendRequestSheet.tsx</files>
  <read_first>
    - apps/mobile/src/screens/tasks/TasksPlaceholderScreen.tsx (CURRENT — preserve the __DEV__ long-press debug affordance verbatim per 06-PATTERNS.md)
    - apps/mobile/src/screens/home/HomeSkeletonScreen.tsx (CURRENT — same TopBar + ScrollView shell pattern)
    - apps/mobile/src/components/ReportProblemSheet.tsx (CURRENT — multipart form sheet pattern + Modal vs Sheet rationale)
    - apps/mobile/src/screens/tutorial/RigTutorialScreen.tsx (CURRENT — Sheet primitive usage at ~lines 162-190)
    - apps/mobile/src/navigation/MainTabs.tsx (CURRENT — locate the Tasks tab registration: `<Tab.Screen name="Tasks" component={TasksPlaceholderScreen} />`)
    - apps/mobile/src/components/Toast.tsx (CURRENT — `showToast(text, durationMs)` API)
    - .planning/phases/06-tasks-history-home-tiles-lexical-search/06-PATTERNS.md "apps/mobile/src/screens/tasks/TasksScreen.tsx", "TaskDetailsSheet.tsx", "SendRequestSheet.tsx" — copy each verbatim
    - .planning/phases/06-tasks-history-home-tiles-lexical-search/06-UI-SPEC.md "Static screen copy" section for Tasks + Task details + Send Request — ALL copy must be verbatim from the UI-SPEC table
    - shared/types/src/task.ts (verify the Task type fields: id, slug, name, description, category, setting, icon_key, instructions)
  </read_first>
  <action>
    Three screens + one nav swap. Implement each verbatim against the UI-SPEC copy table:

    (a) `apps/mobile/src/screens/tasks/TasksScreen.tsx`:
    - Shell: `<ScreenContainer accessibilityLabel="Tasks screen" padding={0}>` + `<TopBar {...useTabTopBarProps()} />`.
    - Below TopBar: `<SearchInput placeholder="Search tasks…" debounceMs={200} analyticsDebounceMs={400} onChangeDebounced={handleSearchDebounced} onAnalyticsDebounced={handleSearchAnalytics} />`.
    - Below SearchInput: `<TaskCategoryPills selected={categoryFilter} onSelect={setCategoryFilter} />`.
    - Body: 2-col grid via FlatList `numColumns={2}` + 12px gap. Switch source based on search state:
      - If `searchQuery.trim() === ''`: render the filtered-by-category list from `fetchTasks({category})` (cached + paginated via cursor).
      - Else: render `useTaskSearch(searchQuery).results` (TASK-03). Show skeleton/spinner while `loading`.
    - Empty state (results.length === 0 AND search active): render TASK-10 empty:
      ```typescript
      <View style={styles.emptyContainer}>
        <SearchX size={48} strokeWidth={1.75} color={colors.text3} />
        <Text variant="body" tone="secondary" style={styles.emptyBody}>
          No tasks match. Try clearing filters or <Pressable onPress={openSendRequestSheet}><Text style={{color: colors.accent}}>send a request</Text></Pressable>.
        </Text>
      </View>
      ```
    - Footer inline link: `Can't find a task? Send request →` (Text + accent-colored Pressable).
    - Mount: `<TaskDetailsSheet visible={detailsOpen} task={selectedTask} onDismiss={...} onStartRecording={...} />` + `<SendRequestSheet visible={requestOpen} onDismiss={closeRequest} />`.
    - On Card tap: `setSelectedTask(task); setDetailsOpen(true);`.
    - __DEV__ long-press affordance on the TopBar (or on a hidden corner Pressable) preserves the Phase 4 dev entry:
      ```typescript
      {__DEV__ && (
        <Pressable onLongPress={() => navigation.navigate('Recording', { taskId: 'dev-task', taskName: 'Dev Task', taskCategory: 'Cooking', taskSetting: 'indoor', isPractice: false })} hitSlop={20}>
          {/* invisible hitbox */}
        </Pressable>
      )}
      ```
    - Emit `tasks_view` analytics on mount; `tasks_pill_changed` on pill tap; `tasks_search` (with `query_length`) on the 400ms analytics-debounce; `task_sheet_opened` on card tap; `task_request_opened` on footer link.

    (b) `apps/mobile/src/screens/tasks/TaskDetailsSheet.tsx`:
    - `<Sheet visible={visible} onDismiss={onDismiss} accessibilityLabel="task-details-sheet">` per UI-SPEC §11:
      - 40×4 grab handle at top.
      - 64px `accentSoft` rounded square (18px radius) housing the 40px `<TaskIcon task={task.slug} strokeWidth={1.75} color={colors.accent} size={40} />`.
      - Category chip (UPPERCASE category name) + optional Outdoor chip (only when `task.setting === 'outdoor'`).
      - Task name `typography.sheetTitle` (24/28/700 -0.3).
      - Task description `typography.body` (16/24/400, secondary tone).
      - `<UniversalRulesBlock />` (hardcoded 4 rows).
      - 'For this task' eyebrow + up to 3 bullets from `task.instructions` (lucide `Check` 16px glyph in accent + 15/22/400 `taskBullet` text).
      - Sticky-footer `<Button variant="primary" label="Start Recording" onPress={onStartRecording} />`.
    - `onStartRecording` closes the sheet then navigates after 200ms:
      ```typescript
      onStartRecording={() => {
        onDismiss();
        setTimeout(() => navigation.navigate('Recording', {
          taskId: task.id,
          taskName: task.name,
          taskCategory: task.category,
          taskSetting: task.setting,
          isPractice: false,
        }), 200);
      }}
      ```

    (c) `apps/mobile/src/screens/tasks/SendRequestSheet.tsx` — mirror `ReportProblemSheet.tsx` per UI-SPEC §12:
    - Modal (NOT Sheet primitive — per UI-SPEC §Component Inventory note: form needs scrolling + sticky footer button).
    - Title: `Request a task` (sheetTitle style).
    - Body: `Tell us what you'd like to record. Our team reviews requests regularly.` (body / secondary).
    - Form fields with inline validation:
      - `TASK NAME` formLabel + TextInput (3-80 chars). Placeholder: `e.g. Iron clothes`.
      - `DESCRIPTION` formLabel + Textarea 3 rows (10-240 chars). Placeholder: `A few words on what the task involves.`.
      - `CATEGORY` formLabel + chip-row select (Cooking / Dishwashing / Kitchen / Cleaning / Tidying / Laundry / Gardening / Pet Care / Home Maintenance / Hobby / **Other**).
      - `SETTING` formLabel + segmented Indoor / Outdoor (default Indoor active).
      - `SAMPLE VIDEO (OPTIONAL)` formLabel + dashed-border tile with placeholder `Choose video (30s max)` + paperclip 24px icon. On tap: open `react-native-document-picker` OR the platform video picker (decide by what's already in the dep list — `react-native-document-picker` is NOT in the dep list per RESEARCH §Environment Availability; the planner picks a fallback: either install it OR use a hand-rolled inline tile that only accepts a hardcoded test URI in __DEV__. For MVP — the sample-video field is OPTIONAL per TASK-08; the sheet can ship without a working picker as long as the form is functional without the video. Document this in the SUMMARY).
    - Inline coral validation lines per UI-SPEC §Validation/error states.
    - Sticky-footer `Send request` button (btn-accent). Disabled until all required fields pass validation.
    - Submit pattern (mirror ReportProblemSheet:49-68):
      ```typescript
      const submit = async () => {
        if (!validate()) return;
        setSubmitting(true);
        try {
          await submitTaskRequest({ name, description, category, setting, sampleVideoUri });
          showToast("Request sent. We'll review and add it to your list.", 2000);
          // emit task_request_submitted({ category, setting, has_video: !!sampleVideoUri })
          onClose();
        } catch (e) {
          setBanner({ kind: 'error', text: "Couldn't send. Try again." });
          // emit task_request_failed({ reason: e.message })
        } finally {
          setSubmitting(false);
        }
      };
      ```

    (d) NOTE: The MainTabs.tsx swap for the Tasks tab is owned by Plan 06-09 (which performs all three tab swaps atomically to avoid same-wave file conflicts). This plan does NOT edit MainTabs.tsx — until 06-09 lands, the Tasks tab continues to render TasksPlaceholderScreen. The new TasksScreen is reachable only via 06-09's swap; this is intentional and the TS compiler will not complain (the new screen is an unreferenced export until the import lands).

    TASK-07 note: the per-task instructions max-3 and no-universal-duplicates constraint is already enforced at Phase 1 seed time via `task-taxonomy.md`. The TaskDetailsSheet renders verbatim what the server returns — no client-side guard needed.

  </action>
  <verify>
    <automated>cd apps/mobile &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 3 screen files + the nav swap exist
    - `grep -c "useTaskSearch" apps/mobile/src/screens/tasks/TasksScreen.tsx` == 1
    - `grep -c "SearchX" apps/mobile/src/screens/tasks/TasksScreen.tsx` >= 1 (TASK-10 empty)
    - `grep -c "No tasks match" apps/mobile/src/screens/tasks/TasksScreen.tsx` == 1
    - `grep -c "Can't find a task" apps/mobile/src/screens/tasks/TasksScreen.tsx` == 1
    - `grep -c "__DEV__" apps/mobile/src/screens/tasks/TasksScreen.tsx` >= 1 (dev affordance preserved)
    - `grep -c "UniversalRulesBlock" apps/mobile/src/screens/tasks/TaskDetailsSheet.tsx` >= 1
    - `grep -c "navigation.navigate.*Recording" apps/mobile/src/screens/tasks/TaskDetailsSheet.tsx` >= 1
    - `grep -c "submitTaskRequest" apps/mobile/src/screens/tasks/SendRequestSheet.tsx` >= 1
    - `grep -c "Request sent.*review.*add it to your list" apps/mobile/src/screens/tasks/SendRequestSheet.tsx` >= 1
    - `grep -c "Couldn't send. Try again" apps/mobile/src/screens/tasks/SendRequestSheet.tsx` >= 1
    - (MainTabs.tsx swap deferred to Plan 06-09 — no grep gate here)
    - `cd apps/mobile && npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Three Tasks-tab screens land; MainTabs swap done; copy strings verbatim from UI-SPEC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vitest coverage — TasksScreen + TaskDetailsSheet + SendRequestSheet + TaskCard</name>
  <files>apps/mobile/__tests__/screens/tasks/TasksScreen.test.tsx, apps/mobile/__tests__/screens/tasks/TaskDetailsSheet.test.tsx, apps/mobile/__tests__/screens/tasks/SendRequestSheet.test.tsx, apps/mobile/__tests__/components/TaskCard.test.tsx</files>
  <read_first>
    - apps/mobile/__tests__/screens/TasksPlaceholderScreen.test.tsx (CURRENT — analog for the Tasks-tab screen test scaffold)
    - apps/mobile/__tests__/screens/HelpCenterScreen.test.tsx OR ReportProblemSheet.test.tsx if exists (analog for sheet testing with mocked services)
    - apps/mobile/__tests__/__mocks__/react-native.ts OR the shared mock setup
    - .planning/phases/06-tasks-history-home-tiles-lexical-search/06-RESEARCH.md §Validation Architecture — the Wave 0 test gaps
  </read_first>
  <behavior>
    `TasksScreen.test.tsx`:
    - Test 1: Renders the 11 category pills.
    - Test 2: Renders all task cards by default (mock `fetchTasks` to return 6 sample tasks).
    - Test 3: Tapping a category pill filters the grid (mocked `fetchTasks` re-called with `{category: 'Cooking'}`).
    - Test 4: Typing in the search input triggers `useTaskSearch` after 200ms — assert `searchTasks` mock called once with the trimmed query.
    - Test 5: When `useTaskSearch` returns empty results, the TASK-10 empty state renders with `SearchX` + the verbatim copy.
    - Test 6: Tapping a task card opens the TaskDetailsSheet (assert the sheet's accessibility label is visible).
    - Test 7: Tapping the footer link opens the SendRequestSheet.

    `TaskDetailsSheet.test.tsx`:
    - Test 1: Renders the 4 UniversalRules rows with the verbatim labels (TASK-06).
    - Test 2: Renders up to 3 instructions verbatim from `task.instructions`.
    - Test 3: When `task.setting === 'outdoor'`, the Outdoor chip is visible.
    - Test 4: When `task.setting === 'indoor'`, the Outdoor chip is NOT rendered.
    - Test 5: Tap on Start Recording closes the sheet AND (after 200ms) calls `navigation.navigate('Recording', {taskId, taskName, taskCategory, taskSetting, isPractice: false})`.

    `SendRequestSheet.test.tsx`:
    - Test 1: Submit button disabled when name < 3 chars.
    - Test 2: Submit button disabled when description < 10 chars.
    - Test 3: Inline coral error "Task name needs at least 3 characters." appears when name is empty.
    - Test 4: Successful submit calls `submitTaskRequest({...})` and shows the success toast + closes the sheet.
    - Test 5: Failed submit shows the in-sheet banner with "Couldn't send. Try again." + Retry link; tapping Retry re-fires the submit.

    `TaskCard.test.tsx`:
    - Test 1: Renders the task name verbatim.
    - Test 2: Renders the category eyebrow UPPERCASE.
    - Test 3: Renders the description with `numberOfLines={2}`.
    - Test 4: Renders the TaskIcon at size 28 + strokeWidth 1.75.
    - Test 5: onPress callback fires when pressed.

  </behavior>
  <action>
    Create the 4 Vitest test files. Use the existing testing-library / RN-testing-library convention from Phase 2's screen tests. Mock `services/tasksApi` and `services/taskRequestService` via `vi.mock(...)`.

    Skeleton for TasksScreen.test.tsx:
    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';
    import { render, fireEvent, waitFor } from '@testing-library/react-native';
    import { TasksScreen } from '../../src/screens/tasks/TasksScreen';

    vi.mock('../../src/services/tasksApi', () => ({
      fetchTasks: vi.fn(),
      searchTasks: vi.fn(),
      useTaskSearch: vi.fn(),
    }));
    // ... ditto for taskRequestService

    describe('TasksScreen', () => {
      beforeEach(() => { /* reset mocks */ });
      it('renders the 11 category pills', async () => { /* ... */ });
      // ... etc
    });
    ```

    Each test must assert on visible-text or accessibility-label. Avoid implementation-detail assertions.

  </action>
  <verify>
    <automated>cd apps/mobile &amp;&amp; npm run test -- --run TasksScreen TaskDetailsSheet SendRequestSheet TaskCard</automated>
  </verify>
  <acceptance_criteria>
    - All 4 test files exist
    - `grep -c "it(\|test(" apps/mobile/__tests__/screens/tasks/TasksScreen.test.tsx` >= 7
    - `grep -c "it(\|test(" apps/mobile/__tests__/screens/tasks/TaskDetailsSheet.test.tsx` >= 5
    - `grep -c "it(\|test(" apps/mobile/__tests__/screens/tasks/SendRequestSheet.test.tsx` >= 5
    - `grep -c "it(\|test(" apps/mobile/__tests__/components/TaskCard.test.tsx` >= 5
    - `grep -c "No tasks match\|send a request" apps/mobile/__tests__/screens/tasks/TasksScreen.test.tsx` >= 1 (TASK-10 verbatim check)
    - `grep -c "Keep your hands in frame" apps/mobile/__tests__/screens/tasks/TaskDetailsSheet.test.tsx` >= 1 (TASK-06 verbatim check)
    - `cd apps/mobile && npm run test -- --run TasksScreen` exits 0
    - `cd apps/mobile && npm run test -- --run TaskDetailsSheet` exits 0
    - `cd apps/mobile && npm run test -- --run SendRequestSheet` exits 0
    - `cd apps/mobile && npm run test -- --run TaskCard` exits 0
  </acceptance_criteria>
  <done>4 Vitest test files green, covering the Tasks-tab surface.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                                             | Description                                                                                                      |
| ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------- |
| User input (search query, task-request form fields) → /tasks/search + /task-requests | Validated client-side (length, presence) + server-side                                                           |
| Sample video URI → multipart POST                                                    | File URI from RN document picker; never decoded client-side                                                      |
| Task instructions string content → UI render                                         | Server-trusted (Phase 1 seed from `task-taxonomy.md`); rendered as Text (no HTML/markdown), no XSS surface in RN |

## STRIDE Threat Register

| Threat ID | Category                   | Component                                               | Disposition | Mitigation Plan                                                                                                                                                             |
| --------- | -------------------------- | ------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-6.7-01  | T (Tampering)              | Search query contains adversarial pg_trgm patterns      | accept      | Plan 06-02 server-side mitigation covers this (Drizzle parameterization + max-200 cap); client doesn't add a new surface.                                                   |
| T-6.7-02  | T (Tampering)              | Task-request form submit with overflow name/description | mitigate    | Client validates 3-80 / 10-240; server also validates (Phase 1 `TaskRequestCreateSchema`).                                                                                  |
| T-6.7-03  | D (DoS)                    | Sample-video upload >50MB                               | mitigate    | Client validates `≤50MB` before submit per CONTEXT D-10 (the Plan 06-05 `submitTaskRequest` rejects oversized files). Server's `@fastify/multipart` cap is the second wall. |
| T-6.7-04  | I (Information Disclosure) | `tasks_search` analytics with query content             | mitigate    | Per UI-SPEC §Analytics: log `query_length` only — never the actual query string. Plan 06-07 emits the event with `{ query_length: q.length }`.                              |
| T-6.7-05  | S (Spoofing)               | Anonymous /tasks/search (no auth)                       | accept      | Existing pattern from Phase 1; anonymous-tier rate-limit applies. The route returns public catalog data only.                                                               |

</threat_model>

<verification>
- typecheck + 4 vitest test files green
- no-hex-literals lint passes
- MainTabs swap landed
- TASK-01..10 all covered by at least one component, screen, or test
</verification>

<success_criteria>

- TASK-01..TASK-10 covered: 65-task catalog, 11 pills, 200ms debounced search, lucide TaskIcon, details sheet with Universal rules + per-task instructions + Start Recording, Send Request multipart submit, no status surface, TASK-10 SearchX empty state
- TasksPlaceholderScreen replaced; the **DEV** long-press dev affordance preserved
- All copy verbatim from 06-UI-SPEC.md
  </success_criteria>

<output>
After completion, create `.planning/phases/06-tasks-history-home-tiles-lexical-search/06-07-SUMMARY.md` per the standard template. Document the sample-video picker decision (install `@react-native-community/datetimepicker` or accept the no-video MVP shipping state — researcher's recommendation per RESEARCH Q-3 is to install but the planner can defer).
</output>
