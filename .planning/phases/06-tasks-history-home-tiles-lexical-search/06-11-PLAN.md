---
phase: 06-tasks-history-home-tiles-lexical-search
plan: 11
type: execute
wave: 7
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10]
files_modified:
  - .planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: []
must_haves:
  truths:
    - '06-MANUAL-SMOKE.md exists per STATE Pattern 56 (the canonical hand-walk script for Phase 6 — mirrors 05-MANUAL-SMOKE.md shape)'
    - 'Runbook covers all 5 ROADMAP §Phase-6 success criteria + the D-09 Wave 1 SoundPool/Vibrator audibility verification'
    - 'ROADMAP.md §Phase 6 Plans section enumerates all 11 plans with their landed status'
    - 'STATE.md current position updated to Phase 6 planning complete; Plan 11 marks the operator-walk checkpoint pending'
  artifacts:
    - path: .planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md
      provides: 'Operator hand-walk runbook for Phase 6 (all 11 plans verified end-to-end on Pixel 10a)'
      contains: '§1'
    - path: .planning/ROADMAP.md
      provides: 'Phase 6 Plans listing + closure status'
      contains: '06-01-PLAN'
    - path: .planning/STATE.md
      provides: 'Phase 6 progress tracking'
      contains: 'Phase 06'
  key_links:
    - from: 06-MANUAL-SMOKE.md §1
      to: D-09 SoundPool audibility verification on Pixel 10a
      via: Plan 06-01 closes here
      pattern: '520 Hz'
    - from: 06-MANUAL-SMOKE.md §2-§5
      to: TASK / HOME / HIST / Player coverage
      via: 11 plans tested end-to-end
      pattern: "Tasks\\|Home\\|History\\|Player"
---

<objective>
Wave 5 — Manual smoke runbook + ROADMAP/STATE refresh.

Purpose: Per CONTEXT D-09 + STATE Pattern 56, every phase produces a `{phase}-MANUAL-SMOKE.md` runbook that the operator walks on real hardware (Pixel 10a, Android 16) end-to-end before the phase is signed off. The runbook covers all 5 ROADMAP §Phase 6 success criteria plus the D-09 Wave 1 audibility verification (which is device-only — Robolectric cannot exercise SoundPool routing).

Also refresh `.planning/ROADMAP.md` (the Phase 6 Plans list — replace `**Plans:** TBD` with the actual 11 plans + their wave structure) and `.planning/STATE.md` (current position → "Phase 6 planned, 11 plans; operator smoke walk pending").

This plan is `autonomous: false` because the runbook authoring is automated but the operator-walk verdict is owner-only (a `checkpoint:human-verify` at the end).

Output: 1 new runbook + 2 doc refreshes + 1 owner checkpoint at the end.
</objective>

<execution_context>
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adnaan/Documents/hl-homelander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-tasks-history-home-tiles-lexical-search/06-CONTEXT.md
@.planning/phases/06-tasks-history-home-tiles-lexical-search/06-RESEARCH.md
@.planning/phases/06-tasks-history-home-tiles-lexical-search/06-UI-SPEC.md
@.planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-MANUAL-SMOKE.md
@.planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Pattern 56 (STATE.md) — manual-smoke runbook shape -->
- Each section heads with a `## §N — <title>` and a checkbox-prefixed step list.
- Each section closes with a `**Re-walked-on:** YYYY-MM-DD <commit>` line that the operator fills in.
- The runbook covers every ROADMAP success criterion AND every UAT-blocker class from prior phases.
- A `[BLOCKING]` annotation flags steps whose failure halts the rest of the walk.

<!-- ROADMAP.md §Phase 6 currently has `**Plans**: TBD` -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 06-MANUAL-SMOKE.md</name>
  <files>.planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md</files>
  <read_first>
    - .planning/phases/05-upload-pipeline-hash-verify-worker-anti-fraud/05-MANUAL-SMOKE.md (CURRENT — the canonical Pattern-56 runbook; copy the section shape, frontmatter, and re-walk timestamp convention)
    - .planning/phases/04-handdetector-recording-ux-practice-tutorial/04-MANUAL-SMOKE.md (CURRENT — adjacent reference for the hardware-test list)
    - .planning/ROADMAP.md §Phase 6 (the 5 success criteria — all 5 must have a §-numbered walk section)
    - .planning/phases/06-tasks-history-home-tiles-lexical-search/06-RESEARCH.md §Validation Architecture / Manual-Only Verifications
  </read_first>
  <action>
    Create `.planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md` modeled on `05-MANUAL-SMOKE.md`.

    Frontmatter:
    ```
    ---
    phase: 6
    slug: tasks-history-home-tiles-lexical-search
    type: manual-smoke
    canonical: true
    created: 2026-05-14
    re_walked_on: pending
    ---
    ```

    Sections (each a `## §N — Title` with checkbox-prefixed steps; commit to walk on Pixel 10a Android 16):

    **§1 — Wave 1 D-09 audibility verification (BLOCKING for sign-off, NOT for Wave 2 entry)**
    Steps:
    - [ ] App build: `apkRolloutDebug`, HEAD commit recorded.
    - [ ] Open Recording surface (any task → Start Recording → hand-gate pass → recording starts).
    - [ ] At MAX media volume, simulate battery ≤15% via `adb shell dumpsys battery set level 14`. Verify:
      - [ ] Visual: AlertPill chrome renders (already passed in Phase 5).
      - [ ] Audio: 520 Hz / 200 ms beep is **audible**.
      - [ ] Haptic: `[100, 50, 100]` ms pattern is **felt**.
      - [ ] Voice: "Battery low. Consider charging soon." (en-US female-leaning) speaks.
    - [ ] Restore battery: `adb shell dumpsys battery reset`.
    - [ ] Trigger thermal kill via `adb shell cmd thermalservice override-status 3` (SEVERE). Verify:
      - [ ] Audio: 440 → 560 → 680 Hz descending sequence is **audible**.
      - [ ] Haptic: 800 ms continuous vibrate is **felt**.
      - [ ] Voice: "Phone too hot, stopping recording" speaks.
      - [ ] Recording ends cleanly within ~2.5 s.
    - [ ] Restore thermal: `adb shell cmd thermalservice override-status 0`.
    - [ ] **Re-walked-on:** YYYY-MM-DD HEAD<commit>

    **§2 — Tasks tab end-to-end (TASK-01..TASK-10 coverage)**
    Steps:
    - [ ] Open Tasks tab. Verify all 65 task cards render across the 10-category grid (TASK-01).
    - [ ] Tap each of the 10 category pills + the "All" pill; verify the grid filters correctly (TASK-02).
    - [ ] Type `make` in the search input. After 200ms, results appear (TASK-03). Verify at least one Cooking task matches.
    - [ ] Type `sweepng` (typo). After 200ms, pg_trgm fallback returns "Sweeping the floor" or similar (TASK-03 fuzzy).
    - [ ] Clear search. Type `zzzzzzzz`. After 200ms, the TASK-10 empty state renders with the `SearchX` icon + "No tasks match. Try clearing filters or send a request." copy.
    - [ ] Tap a card. The Task Details sheet opens (TASK-05). Verify:
      - [ ] Category chip + (if Outdoor) Outdoor chip render.
      - [ ] Universal-rules block renders with all 4 verbatim labels (TASK-06).
      - [ ] Per-task instructions render verbatim (up to 3 bullets) (TASK-07).
      - [ ] Start Recording CTA opens the Recording screen.
    - [ ] Tap "Send request →" link. The Send Request sheet opens (TASK-08).
    - [ ] Submit with name="X" (1 char). Inline coral: "Task name needs at least 3 characters."
    - [ ] Fill name=3-80 / desc=10-240 / category=Other / setting=Indoor. Tap Send. Verify success toast "Request sent. We'll review and add it to your list." and the sheet closes (TASK-09 — no status surfaced anywhere afterwards).
    - [ ] **Re-walked-on:** YYYY-MM-DD HEAD<commit>

    **§3 — Home tab end-to-end (HOME-01..06 + 09 + 10)**
    Steps:
    - [ ] First-time user (fresh install OR `adb shell pm clear ai.humynlabs.capture.apk`): Home shows the empty hero "Record your first task" + tile pair both showing `0s / 0` (HOME-01).
    - [ ] Tap Start Recording → completes a 60s+ recording → returns to Home. Now hero shows the returning variant with the lifetime numeric (HOME-02).
    - [ ] Tap the time-range chevron on the duration tile. FilterSheet opens (16a). Tap "Yesterday". Verify the tile re-fetches and the label updates to "yesterday ▾" (HOME-03).
    - [ ] Repeat for the tasks-recorded tile (HOME-04).
    - [ ] Tap "Custom range". 16b layer pushes in. Pick valid From/To. Tap Apply. Tile updates.
    - [ ] Pull down on the Home ScrollView. RefreshControl spinner shows; lifetime + aggregate re-fetch (HOME-09).
    - [ ] When 0 uploads pending: Pending Uploads section is HIDDEN (HOME-05).
    - [ ] When >=1 upload pending: section is VISIBLE.
    - [ ] Toggle airplane mode while uploads pending: Offline banner appears inside the section header (HOME-10).
    - [ ] **Re-walked-on:** YYYY-MM-DD HEAD<commit>

    **§4 — History tab end-to-end (HIST-01..06 + 10 + 11)**
    Steps:
    - [ ] Open History tab with ≥1 successful recording (≥60s). Rows render grouped by day (HIST-02).
    - [ ] Verify day-group headers: Today / Yesterday / This week / This month / `{MonthName YYYY}` per UI-SPEC.
    - [ ] Verify each row: filename + duration + task name + recorded-at + UploadStatusChip + 64×64 thumbnail (HIST-06).
    - [ ] Verify the disabled "Feedback (coming soon)" slot is present and NOT pressable (HIST-11).
    - [ ] Tap the All time chip. FilterSheet opens. Select Today. Rows narrow to today only (HIST-03).
    - [ ] If no recordings yet: HIST-04 empty state shows "Your recordings will live here." (HIST-04).
    - [ ] With filter active + zero rows in window: HIST-05 empty state shows "No recordings in this range." (HIST-05).
    - [ ] Verify no Delete affordance anywhere on the row or in any context menu (HIST-10).
    - [ ] **Re-walked-on:** YYYY-MM-DD HEAD<commit>

    **§5 — Player + streaming (HIST-07..09)**
    Steps:
    - [ ] Tap a History row with a local MP4 still present. Player opens full-bleed (HIST-07).
      - [ ] Top bar: X-close left + centered task name + "View-only" lock badge.
      - [ ] Tap play overlay. Video plays. Scrub bar advances. Mono time row updates.
      - [ ] Footer: "View only — not downloadable."
      - [ ] Tap X-close. Returns to History.
    - [ ] Tap a History row whose local MP4 was deleted post-`verified` event. Player opens; `getRecordingStreamUrl` returns `presignedUrl` + `archiveState='available'` (HIST-08). Video streams.
    - [ ] Simulate a >90-day-old recording (or seed via dev tool). Player opens with `archiveState='deep-archive'` and the disabled overlay: "This recording has been archived. Contact support for retrieval." (HIST-08 deep-archive path).
    - [ ] Simulate a recording with `qa_status='pending'`. Player opens with `archiveState='unavailable'` and the disabled overlay: "Still uploading — try again in a moment." (HIST-09).
    - [ ] **Re-walked-on:** YYYY-MM-DD HEAD<commit>

    **§6 — Cross-cutting checks**
    Steps:
    - [ ] Phase-5 carry-over: Pending Uploads section continues to render correctly (Phase 5 D-10 wiring preserved).
    - [ ] No-hex-literals lint passes the full mobile test suite.
    - [ ] All Vitest test files green (mobile + api).
    - [ ] Robolectric tests green (HumynBeepModuleTest, ThumbnailExtractorTest, HumynPlayerModuleTest).
    - [ ] Full backend Vitest green including recordings-stream-url.test.ts + tasks-search.test.ts + recordings-list.test.ts + contributions-timeseries.test.ts.
    - [ ] `npx drizzle-kit push` against dev DB shows 0 pending migrations (Plan 06-02 applied).
    - [ ] **Re-walked-on:** YYYY-MM-DD HEAD<commit>

    **§7 — Sign-off**
    - [ ] All §1-§6 walked. Outstanding findings (if any) listed below.
    - [ ] Findings: (operator fills in OR "none")
    - [ ] Phase 6 sign-off: YES / NO / PARTIAL (with operator name + commit).

    The runbook is markdown — no code blocks needed beyond the `adb shell` commands.

    Cross-reference: 05-MANUAL-SMOKE.md is the closest analog for shape, the `[BLOCKING]` annotation convention, and the re-walk timestamp pattern.

  </action>
  <verify>
    <automated>test -f .planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md &amp;&amp; grep -c '## §[1-7]' .planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists
    - `grep -c "^## §[1-7]" .planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md` == 7
    - `grep -c "Re-walked-on:" .planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md` >= 6
    - `grep -c "520 Hz\|440.*560.*680" .planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md` >= 1 (D-09 verification)
    - `grep -c "TASK-01\|TASK-10" .planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md` >= 2
    - `grep -c "HIST-04\|HIST-05\|HIST-07\|HIST-08\|HIST-09" .planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md` >= 5
    - `grep -c "HOME-01\|HOME-05\|HOME-09\|HOME-10" .planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md` >= 4
  </acceptance_criteria>
  <done>Runbook ships; mirrors 05-MANUAL-SMOKE.md shape.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh ROADMAP.md §Phase 6 + STATE.md current position</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (CURRENT — §Phase 6 section currently has `**Plans**: TBD` and `0/TBD` in the progress table)
    - .planning/STATE.md (CURRENT — "Current focus: Phase 06 ... not yet planned" line; the Progress table; the Recent Trend section)
  </read_first>
  <action>
    (a) Edit `.planning/ROADMAP.md` §Phase 6:
    - Replace `**Plans**: TBD` with `**Plans:** 11 plans` (matches the actual plan count: 06-01..06-11).
    - After the **UI hint** line, add the Plans listing block (mirror the Phase 5 listing convention):
      ```
      Plans:

      **Wave 1** _(Phase-5 carry-over D-09; mirrors how 04-COSMETIC-GAPS.md was Phase 5 Wave 1 and 02-COSMETIC-GAPS.md was Phase 3 Wave 1)_

      - [ ] 06-01-PLAN.md — Wave 1 D-09 HumynBeep/Vibrator audibility restore on Android 16 / Pixel 10a (USAGE_MEDIA flip + VIBRATE manifest + Robolectric)

      **Wave 2** _(parallel — backend half — depends on Wave 1)_

      - [ ] 06-02-PLAN.md — Backend lexical-only /tasks/search + pg_trgm migration 0007 + drizzle-kit push + Vitest (TASK-03, TASK-10)
      - [ ] 06-03-PLAN.md — /recordings + /contributions/timeseries start/end + /recordings/:id/stream-url + REQUIREMENTS HIST-07/08/09 rewording (HOME-03/04, HIST-01/03/07/08/09)

      **Wave 3** _(parallel — JS foundation + native player — depends on Wave 1; the only Wave 2 dependency is the shared/types regen which is part of 06-02/06-03)_

      - [ ] 06-04-PLAN.md — ThumbnailExtractor.kt + FinalizeWorker step 7.5 + JS-side thumbnailLedger.ts (HIST-06)
      - [ ] 06-05-PLAN.md — tokens.ts extension + TaskIcon.native.tsx port + appStore slices + timeRange/historyGrouping services + 4 API service wrappers (TASK-03, TASK-08, HOME-03/04, HIST-02/03, HOME-06)
      - [ ] 06-06-PLAN.md — HumynPlayer native module (5 Kotlin files) + media3-exoplayer:1.10.0 dep + JS bridge (HIST-07/08/09)

      **Wave 4** _(parallel — three screens; depends on Wave 3)_

      - [ ] 06-07-PLAN.md — TasksScreen + TaskDetailsSheet + SendRequestSheet + 4 components (TASK-01..10)
      - [ ] 06-08-PLAN.md — HomeScreen + HomeHero + ContributionTile + OfflineBanner + FilterSheet (HOME-01..06, 09, 10)
      - [ ] 06-09-PLAN.md — HistoryScreen + HistoryRow + HistoryDayHeader + FilterChip + RecordingScreen ledger write wireup (HIST-01..06, 10, 11)

      **Wave 5** _(depends on Wave 4)_

      - [ ] 06-10-PLAN.md — PlayerScreen + RootNativeStack Player route + route-registry update (HIST-07/08/09 close)
      - [ ] 06-11-PLAN.md — 06-MANUAL-SMOKE.md runbook + ROADMAP/STATE refresh + operator-walk checkpoint
      ```
    - Update the Progress table row for Phase 6: `| 6. Tasks, History, Home Tiles & Lexical Search             | 0/11           | Planned     | -          |`.

    (b) Edit `.planning/STATE.md`:
    - Update the YAML frontmatter `stopped_at:` to `Phase 6 planned, 11 plans`.
    - Update the "Current Position" section:
      ```
      Phase: 06 (tasks-history-home-tiles-lexical-search) — PLANNED, 11 plans landed
      Plan: 0 of 11 (Wave 1 + 4 + 5 + 11 plans)
      Status: Phase 06 fully planned; awaiting `/gsd-execute-phase 6` (Wave 1 → 2 → 3 → 4 → 5).
      ```
    - Update the Progress table: `| 6 | 0/11 | Planned | - |`.
    - Append a "Last activity" line:
      ```
      Last activity: 2026-05-14 -- Phase 06 planning complete (11 plans, 5 waves; carries D-09 SoundPool/Vibrator restore as Wave 1 mirror of Phase 3+5 cosmetic-first pattern).
      ```

    Do NOT touch any other STATE.md section (the Performance Metrics table, the Patterns log, the Phase 1-5 close-out narrative — all preserved).

  </action>
  <verify>
    <automated>grep -c '06-01-PLAN\|06-11-PLAN' .planning/ROADMAP.md &amp;&amp; grep -c 'Phase 06 (tasks-history-home-tiles-lexical-search) — PLANNED, 11 plans' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '06-01-PLAN\|06-02-PLAN\|06-03-PLAN\|06-04-PLAN\|06-05-PLAN\|06-06-PLAN\|06-07-PLAN\|06-08-PLAN\|06-09-PLAN\|06-10-PLAN\|06-11-PLAN' .planning/ROADMAP.md` >= 11
    - `grep -c "Phase 6.*0/11\|0/11.*Planned" .planning/ROADMAP.md` >= 1
    - `grep -c "Phase 06.*PLANNED.*11 plans\|11 plans landed" .planning/STATE.md` >= 1
    - The Plans listing for Phase 5 in ROADMAP.md is UNCHANGED: `grep -c '05-01-PLAN\|05-15-PLAN' .planning/ROADMAP.md` >= 2 (preserves the existing Phase 5 listing)
  </acceptance_criteria>
  <done>ROADMAP §Phase 6 enumerates all 11 plans; STATE.md reflects "planned, 11 plans".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator on-device walk-through of 06-MANUAL-SMOKE.md (Phase 6 sign-off)</name>
  <files>.planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md</files>
  <action>
    Owner walks the 7 sections of `06-MANUAL-SMOKE.md` on a Pixel 10a (Android 16) using the apkRolloutDebug build. Each section's `Re-walked-on:` line gets the YYYY-MM-DD timestamp + HEAD commit filled in. The §7 sign-off line is written with verdict YES / NO / PARTIAL plus a findings note. This is the canonical Phase-6 sign-off gate per STATE Pattern 56.
  </action>
  <verify>
    Manual on-device verification (the verify step IS the walk; no automated command applies). Each section closes with the operator's typed `Re-walked-on:` timestamp.
  </verify>
  <done>All 7 sections marked re-walked; §7 sign-off line carries a YES / NO / PARTIAL verdict + operator name + commit hash.</done>
  <what-built>
    Phase 6 — all 11 plans landed in code:
    - Wave 1: HumynBeep audibility restore (D-09) — 06-01.
    - Wave 2: Backend lexical-only + start/end ranges + stream-url + REQUIREMENTS HIST-07/08/09 rewording — 06-02, 06-03.
    - Wave 3: ThumbnailExtractor + tokens/services foundation + HumynPlayer native — 06-04, 06-05, 06-06.
    - Wave 4: Tasks + Home + History screens — 06-07, 06-08, 06-09.
    - Wave 5: PlayerScreen + manual-smoke runbook + ROADMAP/STATE — 06-10, 06-11.

    The 06-MANUAL-SMOKE.md runbook (this plan, Task 1) is the canonical operator hand-walk script. ROADMAP.md + STATE.md are refreshed.

  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/06-tasks-history-home-tiles-lexical-search/06-MANUAL-SMOKE.md`.
    2. Build the apkRolloutDebug variant: `cd apps/mobile/android && ./gradlew :app:assembleApkRolloutDebug`.
    3. Install on a Pixel 10a (or Pixel 7a/8a-class with Android 16): `adb install -r apps/mobile/android/app/build/outputs/apk/apkRollout/debug/app-apkRollout-debug.apk`.
    4. Walk the 7 sections of 06-MANUAL-SMOKE.md in order. Fill in the `Re-walked-on:` timestamp at the end of each section.
    5. The `[BLOCKING]` annotation on §1 (D-09 audibility) means: if the SoundPool tones or the haptics are still silent after Plan 06-01 landed, file a defect and re-investigate before continuing the rest of the walk. Other sections may continue independently.
    6. After all sections complete, write the §7 sign-off line with verdict YES / NO / PARTIAL.
  </how-to-verify>
  <resume-signal>
    Type `approved` to mark Phase 6 sign-off YES (or describe issues: e.g. "PARTIAL — §1 still silent; SoundPool fix needs re-investigation" / "NO — HIST-08 disabled-overlay copy regressed").
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                          |
| -------------------------------------------- | -------------------------------------------------------------------- |
| Runbook (markdown text) → operator           | No code execution; operator walks the steps and records observations |
| ROADMAP.md / STATE.md edits → planning state | Doc-only changes; no code surface                                    |

## STRIDE Threat Register

| Threat ID | Category                   | Component                                   | Disposition | Mitigation Plan                                                                                                 |
| --------- | -------------------------- | ------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------- |
| T-6.11-01 | I (Information Disclosure) | Runbook embeds adb commands                 | accept      | `adb shell` commands are dev-only; the runbook is committed to the repo which is operator-private. No PII leak. |
| T-6.11-02 | T (Tampering)              | Operator marks sign-off YES without walking | accept      | Trust-based; the GSD workflow assumes a single owner operator.                                                  |

</threat_model>

<verification>
- 06-MANUAL-SMOKE.md exists with 7 §-numbered sections
- ROADMAP.md §Phase 6 lists all 11 plans
- STATE.md current position updated
- Owner checkpoint pauses until sign-off
</verification>

<success_criteria>

- The operator can run the runbook end-to-end and produce a YES / NO / PARTIAL verdict
- ROADMAP.md and STATE.md accurately reflect the post-planning state
- Phase 6 is closed (or has a documented set of findings) before /gsd-execute-phase 8
  </success_criteria>

<output>
After completion, create `.planning/phases/06-tasks-history-home-tiles-lexical-search/06-11-SUMMARY.md` per the standard template. Record the operator's sign-off verdict and any findings.
</output>
